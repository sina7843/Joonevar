# Variable NAMES only. Claude fills this contract after selecting the actual stack.
# No provider, credential, database choice, or production tariff is implied here.
APP_ENV=development
INTEGRATION_MODE=local
# Real secrets must be supplied through the environment/secret manager.
# referral_validity_days=21 and membership_fee_toman=300000 belong in DB settings,
# NOT in environment variables or frontend/backend constants.
