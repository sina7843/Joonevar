# قرارداد اجرای همزیست برای Claude Code

این فایل دستور اجرایی مستقیم درخواست فعلی کاربر است. متن Requirements.md عمداً بدون تغییر نگهداری شده؛ عبارت «هنوز خروجی کلاد نیست» در آن توصیف کاربرد قبلی سند است و مانع اجرای این بسته نیست.

## اختیار اجرای کار

- هدف، تکمیل واقعی Phase 1 در کد بر اساس سند است؛ تنها ساخت طرح، اسکلت، صفحه نمایشی یا گزارش کافی نیست.
- کلاد پس از هر واحد کاری کامل و بررسی‌شده، خودش Git commit محلی می‌سازد و هش آن را گزارش می‌دهد. درخواست تأیید برای کامیت معمول یا پایان مرحله لازم نیست.
- ابهام فنی یا جزئیات تعیین‌نشده را کلاد با بررسی رفرنس‌ها، کد موجود و کم‌تغییرترین راه سازگار تصمیم می‌گیرد؛ در DECISIONS.md ثبت می‌کند و در گزارش فارسی همان مرحله به کاربر اعلام می‌کند. گزارش تصمیم، درخواست اجازه نیست.
- تصمیم‌های قطعی D01–D19، ترتیب فلو، Actorها، شروط پرداخت و مرز فاز یک قابل بازنویسی با عنوان «فرض فنی» نیستند.
- انتخاب Stack، schema، API، ORM، ساختار پوشه‌ها، تنظیمات فنی OTP، تقویم و الگوریتم تاریخ، سیاست نسخه تنظیمات و UI جزئیِ فاقد طراحی به کلاد سپرده شده است؛ ابتدا قرارداد موجود را حفظ کند.
- داده واقعی را نمی‌توان با تصمیم‌گیری تولید کرد: تعرفه نامعلوم، شماره حساب، صادرکننده معتبر، پروانه، credential، نتیجه ژنتیک و موفقیت پرداخت نباید جعل شوند. مسیر تنظیم/ورود داده را بساز؛ داده synthetic فقط در محیط demo/test با برچسب و جداسازی روشن. نبود داده بیرونی اجرای بخش‌های مستقل را متوقف نکند.
- اگر دسترسی Figma موجود نبود، از سند و دارایی موجود پیش برو؛ تشخیص بصری را UNVERIFIED بنویس و در اولین دسترسی تطبیق بده. نبود دسترسی مجوز طراحی مجدد برند یا ادعای مشاهده رفرنس نیست.
- درخواست شروع کلی، مجوز ادامه ترتیبی مراحل بدون توقف برای تصویب برنامه است. اگر کاربر یک PROMPT مشخص را خواست فقط همان مرحله را انجام بده. محدودیت نشست را با checkpoint حل کن؛ نشست بعد از اولین مرحله ناتمام ادامه می‌دهد.

## مرز اقدامات بیرونی

Git commit محلی مجاز است. این درخواست Git push، انتشار عمومی، deploy واقعی، ارسال پیام واقعی به افراد، پرداخت واقعی یا تخریب داده موجود را درخواست نکرده است. بسته را برای اجرا آماده کن؛ تست‌ها با fixture/provider sandbox باشند. محدودیت واقعی محیط/حساب را دور نزن؛ بخش دقیق مسدود را گزارش بده و کارهای مستقل را ادامه بده.

## وضعیت و صداقت گزارش

COMPLETE فقط برای خروجی اجراشده با شواهد لازم. PARTIAL/BLOCKED برای بخش ناقص. نتیجه mock یا تست واحد، تأیید اتصال زنده، کیفیت بصری یا پذیرش نهایی محصول نیست. نبود داده واقعی می‌تواند readiness تولید را مسدود کند بدون اینکه همه کدنویسی متوقف شود. مراحل دارای Gate لازمِ اجرا‌نشده نباید done شوند.

کامیت نتیجه نیست؛ نتیجه + بررسی + گزارش + کامیت، یک تحویل قابل پیگیری می‌سازد.
