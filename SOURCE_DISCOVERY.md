# Package source inspection

- Product source: uploaded Hamzist_Project_Reference_v1.1(1).md; 111876 bytes, 1059 lines, 29 principal sections, 19 final D decisions.
- SHA-256: `cc441a4932b1a7ee2f29ca1ef3d15697752c73e854b4ce975454bd163dd8282d`.
- Structural sample: Targol-Sahra-Prompt-Driven-Project-v1.1-10-prompts.zip. Inspected directory inventory, requirements/planning structure, prompt format, manifest/traceability, runner setup/prepare/complete/reopen, wrappers, permissions/hooks, role descriptions and test structure.
- Source retained byte-for-byte twice with a clear canonical copy. References retained verbatim.
- No source app repository was supplied. Existing repository inspection and stack selection belong to PROMPT-001.
- Figma URLs and old incident names are references; no live node counts, playback success, incident closure or visual approval is asserted by this package.
- New package has 20 ordered prompts because account, genetics, reproduction and multiple operational actors need separate verifiable slices. It does not mechanically retain 10 broad inventory prompts.

## Material changes to sample workflow

Manual user done gates become Claude-run completion with committed report evidence. Git history remains local. No blanket auto-stage baseline. Runtime accepts a real git worktree and preserves a dirty existing index. Default bypassPermissions and shell policy bypass are removed; routine local git commands receive explicit allow rules. Hooks use the documented command form. Technical gaps can be decided without unlocking/replanning the source. Product originals and source hierarchy remain protected by instructions and content-hash validation.

## Claude configuration reference

Permission allow rules use the documented Bash command patterns; hook configuration uses a complete command string, with payload received on stdin. Checked against [permissions](https://code.claude.com/docs/en/permissions), [settings](https://code.claude.com/docs/en/settings), and [hooks](https://code.claude.com/docs/en/hooks). Actual provider/CLI execution must still be validated in the target environment.
