# تصمیم‌های اجرایی کلاد

هر ورودی جدید: DEC-NNNN، تاریخ، PROMPT، موضوع، منابع بررسی‌شده، ابهام، تصمیم، دلیل، اثر بر داده/UI/امنیت، محدوده، راه بازگشت در صورت نیاز، گزارش به کاربر.

نوع تصمیم را مشخص کن: TECHNICAL_CHOICE / GAP_COMPLETION / SOURCE_CONFLICT_RESOLVED / EXTERNAL_DATA_REQUIRED. سه نوع اول را خودت تعیین و اجرا کن؛ مورد چهارم به معنی اجازه ساخت داده واقعی نیست. D01–D19 دوباره باز نمی‌شوند.

---

## DEC-0001 — انتخاب Stack پایه

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 001 · **نوع:** TECHNICAL_CHOICE
- **منابع:** `ARCHITECTURE_BASELINE.md` («انتخاب Stack با کلاد؛ Stack نمونه الزام نیست»)، Requirements §۲۳.۴ آخر، بازرسی مخزن (خالی از کد محصول، بدون commit)، بررسی محیط: Node v24.13.1، npm 11.8.0، pnpm 11.5.2، docker 29.6.2، دسترسی registry تأییدشده.
- **ابهام:** سند مرجع محصول عمداً فریم‌ورک، دیتابیس و استقرار را تعیین نکرده و کد قابل حفظی وجود ندارد.
- **تصمیم:** یک اپلیکیشن قابل استقرار با **Next.js 15 (App Router) + TypeScript strict**، **PostgreSQL 16** به‌عنوان دیتابیس، **Drizzle ORM + drizzle-kit** برای schema و migration خام SQL، **npm** به‌عنوان package manager و **`node:test`** برای تست‌های واحد/سرویس.
- **دلیل:** الزامات §۲۳.۴ (تراکنش اتمیک، یکتایی سطح داده، کنترل نسخه، جلوگیری از دوبارپردازش) تضمین سطح دیتابیس می‌خواهند که Postgres ارزان می‌دهد. App Router باعث می‌شود مجوز سرور مسیر پیش‌فرض باشد. Drizzle خروجی SQL قابل بازبینی می‌دهد. npm و `node:test` هیچ ابزار تازه‌ای وارد نمی‌کنند و `Bash(node --test *)` از قبل مجاز است.
- **اثر:** داده — schema رابطه‌ای با constraintهای واقعی. UI — RTL و i18n بدون کتابخانه اضافه. امنیت — مجوز سمت سرور به‌صورت پیش‌فرض.
- **محدوده:** کل پروژه. **بازگشت:** لایه `src/domain` عمداً بدون وابستگی به فریم‌ورک و ORM نوشته می‌شود؛ تعویض لایه بیرونی، قواعد محصول را بازنویسی نمی‌کند.

## DEC-0002 — Postgres محلی از طریق docker به‌جای نصب سیستمی

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 001 · **نوع:** TECHNICAL_CHOICE
- **منابع:** `psql` روی این ماشین نصب نیست؛ `docker 29.6.2` در دسترس است.
- **تصمیم:** `docker-compose.yml` فقط شامل سرویس Postgres برای توسعه و تست. تست‌های نیازمند هم‌زمانی واقعی (مصرف Referral، اتصال چیپ، Callback تکراری) روی همین نمونه اجرا می‌شوند.
- **دلیل:** هم‌زمانی واقعی جزء اصلی الزامات صحت است و با دیتابیس درون‌فرایندی تک‌اتصال قابل اثبات نیست. افزودن یک وابستگی دیتابیس تعبیه‌شده صرفاً برای راحتی، ریسک واگرایی رفتار می‌سازد.
- **اثر:** اجرای تست به بالا بودن docker وابسته است؛ در نبود آن تست‌های DB **SKIPPED گزارش می‌شوند، نه PASS**.
- **بازگشت:** رشته اتصال از env خوانده می‌شود؛ هر Postgres دیگری قابل استفاده است.

## DEC-0003 — Session دیتابیسی به‌جای کتابخانه احراز هویت

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 001 · **نوع:** TECHNICAL_CHOICE
- **منابع:** Requirements §۶.۱، §۶.۴، §۲۰ (OTP جدید برای امضای توافق ساخته نمی‌شود).
- **تصمیم:** جریان OTP اختصاصی + جدول `session` + کوکی `httpOnly`/`SameSite=Lax`/`Secure`. بدون NextAuth یا مشابه.
- **دلیل:** هیچ Provider بیرونی هویتی در دامنه نیست؛ کتابخانه فقط لایه پیکربندی اضافه می‌کند. ابطال Session و Audit تغییر موبایل مستقیم‌تر می‌شود.
- **اثر:** امنیت — rate limit و قفل OTP سمت سرور و روی داده، طبق مقادیر Product Settings.

## DEC-0004 — نگهداری تاریخ به‌صورت میلادی، نمایش شمسی

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 001 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۲۹.۲ («قرارداد تقویمی سازگار با محصول در تعریف فنی روشن می‌شود»)، §۱۷.۲.
- **تصمیم:** ذخیره `date`/`timestamptz` میلادی؛ نمایش با `Intl.DateTimeFormat('fa-IR-u-ca-persian')` موجود در Node 24 و مرورگر. رویداد مدنی (تاریخ تولد، تاریخ جفت‌گیری) از timestamp رکورد جدا نگهداری می‌شود.
- **دلیل:** محاسبه بازه‌ها روی تقویم میلادی پایدار است و نمایش شمسی بدون هیچ وابستگی ممکن است.
- **بازگشت:** لایه نمایش متمرکز است؛ تعویض کتابخانه فرمت، منطق را تغییر نمی‌دهد.

## DEC-0005 — تعریف اجرایی «شش ماه» Cooldown ماده

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 001 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۷.۲ («شش ماه نباید بدون سیاست مصوب به عدد دلخواه روز تبدیل شود»)، `ARCHITECTURE_BASELINE.md`.
- **ابهام:** سند مقدار «۶ ماه» را قطعی می‌داند ولی قرارداد تقویمی را باز گذاشته است.
- **تصمیم:** `end = addCalendarMonths(baseDate, 6)` با clamp به آخرین روز معتبر ماه مقصد (مثلاً ۳۱ مرداد + ۶ ماه = ۳۰ بهمن در سال عادی). نر: `addDays(baseDate, 14)`. جایگزینی با ۱۸۰ روز انجام **نمی‌شود**.
- **اثر:** UI — متن هشدار تاریخ مبنا و پایان بازه را از همین محاسبه می‌گیرد. آزمون‌های مرزی پایان ماه و سال کبیسه در PROMPT-014 اجباری‌اند.
- **بازگشت:** تابع واحد و متمرکز؛ اگر سیاست مصوب دیگری اعلام شود، فقط همان تابع عوض می‌شود.

## DEC-0006 — مقادیر فنی OTP به‌عنوان پیش‌فرض قابل تغییر، نه سیاست مصوب

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 001 · **نوع:** EXTERNAL_DATA_REQUIRED + TECHNICAL_CHOICE
- **منابع:** §۶.۱ («عدد جدیدی در این سند تعیین نمی‌شود»)، §۲۹.۲.
- **تصمیم:** TTL کد، فاصله ارسال مجدد، سقف تلاش و مدت قفل به‌عنوان **رکورد Product Settings** با مقدار پیش‌فرض فنی محافظه‌کارانه seed می‌شوند و در پنل مدیریت قابل تغییرند. این مقادیر در گزارش‌ها **سیاست تأییدشده مالک محصول معرفی نمی‌شوند**.
- **دلیل:** بدون مقدار، جریان ورود اصلاً قابل اجرا نیست؛ ولی جعل «سیاست مصوب» ممنوع است. تفکیک با ثبت مبدأ مقدار انجام می‌شود.
- **اثر:** امنیت — rate limit از روز اول فعال است. **بازگشت:** تغییر مقدار بدون تغییر کد.

## DEC-0007 — فونت جایگزین موقت به‌جای IRANSansX

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 001 · **نوع:** EXTERNAL_DATA_REQUIRED
- **منابع:** §۲۴.۱، `ARCHITECTURE_BASELINE.md` («IRANSansX فقط در صورت دسترسی قانونی»). فایل فونت در بسته موجود نیست و مجوز آن اینجا تأیید نشده.
- **تصمیم:** تا تأمین قانونی، یک فونت فارسی آزاد به‌عنوان fallback **با برچسب صریح در کد و گزارش** استفاده می‌شود. هر ادعای تطبیق تایپوگرافی تا آن زمان `UNVERIFIED` است.
- **اثر:** UI — متریک متفاوت ممکن است طول متن فارسی را جابه‌جا کند؛ در بررسی بصری PROMPT-019 صریحاً بازبینی می‌شود.
- **بازگشت:** یک متغیر font-family متمرکز.

## DEC-0008 — توکن‌های DS در کد ساخته می‌شوند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 001 · **نوع:** GAP_COMPLETION
- **منابع:** بازرسی واقعی فایل DS `2GMJPgnnenGnr1zBN2yH5h`: فقط دو صفحه (`00 Cover`, `02 Brand & Logo`) و هفت متغیر که همگی مربوط به لوگو هستند. هیچ Master کامپوننت یا مقیاس تایپوگرافی/فاصله منتشر نشده. جزئیات در `docs/discovery/reference-access.md` §۳.
- **تصمیم:** مقادیر قطعی برند (E46A1D، `#FFF1D6`، `#171A17`، `#3A3B36`، Radius 8/12/16/999) مبنا می‌شوند و بقیه توکن‌های معنایی و کامپوننت‌ها طبق D06 در `src/ui` تعریف می‌شوند.
- **دلیل:** بدون لایه توکن هیچ صفحه‌ای ساخته نمی‌شود؛ ولی این ساخت با بازطراحی برند یا ادعای «مطابق DS» یکی نیست.
- **اثر:** UI — لایه توکن در کد مرجع اجرایی می‌شود. Figma تغییر نمی‌کند. **بازگشت:** اگر بعداً صفحه کامپوننت DS منتشر شد، توکن‌ها به آن نگاشت می‌شوند.

## DEC-0009 — قرارداد چیدمان RTL دکمه‌ها از ERRATA v2.0

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 001 · **نوع:** SOURCE_CONFLICT_RESOLVED
- **منابع:** بخش `ERRATA — v2.0` پروتوتایپ (`SPDPe6xmPR4fDGXIT5fvni` node `395:10183`)، خوانده‌شده به‌صورت بصری.
- **ابهام:** یادداشت تاریخی A-08 در `Hamzist_Prototype_Change_Control_v1.0.md` مدعی نقص در سه صفحه تأیید بود.
- **تصمیم:** یادداشت A-08 و مشتق آن **باطل** است. قرارداد پذیرفته‌شده: در RTL، **جایگاه راست = `Button/Primary`** («تأیید»/«ادامه») و **جایگاه چپ = `Button/Secondary`** («انصراف»/«بازگشت»). همین برای `VET-009 / Sensitive confirm`, `GEN-009 / Technical approval`, `MAT-009 / Same-owner re-auth` اعمال می‌شود. مورد `MAT-015 / Litter detail` نیز از فهرست اشکالات حذف است.
- **اثر:** UI — قاعده چیدمان همه دیالوگ‌ها و فرم‌ها در PROMPT-003.

## DEC-0010 — مجوز تنظیمات به تفکیک گروه، نه به تفکیک پنل

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 002 · **نوع:** GAP_COMPLETION
- **منابع:** §۲۱.۴ («سطح دسترسی هر گروه تنظیم مطابق مجوز مسئول همان بخش؛ تنظیمات به معنی اختیار نامحدود همه اپراتورها نیست»)، §۴ قواعد دسترسی، §۲۹.۲.
- **ابهام:** سند تصریح می‌کند دسترسی باید گروه‌به‌گروه باشد ولی جدول دقیق آن را نمی‌دهد.
- **تصمیم:** هفت گروه تعریف شد — `DEADLINES`, `FEES`, `GENETICS_CENTRE`, `REFERENCE_DATA`, `GUIDE_TEXT`, `OTP_TECHNICAL`, `BREEDING_POLICY` — با ماتریس خواندن/نوشتن در `src/authz/policy.ts`. سوپرادمین همه‌جا می‌نویسد به‌جز `BREEDING_POLICY`؛ اپراتور انجمن داده مرجع و متن راهنما را می‌نویسد و تعرفه را فقط می‌بیند؛ اپراتور مرکز ژنتیک فقط گروه مرکز و متن راهنما را می‌بیند؛ کاربر و دامپزشک هیچ سطح تنظیمات ندارند.
- **اثر:** امنیت — داشتن Shell دیگر معادل اختیار کامل نیست. تست `settings permissions are scoped per group, not per shell` این را اثبات می‌کند.
- **بازگشت:** ماتریس یک ثابت متمرکز است.

## DEC-0011 — گروه BREEDING_POLICY خواندنی است و از پنل تغییر نمی‌کند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 002 · **نوع:** SOURCE_CONFLICT_RESOLVED
- **منابع:** §۱۷.۲ («شش ماه نباید بدون سیاست مصوب به عدد دلخواه روز تبدیل شود»)، §۲۱.۴ («تعیین قابل‌تنظیم‌بودن یک پارامتر با تعیین مقدار آن فرق دارد»).
- **ابهام:** D16 تنظیم‌پذیری گسترده می‌خواهد، ولی ۱۴ روز و شش ماه قاعده مستند محصول‌اند نه پارامتر عملیاتی.
- **تصمیم:** `cooldown.male_days=14` و `cooldown.female_months=6` در دیتابیس ذخیره می‌شوند تا کد آن‌ها را هاردکد نکند، اما هیچ Context اجازه نوشتن آن‌ها را از پنل ندارد. تغییر واقعی نیازمند تصمیم محصول و migration است.
- **اثر:** داده — مقدار از DB خوانده می‌شود؛ UI — در پنل «قابل تغییر نیست» نمایش داده می‌شود.

## DEC-0012 — واحد پول و تبدیل درگاه

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 002 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۲۲، `ARCHITECTURE_BASELINE.md` («مبالغ پولی تغییرناپذیر»).
- **تصمیم:** واحد محصول **تومان** به‌صورت `bigint` صحیح است و در ستون به‌صورت رشته دقیق ذخیره می‌شود؛ هیچ float در مسیر پول نیست (parserهای `numeric` و `int8` در pg غیرفعال شده‌اند). درگاه‌های ایرانی به **ریال** تسویه می‌کنند: `ریال = تومان × ۱۰` و برگشت آن فقط برای مبالغ مضربِ ۱۰ مجاز است؛ باقی‌مانده غیرصفر خطای مغایرت است، نه گرد کردن.
- **اثر:** داده و مالی — مبلغ هر قلم Batch به‌صورت snapshot ذخیره می‌شود و با ویرایش تعرفه بازنویسی نمی‌شود. تست‌های `money.test.ts` هر دو جهت و حالت غیرمضرب را پوشش می‌دهند.

## DEC-0013 — مقدار پولیِ واردنشده NULL است، نه صفر

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 002 · **نوع:** GAP_COMPLETION
- **منابع:** §۲۲ («مبلغ نامعلوم تعیین‌نشده بماند؛ عدد ساختگی درج نشود»)، §۲۹.۲.
- **تصمیم:** ستون `value` تنظیم می‌تواند NULL باشد و معنی آن `NOT_CONFIGURED` است. `readMoney` مقدار `{configured:false}` برمی‌گرداند و `requireConfiguredToman`/`snapshotSetting` خطا می‌دهند. هیچ مسیر پرداختی نمی‌تواند مبلغ نداشته را صفر فرض کند.
- **اثر:** UI — «تعیین‌نشده» نمایش داده می‌شود و CTA پرداخت آن قلم باز نمی‌شود. `/api/health` فهرست کلیدهای واردنشده را گزارش می‌کند.

## DEC-0014 — یک جدول Audit، بدون جدول تاریخچه جداگانه تنظیمات

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 002 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۲۳.۳، §۲۱.۴ («تغییرات Actor، زمان و مقدار قبلی/جدید دارند»).
- **تصمیم:** `audit_event` با `actor/at/target/before/after/target_version` هم تاریخچه تنظیمات و هم بقیه رویدادهای §۲۳.۳ را پوشش می‌دهد؛ جدول جداگانه `setting_history` ساخته نشد. `version` روی خود ردیف تنظیم، مبنای snapshot است.
- **دلیل:** یک منبع تاریخچه، یک مسیر Redaction و یک الگوی کوئری؛ دو جدول موازی فقط واگرایی می‌سازد.
- **اثر:** رویدادها پیش از نوشتن Redact می‌شوند (کلیدهایی مثل otp، کد ملی، شماره کارت/حساب، توکن). Audit در همان تراکنش تغییر نوشته می‌شود.

## DEC-0015 — سوپرادمین به فایل هویتی خصوصی دسترسی ندارد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 002 · **نوع:** GAP_COMPLETION
- **منابع:** §۲۱.۴ («نبود جزئیات مجوزها با اعطای همه اختیارات به همه کارکنان جبران نشود»)، §۶.۲ (مدارک KYC خصوصی).
- **ابهام:** سند مشخص نکرده چه کسی دقیقاً فایل KYC یا فیش را می‌بیند.
- **تصمیم:** فایل خصوصی را فقط مالک آن و **Context بررسی‌کننده همان نوع فایل** می‌بیند: KYC و شجره‌نامه خارجی → اپراتور انجمن؛ فیش ژنتیک → اپراتور مرکز ژنتیک. سوپرادمین به‌صورت پیش‌فرض در فهرست نیست، چون هیچ فلوی مصوبی این دسترسی را لازم ندارد.
- **اثر:** امنیت — کمترین دسترسی. اگر بعداً نیاز عملیاتی مستند شود، افزودن آن یک تغییر آگاهانه با ثبت DEC خواهد بود.

## DEC-0016 — مقادیر عددی پیش‌فرض OTP

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 002 · **نوع:** TECHNICAL_CHOICE (تکمیل DEC-0006)
- **تصمیم:** مقادیر seed: TTL کد ۱۲۰ ثانیه، فاصله ارسال مجدد ۶۰ ثانیه، سقف تلاش ۵، مدت قفل ۱۵ دقیقه؛ همگی با `source = TECHNICAL_DEFAULT` و بازه معتبر در دیتابیس ثبت شدند.
- **دلیل:** بدون مقدار، جریان ورود PROMPT-004 قابل اجرا نیست. برچسب منبع باعث می‌شود این اعداد هرگز به‌عنوان سیاست تأییدشده مالک محصول گزارش نشوند.
- **اثر:** امنیت — rate limit از روز اول فعال. **بازگشت:** تغییر از پنل مدیریت، بدون تغییر کد.

## DEC-0017 — دیتابیس ایزوله به‌ازای هر Suite، بدون دیتابیس تعبیه‌شده جایگزین

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 002 · **نوع:** TECHNICAL_CHOICE (تکمیل DEC-0002)
- **تصمیم:** هر Suite دیتابیس تازه‌ای روی همان سرور Postgres می‌سازد، migration واقعی را اجرا می‌کند و در پایان آن را drop می‌کند. وابستگی `@electric-sql/pglite` که ابتدا نصب شده بود حذف شد.
- **دلیل:** دو مسیر دیتابیس یعنی دو رفتار؛ الزامات هم‌زمانی (§۲۳.۴) با یک اتصالِ تعبیه‌شده اصلاً قابل اثبات نیست. نبودن سرور، شکست صریح می‌دهد نه PASS ساختگی.
- **اثر:** اجرای تست به `docker compose up -d` وابسته است و این در گزارش ثبت می‌شود.

## DEC-0018 — جدول Account در این مرحله حداقلی است

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 002 · **نوع:** TECHNICAL_CHOICE
- **منابع:** ترتیب PROMPTها؛ هویت، OTP، KYC و پروفایل متعلق به PROMPT-004 هستند.
- **تصمیم:** `account` فقط موبایل و وضعیت دارد و `account_role` نقش‌های فعال را نگه می‌دارد؛ همین‌قدر برای مالکیت فایل، گیرنده اعلان و Actor در Audit لازم است. `currentActor()` فعلاً `null` برمی‌گرداند و مسیر فایل خصوصی ۴۰۱ می‌دهد.
- **دلیل:** ساختن یک Actor ساختگی برای «کارکردنِ ظاهری» مسیر، دقیقاً همان ادعای دروغی است که قرارداد اجرا منع کرده است.
- **اثر:** جدول‌های هویت در migration مستقل PROMPT-004 اضافه می‌شوند.

## DEC-0019 — توکن‌های محصول از فایل پروتوتایپ خوانده شدند (تصحیح DEC-0008)

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 003 · **نوع:** SOURCE_CONFLICT_RESOLVED
- **منابع:** `get_variable_defs` روی نودهای `109:360`، `156:397`، `349:8324` و `178:914` از فایل پروتوتایپ `SPDPe6xmPR4fDGXIT5fvni`.
- **ابهام:** DEC-0008 بر اساس بازرسی فایل Design System نتیجه گرفته بود که توکن معنایی محصول منتشر نشده است.
- **تصمیم:** آن نتیجه‌گیری **ناقص بود و تصحیح می‌شود**. مجموعه کامل توکن‌های محصول — رنگ‌های متن/سطح/مرز/اقدام، پنج گروه وضعیت، مقیاس فاصله، Radius، اندازه کنترل و سبک‌های تایپوگرافی — در فایل پروتوتایپ منتشر شده و به نودهای صفحات bind است. همان نام‌ها عیناً در `app/globals.css` تعریف شدند.
- **دلیل:** ساختن توکن دلخواه وقتی مقدار واقعی منتشر شده، همان بازطراحی است که سند منع کرده.
- **اثر:** UI — هیچ رنگ، فاصله یا Radius اختراع‌شده‌ای در کد نیست. بخش برند DEC-0008 (لوگو و E46A1D از فایل DS) همچنان معتبر است. جزئیات در `docs/architecture/design-mapping.md` §۱.

## DEC-0020 — دو توکن CODE-ONLY برای Scrim و حلقه فوکوس

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 003 · **نوع:** GAP_COMPLETION
- **منابع:** بررسی متغیرهای چهار نود بالا؛ هیچ توکن Overlay یا Focus منتشر نشده است.
- **تصمیم:** `--hz-scrim` (از `--color-text-primary` با شفافیت) و `--hz-focus-ring` (برابر `--color-action-primary-default`) در کد تعریف و در سند نگاشت با برچسب CODE-ONLY ثبت شدند. هیچ رنگ تازه‌ای وارد پالت نشد.
- **دلیل:** Modal/Drawer/BottomSheet بدون Scrim و ناوبری کیبورد بدون حلقه فوکوس اصلاً کار نمی‌کنند.
- **اثر:** دسترس‌پذیری — فوکوس در همه عناصر قابل تمرکز دیده می‌شود.

## DEC-0021 — برچسب نقش‌ها از §۲۴.۴ گرفته شد، نه از Copy پروتوتایپ

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 003 · **نوع:** SOURCE_CONFLICT_RESOLVED
- **منابع:** F03 `116:481` چیپ‌های «کاربر عادی» و «صاحب حیوان همزیست» را نشان می‌دهد؛ §۲۴.۴ واژگان ثابت و D10 سوییچر پویا را تعیین می‌کنند.
- **تصمیم:** برچسب‌ها «کاربر»، «پرورش‌دهنده» و «دامپزشک معتمد» شدند و اسلات ثابت حذف شد. سوییچر وقتی فقط یک Context در دسترس باشد اصلاً رندر نمی‌شود.
- **دلیل:** ترتیب اعتبار — تصمیم نهایی و واژگان §۲۴.۴ بر Copy پروتوتایپ مقدم‌اند. این تغییر چیدمان یا رفتار مصوب را عوض نمی‌کند، فقط واژه را با سند یکی می‌کند.
- **اثر:** UI — سوییچر تعداد متغیر دارد و Shell عملیاتی هرگز در آن ظاهر نمی‌شود.

## DEC-0022 — Actor توسعه‌ای محدود به fixtureهای ساختگی

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 003 · **نوع:** TECHNICAL_CHOICE
- **منابع:** Gateهای الزامی `shell-permissions` و `rtl-browser-review`؛ نبود احراز هویت تا PROMPT-004.
- **ابهام:** بدون هیچ Actor، هیچ پوسته‌ای قابل رندر و هیچ بررسی مرورگری قابل اجرا نبود.
- **تصمیم:** کوکی `hz_dev_actor` با مقدار `<accountId>:<CONTEXT>` فقط وقتی خوانده می‌شود که `APP_ENV != production` **و** `INTEGRATION_MODE = local` باشد؛ شناسه باید UUID معتبر و متعلق به یک حساب fixture با پیشوند رزروشده `0999` باشد؛ و ورود به Context همان بررسی نقش production را می‌گذراند.
- **دلیل:** override فقط تعیین می‌کند «چه کسی هستی»، نه «چه اجازه‌ای داری». مسیر مجوز همان مسیری است که با Session واقعی هم اجرا می‌شود.
- **اثر:** امنیت — در production همیشه `null` برمی‌گردد (تست‌شده). حساب با شماره واقعی حتی در توسعه پذیرفته نمی‌شود (تست‌شده). **بازگشت:** در PROMPT-004 فقط `request-actor.ts` به Session واقعی وصل می‌شود؛ قواعد مسیر دست‌نخورده می‌مانند.

## DEC-0023 — مجوز مسیر در خود صفحه اجرا می‌شود و مسیر ثبت‌نشده بسته است

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 003 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۴ قواعد دسترسی، §۲۳.۴ («کنترل مجوز روی سرور، علاوه بر نمایش یا قفل UI»)، رفتار App Router.
- **ابهام:** در App Router، layout و page به‌صورت موازی رندر می‌شوند.
- **تصمیم:** بررسی دسترسی در خود page و کنار داده‌ای که محافظت می‌کند انجام می‌شود، نه فقط در layout. جدول مسیرها در `src/authz/routes.ts` متمرکز است و **مسیر ثبت‌نشده به‌صورت پیش‌فرض بسته است**.
- **دلیل:** اگر فقط layout بررسی می‌کرد، page همچنان کوئری‌هایش را اجرا می‌کرد. پیش‌فرضِ بسته یعنی افزودن صفحه جدید بدون قاعده، آن را عمومی نمی‌کند.
- **اثر:** امنیت — تست‌های مرورگر ثابت کردند تایپ‌کردن `/admin`، `/assoc`، `/genetics` و `/vet` با نقش نادرست، پاسخ FORBIDDEN می‌گیرد و بازدیدکننده ناشناس UNAUTHENTICATED.

## DEC-0024 — تغییر نقش با redirect نسبی

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 003 · **نوع:** TECHNICAL_CHOICE
- **منابع:** خطای واقعی مشاهده‌شده در بررسی مرورگر.
- **ابهام:** `NextResponse.redirect(new URL(returnTo, request.url))` مقصد را روی `localhost` می‌ساخت درحالی‌که مرورگر روی `127.0.0.1` بود؛ کوکی Session بی‌صدا حذف می‌شد و کاربر به صفحه «ورود لازم است» می‌رفت.
- **تصمیم:** پاسخ `303` با هدر `Location` **نسبی** برگردانده می‌شود تا مبدأ دقیقاً همان چیزی بماند که مرورگر استفاده کرده. مقصد بازگشت هم فقط اگر با `/` شروع شود و `//` نباشد پذیرفته می‌شود تا Open Redirect ممکن نباشد.
- **اثر:** امنیت و پایداری — این کلاس خطا در همه Redirectهای بعدی (بازگشت از درگاه پرداخت) تکرار نمی‌شود.

## DEC-0025 — اعتبارسنجی رقم کنترلی کد ملی

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 004 · **نوع:** GAP_COMPLETION
- **منابع:** §۶.۲ («کد ملی ده‌رقمی یکتا»)، §۶.۴ («کد ملی پس از تأیید فقط‌خواندنی است»)، §۶.۲ درباره کدپستی («اختیاری‌بودن فیلد با حذف اعتبارسنجی مقدار واردشده یکی نیست»).
- **ابهام:** سند فقط «ده‌رقمی و یکتا» را می‌گوید و درباره رقم کنترلی ساکت است.
- **تصمیم:** علاوه بر ده رقم و یکتایی، **رقم کنترلی استاندارد** هم بررسی می‌شود و ارقام فارسی/عربی پیش از هر بررسی به لاتین تبدیل می‌شوند.
- **دلیل:** رقم کنترلی بخشی از قالب خود شناسه است، نه یک قاعده محصولی تازه. چون این مقدار پس از تأیید KYC فقط‌خواندنی می‌شود، یک رقم اشتباه تایپی **دائمی** خواهد بود؛ گرفتن آن در لحظه ورود ارزان است و بعداً گران.
- **اثر:** داده — هیچ کد ملی بدساخت وارد نمی‌شود. UI — پیام خطای جدا برای «ده رقم نیست» و «معتبر نیست». **بازگشت:** یک تابع متمرکز `isValidNationalId`؛ اگر مالک محصول بخواهد فقط ده رقم کافی باشد، حذف یک شرط است.

## DEC-0026 — سقف ارسال ساعتی و طول عمر نشست

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 004 · **نوع:** TECHNICAL_CHOICE (تکمیل DEC-0006/DEC-0016)
- **منابع:** §۶.۱ («طول عمر کد، زمان ارسال مجدد و تعداد تلاش‌ها از تنظیمات مصوب می‌آید؛ عدد جدیدی در این سند تعیین نمی‌شود»)، §۲۹.۲، الزام Rate limiting.
- **ابهام:** فاصله ارسال مجدد از هرزنویسی روی **یک** Challenge جلوگیری می‌کند، ولی سدی برای ساخت پیاپی Challengeهای تازه روی یک شماره وجود نداشت.
- **تصمیم:** دو کلید تازه با `source = TECHNICAL_DEFAULT`: `otp.max_sends_per_hour = 5` و `session.ttl_days = 30`. هر دو از پنل مدیریت قابل تغییرند و در گزارش‌ها **سیاست تأییدشده مالک محصول معرفی نمی‌شوند**.
- **اثر:** امنیت — هزینه پیامک و سوءاستفاده محدود می‌شود. تست‌ها هر دو مرز را روی دیتابیس واقعی می‌سنجند.

## DEC-0027 — بررسی احراز هویت با مجوز موجود اپراتور انجمن

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 004 · **نوع:** GAP_COMPLETION
- **منابع:** §۲۱.۲ («صف و جزئیات پرونده‌های موجود … و بررسی‌های مصوب مرتبط با کاربر»)، §۲۱.۴ («نبود جزئیات مجوزها با اعطای همه اختیارات به همه کارکنان جبران نشود»)، DEC-0015.
- **ابهام:** سند صریحاً نمی‌گوید کدام نقش عملیاتی پرونده KYC را بررسی می‌کند.
- **تصمیم:** همان مجوز محدودشده موجود **اپراتور انجمن** استفاده می‌شود؛ نه نقش تازه ساخته شد، نه Gate محصولی تازه‌ای اضافه شد. مرکز ژنتیک و سوپرادمین این صف را نمی‌بینند و فایل هویتی را هم نمی‌خوانند (سازگار با DEC-0015).
- **دلیل:** §۲۱.۲ بررسی‌های مرتبط با کاربر را در صف انجمن می‌گذارد و هیچ بازبین دیگری نام نمی‌برد؛ کوچک‌ترین راه سازگار، استفاده از همان مجوز است.
- **اثر:** امنیت — دسترسی به مدرک هویتی به کمترین گروه ممکن محدود می‌ماند. **افشا:** اگر مالک محصول بازبین دیگری تعیین کند، فقط یک ورودی در ماتریس مجوز عوض می‌شود.

## DEC-0028 — ورود به محیط عملیاتی از راه URL همان محیط

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 004 · **نوع:** GAP_COMPLETION
- **منابع:** D11 و §۲۱ (محیط عملیاتی مستقل)، D10 (سوییچر فقط نقش‌های عمومی فعال).
- **ابهام:** وقتی Context عملیاتی در سوییچر عمومی نمایش داده نمی‌شود، اپراتور چطور وارد محیط خودش می‌شود؟ سند مسیر ورود جداگانه‌ای تعریف نکرده است.
- **تصمیم:** Context مورد نیاز هر مسیر روی سرور انتخاب می‌شود: اگر حساب نقش فعال آن محیط را داشته باشد، بازکردن URL همان محیط او را در همان Context قرار می‌دهد و همان حساب در اپ عمومی دوباره «کاربر» است. انتخاب فقط میان مجوزهایی است که حساب **از قبل دارد**؛ هیچ مجوزی اعطا نمی‌شود و حساب بدون نقش همچنان FORBIDDEN می‌گیرد.
- **دلیل:** این دقیقاً «محیط مستقل» را حفظ می‌کند بدون اینکه Context عملیاتی وارد سوییچر عمومی شود یا ورود جداگانه‌ای اختراع شود.
- **اثر:** امنیت — تست‌های واحد و مرورگر ثابت می‌کنند کاربر عادی روی `/admin`، `/assoc` و `/genetics` رد می‌شود و سوپرادمین روی `/assoc` هم رد می‌شود.

## DEC-0029 — صندوق خروجی پیامک توسعه به‌جای کد دور زدن

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 004 · **نوع:** TECHNICAL_CHOICE
- **منابع:** الزام صریح «No production backdoor OTP»؛ نبود ارائه‌دهنده پیامک واقعی.
- **ابهام:** بدون ارائه‌دهنده، نه توسعه‌دهنده می‌تواند وارد شود و نه Gate مرورگر قابل اجراست.
- **تصمیم:** جدول `dev_outbound_sms`. فرستنده محلی پیام را همان‌جا می‌نویسد و **فقط** خارج از production با `INTEGRATION_MODE=local` ساخته می‌شود؛ در production ساخت آن خطا می‌دهد و نبود ارائه‌دهنده به‌جای «موفقیت خاموش»، خطای `NOT_CONFIGURED` می‌دهد. خواندن این جدول به دسترسی دیتابیس نیاز دارد؛ **هیچ مسیر HTTP و هیچ کد ثابتی برای دور زدن OTP وجود ندارد**.
- **اثر:** امنیت — کد فقط به‌صورت hash ذخیره می‌شود و در Audit، لاگ یا پاسخ HTTP ظاهر نمی‌شود (تست‌شده). Actor توسعه‌ای PROMPT-003 (کوکی `hz_dev_actor`) کاملاً **حذف شد**؛ اکنون تنها راه Actor شدن، نشست واقعی است.

## DEC-0030 — تغییر موبایل، نشست‌های دیگر را می‌بندد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 004 · **نوع:** GAP_COMPLETION
- **منابع:** §۶.۴ (تغییر موبایل با OTP شماره جدید)، §۲۳.۴ (ذخیره امن و کنترل دسترسی).
- **ابهام:** سند درباره سرنوشت نشست‌های باز پس از تغییر شماره ساکت است.
- **تصمیم:** پس از تغییر موفق شماره، همه نشست‌های دیگر همان حساب باطل می‌شوند و فقط نشستی که تغییر را انجام داده باقی می‌ماند.
- **دلیل:** شماره موبایل تنها اعتبارنامه ساخت نشست است؛ اگر جابه‌جا شود، نشست‌هایی که با اعتبارنامه قبلی ساخته شده‌اند نباید ادامه پیدا کنند. کاربر فعلی از کار خودش بیرون انداخته نمی‌شود.
- **اثر:** امنیت — سناریوی «دستگاه گم‌شده با نشست باز» بسته می‌شود. **بازگشت:** یک فراخوانی متمرکز `revokeOtherSessions`.

## DEC-0031 — تأیید پرداخت فقط سمت سرور، با داور بودن وضعیت Attempt

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 005 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۷ («نمایش موفقیت در Frontend به‌تنهایی سند پرداخت نیست»)، §۲۲ («پردازش تکراری Callback نباید دوباره سند، عضویت یا درخواست ایجاد کند»)، §۲۳.۴.
- **ابهام:** سند idempotency را الزام می‌کند ولی سازوکار آن را تعیین نکرده است.
- **تصمیم:** دو لایه. لایه اول جدول `payment_callback` با کلید یکتای `(provider, external_ref)` که دریافت هر Callback را یک‌بار ثبت می‌کند. لایه دوم و **داور واقعی**، به‌روزرسانی شرطی `UPDATE payment_attempt SET status=... WHERE id=? AND status='PENDING' RETURNING`؛ فقط تراکنشی که این ردیف را از PENDING بیرون می‌آورد اثر را اعمال می‌کند. اثر محصولی (فعال‌سازی عضویت) داخل همان تراکنش اجرا می‌شود.
- **دلیل:** اگر فقط ردیف Callback ملاک بود، دو Callback با شناسه‌های ارائه‌دهنده متفاوت هر دو اثر می‌گذاشتند. اگر فقط وضعیت Attempt ملاک بود، تحویل تکراری بی‌ردپا می‌ماند. هر دو با هم، هم قابل حسابرسی است و هم اتمیک.
- **اثر:** تست‌ها سه Callback هم‌زمان را روی دیتابیس واقعی اجرا می‌کنند و دقیقاً یک فعال‌سازی، یک اعلان و یک رکورد Audit می‌ماند. شکست اثر، کل تراکنش را برمی‌گرداند و Attempt در PENDING می‌ماند تا تلاش دوباره آن را درست ببندد.

## DEC-0032 — مبلغ در لحظه ایجاد Intent منجمد می‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 005 · **نوع:** GAP_COMPLETION
- **منابع:** §۲۲ («مبلغ پرداخت ثبت‌شده در سابقه باقی می‌ماند و با ویرایش تعرفه جدید بازنویسی نمی‌شود»)، D16.
- **تصمیم:** هر `payment_item` علاوه بر مبلغ، **کلید تنظیم و نسخه آن** را هم ذخیره می‌کند. مبلغ هرگز از درخواست کاربر خوانده نمی‌شود؛ سرور آن را از تنظیمات می‌گیرد. تبدیل به ریال (`×۱۰`، DEC-0012) از همین مقدار منجمد انجام می‌شود و تأیید، مبلغ گزارش‌شده درگاه را با همان عدد مقایسه می‌کند.
- **دلیل:** ثبت نسخه تنظیم یعنی بعداً می‌شود گفت این مبلغ از کدام نسخه تعرفه آمده، نه فقط اینکه چقدر بوده.
- **اثر:** مبلغ دستکاری‌شده به‌جای «موفقیت خاموش»، `AMOUNT_MISMATCH` می‌شود و هیچ عضویتی فعال نمی‌شود. تغییر تعرفه پس از ایجاد Intent، مبلغ قبلی را تغییر نمی‌دهد و Intent بعدی با نسخه جدید ساخته می‌شود — هر دو تست‌شده.

## DEC-0033 — درگاه توسعه‌ای دیتابیسی به‌جای پرداخت جعلی درون‌فرایندی

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 005 · **نوع:** TECHNICAL_CHOICE
- **منابع:** نبود درگاه واقعی؛ الزام اجرای واقعی مسیر پرداخت در مرورگر.
- **ابهام:** درگاه محلی PROMPT-002 وضعیت را در حافظه فرایند نگه می‌داشت و در یک مسیر redirect واقعی بین دو درخواست دوام نمی‌آورد.
- **تصمیم:** یک صفحه `/dev/gateway` که نقش صفحه بانک را بازی می‌کند و تصمیم را در `dev_payment_outcome` می‌نویسد؛ اپلیکیشن پس از بازگشت مرورگر همچنان **خودش** verify می‌کند. هر دو فقط خارج از production با `INTEGRATION_MODE=local` وجود دارند و در غیر آن ۴۰۴ می‌دهند. در production نبود درگاه، خطای صریح `NOT_CONFIGURED` می‌دهد.
- **دلیل:** این ساختار همان شکل واقعی را دارد — تصمیم بیرونی، تأیید داخلی — پس مسیر تولیدی چیزی جز تعویض آداپتور لازم ندارد. `markPaid` درون‌فرایندی فقط برای تست واحد باقی ماند و از اپلیکیشن قابل فراخوانی نیست.
- **اثر:** تست مرورگر ثابت کرد بازگشت مستقیم به `/membership/return` با شناسه واقعی و بدون عبور از درگاه، پرداخت را تأیید **نمی‌کند**.

## DEC-0034 — Eligibility به‌صورت تابع خالص روی مجموعه‌ای از واقعیت‌ها

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 005 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۵ و ماتریس دسترسی Flow Map نود `2:6`؛ §۲۳.۴ («کنترل مجوز روی سرور، علاوه بر نمایش یا قفل UI»).
- **تصمیم:** قواعد §۵ به‌صورت `evaluate(service, facts)` نوشته شدند و یک resolver واقعیت‌ها را از دیتابیس می‌خواند. شمارش‌های مربوط به قابلیت‌های ساخته‌نشده **صفر** خوانده می‌شوند، نه حدس زده.
- **دلیل:** یک منبع قاعده برای کارت داشبورد و برای رد کردن سمت سرور؛ دو پیاده‌سازی موازی همان جایی است که قفل UI و مجوز سرور از هم جدا می‌شوند. صفر بودن شمارش‌ها هم صادقانه است: وقتی هیچ حیوانی ثبت نشده، واقعاً هیچ حیوانی برگه ثبتی ندارد.
- **اثر:** PROMPT-006 به بعد فقط همان resolver را با شمارش واقعی پر می‌کنند و هیچ قاعده‌ای بازنویسی نمی‌شود. Cooldown عمداً حتی به‌عنوان یک fact وجود ندارد تا نتواند به شرط مسدودکننده تبدیل شود (§۱۷.۲).

## DEC-0035 — سیاست غیرفعال‌سازی عضویت اختراع نشد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 005 · **نوع:** GAP_COMPLETION
- **منابع:** §۳ («علت‌های غیرفعال‌سازی یا اختیارات جدید آن در این سند اختراع نمی‌شوند»)، §۷.۱، D05.
- **ابهام:** D05 رفتار عضویت غیرفعال را تعریف می‌کند ولی سند نمی‌گوید چه چیزی باعث غیرفعال‌شدن می‌شود.
- **تصمیم:** فقط **سازوکار** ساخته شد: تغییر وضعیت از محیط عملیاتی، با دلیل اجباری، Audit و اعلان. هیچ قاعده خودکاری — نه انقضا، نه job، نه شرط — عضویت را غیرفعال نمی‌کند. عضویتی که هرگز فعال نشده هم قابل غیرفعال‌سازی نیست.
- **اثر:** D04 دست‌نخورده می‌ماند و §۷.۱ قابل اجراست. تصمیم مالک محصول درباره علت‌ها، فقط داده و متن لازم دارد، نه تغییر کد.

## DEC-0036 — قاعده کار دامپزشک جدا از جدول‌های Finder نوشته شد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 005 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۷.۱، D05، §۱۱.۱؛ جدول‌های دامپزشک/Location/درخواست متعلق به PROMPT-007 هستند.
- **تصمیم:** `vetWorkEligibility` سه خروجی جدا می‌دهد: پذیرش کار جدید، حضور در Finder، و امکان تکمیل کار فعال قبلی. شرط عضویت هم به‌صورت یک قطعه SQL آماده (`finderEligibleVetCondition`) در دسترس است تا در PROMPT-007 داخل خود کوئری Finder بنشیند، نه به‌صورت فیلتر بعدی.
- **دلیل:** فیلترکردن پس از کوئری یعنی یک روز کسی فراموشش می‌کند؛ شرط داخل کوئری یعنی دامپزشک غیرواجد شرایط اصلاً به فهرست نمی‌رسد.
- **اثر:** تفکیک «کار قبلی ادامه دارد» از «کار جدید نه» و تفکیک «رد صلاحیت حرفه‌ای» از «عضویت غیرفعال» همین حالا تست شده است. اتصال به صف واقعی و Finder در PROMPT-007 انجام می‌شود و این محدودیت صریحاً گزارش شده است.

## DEC-0037 — برچسب «نسل بعدی» پروتوتایپ کنار گذاشته شد و نسل از رکورد والدین محاسبه می‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 006 · **نوع:** SOURCE_CONFLICT_RESOLVED
- **منابع:** §۹.۳ («نسل از رکورد والدین محاسبه می‌شود … G0×G0→G1 و G2×G1→G2»)؛ صفحه PET-008 پروتوتایپ که کنار انتخاب G1+ عبارت «نسل بعدی» را به‌صورت یک انتخاب نشان می‌دهد.
- **ابهام:** پروتوتایپ نسل را کنار فرم نمایش می‌دهد به‌گونه‌ای که می‌توان آن را ورودی کاربر خواند؛ §۹.۳ آن را خروجی محاسبه سرور می‌داند.
- **تصمیم:** طبق ترتیب منابع، §۹.۳ مقدم است. در فرم هیچ ورودی `generation` وجود ندارد (تست مرورگر نبودش را اثبات می‌کند) و مقدار فقط‌خواندنی `G{n}` از `1 + min(نسل پدر، نسل مادر)` می‌آید. `generation` در فهرست فیلدهای محافظت‌شده ویرایش عمومی است و تلاش برای تغییرش رد می‌شود.
- **اثر:** چیدمان و ترتیب گام‌های پروتوتایپ حفظ شد و فقط معنای فیلد اصلاح شد. صدور کد شجره‌نامه و Pet ID همچنان به PROMPT بعدی تعلق دارد.

## DEC-0038 — گام فرم فقط پس از ذخیره واقعی سرور جلو می‌رود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 006 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۹.۱ («هر گام ذخیره می‌شود و بازگشت، مقادیر واردشده را برمی‌گرداند»)، §۲۳.۴.
- **مسئله واقعی:** نسخه اول دکمه «ادامه» هم‌زمان با submit، گام محلی را جلو می‌برد؛ فرم گام قبل unmount می‌شد و اکشن سرور با فیلدهای خالی اجرا می‌شد. در مرورگر واقعی نتیجه‌اش پرونده‌ای بدون نام، نژاد و جنسیت بود که ظاهراً «ذخیره شده» بود.
- **تصمیم:** گام محلی از `draftStep` ذخیره‌شده در دیتابیس همگام می‌شود، نه از کلیک. اگر ذخیره شکست بخورد، کاربر روی همان گام با پیام خطا می‌ماند.
- **اثر:** ذخیره‌شدن هر گام حالا در تست مرورگر واقعاً بررسی می‌شود (`animal-registration-browser`)؛ پیش از این تنها تست API می‌توانست سبز باشد در حالی که فرم داده را از دست می‌داد.

## DEC-0039 — پرونده متعلق به دیگری همان پاسخ «پیدا نشد» را می‌گیرد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 006 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۲۳.۴ (کنترل مجوز روی سرور برای هر رکورد)، قواعد امنیتی بسته.
- **تصمیم:** `requireOwnedAnimal` برای «رکورد وجود ندارد» و «رکورد مال شما نیست» یک خطای یکسان `NOT_FOUND` می‌دهد و صفحه‌ای با همان متن نمایش داده می‌شود.
- **دلیل:** تفاوت پاسخ، شناسه‌های واقعی را قابل حدس‌زدن می‌کند. پیش از این صفحه پروفایل برای مالک دیگر خطای ۵۰۰ می‌داد که هم نشت اطلاعات بود و هم تجربه خراب.
- **اثر:** تست دیتابیس و تست مرورگر هر دو این رفتار را تثبیت کرده‌اند؛ ویرایش، مدرک خارجی و پروفایل همگی از همین مسیر عبور می‌کنند.

## DEC-0040 — مسیر بازگشت با query string معتبر است و پیش‌نویس فرزند به‌عنوان والد بازاستفاده نمی‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 006 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۸ و §۲۳.۴ (Resume Context)، §۹.۳ (ثبت والد گمشده و بازگشت بدون ساخت رکورد تکراری).
- **مسئله واقعی:** الگوی مسیر بازگشت، `?step=6` را رد می‌کرد؛ و `startDraft` هنگام رفتن به «ثبت والد گمشده» همان پیش‌نویس فرزند را برمی‌گرداند و کاربر با پیام «یک حیوان نمی‌تواند والد خودش باشد» روبه‌رو می‌شد.
- **تصمیم:** مسیر نسبی با query مجاز شد (مسیر مطلق و `//host` همچنان رد می‌شود) و بررسی «فهرست نبودن» فقط روی path انجام می‌شود. `startDraft` گزینه `excludeAnimalId` گرفت تا پیش‌نویس در حال انتظار به‌عنوان پیش‌نویس جدید بازاستفاده نشود.
- **اثر:** رفت‌وبرگشت ثبت والد گمشده در مرورگر واقعی بدون ساخت رکورد تکراری کار می‌کند و کدهای واردشده کاربر حفظ می‌شوند.

## DEC-0041 — رنگ و نشانه‌های ظاهری به‌صورت متن آزاد اعلامی ثبت می‌شوند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 006 · **نوع:** GAP_COMPLETION
- **منابع:** §۹.۱ (فیلدهای فرم ثبت)، §۳ (اختراع نکردن داده‌های مرجع).
- **ابهام:** سند فهرست مرجع رنگ یا الگوی نشانه‌ها را منتشر نکرده است.
- **تصمیم:** این دو فیلد متن آزاد اعلامی مالک هستند و هیچ فهرست ساختگی برای آن‌ها ساخته نشد. نژاد برعکس، از جدول مرجع `reference_breed` می‌آید.
- **اثر:** اگر بعداً فهرست رسمی رنگ منتشر شود، تبدیل آن به فیلد مرجع یک مهاجرت داده است، نه بازنویسی فرم. شماره میکروچیپ هم به‌صورت «اعلامی» جدا نگهداری می‌شود و تا اسکن دامپزشک معتمد رسمی شمرده نمی‌شود.

## DEC-0042 — داده دامپزشکان معتمد از محیط سوپرادمین وارد می‌شود، نه فرم عمومی

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 007 · **نوع:** GAP_COMPLETION
- **منابع:** D01 (Onboarding دامپزشک در فاز بعد)، §۲۱.۲ («Onboarding جدید دامپزشک در MVP ساخته نشود»)، §۱۱.۱ (فقط دامپزشک واجد شرایط و Location تأییدشده نمایش داده شود).
- **ابهام:** Finder به داده دامپزشک و مرکز نیاز دارد، ولی سند هیچ مسیر ورودی برای این داده در MVP تعریف نکرده و ساخت فرم درخواست معتمدشدن ممنوع است.
- **تصمیم:** صفحه `/admin/vets` در محیط سوپرادمین ساخته شد که فقط داده دامپزشک از قبل تأییدشده و مراکز او را ثبت می‌کند. حساب بدون نقش `TRUSTED_VET` پذیرفته نمی‌شود؛ این صفحه نقش نمی‌دهد و گردش تأیید ندارد. صفحه صریحاً می‌گوید فرم درخواست معتمدشدن نیست.
- **اثر:** فهرست دامپزشکان تا ورود داده واقعی خالی می‌ماند و Finder صادقانه «نتیجه‌ای نیست» نشان می‌دهد؛ هیچ دامپزشک نمونه‌ای اختراع نشد. وقتی D01 در فاز بعد اجرا شود، همین جدول‌ها مقصد آن گردش‌اند.

## DEC-0043 — کد نظام دامپزشکی و پروانه Location دو تأیید کاملاً جدا نگهداری می‌شوند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 007 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۱.۱ («کد نظام دامپزشکی و پروانه Location دو تأیید مستقل‌اند»)، §۷.۱، D02.
- **تصمیم:** `vet_profile.council_code` (یکتا) و `vet_location.licence_status` دو ستون روی دو جدول جدا هستند و هیچ‌کدام دیگری را نتیجه نمی‌دهد. شرط حضور در Finder ترکیب هر سه است: نقش فعال، عضویت فعال، و مرکز فعالِ دارای پروانه معتبر با تمام امکانات اجباری.
- **دلیل:** یکی‌کردن این دو یعنی باطل‌شدن پروانه یک مرکز، صلاحیت حرفه‌ای فرد را هم پاک کند — چیزی که سند صریحاً رد می‌کند.
- **اثر:** تست‌ها هر چهار حالت را جدا می‌سنجند: پروانه نامعتبر، مرکز غیرفعال، نقش تعلیق‌شده و عضویت غیرفعال.

## DEC-0044 — امکانات ناقص، مسیر کم‌شده نمی‌سازد؛ مرکز از Finder همان Context حذف می‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 007 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۱.۱ («Location ناقص وارد Finder نمی‌شود؛ قابلیت جزئی آن مسیر جایگزین ایجاد نمی‌کند»)، §۱۲.۴ (نمونه خون در هر دو مسیر اجباری است).
- **تصمیم:** امکانات اجباری هر Context به‌صورت قاعده صریح تعریف شد: میکروچیپ = کاشت + نمونه‌گیری خون، DNA = نمونه‌گیری خون، بارداری = بررسی بارداری. مرکزی که همه امکانات لازم را ندارد اصلاً در نتایج نمی‌آید و شرط آن داخل خود کوئری SQL است، نه فیلتر بعدی.
- **دلیل:** چون §۱۲.۴ نمونه خون را در کاشت و تأیید هر دو اجباری می‌کند، مرکزی که فقط کاشت دارد نمی‌تواند خدمت را تمام کند. پیشنهاد «نصفه» همان مسیر جایگزینی است که سند رد کرده.
- **اثر:** در تست مرورگر، مرکزی که فقط کاشت داشت پس از ثبت هم در Finder نیامد و تنها با تکمیل نمونه‌گیری خون وارد فهرست شد.

## DEC-0045 — انتخاب حیوان و خدمت تا لحظه تأیید در URL می‌ماند، نه در رکورد نیمه‌ساخته

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 007 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۱.۲ (ترتیب انتخاب حیوان‌ها → خدمت هر حیوان → دامپزشک و Location → مرور → ساخت درخواست)، §۸ (Resume).
- **تصمیم:** تا پیش از تأیید در صفحه مرور، هیچ ردیفی ساخته نمی‌شود؛ انتخاب‌ها به‌صورت `animalId:SERVICE` در query منتقل می‌شوند و در سرور دوباره اعتبارسنجی می‌شوند. هر مقدار خراب کنار گذاشته می‌شود، نه حدس زده.
- **دلیل:** رفت‌وبرگشت بین Finder و انتخاب حیوان زیاد است؛ رکورد نیمه‌ساخته یعنی درخواست‌های یتیم و کد مراجعه‌ای که هرگز خواسته نشده. لینک قابل‌اشتراک و دکمه بازگشت هم درست کار می‌کند.
- **اثر:** ساخت درخواست یک عملیات اتمی است: گروه، درخواست هر حیوان و کد هر درخواست در یک تراکنش ساخته می‌شوند.

## DEC-0046 — گروه درخواست فقط نمایش است و هیچ وضعیتی ندارد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 007 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۱.۲ («این گروه‌بندی نباید به رزرو نوبت تبدیل شود. هر حیوان کد و وضعیت مستقل دارد»)، §۱۱.۴.
- **تصمیم:** `vet_visit_batch` هیچ ستون وضعیتی ندارد. وضعیت، کد، مهلت و اصلاح خدمت همگی روی `vet_visit_request` هر حیوان هستند. اصلاح خدمت فقط همان حیوان را SUPERSEDED می‌کند و درخواست جدید در همان گروه ساخته می‌شود.
- **اثر:** تست دیتابیس و تست مرورگر هر دو نشان می‌دهند حیوان دوم گروه پس از اصلاح خدمت حیوان اول، وضعیت و کد دست‌نخورده دارد.

## DEC-0047 — مهلت مراجعه روی خود کد ثبت می‌شود و تغییر تنظیمات عطف به ماسبق نمی‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 007 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۱.۴ («مقدار فعال از دیتابیس خوانده و تاریخ اعتبار کد ثبت و نمایش داده می‌شود … عدد ۲۱ هاردکد نمی‌شود»)، D15، §۲۱.۴.
- **تصمیم:** هنگام صدور، `validity_days`، `settings_version` و `expires_at` روی ردیف کد ذخیره می‌شوند. نمایش مهلت و اعتبارسنجی سرور هر دو از همین ستون می‌خوانند. تغییر تنظیم فقط روی صدورهای بعدی اثر دارد و هیچ کد صادرشده‌ای را جابه‌جا نمی‌کند و هیچ سابقه‌ای را پاک نمی‌کند.
- **اثر:** تست تغییر تنظیم از ۲۱ به ۷ روز نشان می‌دهد کد قبلی دقیقاً همان تاریخ انقضا را نگه می‌دارد و کد بعدی با نسخه جدید صادر می‌شود. تمدید پس از انقضا، کد قبلی را EXPIRED نگه می‌دارد و کد جدید کنارش می‌نشیند.

## DEC-0048 — QR و ورود دستی یک کد و یک مسیر پذیرش‌اند؛ تصویر QR هنوز تولید نمی‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 007 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۱.۲ («QR و ورود دستی دو نمایش از یک کد هستند، نه دو شناسه»)، §۱۱.۳.
- **تصمیم:** یک ستون `code`، یک فرم پذیرش و یک مسیر سروری وجود دارد؛ اسکنر و تایپ دستی هر دو همان مقدار را وارد می‌کنند. تولید تصویر QR در این مرحله انجام نشد چون هیچ کتابخانه‌ای برای آن پیکربندی نشده و افزودن وابستگی جدید تصمیم مستقلی است.
- **اثر:** رفتار مورد نیاز — یکی‌بودن شناسه و یک‌بار مصرف‌شدن آن — کامل و تست‌شده است. تولید تصویر QR روی همان مقدار، یک افزودنی نمایشی برای مرحله بعد است و در محدودیت‌های گزارش آمده است.

## DEC-0049 — پاسخ رد کد هرگز پرونده کس دیگری را لو نمی‌دهد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 007 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۱.۳ («این خطاها نباید به پذیرش حیوان دیگری منتهی شوند»)، §۲۳.۴، قواعد امنیتی بسته.
- **تصمیم:** ترتیب بررسی با دامپزشک و مرکز شروع می‌شود: کدی که برای این میز صادر نشده، پیش از هر چیز به همین دلیل رد می‌شود و متن پاسخ نه نام حیوان دارد، نه مالک و نه اشاره‌ای به اینکه کد جای دیگری معتبر است. اطلاعات حیوان و کاربر فقط پس از پذیرش موفق روی همان درخواست نمایش داده می‌شود.
- **اثر:** در تست مرورگر، صف دامپزشک پیش از پذیرش هیچ نام حیوان یا کاربری نشان نمی‌دهد و در تست دیتابیس، دامپزشک دیگر با کد معتبر همان پاسخ خنثی می‌گیرد.

## DEC-0050 — قاعده «یک میکروچیپ در طول عمر» به‌صورت قید دیتابیس اجرا می‌شود، نه بررسی برنامه‌ای

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 008 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۲.۲، §۱۲.۳ («هر حیوان در طول عمر فقط یک میکروچیپ دارد؛ تعویض، انتقال خودکار، اتصال شماره دوم، overwrite یا حذف سابقه مجاز نیست»)، D08، §۲۳.
- **تصمیم:** دو ایندکس یکتا روی جدول `microchip` — یکی روی `animal_id` و یکی روی `number` — داور نهایی‌اند. هیچ مسیر `update` یا `delete` روی این جدول وجود ندارد؛ ماژول فقط ردیف جدید می‌سازد یا تعارض ثبت می‌کند.
- **دلیل:** بررسی برنامه‌ای می‌تواند داده کهنه بخواند و دو مراجعه هم‌زمان از کنارش رد شوند. با قید دیتابیس، بازنده رقابت خطای یکتایی می‌گیرد و همان لحظه به تعارض ثبت‌شده تبدیل می‌شود.
- **اثر:** تست رقابت دو مراجعه روی یک شماره دقیقاً یک اتصال و یک تعارض `DUPLICATE_NUMBER` می‌سازد. حل تعارض هیچ مسیر بازنویسی ندارد و عمداً هم ساخته نشد.

## DEC-0051 — شماره میکروچیپ ۱۵ رقم استاندارد پذیرفته می‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 008 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۲.۱ («همه به یک فیلد Canonical به نام Microchip Number ختم می‌شوند»)؛ سند قالب شماره را تعیین نکرده است.
- **ابهام:** بدون قالب، هر رشته‌ای می‌توانست ثبت شود و «یکتایی سراسری» بی‌معنا می‌شد.
- **تصمیم:** قالب ISO 11784/11785 یعنی ۱۵ رقم پذیرفته می‌شود؛ فاصله و خط تیره حذف و بقیه رد می‌شود. عدد ساختگی یا الگوی اختراعی اضافه نشد.
- **اثر:** اگر بعداً قالب دیگری اعلام شود، فقط همین یک تابع تغییر می‌کند. شماره‌های تست با پیشوند مشخص و خارج از محدوده صادرشده ساخته می‌شوند.

## DEC-0052 — خواندن، کاشت و خواندن مجدد سه رویداد جدا ثبت می‌شوند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 008 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۲.۲ (خواندن سریال پیش از کاشت → کنترل یکتایی → کاشت → خواندن مجدد → تطبیق → اتصال دائمی).
- **تصمیم:** جدول `chip_procedure` سه لحظه را جدا نگه می‌دارد. اتصال فقط وقتی انجام می‌شود که خواندن دوم با خواندن اول یکی باشد؛ ناهمخوانی، تعارض `SERIAL_MISMATCH` ثبت می‌کند و هیچ اتصالی نمی‌سازد. کنترل یکتایی پیش از کاشت دوباره در لحظه ثبت کاشت اجرا می‌شود.
- **اثر:** «سریال تطبیق داده شد» یک واقعیت درباره دو خواندن ثبت‌شده است، نه یک ادعای یک‌باره. در مرورگر، ناهمخوانی متوقف شد و پس از خواندن درست، اتصال انجام شد.

## DEC-0053 — روش‌های خواندن آداپتور یک فیلد واحدند و آمادگی سخت‌افزار صادقانه گزارش می‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 008 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۲.۱ («نمایش این روش‌ها در UI اثبات آماده‌بودن اتصال سخت‌افزاری نیست»)، ARCHITECTURE_BASELINE (چهار وضعیت آداپتور).
- **تصمیم:** چهار روش (ریدر بلوتوث، ریدر موبایل، بارکد بسته، ورود دستی) فقط برچسب روی همان فیلد Canonical‌اند و روی رکورد ثبت می‌شوند. آداپتور جدید `chip-reader` به رجیستری اضافه شد و تا پیکربندی `CHIP_READER_PROVIDER` وضعیتش `NOT_CONFIGURED` است و صفحه همین را می‌گوید. ورود دستی همیشه در دسترس است.
- **اثر:** Gate مرورگر کل مسیر کاشت را با ورود دستی و بدون هیچ دستگاهی انجام داد؛ هیچ ادعای آماده‌بودن سخت‌افزار ثبت نشده است.

## DEC-0054 — کد رهگیری نمونه فقط پس از نمونه‌گیری واقعی و پس از تعیین‌تکلیف میکروچیپ صادر می‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 008 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۲.۴ («پس از انجام واقعی نمونه‌گیری، Sample Tracking Code یکتا صادر … می‌شود»)، §۱۱.۲ (کد مراجعه پیش از نمونه‌گیری صادر می‌شود).
- **تصمیم:** صدور کد نمونه فقط در همان تراکنشِ ثبت نمونه‌گیری انجام می‌شود و پیش از تعیین‌تکلیف میکروچیپ رد می‌شود. کد مراجعه (`HZ-`) و کد نمونه (`SM-`) دو شناسه با دو پیشوند و دو جدول‌اند و هیچ‌جا یکی نمی‌شوند.
- **اثر:** تست دیتابیس نشان می‌دهد پیش از اتمام کار میکروچیپ هیچ نمونه‌ای وجود ندارد و درخواست دوم نمونه هم کد یدکی نمی‌سازد. در مرورگر، بخش نمونه پیش از ثبت نمونه‌گیری اصلاً کد نشان نمی‌دهد.

## DEC-0055 — نگهداری نمونه بدون انقضا؛ ارسال فقط با دستور مرکز ژنتیک و به‌صورت رویداد روی همان کد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 008 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۲.۴ («نمونه انقضای خودکار ندارد؛ تصمیم قابلیت استفاده با مرکز ژنتیک است … ثبت Shipment یک رویداد روی همان Sample Tracking Code است»)، D07، §۲۱.۳.
- **ابهام:** سند نمی‌گوید چه کسی دقیقاً «دستور ارسال» را صادر می‌کند و با چه فرمی.
- **تصمیم:** حداقل سازوکار ساخته شد: مرکز ژنتیک دستور ارسال صادر می‌کند و دامپزشک نگهدارنده ارسال را ثبت می‌کند. هیچ job، انقضا یا وضعیت زمان‌مبنایی وجود ندارد. تأیید فیش و دریافت در مرکز به PROMPT-010 تعلق دارد و اینجا اختراع نشد.
- **اثر:** ارسال پیش از دستور رد می‌شود و دستور توسط دامپزشک صادر نمی‌شود؛ هر دو تست شده‌اند. کد رهگیری در ارسال تغییر نمی‌کند.

## DEC-0056 — نمونه‌گیری مجدد نمونه قبلی را حفظ می‌کند و هرگز کاشت دوباره نیست

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 008 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۲.۴ («نمونه نامعتبر/ناکافی/خراب/مفقود → نمونه‌گیری مجدد در همان درخواست → حفظ نمونه و کد قبلی در Audit → صدور کد جدید فقط بعد از نمونه‌گیری مجدد. این مسیر کاشت میکروچیپ دوم نیست»).
- **تصمیم:** نمونه قبلی با وضعیت و دلیل خود در جدول می‌ماند و فقط `superseded_by_sample_id` می‌گیرد؛ نمونه جدید ردیف و کد تازه دارد و در همان درخواست ثبت می‌شود. مسیر نمونه‌گیری مجدد هیچ عملیات میکروچیپی ندارد و درخواست تکمیل‌شده فقط برای همین یک کار دوباره باز می‌شود.
- **اثر:** تست‌ها نشان می‌دهند پس از نمونه‌گیری مجدد، رکورد میکروچیپ دست‌نخورده و رویداد کاشت همچنان یکی است، و هر دو کد نمونه با وضعیت‌های خود در فهرست می‌مانند.

## DEC-0057 — تصویر حیوان برای دامپزشک تخصیص‌یافته، فقط در همان پرونده باز می‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 008 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۲.۵ و §۲۱.۱ (نمایش حیوان و تصویر در پرونده مراجعه)، §۲۳.۴، قواعد امنیتی بسته (فایل خصوصی و مجوز سمت سرور).
- **ابهام:** جدول مجوز فایل بر اساس «هدف فایل» است؛ افزودن نقش دامپزشک به آن یعنی هر دامپزشکی تصویر هر حیوانی را ببیند.
- **تصمیم:** یک استثنای رکورد-محور اضافه شد: دامپزشک فقط وقتی تصویر حیوان را می‌بیند که همان پرونده مراجعه به او تخصیص یافته و پذیرش شده باشد. این بررسی به‌صورت پرسش درباره همان فایل انجام می‌شود، نه به‌صورت مجوز روی کل هدف فایل.
- **اثر:** در تست مرورگر، دامپزشک تخصیص‌نیافته هم صفحه را نمی‌بیند و هم درخواست مستقیم فایل با ۴۰۳ رد می‌شود.

## DEC-0058 — تأیید عملیات از روی وضعیت پایدار صفحه خوانده می‌شود، نه از پیام گذرای فرم

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 008 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۲۶ («وجود صفحه Success به معنی تأیید واقعی عملیات بیرونی نیست … وضعیت نتیجه باید با رخداد واقعی هم‌خوان باشد»)، §۸.
- **مسئله واقعی:** پس از هر اقدام موفق، فرم مربوط از صفحه حذف می‌شود و پیام موفقیت داخل آن هم با آن می‌رود.
- **تصمیم:** خود صفحه نتیجه را نشان می‌دهد: کارت «شماره رسمی میکروچیپ»، فهرست نمونه با کد و وضعیت، و در مرحله قبل تأیید تمدید روی خود پرونده. تست‌ها هم روی همین وضعیت پایدار ادعا می‌کنند، نه روی پیام گذرا.
- **اثر:** شواهد تست همان چیزی است که کاربر بعداً هم می‌بیند؛ یک پیام موفقیت گذرا نمی‌تواند تست را سبز کند در حالی که داده ثبت نشده باشد.

## DEC-0059 — پرداخت برگه ثبتی فقط پس از میکروچیپ و نمونه باز می‌شود و مبلغ خدمت دامپزشک در آن نیست

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 009 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۳ («پرداخت برگه ثبتی از مرحله میکروچیپ و نمونه‌گیری جلو نیفتد. مبلغ خدمت دامپزشک با هزینه صدور برگه ثبتی یکی نیست»)، §۵.
- **تصمیم:** تابع `readinessOf` ترتیب مراحل §۱۳ را به‌صورت قاعده اجرا می‌کند: ثبت اولیه، نبود برگه قبلی، میکروچیپ متصل و نمونه قابل استفاده. ساخت Batch پیش از این‌ها با همان دلیل مشخص رد می‌شود. این Batch فقط قلم‌های `fee.registration_sheet_toman` دارد و هیچ مبلغ خدمت دامپزشکی در آن نیست.
- **اثر:** تست‌ها نشان می‌دهند پیش از نمونه‌گیری هیچ Batchی ساخته نمی‌شود و پس از نمونه‌گیری همان حیوان واجد شرایط می‌شود.

## DEC-0060 — شماره برگه و Pet ID محلی و بدون ادعای مرجع بیرونی صادر می‌شوند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 009 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۳، §۲۳.۲؛ سند قالب شماره برگه یا Pet ID را تعیین نکرده و هیچ نهاد شماره‌گذار بیرونی معرفی نشده است.
- **تصمیم:** حداقل پیاده‌سازی سازگار: `RS-` برای شماره برگه و `PET-` برای شناسه رسمی، هر دو با همان الفبای خوانای کدهای دیگر محصول و با ایندکس یکتا. هیچ گواهی یا اعتبار بیرونی ادعا نمی‌شود و آداپتور `document-render` همچنان NOT_CONFIGURED است، بنابراین نسخه چاپی رسمی تولید نمی‌شود و صفحه سند همین را می‌گوید.
- **اثر:** اگر بعداً قالب رسمی اعلام شود، فقط دو تابع تولید شناسه و قالب نمایش تغییر می‌کنند؛ داده صادرشده و یکتایی آن دست‌نخورده می‌ماند.

## DEC-0061 — صدور هر حیوان مستقل است و توقف یکی، صدور بقیه را متوقف نمی‌کند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 009 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۳ قواعد Batch («نقص، رد یا تأخیر یک حیوان نباید وضعیت بقیه را پنهان کند یا صدور حیوان واجد شرایط و پرداخت‌شده را متوقف کند»).
- **تصمیم:** جدول `registration_sheet_item` وضعیت صدور هر حیوان را جدا از وضعیت پرداخت Batch نگه می‌دارد. اثر پرداخت روی هر قلم جداگانه اجرا می‌شود؛ حیوانی که شرایطش کامل نیست BLOCKED با دلیل ثبت می‌شود و بقیه صادر می‌شوند. صفحه هم این دو وضعیت را جدا نشان می‌دهد.
- **اثر:** در تست دیتابیس و در مرورگر، یک حیوان صادر شد و حیوان دیگر همان Batch با دلیل متوقف ماند، بدون آنکه پرداخت تأییدشده زیر سؤال برود.

## DEC-0062 — تنها ویرایش مجاز پس از پرداخت، تلاش دوباره برای صدور است

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 009 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۳ («سازوکار حذف حیوان از Batch یا تغییر مبلغ پس از پرداخت، اگر سیاست موجود ندارد، خودسرانه با بازپرداخت یا پرداخت اضافه تکمیل نشود»)، §۲۲.
- **ابهام:** سند سیاست بازپرداخت یا پرداخت اضافه ندارد.
- **تصمیم:** هیچ حذف قلم، تغییر مبلغ، بازپرداخت یا پرداخت اضافه‌ای ساخته نشد. تنها اقدام مجاز پس از پرداخت، `retryIssuance` است که پس از رفع پیش‌نیاز، همان قلم پرداخت‌شده را صادر می‌کند و هیچ پولی جابه‌جا نمی‌شود؛ snapshot مبالغ دست‌نخورده می‌ماند.
- **اثر:** تست مقایسه می‌کند که مبالغ قلم‌ها پیش و پس از تلاش دوباره دقیقاً یکسان‌اند و تعداد قلم‌ها تغییر نمی‌کند.

## DEC-0063 — صدور بارها قابل اجرا است ولی سند یکی می‌ماند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 009 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۲۲ (Callback تکراری بی‌اثر)، §۱۳، §۲۳.
- **تصمیم:** اثر صدور داخل همان تراکنش تأیید پرداخت اجرا می‌شود و ایندکس یکتا روی `animal_id` جدول `registration_sheet` داور نهایی است. اجرای دوباره اثر — چه از Callback تکراری و چه از تلاش دستی — قلم را ISSUED می‌بیند یا سند موجود را پیدا می‌کند و سند دوم نمی‌سازد.
- **اثر:** تست سه بار تأیید و یک تلاش دوباره را اجرا می‌کند و در پایان دقیقاً یک سند و یک رویداد Audit صدور وجود دارد.

## DEC-0064 — پرداخت لغوشده یا ناموفق دیگر Batch را برای همیشه نمی‌بندد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 009 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۲۶ («پرداخت ناموفق → Retry یا بازگشت؛ پرداخت لغوشده → Draft و مبدأ → بازگشت به Review»)، §۲۲.
- **مسئله واقعی:** `startAttempt` هر Batch با وضعیت CANCELLED را رد می‌کرد، بنابراین یک لغو ساده مسیر پرداخت را برای همیشه می‌بست — هم برای برگه ثبتی و هم برای عضویت.
- **تصمیم:** فقط پرداخت تأییدشده پایانی است. Batch لغوشده یا ناموفق با همان اقلام و همان مبالغ منجمد دوباره قابل پرداخت است و تلاش قبلی به‌عنوان ردیف مستقل در سابقه می‌ماند.
- **اثر:** تست پس از لغو و پس از شکست، پرداخت دوم را انجام می‌دهد و سند صادر می‌شود؛ ۲۴ تست پرداخت و عضویت قبلی هم بدون تغییر سبز ماندند.

## DEC-0065 — فرم ویرایش تنظیمات همین حالا ساخته شد، چون تعرفه بدون آن قابل پیکربندی نبود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 009 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۲۱.۴ (تنظیمات محصول از داده مدیریت‌شده)، D16، دستور «تعرفه‌های نامعلوم واقعیت‌هایی برای پیکربندی‌اند، نه اختراع».
- **تصمیم:** پنل تنظیمات از حالت فقط‌خواندنی خارج شد و برای گروه‌هایی که همان Actor مجاز نوشتن است، فرم ویرایش دارد. مقدار خالی یعنی بازگشت به «تعیین‌نشده»؛ اعتبارسنجی، مجوز گروهی، نسخه و Audit همگی در همان سرویس موجود انجام می‌شوند.
- **دلیل:** بدون این فرم، تعرفه برگه ثبتی هیچ مسیر واقعی ورود نداشت و تنها راه اجرای مسیر پرداخت، نوشتن مستقیم در دیتابیس بود — یعنی همان چیزی که ارزیابی صادقانه را از بین می‌برد.
- **اثر:** تعرفه در Gate مرورگر از همین فرم وارد شد و با پیشوند SYNTHETIC و دلیل ثبت شد. تعرفه واقعی همچنان اعلام نشده است و در محدودیت‌ها ذکر شده. PROMPT-018 می‌تواند همین پنل را گسترش دهد.

## DEC-0066 — مسیر معمول شجره‌نامه از داده موجود شروع می‌شود و هیچ انتخاب مرکزی ندارد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 010 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۴.۱ («در این مسیر معمول، دامپزشک و نمونه‌گیری از نو انتخاب/اجرا نمی‌شوند»)، §۱۴.۲ («عبارت مرکز ژنتیک مقصد اطلاعاتی است؛ Selector مرکز ایجاد نشود»)، D07.
- **تصمیم:** شرط ورود این مسیر «برگه ثبتی صادرشده + نمونه قابل استفاده + Custodian مشخص» است و هر سه از رکورد موجود خوانده می‌شوند. صفحه هیچ فرم انتخاب دامپزشک، نمونه‌گیری یا مرکز ندارد؛ اطلاعات یک مرکز ثابت فقط نمایش داده می‌شود.
- **اثر:** تست‌ها نشان می‌دهند حیوان بدون برگه ثبتی وارد این مسیر نمی‌شود و کد رهگیری نمونه نمایش‌داده‌شده همان کدی است که دامپزشک نگهدارنده ثبت کرده بود.

## DEC-0067 — تا ثبت داده واقعی پرداخت مرکز، مسیر فیش باز نمی‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 010 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۴.۱ گام ۲، §۲۱.۴، D16 و یادداشت خود تنظیم: «داده مالی واقعی. هیچ شماره نمونه یا ساختگی وارد نمی‌شود».
- **ابهام:** بدون نام مرکز و مقصد پرداخت، «واریز مستقیم و آپلود فیش» بی‌معناست؛ از طرفی درج شماره حساب یا کارت ساختگی ممنوع است.
- **تصمیم:** مسیر فیش فقط وقتی باز می‌شود که نام مرکز و دست‌کم یکی از شماره حساب/کارت واقعاً ثبت شده باشد؛ در غیر این صورت صفحه صریح می‌گوید و دکمه غیرفعال است. در تست‌ها مقدار به‌صورت رشته آشکارا آزمایشی (`SYNTHETIC-TEST-ACCOUNT-…`) وارد می‌شود، نه عددی که شبیه حساب واقعی باشد.
- **اثر:** هیچ داده مالی جعلی وارد محصول نشد و مسیر همچنان قابل اجرا و قابل اثبات ماند. تعرفه و حساب واقعی مرکز در محدودیت‌ها به‌عنوان NOT_CONFIGURED گزارش شده است.

## DEC-0068 — تأیید فیش، دستور ارسال را برای همان نگهدارنده هر نمونه صادر می‌کند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 010 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۴.۱ گام‌های ۵ و ۶، §۱۲.۴، D07.
- **تصمیم:** با تأیید فیش، هر نمونه همان فیش از `IN_CUSTODY` به `SEND_INSTRUCTED` می‌رود و اعلان ارسال به دامپزشکی می‌رسد که واقعاً آن نمونه را نگه داشته است. ثبت ارسال همچنان فقط از همان نگهدارنده پذیرفته می‌شود و روی همان کد رهگیری ثبت می‌شود. صدور دستی دستور ارسال از پنل مرکز هم باقی ماند.
- **اثر:** تست نشان می‌دهد فیش ردشده هیچ دستور ارسالی نمی‌سازد، دامپزشک دیگر نمی‌تواند نمونه را ارسال کند، و کد رهگیری در ارسال و دریافت عوض نمی‌شود.

## DEC-0069 — پردازش و ثبت نتیجه هیچ وابستگی به پرداخت صدور در هم‌زیست ندارند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 010 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۴.۲ («پرداخت صدور همزیست شرط شروع پردازش یا نمایش Result نیست»)، فلوچارت §۱۴.۱.
- **تصمیم:** دریافت، پردازش و ثبت نتیجه هیچ شرطی درباره Batch پرداخت ندارند و نتیجه به‌محض ثبت در پروفایل حیوان دیده می‌شود. تنها چیزی که به پرداخت صدور گره می‌خورد، خود سند شجره‌نامه است که در PROMPT بعدی ساخته می‌شود.
- **اثر:** تست دیتابیس و تست مرورگر هر دو نتیجه را بدون هیچ پرداخت صدوری ثبت و نمایش می‌دهند و صفحه صریح می‌گوید فقط صدور سند در انتظار پرداخت است.

## DEC-0070 — خروجی ژنتیکی فقط Parentage Result است و نسخه‌دار می‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 010 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۴.۳ («خروجی موازی با نام DNA Profile یا کد نتیجه جدید اختراع نشود»)، §۱۴.۵.
- **تصمیم:** یک جدول `parentage_result` با `result_version` و پیوند `supersedes_result_id`. فیلدهای فنی حداقلی‌اند: وضعیت، نسخه، یادداشت فنی، زمان پردازش و پیوند نتیجه والدین. هیچ شناسه دوم، پروفایل DNA یا کد نتیجه جداگانه ساخته نشد.
- **اثر:** نتیجه اصلاحی در PROMPT‌های بعدی فقط نسخه بعدی همین رکورد است و نتیجه قبلی حفظ می‌شود.

## DEC-0071 — نتیجه G1+ بدون نتیجه کامل هر دو والد، «در انتظار» می‌ماند و نهایی ثبت نمی‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 010 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۴.۳ («نبود نتیجه یکی از والدین → در انتظار تکمیل نتایج والدین؛ نتیجه ناقص به‌عنوان نتیجه نهایی تأییدشده ثبت نشود»).
- **تصمیم:** برای G0 نتیجه مستقیم به حیوان نسبت داده می‌شود. برای G1+، نتیجه نهایی فقط وقتی ثبت می‌شود که نتیجه FINAL هر دو والد مستقیم قابل Resolve باشد؛ در غیر این صورت همان رکورد با وضعیت `WAITING_PARENT_RESULTS` ثبت می‌شود و بعداً **همان رکورد** نهایی می‌شود، نه رکورد جدید.
- **اثر:** تست کامل زنجیره پدر/مادر/فرزند نشان می‌دهد یک والد کافی نیست، و پس از کامل‌شدن هر دو، همان نتیجه با همان شناسه نهایی می‌شود.

## DEC-0072 — نمونه نامعتبر در مرکز، به مسیر موجود نمونه‌گیری مجدد برمی‌گردد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 010 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۴.۴، §۱۲.۴ («نمونه نامعتبر → نمونه‌گیری مجدد در همان درخواست → حفظ نمونه و کد قبلی در Audit»).
- **تصمیم:** مرکز فقط وضعیت نمونه را غیرقابل‌استفاده ثبت می‌کند؛ نمونه‌گیری مجدد همان مسیر PROMPT-008 است و توسط همان دامپزشک روی همان درخواست انجام می‌شود. مرکز هیچ مسیر نمونه‌گیری ندارد.
- **اثر:** تست نشان می‌دهد پس از ثبت نمونه نامعتبر، ثبت نتیجه ممکن نیست و نمونه قبلی با دلیلش در سابقه می‌ماند.

## DEC-0073 — اختیارات مرکز به همان چهار کار محدود است

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 010 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۴.۲، §۲۱.۳ («مرکز حق تغییر مالکیت، میکروچیپ یا وضعیت مجوز جفت‌گیری را از این پنل ندارد»).
- **تصمیم:** سرویس مرکز فقط بررسی فیش، ثبت دریافت، اعتبارسنجی/پردازش و ثبت نتیجه دارد. هیچ تابعی برای تغییر مالکیت، میکروچیپ یا مجوز در اختیار Context مرکز نیست و تلاش برای آن با همان پاسخ‌های موجود رد می‌شود.
- **اثر:** تست صریحاً نشان می‌دهد مرکز نمی‌تواند پرونده حیوان را ویرایش کند، عملیات میز دامپزشک را انجام دهد یا نمونه‌ای را که نگه نداشته ارسال کند.

## DEC-0074 — صدور شجره‌نامه یک اتصال دو شرطی است و هر کدام دیرتر برسد، کار را تمام می‌کند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 011 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۴.۱ گام‌های ۷ و ۸ و فلوچارت همان بخش، §۱۴.۲ («پرداخت صدور همزیست شرط شروع پردازش یا نمایش Result نیست»).
- **تصمیم:** ورود به Checkout صدور فقط با نتیجه FINAL ممکن است، و اثر صدور داخل تراکنش تأیید پرداخت اجرا می‌شود. اگر نتیجه بعداً نهایی شود، همان قلم پرداخت‌شده با `retryIssuance` صادر می‌شود و هیچ پول جدیدی جابه‌جا نمی‌شود. تأیید فیش مرکز هیچ‌جا جای این پرداخت را نمی‌گیرد و صفحه صریح همین را می‌گوید.
- **اثر:** تست هر دو ترتیب را اجرا می‌کند: نتیجه→پرداخت و پرداخت→نتیجه، و در هر دو دقیقاً یک سند صادر می‌شود.

## DEC-0075 — کد شجره‌نامه محلی و بدون ادعای مرجع بیرونی صادر می‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 011 · **نوع:** GAP_COMPLETION
- **منابع:** §۲۳.۲؛ سند قالب کد شجره‌نامه را تعیین نکرده و هیچ مرجع شماره‌گذار بیرونی معرفی نشده است.
- **تصمیم:** پیشوند `PD-` با همان الفبای خوانای بقیه کدها و ایندکس یکتا. سند به‌صورت رکورد داده‌ای با صفحه امن مالک ارائه می‌شود؛ آداپتور `document-render` همچنان NOT_CONFIGURED است و نسخه چاپی رسمی تولید نمی‌شود.
- **اثر:** کد شجره‌نامه روی حیوان هم ثبت می‌شود و همان کدی است که در PROMPT-006 برای Resolve نسب استفاده می‌شود.

## DEC-0076 — نتیجه اصلاحی، سند صادرشده را بازنویسی یا باطل نمی‌کند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 011 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۴.۵ («نتیجه اصلاحی نسخه جدید است و نتیجه قبلی و ارتباط آن با اعتراض در تاریخچه حفظ می‌شوند»)، §۱۹ (حفظ پروفایل‌های تاریخی)، §۲۶.
- **ابهام:** سند نمی‌گوید با سندی که پیش از اصلاح صادر شده چه باید کرد.
- **تصمیم:** محافظه‌کارانه‌ترین رفتار سازگار: سند صادرشده دست‌نخورده می‌ماند با همان `issued_from_result_version`، و فقط یک یادداشت کنارش ثبت و به مالک اعلان داده می‌شود. هیچ بازنویسی، ابطال، صدور خودکار سند جدید یا لغو مجوزی انجام نمی‌شود.
- **اثر:** تست نشان می‌دهد پس از اصلاح، شناسه، کد، منبع صدور و تاریخ سند دقیقاً همان‌اند و فقط یادداشت و اعلان اضافه شده است. تصمیم درباره صدور سند جدید پس از اصلاح، تصمیم مالک محصول است و اینجا اختراع نشد.

## DEC-0077 — اعتراض فقط یک درخواست است، نه اجازه ویرایش نتیجه

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 011 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۴.۵، D19 («اعتراض، مجوز ویرایش مستقیم نتیجه رسمی توسط کاربر نیست. مهلت یا هزینه مستقل تازه برای اعتراض تعیین نشده است»).
- **تصمیم:** فرم اعتراض فقط متن کاربر را می‌نویسد؛ هیچ مسیری برای تغییر نتیجه از سمت کاربر وجود ندارد. هیچ هزینه یا مهلتی برای اعتراض ساخته نشد و ثبت اعتراض به‌تنهایی نمونه‌گیری مجدد را شروع نمی‌کند. برای یک نتیجه فقط یک اعتراض باز ممکن است.
- **اثر:** تست نشان می‌دهد مالک نمی‌تواند به اعتراض خودش پاسخ دهد و نتیجه مورد اعتراض پاک نمی‌شود.

## DEC-0078 — درخواست ارسال پستی فقط ثبت درخواست است و به سند صادرشده گره می‌خورد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 011 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۴.۶، D17 («ارسال پستی در فاز اول فقط ثبت درخواست است»)، §۶.۲ (اختیاری‌بودن نشانی در KYC).
- **تصمیم:** جدول `postal_request` هیچ ستون تعرفه، برچسب، کد رهگیری، شرکت پستی یا وضعیت تحویل ندارد و هیچ وضعیت جریانی هم ندارد. ثبت درخواست فقط پس از احراز مالکیت همان سند صادرشده ممکن است. نشانی از داده ذخیره‌شده کاربر پیش‌پر می‌شود ولی خالی‌بودن آن هرگز مانع نیست، چون نشانی روی خود درخواست گرفته می‌شود.
- **اثر:** صفحه تأیید صریح می‌گوید ثبت درخواست به معنی ارسال واقعی سند نیست، و تست وجودنداشتن هر فیلد حمل‌ونقل را بررسی می‌کند.

## DEC-0079 — تأیید عملیات از وضعیت پایدار خوانده می‌شود، نه پیام گذرای فرم

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 011 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۲۶، ادامه DEC-0058.
- **مسئله واقعی:** پس از ثبت اعتراض، فرم اعتراض از صفحه حذف می‌شود (چون اعتراض باز وجود دارد) و پیام موفقیت هم با آن می‌رود.
- **تصمیم:** خود صفحه نتیجه را نشان می‌دهد: اعتراض ثبت‌شده در فهرست پرونده حیوان با وضعیت خودش. تست‌ها روی همین وضعیت پایدار ادعا می‌کنند.
- **اثر:** شواهد تست همان چیزی است که کاربر بعداً هم می‌بیند.

## DEC-0080 — نقش پرورش‌دهنده با تأیید کنل فعال می‌شود و شرط ورود به فرم نیست

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 012 · **نوع:** SOURCE_CONFLICT_RESOLVED
- **منابع:** §۱۵.۱ («رابطه دقیق فعال‌شدن نقش با ثبت کنل از رفتار مصوب موجود حفظ شود؛ وجود Role در معماری نباید Gate جدیدی خارج از فلو ثبت کنل ایجاد کند»)، §۱۵.۲ (پیش‌نیازها فقط عضویت فعال و حداقل یک برگه ثبتی).
- **مسئله واقعی:** مسیر `/kennels` در PROMPT-003 فقط برای Context پرورش‌دهنده باز بود؛ یعنی برای رسیدن به فرم ثبت کنل باید نقشی را می‌داشتید که فقط با تأیید همان کنل به دست می‌آید.
- **تصمیم:** کم‌محدودکننده‌ترین رفتار سازگار: `/kennels` از Context عمومی باز است و تأیید انجمن است که نقش `BREEDER` را فعال می‌کند. محیط اختصاصی `/breeder` همچنان نقش می‌خواهد.
- **اثر:** تست مسیرها به‌روزرسانی شد و Gate مرورگر کل مسیر را از حساب بدون نقش اجرا می‌کند؛ پس از تأیید، Role Switcher واقعاً «پرورش‌دهنده» را نشان می‌دهد.

## DEC-0081 — پرداخت ثبت کنل، تأیید کنل نیست

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 012 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۵.۲ (فرم → نژادها → Review → پرداخت → ارسال → در حال بررسی)، §۲۲.
- **تصمیم:** وضعیت `READY_TO_SUBMIT` بین پرداخت تأییدشده و ارسال کاربر قرار گرفت. پرداخت هیچ پرونده‌ای را وارد صف انجمن نمی‌کند و تأیید نهایی همیشه با انجمن است. برای «فعال‌سازی نقش» هیچ پرداخت جداگانه‌ای ساخته نشد؛ تنها پرداخت این مسیر، `fee.kennel_registration_toman` است.
- **اثر:** تست نشان می‌دهد پرونده پرداخت‌شده تا ارسال در صف نیست و در کل مسیر فقط یک Batch پرداخت کنل وجود دارد.

## DEC-0082 — اصلاح پرونده کنل پرداخت دوباره نمی‌خواهد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 012 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۵.۲ (حالت «نیازمند اصلاح»)، §۲۶ («نیازمند اصلاح → داده و فایل معتبر قبلی → اصلاح همان پرونده و ارسال دوباره»).
- **ابهام:** سند نمی‌گوید اصلاح، پرداخت جدید لازم دارد یا نه.
- **تصمیم:** کم‌محدودکننده‌ترین رفتار سازگار: همان پرداخت تأییدشده معتبر می‌ماند، داده و نژادهای انتخاب‌شده حفظ می‌شوند و ارسال دوباره بدون پرداخت جدید انجام می‌شود.
- **اثر:** تست شمارش می‌کند که در کل چرخه اصلاح، دقیقاً یک Batch پرداخت کنل ساخته شده است.

## DEC-0083 — فیلدهای کنل از همان چیزی که سند نام برده ساخته شد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 012 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۵.۲ جدول داده («نام کنل و فیلدهای مصوب مطابق پروتوتایپ»؛ «نشانی و Location کنل اجباری برای Submit»)، §۲۴؛ Figma فقط‌خواندنی است.
- **ابهام:** فهرست کامل فیلدهای مصوب پروتوتایپ در دسترس این اجرا نبود.
- **تصمیم:** فقط فیلدهایی ساخته شد که خود سند نام می‌برد — نام کنل، نشانی و موقعیت — به‌علاوه چند فیلد اختیاری هم‌خانواده (نام لاتین، تلفن، استان/شهر، مختصات، توضیح). هیچ فیلد اجباری تازه‌ای اختراع نشد و هیچ آپلود مدرکی اضافه نشد.
- **اثر:** اگر فهرست دقیق پروتوتایپ بعداً در دسترس باشد، افزودن فیلد اختیاری یک تغییر کوچک است؛ قاعده اجباری‌ها همان سند است.

## DEC-0084 — ویرایش نژادها پس از تأیید، نه پرداخت می‌سازد و نه بررسی دوباره

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 012 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۵.۳ («تغییر نژادها در نسخه فعلی پرداخت یا Review مجدد ایجاد نمی‌کند و Before/After، Actor و زمان آن ثبت می‌شود»).
- **تصمیم:** افزودن و حذف نژاد از همان فهرست مرجع انجام می‌شود، حذف آخرین نژاد رد می‌شود، وضعیت کنل تغییر نمی‌کند و هر تغییر با فهرست قبلی و بعدی در Audit ثبت می‌شود. در حالت «در حال بررسی» فهرست قفل است تا بررسی روی همان داده انجام شود.
- **اثر:** تست دیتابیس و تست مرورگر هر دو نشان می‌دهند پس از ویرایش، کنل همچنان «تأییدشده» است، در صف انجمن نمی‌آید و Batch جدیدی ساخته نمی‌شود.

## DEC-0085 — مسیر رسمی مجوز، موجودیت و شناسه خودش را دارد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 013 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۶ («دو مسیر باید شناسه، وضعیت و Route قابل‌تفکیک داشته باشند»؛ «پرداخت یا Submit این مسیر نباید کاربر را وارد Declaration شخصی بدون پرداخت کند»)، §۲۰، D12، A-015.
- **تصمیم:** جدول `mating_permit` با وضعیت‌های خودش، شماره مجوز `MP-…` و مسیرهای `/mating/permits/*` ساخته شد؛ سرویس پرداخت `MATING_PERMIT` فقط همین پرونده را جلو می‌برد. هیچ تابع، دکمه یا Redirectی از این مسیر به Declaration وجود ندارد و داشبورد هر دو خدمت را با دو ورودی جدا نشان می‌دهد.
- **اثر:** تست مرورگر وجود هم‌زمان و جدایی `/mating/permits/new` و `/declaration/new` را بررسی می‌کند و تست دیتابیس نشان می‌دهد قلم پرداخت با `targetType = MATING_CASE` و کلید تعرفه مجوز ثبت می‌شود.

## DEC-0086 — Resolve پیش از دعوت، با نام کوتاه‌شده مالک

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 013 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۶ گام‌های ۲ تا ۴ (کد شجره‌نامه → Resolve حیوان و کاربر → دعوت و تأیید)، §۲۳.۳ (حداقل افشای داده).
- **ابهام:** سند نمی‌گوید در گام Resolve چه مقدار از هویت طرف مقابل نمایش داده شود.
- **تصمیم:** دکمه دعوت فقط پس از Resolve موفق ظاهر می‌شود و نام مالک به شکل «نام + حرف اول نام خانوادگی» نمایش داده می‌شود؛ هیچ شماره تماس، کد ملی یا نشانی افشا نمی‌شود. حساب طرف مقابل همیشه از مالک واقعی همان حیوان خوانده می‌شود و هرگز از فرم پذیرفته نمی‌شود.
- **اثر:** کد اشتباه هیچ دعوتی نمی‌سازد (تست مرورگر) و در تست دیتابیس، `counterpartyAccountId` برابر مالک واقعی حیوان Resolve‌شده است.

## DEC-0087 — پرداخت مجوز فقط مرحله ارسال را باز می‌کند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 013 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۶ گام‌های ۷ تا ۹ (پرداخت پیش از Submit نهایی؛ بررسی انجمن؛ صدور مجوز)، §۲۲، §۲۶.
- **تصمیم:** وضعیت `READY_TO_SUBMIT` بین پرداخت تأییدشده و ارسال کاربر قرار گرفت. اثر پرداخت (`markPermitPaid`) هیچ شماره مجوزی نمی‌سازد؛ شماره فقط در تصمیم «صدور» انجمن و دقیقاً یک بار ساخته می‌شود و تصمیم دوباره روی پرونده صادرشده رد می‌شود. بازگشت برای اصلاح، پرداخت تأییدشده را نگه می‌دارد و ارسال دوباره پرداخت جدید نمی‌خواهد.
- **اثر:** تست دیتابیس بررسی می‌کند که پس از پرداخت `permitNo` هنوز خالی است، ارسال پیش از پرداخت رد می‌شود و چرخه اصلاح، Batch جدیدی نمی‌سازد.

## DEC-0088 — توافق تقسیم فقط از نظر ساختار اعتبارسنجی می‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 013 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۶ گام ۵ (سهم ثابت، درصدی یا ترکیبی)، §۱۹ («Allocation Rule پیش از تولد، مالکیت یک توله مشخص را نهایی نمی‌کند»).
- **ابهام:** سند قاعده گرد کردن و نگاشت توافق به توله‌های واقعی را در این مرحله تعیین نمی‌کند.
- **تصمیم:** فقط ساختار بررسی می‌شود — مجموع درصدها ۱۰۰، سهم ثابت عدد صحیح نامنفی، حالت ترکیبی هر دو نیمه را لازم دارد — و هیچ‌جا تعداد توله‌ها فرض نمی‌شود. تخصیص واقعی و گرد کردن به مرحله پس از تولد و تأیید دوطرفه (§۱۹) واگذار شد و همین جمله در هر صفحه‌ای که قاعده را نشان می‌دهد چاپ می‌شود.
- **اثر:** تست‌های دامنه و مرورگر رد شدن درصدهای ناسازگار و وجود همان جمله را بررسی می‌کنند.

## DEC-0089 — درگاه Cooldown بدون تاریخ تأییدشده هشدار نمی‌سازد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 013 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۷.۲ («نبود سابقه تأییدشده، تاریخ مبنا نمی‌سازد»)، D06، A-017؛ محاسبه نهایی تاریخ به PROMPT-014 تعلق دارد.
- **تصمیم:** `cooldownAdvisory` در همین مرحله به تنظیمات `cooldown.male_days` و `cooldown.female_months` وصل شد اما تاریخ مبنا را فقط از تاریخ‌های جفت‌گیری تأییدشده دوطرفه می‌گیرد؛ چون چنین رکوردی هنوز وجود ندارد، خروجی همیشه «بدون هشدار» است و صفحه صریحاً همین را می‌گوید. هشدار، هر وقت ساخته شود، ادامه مسیر را نمی‌بندد.
- **اثر:** هیچ تاریخ ساختگی ساخته نشد و اتصال آماده است؛ تست دیتابیس نبود هشدار و تست مرورگر متن صریح آن را بررسی می‌کند.

## DEC-0090 — هر اعلام تاریخ یک نسخه است و تأیید به همان نسخه گره می‌خورد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 014 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۷.۱ («اصلاح، نسخه جدید است؛ رکورد قبلی حذف یا overwrite نمی‌شود… نسخه اصلاح‌شده دوباره باید برای تأیید طرف مقابل ارسال شود»)، §۲۶.
- **تصمیم:** جدول `mating_date_declaration` فقط append می‌شود: هر اعلام نسخه ترتیبی خودش را در همان پرونده دارد (ایندکس یکتای پرونده+نسخه) و اصلاح، نسخه تازه می‌سازد و نسخه قبلی را `SUPERSEDED` می‌کند بدون آنکه مقدار یا یادداشتش تغییر کند. تأیید همیشه با `declarationId` و `expectedVersion` انجام می‌شود و شرط `status = 'PROPOSED'` داخل همان `UPDATE` است؛ بنابراین تأییدی که برای نسخه قبلی باز مانده بود، نسخه جدید را تأیید نمی‌کند.
- **اثر:** تست Gate هر دو شکل ارسال کهنه از مرورگر (شناسه نسخه قدیمی و شماره نسخه قدیمی) را رد می‌کند و نشان می‌دهد مقدار و یادداشت نسخه قدیمی دست‌نخورده مانده است.

## DEC-0091 — پاسخ با تاریخ متفاوت، DATE_CONFLICT با هر دو مقدار می‌سازد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 014 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۷.۱ («تاریخ متفاوت DATE_CONFLICT می‌سازد و هر دو مقدار دیده می‌شوند»).
- **ابهام:** سند نمی‌گوید مغایرت خودش رکورد جداگانه است یا وضعیت روی همان رکورد.
- **تصمیم:** کم‌محدودکننده‌ترین شکل سازگار: نسخه پاسخ‌داده‌شده `CONFLICTED` می‌شود و نسخه تازه با `conflictsWithId` به آن اشاره می‌کند. هیچ مقداری پاک نمی‌شود، هر دو تاریخ در همان فهرست دیده می‌شوند و حل مغایرت با تأیید یکی از دو نسخه انجام می‌شود.
- **اثر:** Audit مغایرت هر دو مقدار را در `before`/`after` نگه می‌دارد و تست مرورگر وجود هم‌زمان دو تاریخ در فهرست را بررسی می‌کند.

## DEC-0092 — مبنای رسمی، جدیدترین تاریخ تأییدشده دوطرفه در همه پرونده‌های همان حیوان است

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 014 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۷.۱ («جدیدترین تاریخ دوطرفه CONFIRMED مبنای Cooldown و Timeline هر دو حیوان است»)، §۱۷.۳ (رکورد شخصی مبنای رسمی را عوض نمی‌کند)، §۱۰.
- **تصمیم:** `latestConfirmedDateOfAnimal` فقط ردیف‌های `CONFIRMED` را می‌خواند، روی همه پرونده‌های مجوزی که حیوان در آن نر یا ماده است، و بیشترین `mated_on` را برمی‌گرداند — نه جدیدترین ثبت، بلکه جدیدترین تاریخ جفت‌گیری. اعلام تأییدنشده، حتی اگر تاریخش جدیدتر باشد، مبنا نمی‌شود و هیچ رکورد شخصی‌ای در این کوئری راه ندارد.
- **اثر:** تست Gate با سه اعلام (دو تأییدشده و یکی تأییدنشده جدیدتر) نشان می‌دهد مبنا همان تاریخ تأییدشده جدیدتر است، و هر دو حیوان همان مبنا و همان رویداد Audit را می‌گیرند.

## DEC-0093 — حساب دقیق بازه: روز پایانی خارج از بازه است و شش ماه تقویمی با Clamp پایان ماه

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 014 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۷.۲ («۱۴ روز» برای نر، «۶ ماه» برای ماده؛ «شش ماه نباید بدون سیاست مصوب به عدد دلخواه روز تبدیل شود»)، DEC-0004/DEC-0005 (تقویم محصول).
- **ابهام:** سند مرز دقیق («روز چهاردهم داخل بازه است یا خارج») و رفتار ۳۱ام در ماه‌های کوتاه را تعیین نمی‌کند.
- **تصمیم:** تاریخ‌ها Civil Date بدون ساعت و بدون منطقه‌اند و «امروز» یک بار و بر پایه UTC ساخته می‌شود؛ مقایسه، مقایسه رشته‌ای `YYYY-MM-DD` است، پس ساعت محلی هیچ حیوانی را یک روز جابه‌جا نمی‌کند. پایان بازه نر `base + 14 روز` و پایان بازه ماده `base + 6 ماه تقویمی` است و شرط داخل بازه `today < endsOn` است؛ یعنی خودِ روز پایان دیگر داخل بازه نیست. جمع ماه تقویمی با Clamp به آخرین روز معتبر ماه مقصد انجام می‌شود: ۳۱ مرداد‌گونه (۳۱ اوت) + ۶ ماه = ۲۸ فوریه و در سال کبیسه ۲۹ فوریه، هرگز ۳ مارس. شش ماه هیچ‌جا به عدد روز تبدیل نمی‌شود.
- **اثر:** Gate `cooldown-boundaries` مرزهای روز ۱۳/۱۴/۱۵، عبور از پایان سال، ۲۹ فوریه، Clamp ماه‌های ۳۰ روزه و رفتار روز پایانی را تثبیت می‌کند.

## DEC-0094 — هشدار در لحظه «ادامه دادن» ثبت می‌شود، نه در لحظه نمایش

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 014 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۷.۲ («هشدار و ادامه آگاهانه در Audit ثبت شود»؛ «CTA ادامه حذف یا قفل نمی‌شود»).
- **ابهام:** سند نمی‌گوید «ثبت هشدار» یعنی ثبت هر بار نمایش یا ثبت ادامه دادن.
- **تصمیم:** رویداد `MATING_COOLDOWN_CONTINUED` دقیقاً وقتی نوشته می‌شود که کاربر با وجود هشدار کاری را انجام دهد — شروع پرونده مجوز تازه یا ثبت تاریخ جدید — و شامل جنسیت، تاریخ مبنا و پایان بازه هر حیوان است. رندر صفحه چیزی نمی‌نویسد تا Audit پر از رویداد بی‌معنی نشود.
- **اثر:** تست Gate نشان می‌دهد بدون هشدار هیچ رویدادی ثبت نمی‌شود، با هشدار دقیقاً یک رویداد برای هر اقدام ثبت می‌شود، و پرونده مجوز تازه با وجود هشدار باز می‌شود.

## DEC-0095 — رکورد کاربر و رکورد دامپزشک دو جدول مستقل نسخه‌دارند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 015 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۸.۱ («اعلام کاربر UNVERIFIED است»؛ «نبود تأیید دامپزشک نقص یا خطای پرونده نیست»)، §۱۸.۳ (جدول دو رکورد مستقل)، D08، D09.
- **تصمیم:** `pregnancy_declaration` (اعلام مالک با نسخه، اعلام‌کننده و زمان) و `vet_pregnancy_result` (نتیجه مستقل با نام دامپزشک، کد نظام، زمان و Location معاینه) دو جدول جدا هستند و هیچ کدام دیگری را به‌روزرسانی یا حذف نمی‌کند. اصلاح در هر دو سمت نسخه تازه می‌سازد و نسخه قبلی دست‌نخورده می‌ماند. هیچ مسیری در این دو فایل به وضعیت مجوز نمی‌نویسد.
- **اثر:** تست Gate پس از ثبت نتیجه دامپزشک بررسی می‌کند که اعلام مالک، تعداد پرونده‌های توله و وضعیت و نسخه مجوز هیچ‌کدام تغییر نکرده‌اند.

## DEC-0096 — اتصال مراجعه بارداری به پرونده با یک رکورد واسط

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 015 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۸.۲ (Finder در Context Pregnancy → دامپزشک و Location کامل → درخواست تخصیص‌یافته → صف همان دامپزشک)، §۱۱.
- **ابهام:** سند نمی‌گوید ارتباط «درخواست مراجعه §۱۱» با «پرونده مجوز §۱۶» چگونه نگهداری شود.
- **تصمیم:** همان درخواست مراجعه موجود استفاده می‌شود و رکورد `pregnancy_check` فقط می‌گوید این مراجعه به کدام پرونده تعلق دارد؛ اتصال را خودِ مالک انجام می‌دهد و فقط برای حیوان ماده همان پرونده مجاز است. صف عمومی ساخته نشد: ثبت نتیجه فقط از پنل دامپزشکِ تخصیص‌یافته، پس از پذیرش کد و از مرکز فعال با پروانه معتبر ممکن است. انقضای کد مراجعه فقط همان کد را باطل می‌کند و اعلام کاربر یا مجوز را دست نمی‌زند.
- **اثر:** تست‌ها نشان می‌دهند مالک و شخص ثالث نمی‌توانند نتیجه ثبت کنند و مراجعه‌ای که به پرونده وصل نشده، نتیجه‌ای در آن پرونده نمی‌نویسد.

## DEC-0097 — مغایرت فقط وضعیت نمایشی و اعلان خنثی است

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 015 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۸.۴ («هر دو مقدار با برچسب خنثی «مغایرت با اعلام مالک»»؛ «مغایرت به معنی تقلب، رد درخواست، اختلاف حقوقی یا نیاز به تأیید طرف مقابل نیست»).
- **تصمیم:** مغایرت هیچ ستون وضعیتی نمی‌سازد؛ از مقایسه دو رکورد محاسبه و نمایش داده می‌شود. رویداد `PREGNANCY_RECORDS_MISMATCH` شناسه و نسخه و مقدار هر دو رکورد را ثبت می‌کند و اعلان با همان Resume به همان پرونده می‌رود. اگر مقادیر برابر باشند، اعلان مغایرت ارسال نمی‌شود.
- **اثر:** تست Gate متن اعلان («اختلاف حقوقی نیست») و ارجاع هر دو رکورد در Audit را بررسی می‌کند و پس از اصلاح نتیجه دامپزشک، وضعیت مغایرت خودبه‌خود برداشته می‌شود.

## DEC-0098 — راهبرد آشتی نسخه‌ها در اصلاح تعداد: افزایش فقط کمبود را می‌سازد، کاهش پرونده مشخص را کنار می‌گذارد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 015 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۹.۲ («کاهش تعداد، دستور حذف خودکار پروفایل، مالکیت یا سند قبلی نیست»؛ «هر اصلاح نسخه جدیدی با Actor، زمان و علت»)، D18.
- **ابهام:** سند راهبرد دقیق تطبیق تعداد جدید با پرونده‌های موجود را تعیین نمی‌کند.
- **تصمیم:** افزایش فقط به اندازه کمبود پرونده تازه می‌سازد و هیچ پرونده موجودی را دست نمی‌زند. کاهش بدون مشخص‌کردن پرونده‌های مشمول رد می‌شود؛ پرونده انتخاب‌شده حذف نمی‌شود بلکه با وضعیت `WITHDRAWN`، شماره نسخه اصلاح و علت ثبت می‌شود و در فهرست باقی می‌ماند. وضعیت `WITHDRAWN` عمداً از `DECEASED` جداست: اولی یعنی گزارش اولیه اشتباه بوده، دومی یعنی توله زنده به دنیا آمده و بعد مرده است.
- **اثر:** تست Gate رد شدن کاهش بدون انتخاب، باقی‌ماندن ردیف با علت، و ساخت‌نشدن پرونده اضافی در افزایش را بررسی می‌کند.

## DEC-0099 — تعداد گزارش‌شده در تولد و تعداد زنده فعلی دو عدد جداگانه نمایش داده می‌شوند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 015 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۹.۲ («تعداد پروفایل‌های تاریخی لزوماً با تعداد فعلی توله‌های زنده برابر نیست»؛ «مشخص‌بودن توله مرتبط با رویداد، پیگیری همین اختلاف را ممکن می‌کند»).
- **تصمیم:** `litterView` چهار عدد جدا برمی‌گرداند: زنده و مرده در گزارش اولیه، تعداد پرونده‌های موجود و تعداد زنده فعلی. مرگ پس از تولد فقط عدد آخر را تغییر می‌دهد و رویداد Audit آن، شناسه توله و هر دو عدد را ثبت می‌کند. نتیجه دامپزشک هیچ‌کدام از این اعداد را بازنویسی نمی‌کند.
- **اثر:** صفحه زایمان همین چهار عدد را با برچسب صریح نشان می‌دهد و تست‌های دیتابیس و مرورگر پس از ثبت مرگ، ثابت‌ماندن «۲ زنده در گزارش اولیه» و کاهش «زنده در حال حاضر» را بررسی می‌کنند.

## DEC-0100 — تخصیص نسخه‌دار با تأیید هر دو طرف؛ تأیید نسخه قبلی به نسخه جدید منتقل نمی‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 016 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۹.۳ («Allocation فقط با تأیید هر دو طرف FINAL می‌شود»؛ «تغییر یا رد تخصیص نسخه جدید می‌سازد و هر دو طرف باید نسخه جدید را تأیید کنند»)، D18.
- **تصمیم:** هر پیشنهاد یک ردیف `puppy_allocation` با نسخه مستقل است و تأییدها در `allocation_approval` به همان نسخه گره می‌خورند؛ نسخه جدید با صفر تأیید شروع می‌شود، پس تأیید نسخه قبلی هرگز داده جدید را تأیید نمی‌کند. پاسخ با `expectedVersion` بسته می‌شود و پاسخ کهنه با VERSION_STALE رد می‌شود. FINAL فقط وقتی ثبت می‌شود که هر دو طرفِ واقعی همان نسخه را تأیید کرده باشند.
- **اثر:** تست Gate نشان می‌دهد یک تأیید تنها، تأیید دوباره همان شخص، و تأیید شخص ثالث هیچ‌کدام نسخه را نهایی نمی‌کنند و پس از تغییر پیشنهاد، تأییدهای قبلی صفر می‌شوند.

## DEC-0101 — نسخه ردشده در تاریخچه «ردشده» می‌ماند، نه «جایگزین‌شده»

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 016 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۹.۳ («اختلاف → انتظار توافق خارج از سامانه → ثبت Allocation اصلاح‌شده»؛ «هم‌زیست داوری یا تعیین خودکار برنده اختلاف نمی‌کند»)، §۲۶.
- **ابهام:** سند نمی‌گوید وضعیت نسخه ردشده پس از ثبت نسخه بعدی چه باشد.
- **تصمیم:** فقط نسخه‌ای که هنوز در انتظار پاسخ است با پیشنهاد جدید `SUPERSEDED` می‌شود؛ نسخه‌ای که رد شده، `REJECTED` می‌ماند تا معلوم بماند آن نسخه چگونه بسته شده است. هیچ مسیر داوری، رأی‌شکنی یا تعیین خودکار برنده ساخته نشد و متن «هم‌زیست داوری نمی‌کند» روی صفحه و در اعلان رد است.
- **اثر:** تست Gate پس از رد و ثبت نسخه اصلاح‌شده، وجود هم‌زمان یک نسخه `REJECTED` و یک نسخه در انتظار را بررسی می‌کند.

## DEC-0102 — کارت توله فقط با تخصیص نهایی و پرداخت، و مستقل از برگه ثبتی

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 016 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۹.۴ (مجوز صادرشده → Birth/Litter → توله زنده → Allocation نهایی → پرداخت Batch → صدور مستقل)، §۲۲، D16.
- **تصمیم:** واجد شرایط بودن هر توله سمت سرور و به‌ازای همان توله محاسبه می‌شود و دلیل هر قفل روی صفحه می‌آید. برگه ثبتی توله پیش‌نیاز نیست و در مدل داده، `puppy_card` جدول جداگانه‌ای با شماره `PC-…` است که با برگه ثبتی، شجره‌نامه و نتیجه ژنتیک یکی نیست. پرداخت پیش از صدور است و خودِ پرداخت هیچ Gateای را دور نمی‌زند: در لحظه تأیید پرداخت، شرط تخصیص نهایی دوباره بررسی می‌شود.
- **اثر:** تست‌ها نشان می‌دهند با یک تأیید، پرداخت اصلاً باز نمی‌شود، و پس از نهایی‌شدن، کارت بدون هیچ برگه ثبتی برای همان توله صادر می‌شود.

## DEC-0103 — برای توله فوت‌شده کارت تازه صادر نمی‌شود، ولی کارت صادرشده حذف نمی‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 016 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۹.۲ («کاهش تعداد، دستور حذف خودکار پروفایل، مالکیت یا سند قبلی نیست»)، §۱۹.۴ («توله زنده ثبت‌شده» به‌عنوان پیش‌نیاز صدور).
- **ابهام:** سند تکلیف صدور کارت برای توله‌ای که پس از تولد مرده است را صریح نمی‌کند.
- **تصمیم:** محافظه‌کارانه‌ترین خوانش سازگار: صدور تازه برای توله‌ای که مرگش ثبت شده انجام نمی‌شود و دلیلش صریح نمایش داده می‌شود؛ اما کارت، مالکیت و سوابقی که پیش از مرگ صادر شده‌اند دست‌نخورده می‌مانند و هیچ سندی پس‌ازآن حذف یا باطل نمی‌شود. اگر بعداً سیاست دیگری تصویب شود، تغییر آن فقط در همین یک شرط است.
- **اثر:** تست Gate صدور کارت، سپس ثبت مرگ همان توله، و بعد رد صدور تازه با حفظ کارت قبلی را بررسی می‌کند.

## DEC-0104 — شرط «توله با تخصیص نهایی» در §۵ از داده واقعی خوانده شد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 016 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۵ (ماتریس دسترسی)، §۱۹.۳، §۱۹.۴.
- **تصمیم:** `puppiesWithFinalAllocation` که تا این مرحله صادقانه صفر بود، حالا از تخصیص‌های `FINAL` روی مجوزهای صادرشده و به‌نام همان حساب شمرده می‌شود؛ پیشنهاد یا تأیید یک‌طرفه در این شمارش نمی‌آید.
- **اثر:** کارت Puppy در داشبورد فقط پس از نهایی‌شدن دوطرفه باز می‌شود و تست Gate همین گذار را بررسی می‌کند.

## DEC-0105 — حیوان طرف مقابل با شناسه رکورد موجود انتخاب می‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 017 · **نوع:** GAP_COMPLETION
- **منابع:** §۲۰ («انتخاب حیوان موجود طرف مقابل»؛ «حیوان دستی خارج از دیتابیس پذیرفته نشود»؛ «برگه ثبتی و شجره‌نامه شرط این سرویس نیستند»)، §۲۳.۳.
- **ابهام:** سند نمی‌گوید آغازکننده چگونه حیوان شخص دیگر را انتخاب کند، در حالی که فهرست حیوان‌های دیگران نباید برای او قابل مرور باشد.
- **تصمیم:** آغازکننده شناسه یک رکورد موجود را وارد می‌کند — شماره میکروچیپ، Pet ID یا کد شجره‌نامه — و سرور آن را به یک حیوان ثبت‌شده Resolve می‌کند. هر سه پذیرفته می‌شوند چون ممکن است حیوانی فقط یکی از آن‌ها را داشته باشد و برگه ثبتی/شجره‌نامه شرط این سرویس نیست. هیچ فهرستی از حیوان‌های دیگران نمایش داده نمی‌شود و نام دستی پذیرفته نمی‌شود.
- **اثر:** تست Gate رد شدن نام دستی و Resolve شدن با شماره میکروچیپِ حیوانی بدون برگه ثبتی و بدون شجره‌نامه را بررسی می‌کند.

## DEC-0106 — شماره دعوت باید متعلق به مالک همان حیوان باشد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 017 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۲۰ (اعتبارسنجی رابطه حیوان/کاربر و گیرنده دعوت)، §۲۳.۴.
- **تصمیم:** سرور شماره واردشده را با شماره مالک همان حیوان مقایسه می‌کند؛ ناهمخوانی، دعوت را رد می‌کند، هیچ رکوردی نمی‌سازد و هیچ پیامکی ارسال نمی‌شود. حساب طرف مقابل همیشه از مالک واقعی همان رکورد خوانده می‌شود، نه از داده‌ای که فرم می‌فرستد.
- **اثر:** تست‌های دیتابیس و مرورگر نشان می‌دهند دعوت ناهمخوان نه ثبت می‌شود و نه ارسال؛ و شماره فقط به‌صورت چهار رقم آخر به آغازکننده بازگردانده می‌شود.

## DEC-0107 — «وجود توافق» تنها چیزی است که ذخیره می‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 017 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۲۰ («متن، فایل، تصویر قرارداد، امضا و شروط حقوقی ذخیره نشوند»؛ «سهم توله، مالکیت توله، مجوز یا lineage رسمی از Declaration ساخته نشود»).
- **تصمیم:** جدول `personal_declaration` هیچ ستونی برای متن، فایل، تصویر، امضا، شروط یا سهم ندارد و هیچ فرمی چنین ورودی‌ای ندارد؛ صفحه پاسخ فقط دو گزینه «هست/نیست» دارد. برای امضا هیچ OTP تازه‌ای ساخته نشد؛ تنها کد یک‌بارمصرف این مسیر همان کد ورود §۶ است. این سرویس هیچ سرویس پرداختی ندارد و هیچ Batch یا قلمی نمی‌سازد.
- **اثر:** تست Gate نام ستون‌ها را برای واژه‌های ممنوع بررسی می‌کند و تست مرورگر نبود ورودی فایل، textarea و هر فیلد امضا/شروط/سهم را می‌سنجد.

## DEC-0108 — یادداشت شخصی روی همان پرونده و کاملاً جدا از مبنای رسمی

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 017 · **نوع:** GAP_COMPLETION
- **منابع:** §۱۷.۳ («تاریخ شخصی … CONFIRMED رسمی تولید نمی‌کند … مبنای رسمی را با داده تأییدنشده عوض نمی‌کند»)، §۲۰.
- **تصمیم:** یادداشت‌های شخصی (تاریخ جفت‌گیری، بارداری، زایمان) در جدول جداگانه `personal_note` روی همان اعلام ذخیره می‌شوند، همیشه UNVERIFIED‌اند و هیچ کوئری‌ای که مبنای Cooldown، Timeline رسمی، تخصیص یا اسناد را می‌سازد این جدول را نمی‌خواند.
- **اثر:** تست Gate پس از ثبت یادداشت تاریخ، خالی‌ماندن `latestConfirmedDateOfAnimal` هر دو حیوان را بررسی می‌کند.

## DEC-0109 — پیش‌نیاز «دو رکورد حیوان» به ماتریس §۵ اضافه شد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 017 · **نوع:** SOURCE_CONFLICT_RESOLVED
- **منابع:** §۲۰ («KYC تأییدشده، عضویت فعال و دو Animal Record موجود»)، §۵.
- **مسئله واقعی:** قاعده Eligibility برای `PERSONAL_DECLARATION` تا این مرحله فقط KYC و عضویت را می‌سنجید و شرط وجود رکورد حیوان را نداشت.
- **تصمیم:** شرط «حداقل یک حیوان ثبت‌شده» به همان قاعده اضافه شد (سمت آغازکننده)؛ حیوان دوم متعلق به طرف مقابل است و در لحظه ارسال دعوت بررسی می‌شود. هیچ شرط برگه ثبتی یا شجره‌نامه‌ای اضافه نشد.
- **اثر:** تست‌های دامنه و عضویت به‌روزرسانی شدند و حالا نشان می‌دهند پس از فعال‌شدن عضویت، تنها پیش‌نیاز باقی‌مانده همان رکورد حیوان است، نه عضویت.

## DEC-0110 — بازدیدکننده بدون نشست به ورود با مقصد هدایت می‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 017 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۸ (بازگشت به همان پرونده و مرحله)، §۲۰ (حفظ مبدأ در جریان OTP/ساخت حساب).
- **مسئله واقعی:** برای کاربر بدون نشست، صفحه «ورود لازم است» نمایش داده می‌شد که لینک ورود نداشت و مبدأ درخواست را نگه نمی‌داشت؛ در نتیجه دعوت‌شده پس از ورود به همان اعلام برنمی‌گشت.
- **تصمیم:** `guardRoute` برای خطای `UNAUTHENTICATED` به `/login?next=<مسیر>` هدایت می‌کند. خطای `FORBIDDEN` بدون تغییر همان صفحه «دسترسی مجاز نیست» را نشان می‌دهد.
- **اثر:** تست مرورگر این مرحله نشان می‌دهد دعوت‌شده با ورود، دقیقاً روی همان اعلام برمی‌گردد؛ تست‌های قبلی مسیرها و خروج نیز به همین رفتار به‌روزرسانی و اجرا شدند.

## DEC-0111 — صفحه‌های ورودی پنل‌ها از داده واقعی پر شدند، نه از متن نمونه

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 018 · **نوع:** SOURCE_CONFLICT_RESOLVED
- **منابع:** §۲۱.۲ و §۲۱.۳ (صف‌های واقعی هر پنل)، §۲۱.۵ (فهرست → جزئیات → اقدام)، دستور «هیچ متریک یا وضعیتی با داده ساختگی ساخته نشود».
- **مسئله واقعی:** صفحه‌های `/assoc` و `/genetics` از PROMPT-003 فهرستی از عنوان‌های ثابت با EmptyState همیشگی بودند؛ عدد و وضعیت واقعی نداشتند و به صف مربوط لینک نمی‌دادند.
- **تصمیم:** `src/operations/service.ts` صف‌ها را از همان جدول‌هایی می‌شمارد که صفحه جزئیات می‌خواند و هر ردیف به همان مسیر باز می‌شود. هیچ متریک تجمیعی تازه‌ای اختراع نشد؛ فقط تعداد در انتظار هر صف.
- **اثر:** Gate مرورگر عدد هر صف را می‌خواند و بررسی می‌کند عدد صف KYC هرگز کمتر از ردیف‌های همان صف نیست و صفر فقط وقتی است که صف واقعاً خالی باشد.

## DEC-0112 — دو ورودی مرده در ناوبری پنل‌ها ساخته شد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 018 · **نوع:** SOURCE_CONFLICT_RESOLVED
- **منابع:** §۲۱.۲، §۲۱.۴، دستور «هیچ دکمه مرده یا موفقیت ساختگی».
- **مسئله واقعی:** `ASSOC_NAV` به `/assoc/members` و `ADMIN_NAV` به `/admin/audit` لینک می‌دادند، اما هیچ‌کدام صفحه نداشتند.
- **تصمیم:** «دفتر عضویت» انجمن با همان سرویس‌های موجود (`issueMembershipNumber` و `setMembershipActive`) و «تاریخچه» سوپرادمین به‌صورت فقط‌خواندنی روی همان رویدادهای Audit ساخته شد. هیچ API موازی تازه‌ای برای تغییر رکورد اضافه نشد.
- **اثر:** Gate مرورگر همه ورودی‌های ناوبری هر سه پنل را باز می‌کند و شکست می‌خورد اگر ورودی مرده‌ای بماند؛ همچنین بررسی می‌کند ناوبری به مسیری بیرون از همان فهرست اشاره نکند.

## DEC-0113 — فهرست مرجع نژادها به داده مدیریت‌شده منتقل شد

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 018 · **نوع:** GAP_COMPLETION
- **منابع:** §۲۱.۴ (فهرست‌های مرجع: صادرکنندگان و «داده‌های مرجع موجود مثل نژادها»).
- **ابهام:** سند فهرست نژادها را داده مدیریت‌شده می‌داند اما رفتار حذف را تعیین نمی‌کند.
- **تصمیم:** افزودن نژاد از پنل سوپرادمین انجام می‌شود و «حذف» وجود ندارد؛ نژاد فقط از انتخاب‌های جدید کنار گذاشته می‌شود (`isActive=false`) و حیوان‌های ثبت‌شده با آن دست‌نخورده می‌مانند. هر تغییر با Actor، زمان و مقدار قبلی در Audit ثبت می‌شود و بازگرداندن هم ثبت می‌شود.
- **اثر:** Gate دیتابیس نشان می‌دهد پس از کنارگذاشتن نژادِ یک حیوان ثبت‌شده، `breedId` همان حیوان تغییر نکرده و رویداد با مقدار قبلی ثبت شده است.

## DEC-0114 — فهرست درخواست‌های پستی فقط «ثبت درخواست» است

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 018 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۱۴ و D17 (درخواست پستی بدون چرخه ارسال)، دستور «no shipping lifecycle».
- **تصمیم:** صف انجمن فقط درخواست‌های ثبت‌شده را با سند، نام گیرنده، شهر و تاریخ نشان می‌دهد و هیچ وضعیت ارسال، کد رهگیری پستی یا اقدام تحویل ندارد؛ صفحه صریحاً می‌گوید این چرخه در این فاز وجود ندارد.
- **اثر:** هیچ ستون یا اقدام ارسالی ساخته نشد و متن صفحه همین محدوده را اعلام می‌کند.

## DEC-0115 — ماتریس رد دسترسی به‌صورت داده‌محور آزمون می‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 018 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۴، §۲۳.۴، دستور «actor/resource denial matrix including URL/ID tampering».
- **تصمیم:** Gate `operations-rbac` فهرست خواننده‌های عملیاتی را با فهرست Actorها ضرب می‌کند و برای هر ترکیب نادرست انتظار FORBIDDEN دارد؛ برای دستکاری شناسه، همان اقدام با شناسه ناموجود اجرا می‌شود و پیام «پیدا نشد» با پیام «مال شما نیست» یکسان می‌ماند تا وجود یا نبود رکورد فاش نشود.
- **اثر:** افزودن هر خواننده عملیاتی تازه به همان جدول، آن را به‌طور خودکار وارد ماتریس می‌کند.

## DEC-0116 — ارتقای drizzle-orm برای بستن یک آسیب‌پذیری واقعی

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 019 · **نوع:** TECHNICAL_CHOICE
- **منابع:** دستور «inspect … dependency risks»، قواعد امنیتی بسته.
- **مسئله واقعی:** `npm audit --omit=dev` سه مورد نشان داد که یکی از آن‌ها SQL injection با شدت high در `drizzle-orm` نسخه‌های پیش از 0.45.2 بود — یعنی روی همان کتابخانه‌ای که همه کوئری‌های محصول از آن عبور می‌کنند.
- **تصمیم:** `drizzle-orm` به `0.45.2` ارتقا یافت و بلافاصله هر ۳۱۸ تست دامنه/دیتابیس و هر ۸۸ تست مرورگر دوباره اجرا شدند تا ارتقا با شواهد تأیید شود، نه با فرض.
- **اثر:** آسیب‌پذیری high بسته شد. مورد باقی‌مانده (`postcss` از زنجیره `next`) رفع رسمی‌اش ارتقای major به Next 16 است؛ چون وابستگی زمان build است و در مسیر درخواست کاربر اجرا نمی‌شود، در همین مرحله انجام نشد و با دامنه دقیق در گزارش پذیرش ثبت شد.

## DEC-0117 — وفاداری بصری «تأییدنشده» ثبت می‌شود، نه «تأییدشده با اسکرین‌شات»

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 019 · **نوع:** TECHNICAL_CHOICE
- **منابع:** §۲۴، D06، دستور «If reference unavailable keep visual match UNVERIFIED and flag required remaining gate, not approved by count».
- **مسئله واقعی:** تصویر مرجع مصوب پروتوتایپ در مخزن نیست و Figma برای این اجرا فقط‌خواندنی است؛ بنابراین هیچ مبنای تطبیق پیکسلی وجود ندارد.
- **تصمیم:** Gate `visual-reference-review` همان چیزی را می‌سنجد که واقعاً قابل سنجش است — جهت RTL در هر دو اندازه، نبود اسکرول افقی، وجود واقعی توکن‌های DS و فونت فارسی، و `dir="ltr"` روی شناسه‌ها — و در پایان، نبودِ مرجع را به‌صورت داده در `visual-review.json` با verdict برابر `UNVERIFIED` ثبت می‌کند. تست اگر روزی تصویر مرجع اضافه شود شکست می‌خورد تا بازنویسی آن فراموش نشود.
- **اثر:** هیچ‌جا تعداد اسکرین‌شات به‌جای تأیید بصری شمرده نمی‌شود و این Gate در گزارش پذیرش صریحاً باز اعلام شده است.

## DEC-0118 — پنج ادعای کیفیت جدا از هم ثبت می‌شوند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 019 · **نوع:** GAP_COMPLETION
- **منابع:** دستور «Track five independent claims … No page count implies quality»، §۲۸.
- **تصمیم:** `docs/qa/phase-1-acceptance.md` پنج ادعا را جداگانه و با مبنای خودشان ثبت می‌کند: درستی دامنه، پوشش صفحه/کنترل، وفاداری بصری، سلامت پیوندها و تعامل واقعی. چهار مورد VERIFIED با فرمان و شاهد، و وفاداری بصری UNVERIFIED. جدول آمادگی یکپارچه‌سازی هم local/sandbox/live را جدا نشان می‌دهد تا «کار می‌کند» با «برای Production آماده است» یکی گرفته نشود.
- **اثر:** هیچ ادعای کلی «فاز ۱ کامل است» ثبت نشد؛ آنچه ثبت شد دقیقاً همان چیزی است که اجرا و دیده شده است.

## DEC-0119 — پشتیبان‌گیری دیتابیس و فایل‌های خصوصی در یک Snapshot

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 020 · **نوع:** GAP_COMPLETION
- **منابع:** دستور «Document database+private-file backup/restore together and verify a restore on disposable data, preserving sample/history/document links»، §۲۳.
- **ابهام:** سند ابزار پشتیبان‌گیری را تعیین نمی‌کند.
- **تصمیم:** `tools/backup.mjs` و `tools/restore.mjs` هر دو نیمه را با هم برمی‌دارند و برمی‌گردانند و `manifest.json` با sha256 خود dump و هر فایل می‌سازند. بازگرداندن **پیش از نوشتن** digestها را بررسی می‌کند و Snapshot دستکاری‌شده را اصلاً اعمال نمی‌کند. برای انتقال، ابتدا `pg_dump`/`psql` روی PATH امتحان می‌شود و در نبود آن‌ها همان باینری داخل کانتینر محلی استفاده می‌شود؛ چون آدرس دیتابیس از داخل کانتینر فرق دارد، URL برای آن مسیر بازنویسی می‌شود.
- **اثر:** Gate `backup-restore` روی داده یک‌بارمصرف واقعاً schema و فایل‌ها را حذف می‌کند، بازمی‌گرداند و بررسی می‌کند نمونه، برگه ثبتی و فایل خصوصی همچنان به هم وصل و خواندنی‌اند.

## DEC-0120 — Gate آمادگی تحویل، خودِ شواهد را بازرسی می‌کند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 020 · **نوع:** TECHNICAL_CHOICE
- **منابع:** دستور «do not mark whole product production-ready solely because prompts ran»، §۲۸.
- **تصمیم:** `tests/config/release.test.ts` به‌جای ادعای کیفیت، شواهد را می‌سنجد: هر Prompt گزارش COMPLETE بدون Blocker دارد، هر Gate لازم با PASS و متن شاهد واقعی آمده، هیچ ردیف ماتریس یا بخش نیازمندی بدون نگاشت نمانده و هر ردیف به فایل شاهد اشاره می‌کند، هیچ گزارشی `production: NOT_READY` و `integrations: NOT_CONFIGURED` را نقض نکرده، ادعای بصری همه‌جا UNVERIFIED مانده، و هیچ تعرفه‌ای جز مبلغ مبنای مستند §۷ مقدار Seed ندارد.
- **اثر:** اگر روزی گزارشی ادعای بزرگ‌تری از شواهدش بکند، همین Gate شکست می‌خورد.

## DEC-0121 — دو ردیف پذیرش که فقط رفتار را نام می‌بردند به فایل شاهد وصل شدند

- **تاریخ:** ۲۰۲۶-۰۹-۰۲ · **PROMPT:** 020 · **نوع:** GAP_COMPLETION
- **منابع:** §۲۸، دستور «map every row to actual code, checks and evidence».
- **مسئله واقعی:** ردیف‌های A-025 و A-030 وضعیت داشتند اما به هیچ فایل تست یا شاهدی اشاره نمی‌کردند؛ یعنی ادعا بود بدون آدرس.
- **تصمیم:** هر دو ردیف با شواهد واقعی همین اجرا تکمیل شدند (بازگشت به همان پرونده پس از ورود، Replay بدون اثر دوم، خوانده‌شدن همه تعرفه‌ها از `product_setting` با Audit و ثابت‌ماندن Snapshot قیمت).
- **اثر:** حالا هر ۳۳ ردیف ماتریس و هر ۲۹ بخش نیازمندی به کد یا شاهد اشاره می‌کنند و Gate آمادگی تحویل همین را بررسی می‌کند.

## DEC-0122 — انتخاب سرویس هر آداپتور، داده مدیریت‌شده شد نه ثابت کد

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (دستور صریح کاربر) · **نوع:** PRODUCT_GAP
- **منابع:** دستور کاربر «All above data must can change from superadmin panel»، §۲۱.۴، D16.
- **ابهام:** تا اینجا انتخاب سرویس پیامک/پرداخت/نقشه فقط از متغیر محیطی خوانده می‌شد و تغییر آن یعنی Deploy دوباره.
- **تصمیم:** گروه تنظیمات `INTEGRATIONS` (مهاجرت `0015`) با ده کلید اضافه شد و فقط SUPERADMIN اجازه خواندن و نوشتن دارد؛ چون تغییر سرویس یعنی تغییر در اینکه واقعاً چه اتفاقی برای پرداخت یا پیامک می‌افتد. `src/adapters/integration-settings.ts` مقدار مؤثر را می‌دهد و `src/adapters/current.ts` تنها جایی است که برنامه آداپتور زنده را از آن می‌گیرد؛ ۱۵ نقطه فراخوانی به همین مسیر منتقل شدند.
- **مرز ایمنی:** در `APP_ENV=production` هیچ Mock ای مؤثر نمی‌شود؛ حتی اگر در دیتابیس `MOCK_AUTO` باشد، حالت مؤثر `PROVIDER` است و نبود سرویس واقعی با خطای صریح می‌شکند، نه با موفقیت ساختگی.
- **اثر:** `/api/health` و پنل، همان سرویسی را گزارش می‌کنند که واقعاً پاسخ می‌دهد و Mock را با برچسب `LOCAL_TEST` نشان می‌دهند، نه یکپارچه‌سازی تأییدشده.

## DEC-0123 — پیامک و درگاه در حالت محلی، Mock با موفقیت خودکار

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (دستور صریح کاربر) · **نوع:** PRODUCT_GAP
- **منابع:** دستور کاربر «use mock auto success for sms-otp / Payment-gateway»، §۲۲، §۲۶.
- **تصمیم:** `mockAutoSmsSender` متن را در صندوق توسعه ثبت می‌کند و به شماره واقعی چیزی نمی‌فرستد؛ کد OTP همچنان واقعی، یک‌بارمصرف، دارای انقضا و مشمول همان محدودیت ارسال است. `mockAutoPaymentGateway` بازگشت را «پرداخت‌شده» تأیید می‌کند اما مبلغ را از رکورد `payment_attempt` می‌خواند، بنابراین بررسی سمت سرور مبلغ همچنان واقعی است و Replay هم اثر دوم ندارد.
- **چیزی که Mock نمی‌کند:** هیچ‌کدام سند «آماده Production» نیستند و هر دو در محیط production خطا می‌دهند.
- **اثر:** جریان کامل ثبت‌نام تا صدور به‌صورت محلی و بدون سرویس بیرونی قابل اجراست، بدون آنکه ادعای تأیید واقعی ثبت شود.

## DEC-0124 — تولید PDF با Chromium محلی

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (دستور صریح کاربر) · **نوع:** TECHNICAL_CHOICE
- **منابع:** دستور کاربر «Search and find free and best options for document-render»، §۱۳، §۱۴، §۱۹.۴.
- **گزینه‌ها:** کتابخانه‌های PDF جاوااسکریپت (pdfkit/pdf-lib/jsPDF) در برابر چاپ صفحه با موتور مرورگر.
- **تصمیم:** موتور، همان Chromium ای است که پروژه برای تست‌های مرورگر دارد، از طریق `page.pdf()`. دلیل تعیین‌کننده فارسی است: خط متصل و راست‌به‌چپ نیاز به Shaping و Bidi واقعی دارد و کتابخانه‌های سبک PDF بدون کار دستی روی گلیف‌ها این را درست نمی‌دهند. رایگان است، کاملاً محلی اجرا می‌شود و هیچ داده‌ای بیرون نمی‌فرستد.
- **مرز صداقت:** خروجی، چاپ داده‌های ثبت‌شده است و قالب رسمی چاپی انجمن نیست؛ همین جمله روی خود صفحه و در گزارش آداپتور آمده است. دانلود از مسیر `/api/documents/[kind]/[id]/pdf` و با همان بررسی مالکیت سمت سرور انجام می‌شود.

## DEC-0125 — نقشه: Neshan، و نبود کلید هیچ مسیری را قفل نمی‌کند

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (دستور صریح کاربر) · **نوع:** PRODUCT_GAP
- **منابع:** دستور کاربر «Use neshan for map-provider»، §۶.۲.
- **تصمیم:** `integration.map.provider` با مقدار `neshan` Seed می‌شود و کلید آن جداگانه و خالی می‌ماند تا مالک محصول کلید واقعی را وارد کند. نقشه فقط وقتی نمایش داده می‌شود که کلید واقعاً وارد شده باشد.
- **اثر:** تا ورود کلید، ثبت نشانی دستی کامل کار می‌کند و هیچ فیلد تأییدشده‌ای حذف نمی‌شود؛ گزارش سلامت هم صادقانه `NOT_CONFIGURED` می‌ماند.

## DEC-0126 — ریدر میکروچیپ، دستگاه بلوتوثی از نوع صفحه‌کلید است

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (دستور صریح کاربر) · **نوع:** PRODUCT_GAP
- **منابع:** دستور کاربر «We don't need a chip-reader we use input Bluetooth keyboard»، §۱۱.
- **تصمیم:** `integration.chip_reader.mode` با `KEYBOARD_WEDGE` Seed می‌شود؛ یعنی دستگاه شماره را مثل صفحه‌کلید در همان فیلد تایپ می‌کند و هیچ یکپارچه‌سازی سخت‌افزاری لازم نیست. هشدار «پیکربندی نشده» از صفحه دامپزشک برداشته شد و جای آن توضیح روش کار آمد؛ ورود دستی و بارکد بسته دست‌نخورده باقی ماندند.
- **اثر:** روش خواندن همچنان روی خود رکورد ثبت می‌شود، پس مسیر ممیزی تغییری نکرد.

## DEC-0127 — مقادیر شروع عملیاتی، با برچسب صریح «تعرفه رسمی نیست»

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (دستور صریح کاربر) · **نوع:** PRODUCT_GAP
- **منابع:** دستور کاربر «Fill data as you generate»، §۷، §۲۱.۴، §۲۲.
- **ابهام:** تا اینجا تعرفه‌ها `NOT_CONFIGURED` می‌ماندند و هیچ مسیر پرداختی محلی باز نمی‌شد.
- **تصمیم:** هر تعرفه و اطلاعات مرکز، مقدار شروع می‌گیرد و یادداشت هر کلید صریحاً می‌گوید «مقدار شروع عملیاتی است و تعرفه رسمی اعلام‌شده انجمن نیست». تنها استثنا `fee.membership_toman` است که مبلغ مستند §۷ را با ذکر منبع دارد. شماره کارت مرکز عمداً خالی ماند: هیچ مقصد مالی ساختگی وارد نمی‌شود.
- **Seed:** فقط ردیفی که هنوز `version = 1` و بدون مقدار است، مقدار شروع را می‌گیرد؛ هر مقداری که اپراتور تغییر داده یا عمداً پاک کرده، دست‌نخورده می‌ماند.
- **اثر:** تست‌هایی که «تعرفه واردنشده» را می‌سنجیدند حالا خودشان مقدار را پاک می‌کنند، پس همان نامتغیر واقعی (مبلغ صفر ساخته نمی‌شود) همچنان بررسی می‌شود.

## DEC-0128 — دفتر عضویت، صفحه‌بندی محدود شد

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ · **نوع:** GAP_COMPLETION
- **مسئله واقعی:** `memberRecords` بدون حد بالا کل دفتر را می‌خواند و صفحه همه ردیف‌ها را می‌ساخت. با رشد داده محلی تا ۱۲۹۲ رکورد، صفحه آن‌قدر بلند شد که Chromium نتوانست از آن تصویر بگیرد؛ همان اتفاق برای اپراتور یعنی صفحه‌ای که عملاً باز نمی‌شود.
- **تصمیم:** ۱۰۰ رکورد تازه‌ترین خوانده می‌شود و تعداد رکوردهای نمایش‌داده‌نشده صریحاً روی صفحه نوشته می‌شود، تا فهرست کوتاه‌شده با «پایان دفتر» اشتباه گرفته نشود. چون فهرست محدود بدون راه رسیدن به یک نفر یعنی عضوی که اصلاً قابل مدیریت نیست، جست‌وجو بر اساس نام، شماره عضویت یا چهار رقم آخر موبایل هم اضافه شد (اپراتور طبق §۲۳.۳ شماره کامل را نمی‌بیند، پس همان چهار رقم قابل جست‌وجوست). همین محدودیت برای فهرست نمونه‌های مرکز ژنتیک هم اعمال شد که به همان دلیل بی‌حد رشد می‌کرد.
- **اثر:** این یک نقص واقعی محصول بود که از طریق تست بصری پیدا شد، نه یک تنظیم تست. تست‌های مرورگر هم به‌جای اتکا به فهرست کامل، از همان جست‌وجو استفاده می‌کنند.
- **حدی که عمداً برداشته نشد:** بقیه صف‌ها (فیش، مجوز، کنل، اعتراض) فهرست کارِ در انتظارند و با انجام کار کوچک می‌شوند؛ آن‌ها بدون شاهد رشد واقعی صفحه‌بندی نشدند.

## DEC-0129 — تست‌های مرورگر روی درگاه توسعه اجرا می‌شوند، حالت پیش‌فرض روی Mock می‌ماند

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ · **نوع:** TECHNICAL_CHOICE
- **مسئله واقعی:** حالت نصب‌شده `MOCK_AUTO` است، اما Mock ای که همیشه موفق است نمی‌تواند دو حالت «بازگشت بدون پرداخت» و «پرداخت ناموفق» را بسازد؛ همان دو حالتی که ارزش اصلی تست مرورگر است.
- **تصمیم:** `tools/browser-tests.mjs` پیش از اجرای Suite حالت را به `DEV_GATEWAY` می‌برد و در پایان — چه Suite موفق باشد چه نه — همان مقدار قبلی را برمی‌گرداند. مسیر Mock بدون تست نماند: `tests/browser/payment-mock.test.ts` خودش حالت را `MOCK_AUTO` می‌کند و ثابت می‌کند بازگشت فوری، تأیید سمت سرور با مبلغ منجمدشده و یک‌بار بودن اثر پرداخت واقعاً برقرارند.
- **اثر:** هر دو مسیر پرداخت شاهد اجرای واقعی دارند و هیچ‌کدام به قیمت دیگری کنار گذاشته نشد.

## DEC-0130 — تداخل نام‌های مقیاس فاصله با مقیاس عرض، صفحه ورود را باریک کرده بود

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (گزارش کاربر: «UI نیست») · **نوع:** GAP_COMPLETION
- **مسئله واقعی:** توکن‌های DS نام‌های `sm/md/lg/xl` را در فضای نام `--spacing-*` تعریف می‌کنند؛ همان نام‌ها در Tailwind v4 برای مقیاس عرض هم به کار می‌روند و مقدار Spacing برنده می‌شود. نتیجه: `max-w-md` یعنی `max-width: 12px`. صفحه ورود، صفحه تکمیل پروفایل، صفحه دسترسی غیرمجاز، درگاه توسعه و صندوق پیامک همگی به عرض ۱۲ تا ۱۶ پیکسل رندر می‌شدند — هر خط یک حرف.
- **چرا تست‌ها آن را نگرفتند:** تنها ادعای چیدمان، نبود اسکرول افقی بود. صفحه‌ای که فرزندانش تا یک حرف جمع می‌شوند دقیقاً اسکرول افقی ندارد؛ یعنی ادعا درست بود و بی‌ربط.
- **تصمیم:** به‌جای بازتعریف `--container-*` (که به‌خاطر اولویت Spacing کار نکرد)، سه توکن بدون تداخل `--size-content-{sm,md,lg}` اضافه شد و همه‌جا با `max-w-(--size-content-md)` ارجاع داده می‌شود. توکن‌ها CODE-ONLY علامت خورده‌اند.
- **گارد:** بررسی بصری حالا عرض واقعی `main` را هم می‌سنجد (حداقل ۳۲۰ پیکسل) و صفحه ورود به فهرست صفحه‌های بررسی‌شده اضافه شد؛ اگر این خطا برگردد، تست می‌شکند به‌جای اینکه سبز بماند.

## DEC-0131 — سه نقص کاربردی که کاربر روی محیط محلی دید

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (گزارش مستقیم کاربر) · **نوع:** GAP_COMPLETION

**۱. داشبورد، هفت بار یک قفل را تکرار می‌کرد.** وقتی احراز هویت انجام نشده، همه سرویس‌ها یک پیش‌نیاز دارند و صفحه هفت کارت با یک جمله یکسان نشان می‌داد؛ تنها کاری که کاربر واقعاً می‌توانست انجام دهد زیر همان تکرار گم می‌شد. حالا سرویس‌های قفل بر اساس پیش‌نیازشان گروه می‌شوند: یک کارت با دلیل و CTA واقعی، و یک خط که می‌گوید با همان کار چند سرویس دیگر و کدام‌ها باز می‌شوند. هیچ سرویسی پنهان نشد — فقط یک‌بار به‌جای هفت‌بار گفته می‌شود.

**۲. راه خروج از حساب فقط در صفحه پروفایل بود.** نوار پایین در عرض دسکتاپ `md:hidden` است و هیچ جایگزینی نداشت، پس روی صفحه بزرگ اصلاً ناوبری و خروج وجود نداشت. حالا سربرگ هر دو Shell نام کاربر (لینک به پروفایل) و دکمه «خروج» را دارد، و همان مقصدهای نوار پایین در دسکتاپ به‌صورت افقی در سربرگ می‌آیند. `signOutAction` به `src/identity/sign-out-action.ts` منتقل شد تا Shell بدون وابستگی معکوس به `app/` از آن استفاده کند؛ Session همچنان اول روی سرور باطل می‌شود.
- **نکته جانبی:** مقصد تب «پذیرش» دامپزشک `/vet/checkin` بود در حالی که مسیر واقعی `/vet/check-in` است؛ اصلاح شد.

**۳. بارگذاری تصویر با خطای سمت‌کلاینت می‌شکست.** علت واقعی: Server Actions در Next.js پیش‌فرض بدنه درخواست را به **۱ مگابایت** محدود می‌کنند، در حالی که قواعد فایل خود محصول تا ۱۰ مگابایت اجازه می‌دهند. هر عکس واقعی موبایل از این حد بزرگ‌تر است، پس درخواست پیش از رسیدن به هر بررسی محصول رد می‌شد و کاربر فقط «Application error» می‌دید. `serverActions.bodySizeLimit` روی `12mb` تنظیم شد تا همه فایل‌های مجاز برسند؛ محدودیت واقعی هر Purpose، نوع‌های پذیرفته و بررسی امضای فایل دست‌نخورده و همچنان سمت سرور اجرا می‌شوند. برای فایل بزرگ‌تر از حد، `FileField` جدید در همان فرم پیام فارسی می‌دهد و انتخاب را پاک می‌کند تا درخواست اصلاً فرستاده نشود.
- **شاهد:** فایل ۳ مگابایتی حالا واقعاً بارگذاری می‌شود؛ فایل ۱۱ مگابایتی در فرم با پیام حد مجاز رد می‌شود؛ هیچ خطای سمت‌کلاینت رخ نمی‌دهد.

## DEC-0132 — کار عملیاتی هرگز به عضویت انجمن وابسته نبود؛ راه رسیدن به پنل کم بود

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (پرسش کاربر) · **نوع:** GAP_COMPLETION
- **پرسش:** چرا اپراتور انجمن، اپراتور مرکز ژنتیک و سوپرادمین باید عضو انجمن شوند تا کار کنند؟
- **واقعیت بررسی‌شده:** نمی‌شوند. `src/authz/routes.ts` برای `/assoc`، `/genetics` و `/admin` فقط نقش را می‌خواهد و هیچ قاعده‌ای عضویت را شرط نمی‌کند؛ هر سه پنل با حساب بدون عضویت باز شدند. تنها جایی که عضویت شرط کار است، دامپزشک معتمد است (§۷.۱) که کار جدید نمی‌گیرد ولی کارهای فعالش را تمام می‌کند — و آن هم قاعده مستند محصول است، نه این پنل‌ها.
- **مسئله واقعی پشت پرسش:** پس از ورود، این حساب‌ها روی `/dashboard` یعنی محیط شهروندی می‌نشستند و کارت «عضویت انجمن — فعال نیست» و سرویس‌های قفل شهروندی را می‌دیدند، در حالی که هیچ لینکی به محیط عملیاتی خودشان وجود نداشت و تنها راه، تایپ‌کردن آدرس بود. این پیام غلط می‌داد که کار عملیاتی به عضویت وابسته است.
- **تصمیم:** داشبورد برای حسابی که نقش عملیاتی فعال دارد، بخش «محیط‌های عملیاتی شما» را نشان می‌دهد با لینک به همان محیط و این جمله که برای کار در آن‌ها عضویت لازم نیست. D11 نقض نشد: این جابه‌جایی نقش عمومی نیست، محیط‌ها همچنان Shell و ناوبری جدا دارند و مجوز هر مسیر سمت سرور و بر اساس نقش بررسی می‌شود. حسابی بدون نقش عملیاتی هیچ ورودی‌ای نمی‌بیند.
- **شاهد:** ۰۹۹۹۰۰۰۰۰۰۴/۵/۶ ورودی خود را می‌بینند و به `/assoc`، `/genetics` و `/admin` می‌رسند بدون هیچ عضویت فعالی؛ ۰۹۹۹۰۰۰۰۰۰۱ صفر ورودی می‌بیند.

## DEC-0133 — برگه ثبتی و میکروچیپ یک مسیرند، نه دو سرویس

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (اعتراض کاربر) · **نوع:** SOURCE_CORRECTION
- **اعتراض:** «کجا گفتم برای برگه ثبتی نیاز به میکروچیپ هست؟ این دوتا باید باهم انجام بشه.»
- **بررسی منبع:** حق با کاربر است.
  - §۱۳ یک زنجیره واحد است: انتخاب حیوان‌ها → انتخاب کاشت/تأیید میکروچیپ برای هر حیوان → انتخاب دامپزشک و Referral → کاشت/تأیید و نمونه‌گیری اجباری → مرور → یک پرداخت Batch → صدور مستقل هر برگه. میکروچیپ و نمونه‌گیری **مراحل ۲ تا ۴ همین سرویس‌اند**.
  - جدول سرویس‌های §۵ اصلاً ردیفی به نام «درخواست مراجعه دامپزشک» ندارد؛ سرویس‌ها عبارت‌اند از داشبورد، ثبت حیوان، برگه ثبتی، شجره‌نامه، کنل، مجوز، Puppy Card و توافق شخصی.
  - Flow Map، بخش «۰۲ — برگه ثبتی ← شجره‌نامه» (`r0HblzL2bxAcswyGpAfbe2`، node `2:4`) همین را نشان می‌دهد: از «انتخاب چند حیوان» تا «صدور مستقل برگه ثبتی هر حیوان» یک زنجیره پیوسته با Finder دامپزشک، Referral، Review اولیه و نهایی در میانه‌اش.
- **اشتباه پیاده‌سازی:** داشبورد دو کارت جدا داشت («کاشت یا تأیید میکروچیپ» و «دریافت برگه ثبتی»)، و در `/registration/new` حیوان بدون میکروچیپ فقط جمله «میکروچیپ این حیوان هنوز ثبت نشده است» می‌گرفت بدون هیچ راه ادامه‌ای. این یعنی مرحله ۴ به‌صورت یک کار جداگانه که کاربر باید خودش پیدا کند نمایش داده می‌شد.
- **تصمیم:** یک سرویس «برگه ثبتی» در داشبورد که به `/registration/new` می‌رود؛ همان صفحه، مراحل کامل مسیر را می‌گوید و برای هر حیوان مرحله بعدی خودش را با لینک نشان می‌دهد: درخواست مراجعه دامپزشک، یا پیگیری درخواست باز همان حیوان، یا انتخاب برای پرداخت. مسیر `/requests/new?context=MICROCHIP` حذف نشد، از داخل همین مسیر و به‌ازای هر حیوان در دسترس است.
- **چیزی که تغییر نکرد:** ترتیب مالی. §۱۳ می‌گوید «پرداخت برگه ثبتی از مرحله میکروچیپ و نمونه‌گیری جلو نیفتد»؛ پرداخت و صدور همچنان فقط برای حیوانی باز می‌شود که میکروچیپ و نمونه قابل‌استفاده دارد. چیزی که برداشته شد، قفلِ **شروع** مسیر بود، نه قفل پرداخت.

## DEC-0134 — مشخصات رسمی حیوان را دامپزشک معتمد در محل ثبت می‌کند و پس از آن تغییر نمی‌کند

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (دستور صریح کاربر) · **نوع:** PRODUCT_GAP
- **دستور:** «در مسیر ثبت میکروچیپ تا نصب میکروچیپ، دامپزشک معتمد باید اطلاعات رسمی حیوان را وارد کند؛ بعد از ورود امکان تغییرش نیست.»
- **پشتوانه در منبع:** §۱۳ مسئولیت را صریح می‌گوید: «دامپزشک برای **تأیید هویت**/میکروچیپ و نمونه». §۱۲.۵ مرور نهایی را شامل خود حیوان، تصویر و شناسه‌ها می‌داند. §۱۰ می‌گوید داده تأییدشده از فرم عمومی پروفایل قابل بازنویسی نیست و اصلاح آن از همان فرایند و Actor مسئول انجام می‌شود. یعنی این دستور، منبع را نقض نمی‌کند؛ همان را دقیق می‌کند.
- **مسئله واقعی:** تا اینجا همه مشخصات هویتی را مالک وارد می‌کرد و هیچ‌کس آن‌ها را ندیده تأیید نمی‌کرد؛ میکروچیپ — که اتصالی مادام‌العمر است — به رکوردی بسته می‌شد که هیچ‌وقت راستی‌آزمایی نشده بود.
- **تصمیم:**
  - آنچه مالک پیش از مراجعه وارد می‌کند **اظهار** است. در مراجعه، دامپزشک معتمد فرم «مشخصات رسمی» را با همان مقادیر اظهارشده می‌بیند، اصلاح می‌کند و ثبت می‌کند: نام، نژاد، جنسیت، تاریخ تولد (و تقریبی‌بودن)، رنگ و علائم ظاهری.
  - **ثبت میکروچیپ تا آن لحظه باز نمی‌شود.** `recordChipRead` سمت سرور رد می‌کند، نه فقط UI.
  - پس از ثبت، **یک‌بار و برای همیشه**: تلاش دوم برای ثبت رد می‌شود و `editAnimal` هم تغییر همان فیلدها را از فرم پروفایل رد می‌کند (§۱۰). نام هم در همین گروه است، چون روی برگه ثبتی چاپ می‌شود.
  - مقادیر اظهارشده مالک از بین نمی‌روند: در ردیف Audit با action `ANIMAL_IDENTITY_VERIFIED` به‌عنوان `before` می‌مانند، همراه Actor و زمان.
  - مالک اعلان می‌گیرد و صفحه پروفایل حیوان صریح می‌گوید مشخصات اظهاری است یا رسمی.
- **مسیر اصلاح بعدی:** عمداً ساخته نشد. §۱۰ می‌گوید اصلاح از همان فرایند و Actor مسئول انجام می‌شود؛ تا وقتی مالک محصول آن مسیر را تعریف نکرده، ساختن یک دکمه ویرایش دلبخواه یعنی همان قفلی که الان گذاشته شد را بی‌اثر کردن.
- **اثر روی داده موجود:** حیوان‌هایی که پیش از این میکروچیپ گرفته‌اند `identity_verified_at` خالی دارند و همان‌طور می‌مانند؛ هیچ تأیید عطف‌به‌ماسبقی ساخته نشد.

## DEC-0135 — بخش «حیوان‌های من» و «درخواست‌های فعال» داشبورد هیچ‌وقت از دیتابیس نمی‌پرسید

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (گزارش کاربر) · **نوع:** GAP_COMPLETION
- **گزارش:** «چرا حیوانات من در حیوان‌های من نشان داده نمی‌شوند؟»
- **مسئله واقعی:** هر دو بخش داشبورد یک `EmptyState` ثابت رندر می‌کردند و اصلاً Query نمی‌زدند. یعنی کاربری که چند حیوان و چند درخواست فعال داشت، روی داشبورد «هنوز حیوانی ثبت نکرده‌اید» می‌دید. صفحه `/animals` و `/requests` درست کار می‌کردند؛ فقط خلاصه داشبورد ساختگی بود.
- **چرا مهم است:** §۸ می‌گوید داشبورد باید چهار پرسش را از داده ماندگار پاسخ دهد: وضعیت عضویت، اینکه هر حیوان چه چیزی را کامل کرده، کدام سرویس‌ها باز است و اقدام بعدی با کیست. یک Empty State ثابت، پاسخ به دوتای این‌ها را جعل می‌کرد.
- **تصمیم:** داشبورد حالا `listAnimals` و `listOwnerRequests` را می‌خواند. حداکثر سه ردیف از هرکدام به‌عنوان خلاصه نشان داده می‌شود با لینک «همه (n)» به فهرست کامل، چون داشبورد خلاصه است نه فهرست. وضعیت هر حیوان از داده واقعی می‌آید، از جمله «مشخصات رسمی ثبت‌شده» پس از تأیید دامپزشک (DEC-0134). «فعال» برای درخواست یعنی `ACTIVE` یا `CHECKED_IN`؛ مراجعه تمام‌شده به پرونده حیوان تعلق دارد نه به فهرست کارهای در جریان.
- **شاهد:** حساب با ۳ حیوان و ۳ درخواست فعال حالا همان‌ها را روی داشبورد می‌بیند؛ پیش از این صفر می‌دید.

## DEC-0136 — پرداخت برگه ثبتی پیش از مراجعه؛ برگه و میکروچیپ یک سرویس واحد

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (دستور صریح مالک محصول) · **نوع:** SOURCE_OVERRIDE
- **دستور:** «دریافت پول برای صدور برگه ثبتی باید قبل درخواست میکروچیپ گرفته بشه. کلاً مرحله برگه ثبتی و میکروچیپ حدوداً یک مرحله حساب می‌شن که هدفش اینه که دامپزشک معتمد اطلاعات سگ رو رسمی تأیید کنه. برای همین دیگر هیچ آپشن اختیاری برایش وجود ندارد؛ همه چیز باید اجباری باشد جز عکس.»

**مغایرت صریح با متن منبع — عمدی و ثبت‌شده.** §۱۳ می‌گوید: «پرداخت برگه ثبتی از مرحله میکروچیپ و نمونه‌گیری جلو نیفتد» و پرداخت را مرحله ۶ (پس از مراجعه) می‌گذارد. طبق ترتیب منابع در CLAUDE.md، دستور صریح فعلی کاربر بالاتر از متن سند است، پس اجرا شد؛ اما به‌جای اعمال بی‌صدا، همین‌جا ثبت می‌شود که کدام جمله منبع کنار گذاشته شد.

- **ترتیب جدید:** انتخاب حیوان‌ها ← یک پرداخت گروهی ← انتخاب نوع خدمت هر حیوان و دامپزشک معتمد ← مراجعه (تأیید رسمی مشخصات + کاشت/تأیید میکروچیپ + نمونه‌گیری) ← صدور خودکار برگه هر حیوان.
- **چه چیزی برای پرداخت بررسی می‌شود:** فقط شرط‌هایی که پرداخت را معنادار می‌کنند — حیوان واقعاً ثبت‌شده و متعلق به همین شخص، برگه‌ای برایش صادر نشده، و در Batch باز دیگری نباشد. میکروچیپ و نمونه دیگر پیش‌شرط پرداخت نیستند، چون **خروجی همان چیزی‌اند که خریداری می‌شود**.
- **پول از بین نمی‌رود:** ماشین حالت موجود همین را پوشش می‌داد — قلمی که هنوز واجد شرایط صدور نیست `BLOCKED` می‌شود با دلیل، مبلغ منجمد باقی می‌ماند و پس از رفع، بدون پرداخت دوباره صادر می‌شود. حالا صدور خودکار است: با ثبت نمونه (آخرین کار مراجعه)، `issuePaidSheetsForAnimal` برگه پرداخت‌شده همان حیوان را صادر می‌کند. فقط Batch با وضعیت `PAID` تأییدشده سمت سرور اثر دارد.
- **همه‌چیز اجباری جز عکس:** ثبت حیوان دیگر نام، رنگ و نشانه‌های ظاهری را اختیاری نمی‌گیرد و فرم هویت رسمی دامپزشک هم همه این‌ها را اجباری می‌خواهد. دلیل: هدف مراجعه، **تأیید رسمی** مشخصات است و تأییدِ فیلد خالی چیزی را تأیید نمی‌کند. عکس تنها استثناست، چون بخشی از آنچه دامپزشک گواهی می‌کند نیست.
- **شماره میکروچیپ اعلامی** اختیاری ماند: حیوانی که هنوز چیپ ندارد چیزی برای اظهار ندارد و شماره رسمی را در هر حال دامپزشک می‌خواند. این مورد را جدا و صریح ثبت می‌کنم تا با «همه‌چیز اجباری» اشتباه گرفته نشود.
- **ریسکی که پذیرفته شد:** پول پیش از دیده‌شدن حیوان گرفته می‌شود، پس اگر مراجعه به نتیجه نرسد (تعارض میکروچیپ، نمونه نامعتبر) مبلغ نزد سامانه می‌ماند تا مسیر تکمیل شود. سیاست بازپرداخت وجود ندارد و ساخته نشد؛ همان قاعده §۱۳ که «سازوکار بازپرداخت خودسرانه ساخته نشود» هنوز برقرار است.

## DEC-0137 — تعداد مراکز درمانی محیط توسعه به چهار مورد کاهش یافت

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (دستور کاربر) · **نوع:** TECHNICAL_CHOICE
- **دستور:** «تعداد مراکز درمانی کم کن، زیر پنج تا باشه.»
- **وضعیت:** ۳۴۱ Location در دیتابیس توسعه بود که همه‌شان را اجراهای تست ساخته بودند؛ Finder همه را نشان می‌داد.
- **تصمیم:** رکوردها **حذف نشدند**، چون هر مراجعه، نمونه و کد مراجعه‌ای که به آن‌ها اشاره می‌کند باید سالم بماند. به‌جای آن ۳۳۷ مورد `licence_status = 'EXPIRED'` شدند — همان قاعده محصول که Finder فقط Location با پروانه معتبر و امکانات لازم را نشان می‌دهد. چهار مورد باقی‌مانده متعلق به دامپزشک Fixture هستند و نام خوانا گرفتند: کلینیک مرکزی، درمانگاه شمال، کلینیک غرب، بیمارستان دامپزشکی.
- **دامنه:** فقط داده محیط توسعه؛ هیچ کدی تغییر نکرد. تست‌های مرورگر Location خودشان را در زمان اجرا با پروانه معتبر می‌سازند، پس دست‌نخورده کار می‌کنند.

## DEC-0138 — فهرست مراکز در پذیرش مراجعه، فقط جاهایی که واقعاً کار آنجاست

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (گزارش کاربر) · **نوع:** GAP_COMPLETION
- **گزارش:** فهرست مرکز در `/vet/check-in` هنوز پر بود و کار دستی را کند می‌کرد.
- **مسئله واقعی:** `myLocations` هر Location آن دامپزشک را برمی‌گرداند، بدون توجه به وضعیت پروانه. یعنی مرکزی که طبق قاعده §۱۱.۱ در Finder دیده نمی‌شود و کار جدید نمی‌گیرد، همچنان در فهرست پذیرش پیشنهاد می‌شد — هم شلوغی، هم امکان انتخاب اشتباه.
- **تصمیم:** فهرست فقط شامل Locationهایی است که پروانه معتبر دارند، **به‌علاوه** جایی که کار باز و قابل‌انجام دارد: مراجعه‌ای که پذیرش شده، یا مراجعه فعالی که کد آن هنوز منقضی نشده. دلیل این استثنا: کاری که رزرو و پرداخت شده باید قابل انجام بماند؛ و کد منقضی طبق §۱۱.۳ جایگزین می‌شود و جایگزینش به مرکز معتبر می‌رود. امنیت تغییری نکرد — پذیرش از قبل هم Location ارائه‌شده را با Location همان درخواست تطبیق می‌داد.
- **`tools/dev-tidy.mjs`:** هر اجرای کامل تست مرورگر، مراکز تازه با پروانه معتبر می‌سازد و مراجعه‌های نیمه‌تمام باقی می‌گذارد؛ برای همین فهرست بعد از هر اجرا دوباره بلند می‌شد. این ابزار مراکز مصنوعی مازاد را بازنشسته و مراجعه‌های رهاشده را لغو می‌کند و چهار مرکز نام‌دار را نگه می‌دارد. هیچ ردیفی حذف نمی‌شود و فقط روی دیتابیس محلی اجرا می‌شود. `npm run dev:tidy`.
- **نتیجه:** فهرست پذیرش از ۲۰۲ گزینه به ۴ رسید.
- **پیگیری — Finder خالی شد:** بازنشسته‌کردن انبوه مراکز، چهار مرکز باقی‌مانده را هم بی‌استفاده کرد، چون هر چهار مورد `is_active = false` بودند و §۱۱.۱ فقط Location فعال با پروانه معتبر و امکانات اجباری را نشان می‌دهد. انتخاب اولیه من «چهار مورد قدیمی‌تر همان دامپزشک» بود، بدون بررسی اینکه اصلاً فعال‌اند یا نه. `dev-tidy` حالا چهار مرکز نگه‌داشته‌شده را قابل‌استفاده هم می‌کند: فعال، پروانه معتبر، کاشت میکروچیپ برای هر چهار، نمونه‌گیری خون برای سه‌تا (یکی عمداً بدون آن می‌ماند تا قاعده «مرکز ناقص اصلاً دیده نمی‌شود» دستی قابل مشاهده باشد) و بررسی بارداری روی یکی، تا هر سه Context مقصدی داشته باشند.

## DEC-0139 — آیکون‌ها از خود دیزاین سیستم خوانده شدند: Phosphor 2.1.1

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (گزارش کاربر) · **نوع:** GAP_COMPLETION
- **گزارش:** «دیزاین بشدت ساده و خشک است؛ آیکون‌ها به‌درستی از دیزاین سیستم خوانده نشده‌اند.»
- **آنچه در DS نوشته شده:** فایل `2GMJPgnnenGnr1zBN2yH5h` (نود ۴۸:۴۷۲) منبع آیکون‌ها را صریح می‌گوید: **Phosphor Icons 2.1.1، مجوز MIT، پایه ۲۴px، Outline پیش‌فرض و Filled برای حالت فعال/انتخاب‌شده**. کامپوننت `Navigation/Bottom Item Icon` هم می‌گوید Appearance از وضعیت آیتم می‌آید و هم سبک Outline/Filled و هم رنگ حالت را حمل می‌کند.
- **آنچه در کد بود:** هیچ آیکونی. سربرگ یک نویسه متنی `⌾` داشت، Stepper یک `✓` متنی، و نوار پایین و ریل عملیاتی فقط متن بودند. یعنی DS خوانده نشده بود، نه اینکه اشتباه خوانده شده باشد.
- **تصمیم:**
  - `src/ui/icon-paths.ts` مسیرهای دقیق Phosphor 2.1.1 را دارد که از `@phosphor-icons/core@2.1.1` **کپی** شده‌اند، نه دوباره کشیده‌شده. آن بسته وابستگی زمان اجرا نیست؛ فقط داده مسیر وارد مخزن شد و متن مجوز MIT در سرصفحه فایل آمده است. ۲۰ آیکون در دو وزن.
  - `src/ui/icon.tsx` دو قاعده DS را خودش اعمال می‌کند تا هر صفحه مجبور نباشد: اندازه از توکن‌های `--size-icon-*` و پیش‌فرض ۲۴px، و `fill` فقط برای حالت فعال. آیکون بدون `label` از درخت دسترس‌پذیری پنهان است، چون متن کنارش معنا را می‌رساند.
  - **نوار پایین، ناوبری دسکتاپ و ریل عملیاتی** حالا Glyph دارند و در مقصد فعلی Filled و رنگ برند می‌گیرند — دقیقاً همان چیزی که `Navigation/Bottom Item` تعریف کرده.
  - سربرگ: زنگ اعلان، حساب و خروج. Alert نشان تن خودش را می‌گیرد. Stepper `check` واقعی. کارت‌های سرویس داشبورد آیکون دامنه‌ای می‌گیرند (سگ، مهر، گواهی، خانه، سپر، تخته‌شست) که همان «HamZist domain icon»های DS‌اند.
- **جهت‌داری در RTL:** آیکون‌هایی که معنایشان جهت است (`sign-out`) آینه می‌شوند؛ کتابخانه برای خواننده چپ‌به‌راست کشیده شده و فلش «خروج» بدون آینه‌شدن به‌سمت داخل صفحه اشاره می‌کرد. زنگ و کاربر جهت ندارند و آینه نمی‌شوند.
- **نقصی که حین کار پیدا و رفع شد:** سربرگ موبایل سرریز می‌کرد و کلمه «خروج» حرف‌به‌حرف عمودی می‌شکست. عنوان حالا truncate می‌شود و نام حساب و برچسب خروج زیر `sm` جای خود را به آیکون می‌دهند؛ هدف لمسی ۴۴px حفظ شد.
- **آنچه عمداً نکردم:** پالت، تایپوگرافی و چیدمان دست‌نخورده ماند. خواسته «از این لینک به‌عنوان دیزاین سیستم استفاده کن» بود، نه بازطراحی؛ توکن‌های رنگ و تایپ از قبل از همین DS آمده بودند و چیزی که کم بود آیکون‌ها بودند.

## DEC-0140 — چیدمان داشبورد و پرونده حیوان از پروتوتایپ خوانده شد

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (گزارش کاربر) · **نوع:** GAP_COMPLETION
- **گزارش:** «چینش داشبورد و پرونده حیوان خوشگل نیست؛ در پروتوتایپ خیلی بهتر است.»
- **منبع خوانده‌شده:** فایل پروتوتایپ `SPDPe6xmPR4fDGXIT5fvni` — `109:360` (F03، OWN-001 در پنج حالت) و `180:397` (F07، PET-009-O). طبق ترتیب اعتبار، پروتوتایپ مرجع چیدمان است.
- **آنچه پروتوتایپ دارد و کد نداشت:**
  - **داشبورد با آدم شروع می‌شود، نه با سرویس:** «سلام، [نام کاربر]» بلافاصله زیر هدر. اضافه شد.
  - **کارت حیوان یک ردیف است، نه یک فراخوان:** کاشی آواتار، نام، «گونه · نژاد»، شناسه و Badge وضعیت — و **بدون دکمه داخل کارت**؛ خودِ ردیف لینک است. پیش از این هر حیوان یک دکمه تمام‌عرض داشت و فهرست به ستونی از دکمه‌ها تبدیل شده بود.
  - **حالت آرام یک خط متن است:** عضویت فعال حالا لینک متنی «مشاهده عضویت» دارد؛ دکمه تمام‌عرض فقط وقتی می‌آید که واقعاً کاری برای انجام هست.
  - **پرونده حیوان با هویت باز می‌شود:** ردیف چیپ‌های وضعیت، بعد کارت هویت با کاشی آواتار و نام و شناسه‌ها. بخش‌های پرونده هرکدام Glyph خودشان را گرفتند تا فایل با آیکون قابل پویش باشد.
- **دو نقص واقعی که در همین بازبینی دیده شد:** صفحه پرونده **UUID خام** نشان می‌داد — «شناسه پرونده: a04a4232-…» و بدتر، «مالک: f159743d-…». §۲۳.۲ کلید داخلی را از Pet ID جدا می‌کند و §۲۳.۳ می‌گوید آدم را با نام می‌شناسند نه با شناسه ردیف. هدر حالا فقط Pet ID را می‌گوید و بخش مالکیت نام مالک را.
- **انیمیشن (درخواست جداگانه کاربر):** دو حرکت، نه بیشتر. محتوا هنگام نشستن صفحه کمی از پایین بالا می‌آید و هر بخش ۴۰ میلی‌ثانیه بعد از قبلی، تا چشم به‌ترتیب خواندن هدایت شود؛ و کارتی که **باز می‌شود** زیر اشاره‌گر کمی بلند می‌شود تا حرکت معنی «این یک در است» بدهد. هر دو زیر `prefers-reduced-motion: reduce` خاموش‌اند، پس این یک ادب است نه یک الزام.
- **آنچه تغییر نکرد:** هیچ فیلد تأییدشده‌ای حذف نشد و پالت و تایپوگرافی دست‌نخورده ماند.

## DEC-0141 — نُه اصلاح گزارش‌شده روی فرم‌ها، وضعیت‌ها و ترتیب ثبت

- **تاریخ:** ۲۰۲۶-۰۹-۰۳ · **PROMPT:** پس از ۰۲۰ (فهرست کاربر) · **نوع:** GAP_COMPLETION

۱. **جست‌وجوی نژاد** — `BreedPicker` جای Select ساده را گرفت: جست‌وجو با نام فارسی یا انگلیسی (پروتوتایپ F04 حالت تایپ «retriever» را دارد) و فهرست نتیجه همان چیزی است که تایپ شده. عبارت بی‌نتیجه، مقدار آزاد نمی‌سازد — نژادی که انجمن منتشر نکرده، نژاد نیست.

۲. **رنگ و نشانه‌های ظاهری** — برای مالک **اختیاری** و برای دامپزشک **اجباری**. مالک حیوانی را توصیف می‌کند که هنوز کسی ندیده؛ دامپزشک جلوی حیوان ایستاده و همان‌ها را گواهی می‌کند. این DEC-0136 را در همین یک مورد اصلاح می‌کند.

۳. **Export Pedigree** — نام مسیر در همه صفحه‌ها به همین شد (§۹.۴ خودش همین را می‌گوید). الزام روی برگه و پشت برگه از قبل سمت سرور بود و دست نخورد؛ حالا متن فرم هم صریح می‌گوید هر دو لازم است.

۴. **دکمه «ذخیره نوع ثبت» حذف شد** — انتخاب نوع ثبت، خودِ آن مرحله است، پس رادیو خودش ذخیره می‌کند. دکمه جدا از آدم می‌خواست چیزی را تأیید کند که همین حالا انتخاب کرده بود.

۵. **رنگ وضعیت‌ها** — یک جدول واحد `REQUEST_STATUS_TONE` و `REFERRAL_STATUS_TONE`؛ پیش از این هر صفحه ternary خودش را داشت و «انجام‌شده»، «لغوشده» و «جایگزین‌شده» همه خاکستری بودند. حالا: انجام‌شده = موفق؛ جایگزین‌شده = اطلاعی، چون کار در جای دیگری زنده است؛ لغوشده = خنثی، چون چیزی خراب نشده؛ منقضی = هشدار.

۶. **تا پرداخت نشدن، چیزی از سند ثبت نمی‌شود** — `createSheetRequest` فقط Batch و قلم‌های قیمت‌گذاری‌شده را می‌سازد. رکوردهای برگه ثبتی داخل همان تراکنشی ساخته می‌شوند که پرداخت را تأیید می‌کند (`markBatchPaid`)، و با Unique Index روی (Batch، حیوان) تکرارپذیرند. نتیجه: هرکس Checkout را رها کند یا Back بزند، فقط یک Batch پرداخت‌نشده جا می‌گذارد و هیچ سند نیمه‌ساخته‌ای وجود ندارد که بعداً پیدا شود.

۷. **KYC همه‌چیز را همان‌جا می‌گیرد** — نام، نام خانوادگی، نام نمایشی و کد ملی اجباری؛ نشانی و کدپستی اختیاری (§۶.۲ صریح است که نشانی خالی هیچ‌چیز را قفل نمی‌کند)؛ بارگذاری کارت ملی پیش از ارسال اجباری. نام نمایشی از اختیاری به اجباری آمد، ولی **دیده‌شدنش** همچنان انتخاب جداگانه است.

۸. **«یادداشت فنی» شد «کد DNA»** در فرم ثبت نتیجه، فرم اعتراض و نمایش شجره‌نامه.

۹. **بارگذاری فایل** — ورودی فایل خام (یک جعبه خاکستری با برچسب انگلیسی مرورگر که معلوم نبود کلیک‌کردنی است) با یک ناحیه انتخاب تمام‌عرض جایگزین شد: آیکون، «انتخاب فایل»، و پس از انتخاب نام و حجم همان فایل. خودِ input یک file input واقعی و قابل تمرکز با صفحه‌کلید می‌ماند و فقط از دید پنهان است.

---

# فاز دو — بخش عمومی و دایرکتوری کشوری

## DEC-0142 — فاز دو با همان Runner و پرچم `--phase 2`، بدون ویرایش بسته تحویلی

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۱ · **نوع:** EXECUTION_TOOLING
- **مشکل:** Runner فقط `prompts/` و `PROJECT_STATUS.md` فاز یک را می‌شناخت (هر ۲۰ ردیف COMPLETE). Manifest بسته فاز دو شناسه `001` بدون پیشوند، مسیر `prompts/…` در حالی که فایل‌ها در `prompts-2/` هستند، بدون `dependsOn` و `requiredChecks` دارد و متن پرامپت‌ها بلوک ```text ندارد. گزارش JSON فاز یک هم از قبل در `docs/reports/PROMPT-001.json` است.
- **تصمیم:** پرچم `--phase 2` روی همه فرمان‌ها: Manifest از `prompts-2/`، وضعیت در `PROJECT_STATUS-PHASE-2.md`، State در `.runner/phase-2/` و گزارش در `docs/reports/phase-2/PROMPT-NNN.json`. Runner در حافظه نرمال می‌کند: شناسه `001` → `PROMPT-001`، مسیر فقط نام فایل، وابستگی ترتیبی به پرامپت قبل، و Gateهای پیش‌فرض `typecheck`، `build`، `product-tests`، `browser-tests` (معیار پذیرش مشترک پرامپت‌ها: Build، typecheck و تست‌های مرتبط + رگرسیون). بدون بلوک متنی، کل فایل پرامپت متن فعال است.
- **دلیل:** قفل Manifest، ترتیب، و بررسی گزارش Commitشده همان تضمین فاز یک را می‌دهد؛ ویرایش Manifest بسته آن را از نسخه تحویلی جدا می‌کرد. رفتار فاز یک بدون پرچم دقیقاً مثل قبل است (تست‌های قبلی Runner بدون تغییر سبزند).
- **اثر:** گزارش خواسته‌شده پرامپت (`docs/reports/PROMPT-001-REPORT.md`) کنار JSON Runner نوشته می‌شود. نسخه‌های ریشه `Requirements-Phase-2.md`، `PRODUCT_DECISIONS.md`، `REQUIREMENTS_TRACEABILITY.json` و `prompts-2/` با بسته `Hamzist-Phase-2-Prompt-Package-v1.0/` هش sha256 یکسان دارند و دست‌نخورده Commit می‌شوند.

## DEC-0143 — PROMPT-001 فاز دو ممیزی و قفل مرز است؛ Schema، Migration و UI تازه ندارد

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۱ · **نوع:** SCOPE
- **مشکل:** بخش «کار اجرایی» هر ۲۰ پرامپت فاز دو یک قالب مشترک است (Schema، Migration، UI، حالت‌های loading/empty/duplicate/archived، تست مرورگر). هدف و خروجی PROMPT-001 اما ممیزی، نقشه reuse/migrate/create و نشکستن فاز یک است.
- **تصمیم:** هیچ جدول، نقش، مسیر یا صفحه‌ای در این مرحله ساخته نشد. «Schema/Route/Permission لازم» برای مرحله کشف یعنی قفل‌کردن قراردادی که بقیه پرامپت‌ها باید رعایت کنند: (۱) دسترسی دقیق هر پیشوند فاز یک و زیرمسیرهایش، (۲) فضای نام عمومی فاز دو که زیر هیچ قاعده فاز یک نمی‌افتد، (۳) وجود هر ۵۹ جدول فاز یک پس از Migration. حالت‌های UI و تست مرورگر در این مرحله با اجرای کامل رگرسیون مرورگر فاز یک پوشش داده شد.
- **دلیل:** ساختن جدول یا صفحه پیش از پرامپت صاحبش، تصمیم آن پرامپت را پیش‌دستی و خطر رکورد موازی (P2-D15) را زیاد می‌کرد.
- **اثر:** نقشه در `docs/architecture/phase-2-boundary.md` زنده است و هر پرامپت ردیف خودش را به‌روز می‌کند.

## DEC-0144 — فضای نام عمومی فاز دو جدا از پیشوندهای فاز یک

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۱ · **نوع:** ROUTING
- **مشکل:** طبیعی‌ترین آدرس دایرکتوری دامپزشکان (`/vets`) امروز Finder احرازشده فاز یک است؛ `/documents` اسناد مالک و `/assoc` Shell انجمن است. بازتعریف هرکدام یا عمومی کردنش، مسیر تثبیت‌شده فاز یک را می‌شکند یا داده خصوصی را در معرض قرار می‌دهد.
- **تصمیم:** پیشوندهای فاز یک با دسترسی فعلی‌شان قفل‌اند. بخش عمومی از فضای جدای انگلیسی و پایدار استفاده می‌کند: `/veterinarians`، `/centers`، `/breeds`، `/articles`، `/news`، `/announcements`، `/associations`، `/clubs`، `/verify`، `/services`، `/about`، `/search`. هر مسیر تا پرامپت خودش طبق DEC-0023 بسته می‌ماند. slug محتوا (فارسی یا لاتین) در پرامپت SEO/CMS تصمیم می‌شود؛ پیشوند بخش ASCII است تا URL پایدار و بدون percent-encoding بماند (§۱۹).
- **اثر:** `tests/ui/routes.test.ts` هر تغییر دسترسی پیشوند فاز یک یا افتادن مسیر رزروشده زیر قاعده فاز یک را شکست می‌دهد. `/` در PROMPT-002/013 جای صفحه وضعیت را می‌گیرد و وضعیت در `/api/health` می‌ماند.

## DEC-0145 — ثبت‌نام عمومی دامپزشک، نقش «دامپزشک معتمد» فاز یک را نمی‌دهد؛ نقش‌های فاز دو افزایشی و جدا هستند

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۱ · **نوع:** PRODUCT_BOUNDARY
- **مشکل:** D01 فاز یک و قاعده دامنه پروژه «Onboarding عمومی دامپزشک» را ممنوع می‌کنند و داده دامپزشک معتمد را فقط از سوپرادمین وارد می‌کنند (DEC-0042). P2-D09 تصمیم صریح و تازه مالک محصول است: همه دامپزشکان می‌توانند حساب بسازند و برای پروفایل و تأیید اقدام کنند. همچنین «مدیر انجمن» فاز دو هم‌نام اپراتور انجمن فاز یک است.
- **تصمیم:** هر دو تصمیم هم‌زمان معتبرند چون دو چیز متفاوت را کنترل می‌کنند. ثبت‌نام و Claim فاز دو فقط پروفایل دایرکتوری و محور Professional Verification (`council_verified_at`) می‌سازد. محور Trusted Hamzist همان نقش `TRUSTED_VET` فعال فاز یک است و فقط از مسیر عملیاتی فاز یک اعطا می‌شود؛ شرط‌های Finder، دریافت کد مراجعه و پنل `/vet` دست نمی‌خورند و پرداخت بسته هیچ‌کدام را نمی‌سازد (P2-D05). نقش‌های فاز دو (AUTHOR، ادمین محتوا، اپراتور بررسی، مدیر مرکز، مدیر انجمن، مدیر کلاب) مقدار افزایشی تازه‌اند و در همان پرامپتی به Enum اضافه می‌شوند که مصرفشان را می‌سازد؛ `ASSOCIATION_OPERATOR` با مدیر پروفایل انجمن ادغام نمی‌شود.
- **منبع:** Requirements-Phase-2 §۳، §۷، §۸؛ P2-D05، P2-D09، P2-D11؛ D01، D11 فاز یک.
- **اثر:** PROMPT-006/007 نباید مسیر تبدیل خودکار «دامپزشک تأییدشده عمومی» به `TRUSTED_VET` بسازند.

## DEC-0146 — نقشه داده فاز دو روی جدول‌های فاز یک؛ جدول‌های فاز یک با تست قفل شدند

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۱ · **نوع:** DATA_BOUNDARY
- **تصمیم:** مبنای هر پرامپت فاز دو جدول نگاشت `docs/architecture/phase-2-boundary.md` §۳ است: `vet_profile` و `vet_location` پروفایل و شعبه دامپزشک، `reference_breed` بانک نژاد، `animal.species` مبدأ گونه، `registration_sheet`/`pedigree`/`puppy_card`/`mating_permit` منبع استعلام، `payment_*` و `product_setting` تبلیغات، و `audit_event`/`notification`/`stored_file` زیرساخت مشترک هستند. سه نیاز Migration شناسایی شد که پرامپت مصرف‌کننده باید طراحی کند: مالک اجباری در `vet_profile`/`vet_location` در برابر رکورد بدون مالک (P2-D06)، جغرافیای متنی آزاد در چهار جدول، و گونه متنی.
- **دلیل:** P2-D15 و Requirements-Phase-2 §۲۳ رکورد موازی را ممنوع می‌کنند؛ حذف یا تغییر نام یک جدول فاز یک، مسیر تثبیت‌شده را بی‌صدا می‌شکند.
- **اثر:** `tests/db/migrations.test.ts` وجود هر ۵۹ جدول فاز یک را پس از اجرای همه Migrationها روی دیتابیس خالی بررسی می‌کند. انقضای بسته‌ها بدون Scheduler و در لحظه خواندن محاسبه می‌شود.

## DEC-0147 — پذیرش کد مراجعه، کد و درخواستش را در یک Snapshot می‌خواند

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۱ (رگرسیون فاز یک) · **نوع:** BUG_FIX
- **مشاهده:** اجرای کامل `npm test` روی HEAD مبنا ۳۳۷/۳۳۸ بود: `tests/db/regression.test.ts` «a referral code is single use…» مقدار `REQUEST_NOT_ACTIVE` گرفت در حالی که `CONSUMED` انتظار داشت. همان فایل به‌تنهایی ۶/۶ سبز بود؛ یعنی شکست به زمان‌بندی زیر بار وابسته بود، نه به تغییر این مرحله.
- **ریشه:** `checkIn` در `src/vets/visits.ts` کد مراجعه و درخواست را با دو SELECT جدا می‌خواند. پذیرش‌کننده بازنده، کد را پیش از Commit برنده `ACTIVE` و درخواست را پس از آن `CHECKED_IN` می‌دید و دلیل رد «درخواست دیگر فعال نیست» می‌شد. پذیرش دوم هرگز رخ نمی‌داد، چون داور رقابت به‌روزرسانی شرطی `ACTIVE → CONSUMED` است؛ ولی دو تست دو قرارداد متفاوت داشتند (`referrals.test.ts` هر دو دلیل را می‌پذیرفت).
- **تصمیم:** هر دو ردیف با یک SELECT و join خوانده می‌شوند. بازنده یا هر دو را پیش از Commit می‌بیند و در به‌روزرسانی شرطی می‌بازد (`CONSUMED`)، یا هر دو را پس از آن می‌بیند (`CONSUMED` در ترتیب قاعده). تست `referrals.test.ts` به همان یک دلیل سخت شد.
- **اثر:** دامپزشکی که کد استفاده‌شده را اسکن می‌کند همیشه پیام «این کد قبلاً استفاده شده» را می‌بیند. ترتیب قاعده `checkInRejection`، داور رقابت و پیام‌ها تغییر نکردند.

## DEC-0148 — هر اجرای تست مرورگر، دیتابیس و سرور جدای خودش را دارد

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۱ (رگرسیون فاز یک) · **نوع:** TEST_INFRASTRUCTURE
- **مشاهده:** اجرای کامل تست مرورگر روی درخت PROMPT-001 به ۶۹/۹۰ رسید. هیچ‌کدام از ۲۱ شکست به کد این مرحله برنمی‌گشت؛ سرور هیچ خطایی ثبت نکرده بود و همه به داده مشترک برمی‌گشتند:
  - Suiteها روی دیتابیس کاری توسعه‌دهنده و سرور ۳۱۱۱ اجرا می‌شدند. حساب Fixture دامپزشک (`09990000003`) پس از ده‌ها اجرا ۵۱۹ مرکز و حدود ۱۲۰۰ نمونه داشت؛ `/admin/vets` و `/vet/samples` آن‌قدر بلند شده بودند که Chromium نتوانست از آن‌ها تصویر کامل بگیرد و در یک Suite Renderer از کار افتاد.
  - `finder.test.ts` فرض می‌کرد **کل** Finder پیش از ساخت مرکزش خالی است، و ۱۱ Suite مراجعه را با `choose-location.first()` — هر مرکزی که اول مرتب شود — رزرو می‌کردند. سه مرکزی که `tools/dev-tidy.mjs` برای تست دستی نگه می‌دارد متعلق به همان دامپزشک Fixture است، پس «اولی» گاهی مرکز دیگری بود و پذیرش در مرکز خود Suite رد می‌شد.
  - `payment-mock.test.ts` حالت پرداخت را بعد از `browser.close()` برمی‌گرداند؛ با از کار افتادن مرورگر، بازگرداندن اجرا نشد و Suiteهای بعدی به‌جای درگاه توسعه با `MOCK_AUTO` پرداخت کردند.
- **تصمیم:**
  - `tools/browser-tests.mjs` برای هر اجرا یک دیتابیس تازه می‌سازد، Migration و Seed پایه و Fixtureهای مصنوعی را روی آن اجرا می‌کند، پوشه ذخیره خصوصی موقت می‌سازد و `next start` را روی یک پورت آزاد با همان دیتابیس بالا می‌آورد. پس از اجرا — موفق یا ناموفق — سرور، دیتابیس و پوشه پاک می‌شوند. دیتابیس و سرور در حال استفاده توسعه‌دهنده دست نمی‌خورند. همان تضمینی است که تست‌های واحد و دیتابیس از قبل با `createTestDb` داشتند.
  - `finder.test.ts` خالی‌بودن را به محله یکتای همان اجرا محدود می‌کند؛ قاعده تحت آزمون «این مرکز فقط پس از پروانه معتبر و امکانات کامل دیده می‌شود» است، نه «کل Finder خالی است».
  - هر ۱۲ محل انتخاب مرکز، ردیفی را انتخاب می‌کند که `finder-location-name` آن نام مرکز همان Suite است.
  - `payment-mock.test.ts` حالت پرداخت را اول و بدون شرط برمی‌گرداند و بستن مرورگر را در `finally` انجام می‌دهد.
- **آنچه عمداً انجام نشد:** فهرست‌های `/admin/vets` و `/vet/samples` صفحه‌بندی نشدند. بلندی آن‌ها از انباشت داده تست روی یک حساب بود، نه از داده واقعی؛ با دیتابیس جدا این شرط دیگر رخ نمی‌دهد. اگر در داده واقعی رشد کنند، همان الگوی DEC-0128 اعمال می‌شود.

## DEC-0149 — سایت عمومی پوسته و گروه مسیر جدا دارد و فقط بخش ساخته‌شده پیوند می‌گیرد

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۲ · **نوع:** UI_ARCHITECTURE
- **تصمیم:** صفحات عمومی در `app/(public)/` با `SiteShell` (`src/public/site-shell.tsx`) رندر می‌شوند: هدر با لوگو، ناوبری دسکتاپ، دکمه «ورود / ثبت‌نام» (برای حساب واردشده «پنل من»)، منوی موبایل از نوع disclosure و فوتر. این پوسته از `PublicShell` برنامه فاز یک جداست و هیچ دسترسی‌ای نمی‌دهد. ترتیب بخش‌ها همان Requirements-Phase-2 §۴ است (`src/public/sections.ts`) ولی فقط بخش `live` پیوند می‌گیرد؛ امروز «خانه» و «درباره همزیست». `/` صفحه وضعیت زیرساخت را با خانه عمومی جایگزین کرد و وضعیت فنی فقط در `/api/health` است. خانه فقط خدمات فاز یکی را معرفی می‌کند که امروز شروع‌شدنی‌اند، با همان توصیف داشبورد و بدون مبلغ یا زمان؛ جست‌وجوی دامپزشک و مرکز، نژاد منتخب، محتوا، انجمن و آمار با پرامپت داده‌شان (003–013) می‌آیند. صفحه ۴۰۴ فارسی سراسری (`app/not-found.tsx`) جای پیش‌فرض انگلیسی Next را گرفت و گروه عمومی `error.tsx` و `loading.tsx` دارد.
- **دلیل:** پیوند به بخش ساخته‌نشده به صفحه ۴۰۴ می‌رسد؛ بلوک خالی یا عدد ساختگی ادعای قابلیتی است که وجود ندارد (قاعده «هیچ تعرفه، زمان، مجوز یا ادعای ساختگی» پرامپت).
- **اثر:** هر پرامپت بخش خودش را با `live: true` و قاعده `PUBLIC` در `src/authz/routes.ts` باز می‌کند و `tests/ui/seo.test.ts` این دو را هم‌گام نگه می‌دارد. فوتر اطلاعات تماس، مجوز یا شبکه اجتماعی ندارد چون داده واقعی آن‌ها وجود ندارد.

## DEC-0150 — مبدأ سایت از SITE_URL خوانده می‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۲ · **نوع:** CONFIGURATION
- **تصمیم:** متغیر `SITE_URL` (فقط origin؛ مسیر و اسلش پایانی حذف می‌شوند) مبنای canonical، sitemap، OpenGraph و JSON-LD است. در production اجباری و فقط `https` است و بدون آن برنامه بالا نمی‌آید؛ بیرون از production مقدار خالی یعنی `http://localhost:3111`. صفحات گروه عمومی `force-dynamic` هستند تا مبدأ زمان اجرا (نه ماشین build) و وضعیت ورود در هدر درست باشد.
- **دلیل:** ساختن URL مطلق از Header درخواست به هر Host جعلی اجازه بازنویسی canonical را می‌دهد؛ حدس زدن دامنه واقعی، ساختن داده است.
- **اثر:** `.env.example` و `docs/ops/runbook.md` به‌روز شدند و پیکربندی‌های production در تست‌ها `SITE_URL` گرفتند. `tools/browser-tests.mjs` مبدأ سرور تست را می‌دهد. کش ایستای صفحات عمومی موضوع PROMPT-018 است.

## DEC-0151 — سیاست ایندکس: پیش‌فرض بسته، صفحه عمومی با buildMetadata باز می‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۲ · **نوع:** SEO_POLICY
- **تصمیم:** `app/layout.tsx` پیش‌فرض `noindex, nofollow` دارد و فقط صفحه‌ای که `buildMetadata` (`src/seo/metadata.ts`) را صدا بزند ایندکس‌پذیر است. canonical همیشه مطلق و بدون query، fragment و اسلش پایانی است. `ARCHIVED` → `noindex, follow` با canonical خودش؛ `DUPLICATE` → canonical به رکورد اصلی و `noindex, follow`، و Duplicate بدون رکورد اصلی خطای برنامه‌نویسی است. بیرون از production همه صفحات `noindex, nofollow` هستند و `robots.txt` کل سایت را می‌بندد. OpenGraph با `fa_IR` و نماد رسمی لوگو؛ JSON-LD فقط Organization، WebSite (بدون SearchAction تا 012) و BreadcrumbList، با escape هر نویسه‌ای که بتواند `</script>` بسازد.
- **دلیل:** Requirements-Phase-2 §۱۹ و §۲۲؛ صفحه تازه برنامه نباید تصادفاً در جست‌وجو بیاید و محیط staging نباید ایندکس شود.
- **اثر:** حالت‌های archived و duplicate در لایه Metadata آماده و تست‌شده‌اند؛ اولین موجودیت محتوایی (003/004) وضعیتش را به همین تابع می‌دهد.

## DEC-0152 — sitemap تفکیک‌شده و robots برگرفته از نقشه دسترسی

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۲ · **نوع:** SEO_POLICY
- **تصمیم:** `/sitemap.xml` فهرست بخش‌هاست و هر بخش در `/sitemaps/<id>.xml` است؛ امروز فقط `pages` (بخش‌های live). `lastmod` فقط با زمان تغییر واقعی نوشته می‌شود. در production، `robots.txt` همه پیشوندهای غیرعمومی `src/authz/routes.ts` (تابع `applicationPrefixes`) به‌علاوه `/api` و `/dev` را به دو شکل `/x$` و `/x/` می‌بندد.
- **دلیل:** robots بر پایه پیشوند رشته‌ای است و `Disallow: /vet` دایرکتوری عمومی `/veterinarians` را هم پنهان می‌کرد؛ فهرست دستی پیشوندها با هر مسیر تازه کهنه می‌شد.
- **اثر:** هر پرامپت محتوایی یک بخش به `SITEMAP_SECTIONS` اضافه می‌کند که فقط رکورد منتشرشده برمی‌گرداند. تست تضمین می‌کند هیچ قاعده robots هیچ بخش عمومی — ساخته‌شده یا رزروشده — را نمی‌بندد.

## DEC-0153 — آیکن‌های list و caret-left از همان Phosphor 2.1.1

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۲ · **نوع:** DESIGN_SYSTEM
- **تصمیم:** دو glyph `list` (دکمه منوی موبایل) و `caretLeft` (جداکننده Breadcrumb) عیناً از `@phosphor-icons/core` 2.1.1 — منبع اعلام‌شده DS 48:472 — به `src/ui/icon-paths.ts` اضافه شدند. caret در RTL به سمت قدم عمیق‌تر (چپ) اشاره می‌کند و آینه نمی‌شود.
- **اثر:** هیچ آیکن دست‌کشیده‌ای اضافه نشد. فریم Figma برای سطح عمومی فاز دو در منابع نیست؛ وفاداری بصری پوسته عمومی UNVERIFIED می‌ماند و فقط با توکن‌ها و اجزای موجود DS ساخته شده است.

## DEC-0154 — گونه و گروه FCI تاکسونومی نسخه‌دار هستند و ویژگی‌های نژاد فهرست بسته

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۳ · **نوع:** DATA_MODEL
- **تصمیم:** جدول `species` با کلید پایدار `code` ساخته شد و `animal.species` که از فاز یک متن آزاد با پیش‌فرض `DOG` بود، بدون بازنویسی هیچ حیوانی به آن FK گرفت. جدول `breed_group` ده گروه رسمی نامگذاری FCI را با شماره و نام انگلیسی FCI و ترجمه فارسی نگه می‌دارد. هر دو با Migration `0017_species-breed-bank` Seed می‌شوند و گونه یا گروه تازه فقط با Migration بعدی اضافه می‌شود. فقط `DOG` Seed شد، چون تنها گونه‌ای است که فاز یک ذخیره می‌کند. ویژگی‌های نژاد Enum بسته‌اند: اندازه (۵ مقدار)، پوشش (۶ مقدار) و سطح کم/متوسط/زیاد برای انرژی، آموزش‌پذیری، نیاز نگهداری، سازگاری با کودکان و سازگاری با حیوانات دیگر. کشور مبدأ کد دوحرفی ISO 3166 است و نام فارسی آن از `Intl.DisplayNames` می‌آید، نه رشته ذخیره‌شده.
- **دلیل:** هدف پرامپت «بدون رشته آزاد» و Requirements-Phase-2 §۲۳ «Taxonomyها Seed نسخه‌دار دارند». فهرست گونه‌هایی که محصول خدمت می‌دهد تصمیم محصولی است و ساخته نشد. شماره و نام گروه‌های FCI نامگذاری عمومی و قابل‌بررسی است، نه ادعای ساختگی.
- **اثر:** فیلتر گونه در دایرکتوری دامپزشک و مرکز (006، 008) به `species.code` وصل می‌شود. گونه دیگر وقتی یک مصرف‌کننده واقعی به آن نیاز دارد، با Migration اضافه می‌شود. آزمایش Migration روی کپی دیتابیس توسعه: ۴۵ نژاد، ۲۳۷۴ حیوان و ۱۱۲ ردیف نژاد کنل بدون تغییر ماندند و هر ۴۵ نژاد نشانی معتبر و یکتا گرفتند.

## DEC-0155 — بانک نژاد روی همان reference_breed؛ انتشار و انتخاب در فرم دو محور مستقل‌اند

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۳ · **نوع:** DATA_BOUNDARY
- **تصمیم:**
  - **جدول:** فیلدهای بانک نژاد به همان `reference_breed` اضافه شدند که `animal.breed_id` و `kennel_breed` به آن FK دارند (P2-D15): گونه، نشانی (`slug`)، نام‌های دیگر، گروه FCI، کشور مبدأ، ویژگی‌ها، تاریخچه، خلاصه استاندارد و پیوند آن، وضعیت صفحه، رکورد اصلی (برای تکراری) و `version`.
  - **دو محور:** `isActive` (فاز یک: آیا فرم‌ها این نژاد را پیشنهاد می‌کنند) و `profileStatus` (DRAFT/PUBLISHED/ARCHIVED: آیا صفحه عمومی دارد) مستقل‌اند. همه نژادهای موجود DRAFT ماندند، چون هیچ محتوایی برایشان نوشته نشده است.
  - **انتشار:** فقط با تاریخچه یا خلاصه استاندارد نوشته‌شده و دلیل. انتقال‌ها: DRAFT→PUBLISHED، PUBLISHED→ARCHIVED یا DRAFT، و ARCHIVED→PUBLISHED. خالی کردن محتوای صفحه منتشرشده رد می‌شود.
  - **هم‌زمانی:** هر ویرایش، تغییر وضعیت و ثبت تکراری نسخه‌ای را که روی آن ساخته شده همراه دارد. نسخه کهنه با CONFLICT رد می‌شود و تغییر دیگری را بازنویسی نمی‌کند. Audit فقط فیلدهای تغییرکرده را با مقدار قبلی و جدید ثبت می‌کند.
  - **نشانی:** از نام لاتین ساخته می‌شود و میان نژادها و نشانی‌های قبلی یکتاست. تغییر آن نشانی قبلی را در `breed_slug_redirect` نگه می‌دارد و درخواست به آن با ۳۰۸ به نشانی تازه می‌رود.
  - **تکراری:** نژاد تکراری به رکورد اصلی اشاره می‌کند و از انتخاب‌های جدید کنار می‌رود. صفحه آن اعلان «تکراری» و canonical به رکورد اصلی (با `noindex`) دارد و در فهرست و sitemap نیست. زنجیره تکراری ممنوع است. حیوان و کنلِ ثبت‌شده با آن دست نمی‌خورند.
  - **نامزد تکراری:** افزودن یا ویرایش نژاد با نام، نام دیگر یا نشانی یکسان رد می‌شود. مقایسه ی/ك عربی، نیم‌فاصله، اعراب، رقم فارسی، حروف بزرگ و کوچک و نشانه‌گذاری را نادیده می‌گیرد.
  - **مدیر:** تا نقش‌های محتوایی 004/016، مدیریت با محیط SUPERADMIN (`/admin/breeds`) است.
- **دلیل:** Requirements-Phase-2 §۶، §۱۹، §۲۲، §۲۳ و P2-D13/P2-D15. جدول نژاد دوم یا جابه‌جا کردن `breed_id` حیوان‌ها، سابقه فاز یک را می‌شکست.
- **اثر:** فرم‌های فاز یک (`breedOptions`، کنل) همان رفتار قبل را دارند؛ فقط نژاد تکراری از انتخاب‌ها بیرون می‌رود. ادغام کامل با انتقال روابط موضوع پنل مدیریت 016 است.

## DEC-0156 — هر ادعای سلامت منبع و تاریخ بازبینی دارد؛ آزمایش پیشنهادی فقط اطلاع‌رسانی است

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۳ · **نوع:** CONTENT_POLICY
- **تصمیم:** نکته سلامت، بیماری‌های مستعد و آزمایش‌های ژنتیکی پیشنهادی در `breed_medical_claim` ذخیره می‌شوند. عنوان منبع و تاریخ بازبینی (روزی معتبر و گذشته) در دیتابیس NOT NULL هستند و پیوند منبع اختیاری و فقط http یا https است. برداشتن یک مطلب با دلیل آن را بایگانی می‌کند و حذف فیزیکی نمی‌شود. صفحه عمومی کنار هر مطلب منبع و تاریخ بازبینی را نشان می‌دهد، پیوندهای بیرونی `nofollow noopener noreferrer` دارند، و متن صفحه می‌گوید این مطالب جای معاینه دامپزشک را نمی‌گیرد و آزمایش از این صفحه ثبت یا سفارش داده نمی‌شود. هیچ محتوای سلامت یا ژنتیکی Seed نشد.
- **دلیل:** Requirements-Phase-2 §۶ («ادعای پزشکی منبع و تاریخ بازبینی دارد»)، قاعده پرامپت («هیچ ادعای پزشکی ساختگی») و مرکز ژنتیک ثابت فاز یک (DEC-0066، Requirements-Phase-2 §۱۸).
- **اثر:** «آزمایش پیشنهادی» هیچ درخواست، پرداخت یا انتخاب مرکزی نمی‌سازد. ویرایش در جای یک مطلب نیست؛ مطلب بازبینی‌شده با تاریخ تازه ثبت و نسخه قبلی بایگانی می‌شود.

## DEC-0157 — مرز بارگذاری فقط جایی که وضعیت پاسخ عوض نمی‌شود؛ فرم ویرایش نژاد بدون بازنشانی خودکار

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۳ · **نوع:** BUG_FIX
- **مشاهده (اجرای مرورگر PROMPT-003، ۹۷/۱۰۰):**
  - `app/(public)/loading.tsx` که در PROMPT-002 برای کل گروه عمومی ساخته شد، پاسخ را پیش از اجرای صفحه stream می‌کرد. در نتیجه `permanentRedirect()` نشانی قبلی نژاد و `notFound()` پیش‌نویس دیگر نمی‌توانستند کد وضعیت را تعیین کنند: نشانی قبلی با ۲۰۰ و هدایت سمت مرورگر جواب می‌داد، نه ۳۰۸. همین برای پیش‌نویس یعنی ۴۰۴ نرم.
  - فرم مشخصات نژاد با `action` ارسال می‌شد. React فرم را پس از هر action بازنشانی می‌کند و `<select>` بازنشانی‌شده به گزینه اول (خالی) برمی‌گردد. پس از ذخیره، گروه FCI، اندازه و سطح‌ها خالی نمایش داده می‌شدند و ذخیره بعدی آن‌ها را پاک می‌کرد.
  - ویرایشگر با مسیر ثابت `/admin/breeds` guard می‌شد و ورود دوباره به فهرست برمی‌گشت، نه به همان نژاد.
- **تصمیم:**
  - مرز بارگذاری فقط برای فهرست نژادها ماند (`app/(public)/breeds/(list)/loading.tsx`)؛ فهرست هرگز redirect یا 404 نمی‌کند. صفحه نژاد، خانه و درباره پیش از ارسال پاسخ کامل رندر می‌شوند تا ۳۰۸ و ۴۰۴ واقعی باشند.
  - فرم مشخصات نژاد از `onSubmit` و `startTransition` ارسال می‌شود، که بازنشانی خودکار ندارد.
  - ویرایشگر مسیر واقعی خود را به `guardRoute` می‌دهد.
  - تست مرورگر کد وضعیت ۳۰۸ و مقصد آن را مستقیم بررسی می‌کند.
- **اثر:** گزارش PROMPT-002 مرز بارگذاری را برای کل گروه عمومی ثبت کرده بود؛ از این مرحله فقط فهرست نژادها آن را دارد. `tools/browser-tests.mjs` می‌تواند Suiteهای مشخص را روی همان راه‌اندازی جدا اجرا کند (`npm run test:browser -- <فایل‌ها>`).

## DEC-0158 — نویسنده و ادمین محتوا نقش‌های مستقل با محیط عملیاتی خودشان هستند

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۴ · **نوع:** ROLES
- **تصمیم:**
  - **نقش‌ها:** `AUTHOR` و `CONTENT_ADMIN` به Enumهای نقش حساب و Context اضافه شدند و مثل Shellهای عملیاتی فاز یک (D11) فقط با نشانی خودشان (`/author`، `/content`) باز می‌شوند، نه با Role Switcher. هیچ نقش دیگری (سوپرادمین، دامپزشک، پرورش‌دهنده، اپراتور) نویسندگی نمی‌آورد (P2-D11).
  - **اعطا:** سوپرادمین در `/admin/roles` نقش را با شماره موبایل حسابِ موجود فعال یا تعلیق می‌کند؛ دلیل لازم است و در Audit ثبت می‌شود. تعلیق ردیف و محتوا را نگه می‌دارد و فقط محیط را می‌بندد.
  - **نوع محتوا:** نویسنده آموزش و خبر می‌نویسد. اطلاعیه صدای رسمی سکو است و فقط با ادمین محتوا ساخته می‌شود. «نوشته کلاب» در مدل هست ولی تا ساخته شدن کلاب‌ها در PROMPT-010 هیچ‌کس نمی‌تواند آن را بسازد.
  - **ادمین محتوا:** همه محتوا را می‌بیند و ویرایش می‌کند، پنهان، بایگانی، حذف نرم و بازگردانی می‌کند، و دسته‌ها را مدیریت می‌کند.
  - **نویسنده:** فقط محتوای خودش را می‌بیند؛ محتوای دیگران برایش همان ۴۰۴ «پیدا نشد» است. محتوای پنهان یا حذف‌شده توسط ادمین را نه ویرایش می‌کند نه تغییر وضعیت می‌دهد.
  - **حساب‌های آزمایشی:** `09990000007` (نویسنده) و `09990000008` (ادمین محتوا) به Fixtureهای توسعه اضافه شدند.
- **رفع امنیتی هم‌راه:** `/api/context` فقط سه Context عملیاتی فاز یک را با نام رد می‌کرد. با اضافه شدن دو Context تازه، ارسال مستقیم به این API می‌توانست آن‌ها را مثل نقش عمومی فعال کند. حالا همان فهرست `OPERATIONAL_CONTEXTS` که Switcher از آن ساخته می‌شود مبنای رد است.
- **دلیل:** P2-D11، P2-D12 (بدون تأیید علمی اجباری، با Moderation ادمین) و Requirements-Phase-2 §۳ و §۱۲.
- **اثر:** PROMPT-005 گزارش و Moderation را روی همین پنهان‌سازی و حذف نرم می‌سازد و PROMPT-016 پنل مدیریت کامل را.

## DEC-0159 — CMS: نسخه در هر ذخیره، زمان‌بندی بدون Scheduler، و منبع برای آموزش

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۴ · **نوع:** CONTENT_MODEL
- **تصمیم:**
  - **ذخیره:** `content_item` مقدار زنده را نگه می‌دارد و هر ذخیره یک Snapshot تغییرناپذیر در `content_revision` می‌سازد. بازگردانی یک نسخه، نسخه تازه است و تاریخچه بازنویسی نمی‌شود. هر ذخیره نسخه‌ای را که روی آن ساخته شده همراه دارد و نسخه کهنه CONFLICT می‌گیرد. Audit فیلدهای تغییرکرده را ثبت می‌کند؛ متن کامل در خود نسخه است.
  - **وضعیت‌ها:** DRAFT، PUBLISHED، HIDDEN، ARCHIVED و DELETED (حذف نرم، P2-D13).
  - **زمان‌بندی:** «زمان‌بندی» یعنی PUBLISHED با `publish_at` در آینده. دیده شدن در لحظه خواندن مقایسه می‌شود و هیچ Job یا Scheduler وجود ندارد. انتشار به گذشته برده نمی‌شود.
  - **دلیل تغییر وضعیت** لازم است برای: بایگانی، پنهان‌سازی، حذف، خروج از پنهان یا حذف، و هر اقدام ادمین روی محتوای نویسنده دیگر.
  - **شرط انتشار:** عنوان، خلاصه و متن. آموزش علاوه بر این دست‌کم یک منبع و تاریخ بازبینی می‌خواهد، چون خواننده بر پایه آن درباره سلامت و نگهداری حیوان تصمیم می‌گیرد. خبر و اطلاعیه منبع اجباری ندارند.
  - **نشانی:** از عنوان ساخته می‌شود و فارسی یا لاتین است (ی/ك عربی یکی می‌شوند، اعراب حذف می‌شود). در هر نوع محتوا یکتاست و نشانی قبلی ۳۰۸ می‌دهد. عنوان تکراری در همان نوع تا تأیید صریح پذیرفته نمی‌شود.
  - **متن:** متن ساده با بند؛ هیچ HTML یا Markdown رندر نمی‌شود.
  - **امضای نویسنده:** نام نمایشی نویسنده فقط اگر `displayNameVisible` روشن باشد دیده می‌شود؛ وگرنه «تیم همزیست» (§۲۰، حداقل‌سازی داده شخصی). Article/NewsArticle JSON-LD همین امضا را دارد.
  - **رابطه با نژاد:** محتوا اختیاری به `reference_breed` و گونه وصل می‌شود (گونه از نژاد پیروی می‌کند) و صفحه نژاد «مطالب مرتبط» منتشرشده را نشان می‌دهد.
- **دلیل:** Requirements-Phase-2 §۱۲، §۱۹، §۲۰، §۲۲، §۲۳؛ قاعده «بدون Scheduler» مرز فاز دو؛ قاعده پرامپت «هیچ ادعای پزشکی ساختگی».
- **اثر:** صفحه‌های عمومی `/articles`، `/news` و `/announcements`، بخش‌های sitemap با `lastmod` واقعی، و ناوبری «آموزش‌ها» و «اخبار». صفحه جزئیات مرز بارگذاری ندارد تا ۳۰۸ و ۴۰۴ واقعی بمانند (DEC-0157).

## DEC-0160 — تصویر محتوا خصوصی ذخیره و فقط برای محتوای قابل‌مشاهده عمومی سرو می‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۱۱ · **PROMPT:** فاز ۲ / ۰۰۴ · **نوع:** FILES
- **تصمیم:**
  - **ذخیره:** هدف فایل تازه `CONTENT_IMAGE` (فقط JPEG/PNG، حداکثر ۵ مگابایت، با بررسی Magic Bytes) در همان ذخیره‌ساز خصوصی فاز یک نوشته می‌شود. مالک فایل نویسنده محتواست تا پیش‌نمایش پنل برایش باز باشد؛ ادمین محتوا هم آن را از مسیر خصوصی می‌خواند.
  - **سرو عمومی:** `/media/[id]` تصویر را فقط وقتی سرو می‌کند که یک محتوای منتشرشده‌ی زمانش رسیده یا بایگانی‌شده آن را نشان بدهد. پنهان کردن، برگرداندن به پیش‌نویس یا حذف محتوا تصویر را هم از دسترس خارج می‌کند و هر شناسه دیگری همان ۴۰۴ را می‌گیرد.
  - **هدرها:** `nosniff` و `max-age=0, must-revalidate`، تا تصمیم Moderation با کپی کش‌شده دور زده نشود.
  - **متن جایگزین:** برای تصویر اجباری است.
- **دلیل:** Requirements-Phase-2 §۱۲ (تصویر محتوا) و §۲۰ (کنترل آپلود)؛ مرز فاز دو: «فایل خصوصی فاز یک هرگز عمومی نمی‌شود».
- **اثر:** پروفایل‌های 006 و 008 می‌توانند همین الگو را برای تصویر عمومی خود به‌کار ببرند. کش واقعی و تغییر اندازه تصویر موضوع PROMPT-018 است.

## DEC-0161 — گزارش کاربر: هر حساب واردشده، فقط محتوای قابل‌مشاهده، یک گزارش باز و سقف روزانه

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۰۵ · **نوع:** MODERATION
- **تصمیم:**
  - **گزارش:** هر حساب واردشده، در هر Context، از صفحه مطلب (`/report/content/[id]`، دسترسی کاربری) گزارش می‌دهد. گزارش فقط برای محتوایی ثبت می‌شود که عموم می‌بینند (منتشرشده‌ی زمانش رسیده یا بایگانی). پیش‌نویس، زمان‌بندی‌شده، پنهان و حذف‌شده «پیدا نشد»اند و مطلب خود نویسنده گزارش‌پذیر نیست.
  - **دلیل گزارش:** از فهرست بسته هفت‌تایی. «دلیل دیگر» توضیح لازم دارد و توضیح حداکثر ۱۰۰۰ نویسه است. شماره نسخه‌ای که گزارش‌دهنده دیده روی گزارش ثبت می‌شود.
  - **ضد سوءاستفاده:**
    - هر حساب برای هر مطلب فقط یک گزارش باز دارد؛ ایندکس یکتای جزئی روی وضعیت OPEN در دیتابیس هم آن را تضمین می‌کند.
    - سقف گزارش هر حساب در ۲۴ ساعت از تنظیم `moderation.report_daily_limit` خوانده می‌شود: گروه تازه `MODERATION`، پیش‌فرض فنی ۱۰. سوپرادمین آن را تغییر می‌دهد و ادمین محتوا آن را می‌بیند.
  - **حریم خصوصی:** هویت گزارش‌دهنده نه به ادمین محتوا نشان داده می‌شود نه به نویسنده. Audit فقط شناسه گزارش و دلیل را ثبت می‌کند و توضیح روی خود گزارش می‌ماند.
  - **دامنه:** هدف گزارش امروز فقط محتواست (`target_kind = CONTENT` با FK واقعی). پروفایل دامپزشک، مرکز، انجمن و کلاب با پرامپت‌های خودشان (006، 008، 010) نوع هدف و FK خود را اضافه می‌کنند.
  - **بررسی‌کننده:** گزارش محتوا را ادمین محتوای PROMPT-004 بررسی می‌کند. «اپراتور بررسی» §۳ برای صف‌های پروفایل و Claim با 007/009 ساخته می‌شود.
- **دلیل:** Requirements-Phase-2 §۱۳ («کاربر واردشده … با دلیل گزارش می‌کند»)، §۲۰ (Rate Limit، ضداسپم، حداقل‌سازی PII) و §۲۲.
- **اثر:** گزارش‌دهنده امروز نتیجه بررسی را نمی‌بیند؛ صفحه «گزارش‌های من» و اعلان نتیجه در Known Gaps است.

## DEC-0162 — تصمیم Moderation همه گزارش‌های باز یک مطلب را می‌بندد؛ درخواست اصلاح و محدودیت ناشر

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۰۵ · **نوع:** MODERATION
- **تصمیم:**
  - **ثبت تصمیم:** ادمین محتوا در `/content/reports/[contentId]` برای همه گزارش‌های باز یک مطلب یکی از پنج تصمیم §۱۳ را ثبت می‌کند: رد گزارش، درخواست اصلاح، مخفی‌سازی، حذف نرم، محدودیت ناشر. دلیل همیشه لازم است. هر گزارش وضعیت DISMISSED (برای رد) یا ACTIONED، همراه تصمیم، دلیل، Actor و زمان، می‌گیرد و یک رویداد `CONTENT_REPORTS_DECIDED` شناسه گزارش‌ها را نگه می‌دارد.
  - **مخفی‌سازی و حذف نرم:** از همان قواعد وضعیت محتوای PROMPT-004 (`applyContentStatus`) در همان تراکنش عبور می‌کنند. یادداشت Moderation برای نویسنده ثبت می‌شود و تصویر و صفحه عمومی ۴۰۴ می‌شوند.
  - **درخواست اصلاح:** محتوا را منتشرشده نگه می‌دارد و یادداشت اصلاح را برای نویسنده ثبت می‌کند. اولین ذخیره خود نویسنده آن را می‌بندد (`CONTENT_CORRECTION_ADDRESSED`) و صف همچنان نشان می‌دهد درخواست باز است یا نه.
  - **محدودیت ناشر:**
    - تا وقتی فعال است، نویسنده نمی‌تواند چیزی منتشر کند یا محتوای منتشرشده/بایگانی‌شده را تغییر دهد. پیش‌نویس‌ها قابل ویرایش می‌مانند و ادمین محتوا محدود نمی‌شود.
    - پایان محدودیت یا تاریخ آینده است که در لحظه خواندن سنجیده می‌شود (بدون Scheduler)، یا «تا برداشتن» با دلیل در `/content/restrictions`.
    - در هر لحظه فقط یک محدودیت فعال برای هر نویسنده ثبت می‌شود.
  - **اعلان:** نویسنده برای همه تصمیم‌ها جز رد گزارش، و برای برداشته شدن محدودیت، اعلان با مسیر بازگشت به همان مطلب یا محیط نویسنده می‌گیرد.
  - **هم‌زمانی:** گزارش‌ها با شرط `status = OPEN` بسته می‌شوند. تصمیم دوم هم‌زمان چیزی برای بستن نمی‌یابد و پیش از هر تغییری CONFLICT می‌گیرد (تست دیتابیس با دو تصمیم هم‌زمان).
- **دلیل:** Requirements-Phase-2 §۱۳ و §۲۲ (Needs Correction، Suspended)، P2-D12 (Moderation ادمین الزامی) و P2-D13 (حذف ابتدا نرم).
- **اثر:** حذف فیزیکی ساخته نشد و تا سیاست نگهداری داده و مجوز مشخص نمی‌شود. پنل مدیریت کامل و گزارش پروفایل‌ها در 016 و پرامپت‌های دایرکتوری می‌آید.

## DEC-0163 — استان و شهر نرمال‌شده با پیوند اختیاری؛ متن آزاد فاز یک سابقه می‌ماند؛ گربه به Species اضافه شد

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۰۶ · **نوع:** DATA
- **تصمیم:**
  - **جغرافیا:** جدول `province` (۳۱ استان با کد لاتین پایدار، نام فارسی و انگلیسی) و `city` (زیر استان، یکتا در هر استان) با Migration `0020_vet-directory` ساخته شد. مرکز ۳۱ استان Seed شد؛ شهر دیگر را سوپرادمین از ویرایشگر پروفایل دامپزشک اضافه می‌کند (نام با ی/ک فارسی و فاصله یکسان ذخیره می‌شود، تکرار در همان استان CONFLICT، Audit `CITY_CREATED`). هیچ شهر یا استانی از متن ناشناخته ساخته نمی‌شود.
  - **پیوند Location:** `vet_location.province_code` و `city_id` اختیاری‌اند. Backfill همان Migration متن آزاد `province_fa`/`city_fa` را با یکسان‌سازی ی/ک عربی به استان و شهر Seed‌شده وصل می‌کند و فقط وقتی نام شهر یکتاست. متن آزاد دست نمی‌خورد و Finder، ارجاع، پستی و لانه همچنان همان متن را می‌خوانند. `kennel`، `residence` و `postal_request` در 015 پیوند می‌گیرند.
  - **Species:** `CAT` (گربه) به Taxonomy اضافه شد تا فیلتر گونه دایرکتوری بیش از یک مقدار داشته باشد (Species زیرساخت مشترک §۶). بانک نژاد همچنان فقط سگ است (P2-D01، §۲۵): سرویس نژاد گونه غیر `DOG` را رد می‌کند و ویرایشگر نژاد فقط سگ را پیشنهاد می‌دهد.
- **دلیل:** Requirements-Phase-2 §۷ (فیلتر استان و شهر)، §۲۳ (migrate/link بدون رکورد موازی، Seed نسخه‌دار)، `docs/architecture/phase-2-boundary.md` ردیف «استان و شهر».
- **اثر:** Location فاز یکی که شهرش با Seed نخواند تا وقتی سوپرادمین شهرش را انتخاب نکند عمومی نمی‌شود. نقشه و صفحات محلی در 015.

## DEC-0164 — پروفایل عمومی دامپزشک روی همان `vet_profile`؛ محورهای وضعیت جدا؛ انتشار با دلیل و نشانی پایدار

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۰۶ · **نوع:** DOMAIN
- **تصمیم:**
  - **بدون جدول موازی:** ستون‌های عمومی (`public_status` DRAFT/PUBLISHED/HIDDEN، `public_slug`، `public_published_at`، `headline_fa`، `experience_fa`، `show_phone`، `show_council_code`) به `vet_profile` و `is_public`/`hours_note_fa` به `vet_location` اضافه شدند. تخصص‌ها (`vet_specialty`، ۱۵ حوزه کاری Seed‌شده) و گونه‌ها (`vet_profile_species`) جدول پیوندند. فهرست‌کردن یک تخصص اظهار پروفایل است، نه گواهی بورد؛ مدرکی ساخته یا ادعا نمی‌شود.
  - **محورها:** Completeness از هفت مورد محتوای پروفایل محاسبه می‌شود و ذخیره نمی‌شود. Ownership امروز همیشه «حساب دامپزشک» است (Claim و بی‌مالک در 007). Professional Verification همان `council_verified_at` فاز یک است. Trusted همان نقش فعال `TRUSTED_VET` است (نقش معلق = نه Trusted، ولی Verification می‌ماند). Advertising تا 011 وجود ندارد. در ویرایشگر هر محور خط خودش را دارد، در صفحه عمومی هر نشان جداست و فیلتر وضعیت یک محور را انتخاب می‌کند؛ نمره ترکیبی ساخته نشد. ترتیب فهرست تا رتبه‌بندی نسخه‌دار 012/015 خنثی (نام) است.
  - **انتشار:** فقط سوپرادمین در `/admin/vets/[accountId]` ویرایش و منتشر می‌کند؛ ویرایش خود دامپزشک با 007. انتشار حداقل «معرفی» و یک محل کار عمومی با شهر لازم دارد (صفحه کم‌محتوا منتشر نمی‌شود، §۱۹). DRAFT→PUBLISHED→HIDDEN→PUBLISHED؛ پیش‌نویس پنهان نمی‌شود و منتشرشده به پیش‌نویس برنمی‌گردد. نشانی `vet-` + ۱۰ رقم هگز تصادفی در اولین انتشار ساخته و هرگز عوض نمی‌شود؛ شناسه داخلی نیست و حدس‌زدنی نیست (§۲۰). پنهان، پیش‌نویس، حساب غیرفعال و نشانی ناشناخته ۴۰۴ و خارج از sitemap‌اند.
  - **تغییر حساس:** هر ذخیره پروفایل، محل کار و وضعیت دلیل لازم دارد، با نسخه مورد انتظار رد تغییر هم‌زمان (CONFLICT) و Audit فقط فیلدهای تغییرکرده با مقدار قبلی/جدید، Actor و زمان (`VET_PUBLIC_PROFILE_UPDATED`، `VET_LOCATION_PUBLIC_UPDATED`، `VET_PUBLIC_STATUS_CHANGED`).
  - **حریم خصوصی:** تلفن دامپزشک و کد نظام فقط با رضایت ثبت‌شده نمایش داده می‌شوند. نشانی و تلفن محل کار فقط وقتی آن محل «عمومی» علامت خورده است. محل کار غیرفعال فاز یک در صفحه عمومی نیست و عمومی نمی‌شود. ساعات فقط متن اطلاع‌رسانی است، نه نوبت (P2-D08).
  - **جدایی از Finder:** انتشار/پنهان‌سازی و پیوند شهر پروانه، امکانات، متن آزاد و شرط‌های Finder را تغییر نمی‌دهد؛ تست دیتابیس نتیجه Finder را پیش و پس از انتشار مقایسه می‌کند.
  - **اصلاح فاز یک:** `upsertVetProfile` وقتی فرم ثبت‌کننده فیلد معرفی ندارد، معرفی را دیگر پاک نمی‌کند.
- **دلیل:** Requirements-Phase-2 §۷، §۱۹، §۲۰، §۲۳؛ P2-D05، P2-D08، P2-D15.
- **اثر:** گزارش پروفایل دامپزشک (§۱۳) در این پرامپت ساخته نشد؛ `moderation_report` هدف پروفایل را همراه صف بررسی 007 می‌گیرد. «محتوای مرتبط» پروفایل ساخته نشد چون CMS رابطه‌ای با دامپزشک ندارد. فیلتر «نوع نمایش» (تبلیغ) با 011.

## DEC-0165 — اپراتور بررسی و درخواست دامپزشک: کد نظام، مدارک خصوصی، اصلاح، رد با یک تجدیدنظر؛ مالک همان حساب پروفایل

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۰۷ · **نوع:** DOMAIN
- **تصمیم:**
  - **نقش:** `REVIEW_OPERATOR` (اپراتور بررسی §۳) نقش و Context عملیاتی تازه است با محیط `/review` که فقط از `/admin/roles` با دلیل داده یا تعلیق می‌شود؛ مثل AUTHOR و CONTENT_ADMIN مستقل است و در Role Switcher نمی‌آید. سوپرادمین در سرویس هم مجاز است.
  - **درخواست:** هر حساب واردشده در Context عمومی خودش (`/account/vet-profile`) یکی از دو درخواست را ثبت می‌کند: PROFILE (پروفایل تازه) یا CLAIM (پروفایل بدون مالک، از `/account/vet-profile/claim/[slug]`). نام، کد نظام، تلفن، شهر، توضیح و ۱ تا ۵ مدرک لازم است و کارت نظام دامپزشکی اجباری است.
  - **کد نظام:** ارقام فارسی/عربی، فاصله و حروف کوچک یکسان‌سازی می‌شوند. قالب رسمی ساخته نشده و فقط شکل معقول (۳ تا ۲۰ نویسه لاتین، رقم، خط تیره) بررسی می‌شود.
  - **مدارک:** نوع تازه `VET_APPLICATION_DOCUMENT`، JPG/PNG/PDF تا ۱۰ مگابایت با بررسی امضای فایل. فقط درخواست‌دهنده، اپراتور بررسی و سوپرادمین از `/api/files/[id]` می‌خوانند.
  - **تکراری (§۲۲):** یک درخواست باز برای هر حساب و یک Claim باز برای هر پروفایل (ایندکس یکتای جزئی). حسابی که پروفایل دارد درخواست تازه نمی‌دهد. کد نظامی که پروفایل دیگری دارد رد می‌شود؛ اگر آن پروفایل بدون مالک است پیام به Claim هدایت می‌کند. بررسی هنگام تأیید دوباره انجام می‌شود.
  - **بررسی:** SUBMITTED → تأیید، درخواست اصلاح یا رد، همیشه با دلیل، نسخه مورد انتظار و Audit. اپراتور درخواست خودش را بررسی نمی‌کند. نتیجه با اعلان و مسیر بازگشت `/account/vet-profile` به درخواست‌دهنده می‌رسد.
  - **پاسخ درخواست‌دهنده:** «نیازمند اصلاح» همان پرونده را با فیلدهای اصلاح‌شده و مدرک افزوده دوباره ارسال می‌کند و Audit مقدار قبلی/جدید را نگه می‌دارد. ردشده فقط یک‌بار تجدیدنظر با دلیل دارد. انصراف، درخواست باز را بایگانی (WITHDRAWN) می‌کند و مانع درخواست تازه نیست.
  - **تأیید PROFILE:** یک `vet_profile` پیش‌نویس روی همان جدول فاز یک با `council_verified_at` می‌سازد. نقش `TRUSTED_VET` داده نمی‌شود (DEC-0145).
  - **مالک:** «دامپزشک» فاز دو نقش جدیدی نیست؛ مالک، حسابی است که `vet_profile.account_id` به آن اشاره می‌کند و فقط در Context عمومی خودش پروفایل را مدیریت می‌کند (P2-D07، دسترسی شیءمحور §۲۰).
- **دلیل:** Requirements-Phase-2 §۳، §۸، §۲۰، §۲۲؛ P2-D05، P2-D07، P2-D09؛ DEC-0145، DEC-0161.
- **اثر:** سقف مجموع درخواست ۱۲ مگابایت (`serverActions.bodySizeLimit`) است. صف بررسی مرکز (009) و انجمن/کلاب (010) همین نقش را به کار می‌گیرند.

## DEC-0166 — پروفایل بدون مالک و Claim روی همان ردیف؛ مدیریت مالک؛ پنهان‌سازی بررسی؛ خرید بسته پیش از Claim ممنوع

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۰۷ · **نوع:** DATA / DOMAIN
- **تصمیم:**
  - **بدون مالک:** `vet_profile.account_id` و `council_code` nullable شدند. Finder (join روی حساب)، ثبت نتیجه بارداری و فهرست ثبت فاز یک فقط پروفایل دارای حساب و کد را می‌بینند؛ نوع‌ها با guard صریح تنگ شدند.
  - **انتشار پیشنهاد:** اپراتور بررسی از `/review/vets/unowned` پیشنهاد بررسی‌شده را با نام، شهر، تماس/نشانی عمومی، منبع (داخلی، هرگز عمومی) و دلیل مستقیم با برچسب «بدون مالک» منتشر می‌کند. نام مشابه تأیید «تکراری نیست» می‌خواهد و کد نظام تکراری رد می‌شود. پروفایل بدون مالک تأیید حرفه‌ای، محل کار و ویرایش محتوایی ندارد و در فهرست با شهر ثبت‌شده‌اش پیدا می‌شود.
  - **فرم پیشنهاد کاربر عادی:** §۱۰ ساخته نشد و همراه صف پیشنهاد مرکز در PROMPT-009 برای هر دو نوع ساخته می‌شود.
  - **Claim:** تأیید فقط وقتی پروفایل هنوز بدون مالک است حساب، کد نظام، `council_verified_at` و `claimed_at` را روی همان ردیف و همان نشانی ثبت می‌کند. دو تأیید هم‌زمان یکی را اعمال و دیگری را CONFLICT می‌کند. Audit انتشار پیشنهاد می‌ماند و `VET_PROFILE_CLAIMED` مقدار قبلی/جدید را اضافه می‌کند: کنترل آینده منتقل می‌شود، نه مالکیت تاریخچه (§۱۰).
  - **مدیریت:**
    - سوپرادمین همه چیز را مدیریت می‌کند.
    - مالک محتوا، رضایت نمایش، شهر/ساعات/نمایش محل‌های کار خودش، افزودن محل کار و انتشار/پنهان‌سازی را مدیریت می‌کند. محل کاری که مالک اضافه می‌کند پروانه NONE و هیچ امکانی ندارد، پس به Finder نمی‌رسد. پروانه و امکانات فقط کار سوپرادمین است.
    - اپراتور بررسی فقط منتشر یا پنهان می‌کند و محتوای کسی را ویرایش نمی‌کند.
    - دلیل برای سوپرادمین و اپراتور اجباری و برای مالک اختیاری است.
    - افزودن شهر به اپراتور بررسی هم داده شد.
  - **پنهان‌سازی بررسی:** پنهان‌سازی سوپرادمین یا اپراتور `hidden_by_review` را روشن می‌کند و مالک تا انتشار دوباره همان‌ها نمی‌تواند منتشر کند. پنهان‌سازی خود مالک Moderation نیست.
  - **خرید بسته:** `packagePurchaseEligibility` فقط برای پروفایل دارای مالک اجازه می‌دهد. محور تبلیغات ویرایشگر همین را نشان می‌دهد و PROMPT-011 باید پیش از هر پرداخت آن را صدا بزند.
  - **یک ویرایشگر:** ویرایشگر سوپرادمین 006 و صفحه مالک از یک View و Actionهای مشترک استفاده می‌کنند. Action محیط ارسال را با guard همان مسیر می‌سنجد و سرویس مالکیت ردیف را.
- **دلیل:** Requirements-Phase-2 §۷، §۸، §۱۰، §۱۴، §۲۰؛ P2-D05، P2-D06، P2-D07، P2-D15.
- **اثر:** گزارش پروفایل (§۱۳) و Merge تکراری‌ها (§۲۱) ساخته نشد و به 016/017 می‌رود. Claim هم‌زمان چند نفر ممکن نیست: نفر دوم تا تصمیم درباره اولی پیام «در حال بررسی» می‌گیرد.

## DEC-0167 — مرکز موجودیت تازه است ولی شعبه همان `vet_location` فاز یک است؛ Taxonomy نوع، خدمت و امکانات

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۰۸ · **نوع:** DATA
- **تصمیم:**
  - **شعبه = Location:** جدول `centre` تازه است، اما هر شعبه یک ردیف `vet_location` است و رکورد موازی ساخته نشد (P2-D15). برای این کار `vet_location.vet_account_id` nullable شد و ستون‌های `centre_id` و `is_open_24h` اضافه شدند.
  - **جدایی از فاز یک:** شعبه‌ای که در مرکز ساخته می‌شود مالک دامپزشک ندارد، پروانه‌اش NONE است و هیچ امکان خدماتی ندارد؛ Finder، ارجاع و مراجعه همه با join روی `vet_account_id` کار می‌کنند، پس چنین ردیفی هرگز به آن‌ها نمی‌رسد. تست دیتابیس نتیجه Finder را پیش و پس از ساخت شعبه مقایسه می‌کند.
  - **پیوند محل کار موجود:** اگر شعبه همان محل کار ثبت‌شده یک دامپزشک باشد، فقط `centre_id` روی همان ردیف ست می‌شود؛ پروانه، امکانات، مالک و جایگاه Finder دست نمی‌خورد و ردیف کپی نمی‌شود. این کار فقط از محیط سوپرادمین انجام می‌شود و پیوند دوباره CONFLICT است.
  - **ساعات:** جدول `location_hours` (یک ردیف برای هر روز هفته، شنبه = ۰) و پرچم شبانه‌روزی. ساعت با ارقام فارسی/عربی و «.» هم خوانده می‌شود و به `HH:MM` تبدیل می‌شود؛ روزی که فقط یک سرِ ساعتش پر باشد یا پایانش پیش از شروع باشد رد می‌شود. این ساعات فقط اطلاع‌رسانی است و نوبت نمی‌دهد (P2-D08).
  - **Taxonomy:** `centre_type` (۱۰ نوع §۹)، `centre_service` (۱۴ خدمت) و `centre_facility` (۱۰ امکان) با Migration نسخه‌دار Seed شدند و مدیریتی‌اند. «امکانات» فقط امکانات ساختمان و پذیرش است، نه ادعای درمانی.
  - **تصاویر:** §۹ تصاویر را هم نام می‌برد؛ در این پرامپت ساخته نشد و در Known Gaps آمد، چون خط لوله تصویر عمومی امروز فقط برای محتوا تعریف شده است (DEC-0160).
- **دلیل:** Requirements-Phase-2 §۹، §۲۳؛ P2-D08، P2-D15؛ `docs/architecture/phase-2-boundary.md` ردیف «مرکز / سازمان».
- **اثر:** نوع `vet_location.vetAccountId` در کد `string | null` شد؛ دو صفحه فاز یک که پروفایل دامپزشک محل را می‌خوانند صریح بررسی می‌کنند که محل، مالک دامپزشک دارد.

## DEC-0168 — مدیریت مرکز: محتوا با مالک، مجوز و انتشار با بررسی، عضویت تیم با پذیرش خود دامپزشک

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۰۸ · **نوع:** DOMAIN
- **تصمیم:**
  - **نقش‌ها:** «مدیر مرکز» نقش حساب تازه نیست؛ مالک همان حسابی است که `centre.owner_account_id` به آن اشاره می‌کند و مرکزش را از `/account/centres` مدیریت می‌کند (P2-D07، مثل DEC-0165 برای دامپزشک). سوپرادمین همه‌چیز، اپراتور بررسی فقط انتشار/پنهان‌سازی و مجوز.
  - **مجوز = محور Verification:** شماره و وضعیت مجوز (ثبت‌نشده/معتبر/منقضی/باطل) را فقط سوپرادمین یا اپراتور بررسی با دلیل ثبت می‌کند؛ مالک مجوز خودش را تأیید نمی‌کند. «معتبر» بدون شماره ثبت نمی‌شود و زمان تأیید فقط برای «معتبر» می‌ماند. هیچ مجوزی ساخته یا فرض نمی‌شود.
  - **محور همکار خدمات همزیست:** یعنی دست‌کم یکی از شعبه‌ها محل کار ثبت‌شده دامپزشک با پروانه معتبر فاز یک است. این محور از مجوز مرکز، مالکیت و تبلیغات جداست (P2-D05).
  - **انتشار:** مثل دامپزشک: معرفی + دست‌کم یک شعبه عمومی با شهر؛ مرکز بدون مالک با شهر ثبت‌شده منتشر می‌شود. پنهان‌سازی بررسی `hidden_by_review` را روشن می‌کند و مالک آن را برنمی‌گرداند. نشانی عمومی `centre-` + ۱۰ رقم هگز است و پس از اولین انتشار تغییر نمی‌کند.
  - **واگذاری مدیریت:** تا ساخت Claim مرکز (PROMPT-009)، سوپرادمین با شماره موبایل و دلیل مدیریت را واگذار می‌کند؛ حساب اعلان می‌گیرد، `claimed_at` ثبت می‌شود و واگذاری دوم CONFLICT است.
  - **تیم حرفه‌ای:** مرکز با نشانی عمومی یا کد نظام دعوت می‌فرستد؛ نام دامپزشک فقط پس از پذیرش خودش در صفحه عمومی می‌آید و پیوند صفحه فقط وقتی پروفایل او منتشر است. پذیرش و رد فقط با حساب خود دامپزشک، برداشتن عضو با دلیل و Audit.
  - **دلیل و نسخه:** هر تغییر سوپرادمین و اپراتور دلیل اجباری دارد، مالک اختیاری؛ همه تغییرها نسخه مورد انتظار و Audit با مقدار قبلی/جدید دارند.
  - **خرید بسته:** همان قاعده دامپزشک؛ مرکز بدون مالک بسته نمی‌خرد و محور تبلیغات این را نشان می‌دهد (DEC-0166، PROMPT-011).
- **دلیل:** Requirements-Phase-2 §۹، §۱۰، §۲۰، §۲۱، §۲۲؛ P2-D05، P2-D06، P2-D07.
- **اثر:** Claim مرکز توسط نماینده، پیشنهاد کاربر عادی و Merge مراکز تکراری در 009 و 016 ساخته می‌شوند. نقشه شعبه فقط مختصات ثبت‌شده را نشان می‌دهد و نقشه واقعی با 015 می‌آید.

## DEC-0169 — پیشنهاد کاربر برای دامپزشک و مرکز، و Claim مرکز با مدرک؛ همان وضعیت‌های بررسی فاز دو

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۰۹ · **نوع:** DOMAIN
- **تصمیم:**
  - **یک جدول برای هر دو نوع پیشنهاد:** `directory_suggestion` با `kind` برابر VET یا CENTRE. §۱۰ هر دو را با هم می‌خواهد و پیشنهاد دامپزشک که در DEC-0166 به این پرامپت موکول شده بود، همین‌جا ساخته شد.
  - **وضعیت‌ها:** همان Enum بررسی فاز دو (`vet_application_status`) برای پیشنهاد و Claim به کار رفت تا صف‌های محیط بررسی با یک واژگان خوانده شوند و Enum تازه ساخته نشود.
  - **چه چیزی لازم است:** نام، شهر و منبع اطلاعات. تماس یا نشانی عمومی اختیاری است. پیش از ثبت، رکوردهای هم‌نام نشان داده می‌شوند؛ اگر رکورد هم‌نامِ منتشرشده بدون مالک باشد، کاربر به Claim هدایت می‌شود و پیشنهاد تکراری ثبت نمی‌شود.
  - **سدهای ضد انبوه:** حداکثر سه پیشنهاد باز برای هر حساب و سقف روزانه از تنظیم تازه `moderation.suggestion_daily_limit` (پیش‌فرض فنی ۵). انصراف، پیشنهاد را بایگانی می‌کند ولی از سقف روزانه کم نمی‌کند.
  - **تأیید:** اپراتور بررسی (نه پیشنهاددهنده و نه هیچ نقش دیگری) تصمیم می‌گیرد و همیشه دلیل می‌نویسد. تأیید، رکورد را **بدون مالک** و منتشرشده می‌سازد: دامپزشک بدون کد نظام و بدون تأیید حرفه‌ای، و مرکز با نوع «سایر مراکز» تا وقتی بررسی نوع دقیق را ثبت کند. پیشنهاددادن هیچ مالکیتی نمی‌آورد (P2-D06) و نام پیشنهاددهنده در صف بررسی نمایش داده نمی‌شود (§۲۰).
  - **Claim مرکز:** نماینده از صفحه همان مرکز بدون مالک درخواست می‌دهد: نام، سمت، تلفن، توضیح و دست‌کم یک مدرک (پروانه مرکز، معرفی‌نامه یا مدرک هویتی) با نوع فایل تازه `CENTRE_CLAIM_DOCUMENT` که فقط خود درخواست‌دهنده، اپراتور بررسی و سوپرادمین می‌خوانند. برای هر مرکز یک Claim باز و برای هر حساب یک Claim باز (ایندکس یکتای جزئی).
  - **انتقال کنترل:** تأیید فقط وقتی مرکز هنوز بدون مالک است `owner_account_id` و `claimed_at` را می‌نویسد، پس دو تأیید هم‌زمان هر دو برنده نمی‌شوند. Auditهای پیشین مرکز سر جایشان می‌مانند و `CENTRE_CLAIMED` مقدار قبلی/جدید را اضافه می‌کند: کنترل ویرایش آینده منتقل می‌شود، نه مالکیت تاریخچه (§۱۰).
  - **اصلاح، رد، تجدیدنظر و انصراف:** پیشنهاد، «نیازمند اصلاح» و انصراف دارد؛ Claim علاوه بر آن یک تجدیدنظر پس از رد دارد، مثل درخواست دامپزشک.
- **دلیل:** Requirements-Phase-2 §۹، §۱۰، §۲۰، §۲۲؛ P2-D06، P2-D07؛ DEC-0165، DEC-0166، DEC-0168.
- **اثر:** Claim دامپزشک همچنان در `vet_application` است و Claim مرکز در `centre_claim`؛ یکی‌کردن این دو در پنل مدیریت ۰۱۶ بررسی می‌شود. Merge رکوردهای تکراری و گزارش پروفایل هم به ۰۱۶ می‌رود.

## DEC-0170 — انجمن و کلاب در یک پرونده با دو دسته قاعده؛ ناشر مجاز فقط از سوپرادمین؛ عضویت بیرون از همزیست

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۱۰ · **نوع:** DOMAIN
- **تصمیم:**
  - **یک جدول، دو نوع:** `community` با `kind` برابر ASSOCIATION یا CLUB. پروفایل، حوزه، گونه/نژاد، مدیران و رویداد مشترک‌اند، ولی قاعده‌ها مشترک نیستند: شماره ثبت و مجوز فقط برای انجمن معنا دارد و نوشته فقط برای کلاب. `community_species`، `community_breed`، `community_manager` و `community_event` جدول‌های وابسته‌اند.
  - **حوزه:** NATIONAL/PROVINCIAL/CITY/BREED/SPORT/OTHER. حوزه استانی استان، حوزه شهری شهر و حوزه نژادی دست‌کم یک نژاد می‌خواهد؛ استان از شهر انتخاب‌شده خوانده می‌شود و دوباره تایپ نمی‌شود.
  - **ثبت و مجوز = محور جدا:** شماره ثبت و وضعیت (ثبت‌نشده/معتبر/منقضی/باطل) را فقط سوپرادمین یا اپراتور بررسی با دلیل ثبت می‌کند، نه مدیر پرونده؛ «معتبر» بدون شماره ثبت نمی‌شود. این محور از تکمیل پروفایل، مالکیت و تبلیغات جداست (P2-D05) و برای کلاب ثبت نمی‌شود.
  - **ناشر مجاز:** «انتشار مستقیم کلاب فقط با مجوز ادمین فعال می‌شود» (§۱۱) یعنی فقط سوپرادمین `can_publish_posts` را روشن می‌کند، فقط برای کلاب، با دلیل و اعلان به مدیر. تا آن لحظه هیچ نوشته‌ای ساخته یا منتشر نمی‌شود و برداشتن مجوز دوباره جلوی نوشتن را می‌گیرد.
  - **نوشته کلاب = محتوای CMS:** نوشته یک ردیف `content_item` با `kind` برابر `CLUB_POST` و `community_id` است، نه سیستم خبری دوم (P2-D15). بازبینی، گزارش، Revision و Audit فاز دو همان‌جا کار می‌کنند. وضعیت‌های کلاب فقط DRAFT/PUBLISHED/ARCHIVED است؛ `HIDDEN` و `DELETED` تصمیم ادمین محتواست و از صفحه کلاب برنمی‌گردد (DEC-0162).
  - **مدیران:** دعوت با شماره موبایل حسابی که یک‌بار وارد همزیست شده است؛ نام مدیر فقط پس از پذیرش خودش در صفحه عمومی می‌آید (§۲۰) و پذیرش مدیریت، مالکیت پرونده نیست. مالک پرونده همان `owner_account_id` است و مثل مرکز با شماره موبایل و دلیل واگذار می‌شود (P2-D07، DEC-0168).
  - **رویداد:** تاریخ‌ها همان‌اند که ثبت شده‌اند؛ همزیست ثبت‌نام نمی‌گیرد و برگزاری را تضمین نمی‌کند. «پیش‌رو» در زمان خواندن از روی آخرین روز حساب می‌شود و Scheduler ندارد. رویداد لغوشده با دلیلش می‌ماند و دلیل لغو اجباری است. تاریخ میلادی با سال کمتر از ۱۹۰۰ پذیرفته نمی‌شود، چون تاریخ شمسیِ تایپ‌شده (مثل ۱۴۰۵-۰۱-۰۱) میلادیِ معتبر به نظر می‌رسد.
  - **انتشار:** معرفی + یک راه ارتباطی؛ تکمیل پروفایل محور جدایی است و کامل‌نبودن جلوی انتشار را نمی‌گیرد. پنهان‌سازی بررسی `hidden_by_review` را روشن می‌کند و مدیر آن را برنمی‌گرداند. نشانی عمومی `assoc-`/`club-` + ۱۰ رقم هگز است و پس از اولین انتشار تغییر نمی‌کند.
  - **عضویت:** همزیست عضویت نمی‌گیرد، عضویت کسی را تأیید نمی‌کند و برای آن هزینه نمی‌گیرد؛ صفحه فقط شرایط و نشانی عضویت خود انجمن یا کلاب را نشان می‌دهد.
- **دلیل:** Requirements-Phase-2 §۱۱، §۱۲، §۱۳، §۱۹، §۲۰، §۲۱، §۲۲؛ P2-D05، P2-D07، P2-D15؛ DEC-0162، DEC-0168.
- **اثر:** Claim انجمن/کلاب توسط نماینده و Merge رکوردهای تکراری ساخته نشده‌اند و به ۰۱۶ می‌روند؛ بسته تبلیغاتی این پرونده‌ها با ۰۱۱ می‌آید و هیچ بسته‌ای مجوز یا تأیید نمی‌سازد.

## DEC-0171 — بسته تبلیغاتی: کاتالوگ در جدول، قیمت در تنظیمات، فعال‌سازی فقط داخل تراکنش پرداخت تأییدشده

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۱۱ · **نوع:** DOMAIN
- **تصمیم:**
  - **پلن در جدول، قیمت در تنظیمات:** `ad_plan` شش ردیف دارد (ویژه و حرفه‌ای × ۳۰، ۹۰ و ۳۶۵ روز) و هر ردیف فقط به کلید قیمت خودش در `product_setting` اشاره می‌کند. هیچ مبلغی در جدول پلن ذخیره نمی‌شود، پس یک عدد از دو جا خوانده نمی‌شود و تغییر قیمت یک ویرایش تنظیماتِ دلیل‌دار و نسخه‌دار است (P2-D03، §۲۲).
  - **هیچ تعرفه ساختگی:** هر شش کلید با `seedValue: null` یعنی «تعیین‌نشده» ساخته می‌شوند. بسته بدون مبلغ ثبت‌شده فروخته نمی‌شود و خطای NOT_CONFIGURED می‌دهد؛ نبودِ قیمت هرگز به معنی رایگان نیست.
  - **خرید = همان پرداخت فاز یک:** سرویس تازه `ADVERTISING_PACKAGE` به Enum پرداخت اضافه شد. خرید یک `payment_batch` می‌سازد که مبلغ را از تنظیمات Snapshot می‌گیرد؛ فعال‌سازی داخل همان تراکنشی انجام می‌شود که پرداخت را روی سرور تأیید می‌کند، و بازگشت تکراری درگاه بار دوم چیزی فعال نمی‌کند.
  - **انقضا بدون Scheduler:** ردیف فقط `starts_at`/`ends_at` دارد؛ «منقضی» چیزی است که خواننده در لحظه درخواست حساب می‌کند، نه وضعیتی که کاری آن را بنویسد. پس هیچ Jobی برای پایان بسته لازم نیست.
  - **تمدید روی هم می‌نشیند:** تمدید زودهنگام روزهای پرداخت‌شده را از بین نمی‌برد؛ بازه تازه از پایان بازه فعلی شروع می‌شود و ردیف قبلی در تاریخچه می‌ماند (§۱۴).
  - **ظرفیت جایگاه:** هر پلن `slot_capacity` دارد؛ وقتی تعداد بسته‌های فعالِ همان پلن به سقف برسد، فروش بعدی رد می‌شود نه اینکه بیش‌فروش شود. خالی‌بودن ظرفیت یعنی «سقفی ثبت نشده»، نه تصمیم به فروش نامحدود.
  - **چه کسی می‌خرد:** فقط مدیر همان پرونده Claim‌شده (پروفایل دامپزشک، مرکز، انجمن یا کلاب). پرونده بدون مالک بسته نمی‌خرد و محیط‌های عملیاتی اصلاً خریدار نیستند (P2-D07، DEC-0166).
  - **محور مستقل:** بسته فقط جای نمایش و برچسب «تبلیغ» را تغییر می‌دهد؛ هیچ تأیید حرفه‌ای، معتمدبودن یا کامل‌بودنی نمی‌سازد (P2-D05). سرویس فقط می‌گوید چه رکوردهایی بسته فعال دارند؛ مرتب‌سازی نسخه‌پذیر و قابل Audit نتایج جست‌وجو (§۱۵) کار پرامپت خودش است.
  - **لغو:** خریدار یا سوپرادمین با دلیل لغو می‌کند؛ تاریخ‌ها و دلیل می‌مانند. هیچ قاعده بازپرداختی اختراع نشد، چون سیاست بازپرداخت در منابع نیست.
  - **گروه تنظیمات `ADVERTISING`:** اپراتور بررسی قیمت‌ها را می‌خواند (برای پاسخ به مدیر پرونده) و فقط سوپرادمین تغییرشان می‌دهد.
- **دلیل:** Requirements-Phase-2 §۱۴، §۱۵، §۲۲؛ P2-D03، P2-D05، P2-D07؛ DEC-0012 (تبدیل تومان/ریال)، DEC-0148.
- **اثر:** مرتب‌سازی تبلیغاتی نتایج و برچسب «تبلیغ» در فهرست‌های عمومی با پرامپت رتبه‌بندی/جست‌وجو می‌آید؛ تخفیف و صورتحساب سازمانی ساخته نشد و به پنل مدیریت ۰۱۶ می‌رود.

## DEC-0172 — رتبه‌بندی پنج‌باندی نسخه‌دار، تبلیغ فقط وقتی مرتبط است، و جست‌وجوی سراسری بدون index

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۱۲ · **نوع:** DOMAIN
- **تصمیم:**
  - **پنج باند، نه امتیاز:** ترتیب §۱۵ به‌صورت باند پیاده شد: `PROMOTED` → `TRUSTED` → `VERIFIED` → `COMPLETE` → `BASE`. هیچ محوری با محور دیگر جمع نمی‌شود و هیچ ترکیبی باند بالاتر نمی‌سازد (P2-D05). داخل هر باند ترتیب الفبایی خنثی است.
  - **تبلیغ فقط وقتی مرتبط است:** `promotedWhenRelevant` جایی نوشته شد که هیچ فراخوانی نتواند از آن رد شود. رکوردی که با عبارت، جغرافیا یا موضوعِ جست‌وجو نمی‌خواند، اصلاً در مجموعه نتیجه نیست و بسته‌اش جایگاهی نمی‌خرد؛ «تبلیغ نامرتبط حق اشغال جایگاه بالاتر را ندارد».
  - **نسخه رتبه‌بندی:** ثابت `RANKING_VERSION` روی هر پاسخ جست‌وجو و روی خود صفحه (`data-ranking-version` و متن قابل خواندن) می‌آید، پس ترتیبی که کاربر دیده به قاعده‌اش قابل ردیابی است (§۱۵ «نسخه‌پذیر و قابل Audit»). تغییر قاعده یعنی تغییر این ثابت با یک Decision تازه.
  - **محور معتمد برای هر دایرکتوری:** دامپزشک، همان معتمد فاز یک؛ مرکز، «همکار خدمات همزیست» که ثبت می‌شود نه خریدنی؛ انجمن و کلاب محور معتمد ندارند و ثبت رسمی‌شان باند `VERIFIED` است. هیچ‌کدام با بسته تبلیغاتی به دست نمی‌آید.
  - **تحمل غلط املایی:** یک ویرایش (جابه‌جایی، حذف یا افزودن یک حرف) روی عبارت‌های چهار نویسه‌به‌بالا، روی متن نرمال‌شده فارسی. عبارت کوتاه دقیق می‌ماند تا «سگ» با «سک» یکی نشود، و دو ویرایش پذیرفته نمی‌شود چون کلمه دیگری است.
  - **ارتباط جغرافیایی و موضوعی اجباری:** فیلتر استان، شهر، تخصص، خدمت یا وضعیت تأیید فقط از دایرکتوری‌ها پاسخ می‌گیرد؛ نژاد، آموزش و خبر شهر و مجوز ندارند و در چنین جست‌وجویی کنار گذاشته می‌شوند و صفحه صریح می‌گوید کدام‌ها و چرا.
  - **`/search` بخش §۴ نیست:** ترتیب بخش‌های عمومی دست‌نخورده ماند؛ جست‌وجو یک مسیر عمومی جدا با قاعده `PUBLIC` است، از جعبه جست‌وجوی هدر باز می‌شود و با گزینه تازه `noindex` در `buildMetadata` هرگز index نمی‌شود (§۱۹: نمای صفحه‌های دیگر، نه صفحه‌ای از خودش).
  - **فعلاً در حافظه:** هر شش منبع با فیلترهای خودشان خوانده و در حافظه رتبه‌بندی می‌شوند، همان کاری که دایرکتوری‌های موجود از قبل می‌کردند. بردن این‌ها پشت یک Query ایندکس‌دار تغییر پایگاه‌داده جداگانه‌ای است، نه بخشی از این برش.
- **دلیل:** Requirements-Phase-2 §۱۵، §۱۶، §۱۹؛ P2-D04، P2-D05؛ DEC-0171.
- **اثر:** فهرست‌های عمومی دامپزشک، مرکز و انجمن هم از همین باندها پیروی می‌کنند و برچسب «تبلیغ» می‌گیرند. ایندکس واقعی جست‌وجو، هم‌مکانی و پیشنهاد خودکار عبارت ساخته نشد.

## DEC-0173 — صفحات خدمات از کاتالوگ کد و تنظیمات مدیریت‌شده، بدون زمان و بدون مسیر دوم

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۱۳ · **نوع:** DOMAIN
- **تصمیم:**
  - **کاتالوگ در کد، نه جدول تازه:** خدمات فاز یک (عضویت، برگه ثبتی، شجره‌نامه، کنل، مجوز جفت‌گیری، کارت توله و مراجعه به دامپزشک معتمد) در `src/services/catalogue.ts` توصیف شدند. این‌ها متن ثابت محصول‌اند، نه داده‌ای که اپراتور ویرایش کند؛ ساختن جدول برایشان داده عمومی موازی با موجودیت‌های فاز یک می‌شد که قواعد قطعی منع کرده‌اند.
  - **هیچ عددی روی صفحه نوشته نشد:** هر هزینه فقط کلید یک `product_setting` است و در زمان درخواست خوانده می‌شود. هزینه ثبت‌نشده «تعیین‌نشده» نشان داده می‌شود، نه صفر و نه رایگان. خدمتی که همزیست بابتش پول نمی‌گیرد (مراجعه به دامپزشک) صراحتاً همین را می‌گوید و متن اطلاع قیمت را از `guide_text.vet_pricing_notice` می‌خواند.
  - **هیچ زمانی وعده داده نشد:** §۱۸ زمان را فقط در صورت وجود داده معتبر اجازه می‌دهد و چنین داده‌ای وجود ندارد؛ پس هیچ مهلت، مدت یا نوبتی روی صفحه‌ها نیست و هر صفحه صریح می‌گوید همزیست نوبت نمی‌دهد. تست دامنه با الگوی زمان‌نما این را قفل می‌کند.
  - **CTA فقط به مسیر موجود:** هر خدمت به همان مسیر محافظت‌شده فاز یک می‌رود که واقعاً آن کار را انجام می‌دهد؛ تست دامنه تأیید می‌کند هیچ CTA به مسیر عمومی نمی‌رود، پس صفحه عمومی راه دوم و بی‌محافظ برای شروع خدمت نمی‌سازد. کاربر مهمان با کلیک، به ورود با `next` دقیق فرستاده می‌شود.
  - **`/services` بخش §۴ نیست:** ناوبری §۴ ده مورد دارد و «خدمات» میانشان نیست؛ پس مثل `/search` مسیر عمومی جداست. برخلاف جست‌وجو، این صفحه‌ها محتوای پایدارند و بخش `services` به `SITEMAP_SECTIONS` اضافه شد.
  - **خانه با داده واقعی:** §۵ بازسازی شد: پیام ارزش، خدمات، روش کار، ورودی جست‌وجو و فهرست‌ها، نژاد منتخب، آموزش و خبر تازه، انجمن منتخب و آمار. هر بلوک وقتی داده‌اش وجود نداشته باشد **اصلاً رندر نمی‌شود** و عددها از شمارش رکوردهای منتشرشده همان لحظه می‌آیند؛ عدد ساختگی یا قفسه خالی نمایش داده نمی‌شود.
  - **FAQ ساختاریافته فقط وقتی نمایش داده می‌شود:** `faqLd` تازه فقط از همان پرسش‌هایی ساخته می‌شود که صفحه واقعاً رندر کرده است (§۱۹).
- **دلیل:** Requirements-Phase-2 §۵، §۱۸، §۱۹، §۲۲؛ قواعد قطعی پرامپت (بدون تعرفه و زمان ساختگی، بدون داده عمومی موازی).
- **اثر:** اگر بعداً متن خدمات ویرایش‌پذیر لازم شود، به CMS فاز دو وصل می‌شود نه جدول تازه. صفحه «درباره همزیست» دست‌نخورده ماند.

## DEC-0174 — استعلام اصالت از روی خودِ اسناد فاز یک؛ بدون وضعیت «باطل» ساختگی

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۱۴ · **نوع:** DOMAIN
- **تصمیم:**
  - **بدون نسخه عمومی موازی:** پاسخ استعلام مستقیماً از `registration_sheet`، `pedigree` و `puppy_card` فاز یک خوانده می‌شود. هیچ جدول «سند عمومی» تازه‌ای ساخته نشد، چون کپی عمومی از اسناد فاز یک دقیقاً همان موازی‌سازی‌ای است که قواعد قطعی منع کرده‌اند و با اصلاح سند اصلی از هم می‌پاشد.
  - **وضعیت‌ها فقط تا جایی که داده واقعی هست:** `VALID`، `REPLACED`، `NOT_FOUND` و `RATE_LIMITED`. **«باطل» ساخته نشد**: فاز یک هیچ ستون یا جریانی برای ابطال سند صادرشده ندارد و اختراع آن یعنی ادعای واقعیتی که سامانه ندارد. `REPLACED` از داده واقعی می‌آید: شجره‌نامه‌ای که `correction_notice_fa` دارد، یعنی نتیجه نسب بعداً اصلاح شده و همان یادداشت نمایش داده می‌شود.
  - **کد و QR یک پرسش‌اند:** پیشوندها از `src/domain/ids.ts` می‌آیند — `RS-` برگه ثبتی، `PD-` شجره‌نامه، `PC-` کارت توله و `PET-` شناسه حیوان که با برگه ثبتی همان حیوان پاسخ داده می‌شود. ورودی می‌تواند نشانی کامل QR باشد؛ فقط بخش کد از آن خوانده می‌شود و ارقام فارسی به لاتین تبدیل می‌شوند.
  - **حداقل داده عمومی (§۱۷، §۲۰):** نوع سند، کد عمومی، تاریخ صدور، وضعیت و چند مشخصه حیوان (گونه، نژاد، جنسیت و **فقط سال تولد**). نام و تماس مالک، کد ملی، نشانی، فایل سند، شماره میکروچیپ، کد رهگیری نمونه و حتی نام حیوان نمایش داده نمی‌شوند؛ فهرست ممنوع‌ها در کد به‌صورت داده نگه داشته شده تا تست روی همان فهرست ادعا کند. کارت توله به توله داخل زایمان وصل است نه حیوان ثبت‌شده، پس برایش مشخصات حیوان نمایش داده نمی‌شود.
  - **محدودکردن تلاش پرتکرار:** کاربر مهمان حساب ندارد، پس تنها چیز قابل شمارش نشانی forwarded درخواست است که **هش‌شده** در جدول تازه `verification_attempt` ذخیره می‌شود. سقف ساعتی از تنظیم مدیریت‌شده `verification.attempt_hourly_limit` (گروه MODERATION، پیش‌فرض فنی ۳۰) خوانده می‌شود؛ پس از آن پاسخ «بیش از حد مجاز» است و به حدس‌زننده نمی‌گوید کدی وجود دارد یا نه.
  - **بخش عمومی و index:** `/verify` بخش §۴ است و `live` شد، ولی چون پاسخ به کدِ داخل Query وابسته است، صفحه با `noindex` منتشر می‌شود.
- **دلیل:** Requirements-Phase-2 §۴، §۱۷، §۱۹، §۲۰؛ قواعد قطعی پرامپت (بدون داده عمومی موازی، بدون ادعای ساختگی).
- **اثر:** اگر بعداً ابطال یا جایگزینی رسمی سند به فاز یک اضافه شود، فقط نگاشت وضعیت در همین ماژول گسترده می‌شود. گواهی‌های مجاز دیگر (§۱۴ «گواهی‌های مجاز») تا وقتی سندی برایشان در فاز یک صادر نشود، کدی برای استعلام ندارند.

## DEC-0175 — نشانی محلی از نام فارسی، صفحه کم‌محتوا index نمی‌شود، و قالب نقشه حدس زده نمی‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۱۵ · **نوع:** DOMAIN
- **تصمیم:**
  - **slug شهر از نام فارسی:** استان از پیش `code` لاتین دارد و همان نشانی‌اش می‌ماند، ولی شهر هیچ نام لاتینی در منابع ندارد. ساختن نام لاتین یعنی جاگذاشتن یک نام اختراعی در نشانی دائمی؛ پس slug شهر از نام فارسی ساخته می‌شود و فاصله و نیم‌فاصله به خط تیره تبدیل می‌شوند. Migration 0027 ستون را اضافه و با همان قاعده پر می‌کند و تست تطابق کد و پایگاه‌داده را قفل می‌کند. نشانی فارسی در این سایت از قبل سرو می‌شود (نوشته کلاب، ۰۱۰).
  - **فضای نام `/places`:** «مکان‌ها» در ناوبری §۴ نیست، پس مثل `/search` و `/services` بخش ناوبری نشد؛ ولی چون محتوای پایدار است، بخش `places` به `SITEMAP_SECTIONS` اضافه شد. قراردادن صفحه شهر زیر `/veterinarians/[slug]` با slug پروفایل‌ها تداخل می‌کرد.
  - **صفحه کم‌محتوا index نمی‌شود (§۱۹):** صفحه هر استان و شهر همیشه پاسخ می‌دهد (بازدیدکننده حق پرسیدن دارد)، ولی تا وقتی رکورد منتشرشده‌ای نداشته باشد `noindex` است و در sitemap هم نمی‌آید.
  - **شمارش فقط از داده واقعی:** شمارش هر مکان از رکوردهای **منتشرشده** با محل **عمومی** می‌آید (سه Query گروهی روی `vet_location`، `centre` و `community`)؛ پیش‌نویس و محل غیرعمومی اصلاً شمرده نمی‌شوند.
  - **قالب نقشه حدس زده نمی‌شود:** هیچ قرارداد URL برای سرویس نقشه در منابع نیست. به‌جای اختراع نشانی، تنظیم تازه `integration.map.embed_url_template` اضافه شد که سوپرادمین با جای‌گذارهای `{lat}`، `{lng}` و `{key}` پر می‌کند؛ تا آن لحظه هیچ نقشه‌ای رندر نمی‌شود و همان متن نشانی سر جایش می‌ماند. قالب بدون مختصات یا بدون https رد می‌شود.
  - **حریم خصوصی مقدم بر نمایش (§۲۰):** ترتیب بررسی در `mapState` عمداً اول «عمومی بودن» را می‌سنجد؛ محلی که مالکش عمومی نکرده است **هیچ چیز** رندر نمی‌کند — نه کادر خالی، نه یادداشت — تا نشانی خصوصی هرگز به یک URL نقشه نرسد. `publicAddress` و `publicCoordinates` همین قاعده را برای متن و مختصات اجرا می‌کنند و مختصات تازه فقط برای محل‌های عمومی به صفحه دامپزشک اضافه شد.
- **دلیل:** Requirements-Phase-2 §۲، §۱۹، §۲۰، §۲۳؛ قواعد قطعی پرامپت (بدون داده ساختگی، بدون انتشار نشانی خصوصی).
- **اثر:** شهرهای تازه‌ای که مدیریت اضافه می‌کند، slug خود را از همان قاعده می‌گیرند. اگر بعداً سرویس نقشه رسمی انتخاب شود، فقط مقدار همان تنظیم پر می‌شود و کد تغییر نمی‌کند.

## DEC-0176 — صف‌ها ساخته نمی‌شوند، پرسیده می‌شوند؛ و ادغام تکراری رکورد را حذف نمی‌کند

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۱۶ · **نوع:** DOMAIN
- **تصمیم:**
  - **هیچ صف تازه‌ای ساخته نشد:** همه صف‌هایی که §۲۱ نام می‌برد از پیش ساخته شده‌اند (درخواست دامپزشک ۰۰۷، مرکز و Claim ۰۰۸/۰۰۹، پیشنهاد ۰۰۹، انجمن و کلاب ۰۱۰، گزارش محتوا ۰۰۵، قیمت‌ها در تنظیمات، بسته‌ها ۰۱۱، تاریخچه ۰۰۵). این پرامپت فقط یک تابلو اضافه کرد که از **همان توابع صفحه‌های صف** می‌پرسد چند مورد در انتظار است، پس عددِ تابلو هرگز با صفحه‌اش اختلاف پیدا نمی‌کند.
  - **صف بسته، صفر نیست:** تابلو با اکتور وارد‌شده می‌پرسد و صفی که دسترسی‌اش را ندارد **اصلاً نمایش داده نمی‌شود**، نه اینکه صفر نشان داده شود؛ صفر یک واقعیت درباره صف است و وقتی اجازه خواندن نداریم آن واقعیت را نداریم (§۲۱ «Permission مستقل»).
  - **ادغام حذف نیست:** ستون `merged_into_*_id` به سه دایرکتوری فاز دو اضافه شد (Migration 0028) دقیقاً مثل قاعده‌ای که بانک نژاد از ۰۰۳ دارد. رکورد تکراری می‌ماند: ردیف، روابط و تاریخچه‌اش دست‌نخورده و نشانی عمومی‌اش همچنان پاسخ می‌دهد و به رکورد اصلی اشاره می‌کند. §۲۱ همین چهار چیز را می‌خواهد: Redirect، روابط، سابقه و امکان بازبینی.
  - **هدایت فقط یک گام:** رکوردی که خودش تکراری ثبت شده دوباره ادغام نمی‌شود، رکورد اصلی نمی‌تواند خودش تکراری باشد، و رکوردی که تکراری دیگری به آن اشاره می‌کند خودش تکراری نمی‌شود. Constraint پایگاه‌داده هم اجازه اشاره به خود را نمی‌دهد.
  - **ادغام از فهرست‌ها بیرون می‌رود، از نشانی خودش نه:** شرط انتشار هر سه دایرکتوری فقط در حالت «فهرست» رکورد ادغام‌شده را کنار می‌گذارد؛ در حالت «یک نشانی مشخص» همچنان پاسخ می‌دهد تا پیوندهای موجود نشکنند.
  - **تنظیمات رتبه‌بندی اختراع نشد:** §۲۱ «تنظیمات رتبه‌بندی» را نام می‌برد، ولی قاعده رتبه‌بندی در DEC-0172 یک ثابت نسخه‌دار در کد است و تبدیل آن به کلید قابل‌تغییر یعنی ساختن سیاستی که مالک محصول تأیید نکرده است؛ پس نسخه رتبه‌بندی فقط خواندنی می‌ماند.
- **دلیل:** Requirements-Phase-2 §۲۱، §۲۲، §۲۳؛ DEC-0172؛ الگوی `markBreedDuplicate` از PROMPT-003.
- **اثر:** اگر بعداً ادغام برگشت‌پذیر لازم شود، همان ستون با یک تصمیم تازه پاک می‌شود و تاریخچه سر جایش می‌ماند. ادغام حساب‌های کاربری و اسناد فاز یک در دامنه این پرامپت نبود.

## DEC-0177 — §۲۰ از قبل اجرا شده بود؛ این پرامپت آن را یک‌جا نوشت، سقف خروجی گذاشت و قاعده نرخ را یکی کرد

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۱۷ · **نوع:** SECURITY
- **تصمیم:**
  - **هیچ‌چیز از نو ساخته نشد.** بررسی کد نشان داد بیشتر بندهای §۲۰ از قبل و توسط پرامپت صاحب همان قابلیت اجرا شده‌اند: دسترسی شیءمحور (`canReadFile` و مجوز پیش از هر دسترسی به دیسک)، کنترل آپلود و اسکن نوع فایل (امضای بایت، سقف حجم هر کاربرد، کلید ذخیره‌سازی ساخته‌شده توسط سرور، عدم بازنویسی، مسیر محصور)، حداقل‌سازی PII (فهرست کلیدهای حذف‌شده در Audit)، رضایت نمایش تماس (با ثبت مقدار قبلی/جدید)، و شناسه عمومی جدا از شناسه داخلی (slug تصادفی). هشت بند §۲۰ هم از قبل در `tests/db/security.test.ts` اثبات شده بودند و **دوباره تست نشدند** (قاعده «پرهیز از تکرار تست»).
  - **قاعده نرخ یکی شد:** چهار جا سقف داشتند و هر چهار، همان حساب «شمردن در پنجره و مقایسه با سقف مدیریت‌شده» را از نو نوشته بودند. این حساب به `src/privacy/limits.ts` منتقل شد و گزارش محتوا، پیشنهاد رکورد و استعلام اصالت از همان استفاده می‌کنند. جریان کد ورود **دست نخورد**: قفل، فاصله ارسال دوباره و یک‌بارمصرف‌بودن یک پروتکل است، نه یک سقف.
  - **پیام رد هرگز شمارش نمی‌دهد:** متن مشترک فقط می‌گوید سقف پر شده و بعداً تلاش کنید؛ هیچ عددی از تلاش باقی‌مانده یا خودِ سقف در آن نیست، چون حدس‌زننده هر دو را راهنما می‌خواند.
  - **سقف خروجی جایی گذاشته شد که واقعاً نداشت:** پنج صف عملیاتی (درخواست دامپزشک، Claim مرکز، پیشنهاد، گزارش باز و گزارش‌های تصمیم‌گرفته) صفحه خود را دستی می‌ساختند و از قرارداد مشترک فهرست رد نمی‌شدند، پس `pageSize` بزرگ یعنی خواندن کل جدول. حالا همه از `boundedRows` می‌گذرند. **عدد تازه‌ای اختراع نشد:** همان `MAX_PAGE_SIZE` موجود است، و خواندنی که سقف مستند خودش را دارد (تاریخچه عملیاتی با ۲۰۰ ردیف) همان را صریح پاس می‌دهد.
  - **بندهای §۲۰ به‌صورت داده نوشته شدند** در `src/privacy/policy.ts`: هر بند با متن قاعده و **مسیر کدی که اجرایش می‌کند**. تست دامنه تأیید می‌کند هر بند صاحبی در کد دارد و به مسیر واقعی اشاره می‌کند؛ پس این فهرست یک ادعا در گزارش نیست، یک چیز آزمون‌پذیر است.
  - **جعل هویت:** قاعده از قبل بود (یکتایی کد ملی در سطح ایندکس، قفل‌شدن آن پس از تأیید احراز هویت، رد کد نظام متعلق به پروفایل دیگر و هدایت به Claim، یک پروفایل دامپزشک برای هر حساب)؛ این پرامپت آن را نوشت و تست پایگاه‌داده برایش اضافه کرد.
- **دلیل:** Requirements-Phase-2 §۲۰؛ قواعد قطعی پرامپت؛ قاعده کیفیت «تمرکز تست روی ماندگاری، پرداخت، مجوز و نسخه‌بندی، نه تکرار».
- **اثر:** هر قابلیت تازه‌ای که سقفی لازم دارد از همان `limits.ts` استفاده می‌کند و پیام یکسان می‌دهد. اگر روزی خروجی واقعی (CSV) لازم شود، باید با تصمیم تازه و سقف و Audit خودش ساخته شود؛ امروز هیچ مسیری خروجی انبوه نمی‌دهد.

## DEC-0178 — کنتراست محاسبه می‌شود نه ادعا؛ رندر per-request می‌ماند؛ تصویر با ETag و فضای رزروشده

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۱۸ · **نوع:** QUALITY
- **تصمیم:**
  - **کنتراست به‌جای ادعا، حساب شد.** «کنتراست بررسی شد» جمله‌ای است که می‌شود بدون هیچ پشتوانه‌ای در گزارش نوشت. پس نسبت‌ها در `src/a11y/contrast.ts` با فرمول WCAG 2.1 روی **مقدار واقعی توکن‌ها** حساب می‌شوند و تست، هر جفتی را که صفحه‌های عمومی واقعاً کنار هم می‌گذارند در سطح لازم خودش می‌سنجد (متن عادی ۴.۵، نشانه غیرمتنی ۳.۰). تست دیگری تأیید می‌کند این مقدارها همان چیزی‌اند که `app/globals.css` منتشر می‌کند، پس این فایل نمی‌تواند به منبع دوم حقیقت تبدیل شود.
  - **متن غیرفعال سنجیده نشد.** WCAG 1.4.3 آن را مستثنا کرده است؛ ادعای نسبت برایش یعنی ساختن قاعده‌ای که وجود ندارد. توکنش ثبت شد تا تصمیم بعدی بتواند بازبینی‌اش کند.
  - **رندر per-request صفحه‌های عمومی دست نخورد.** `app/(public)/layout.tsx` کامنت خودش می‌گفت caching ایستا کار ۰۱۸ است؛ ولی همان لایه، هدر را بر اساس «چه کسی وارد شده» و canonical را از `SITE_URL` زمان اجرا می‌سازد. ایستاکردن آن یعنی نمایش وضعیت حساب اشتباه — پس دلیلش مستند شد و تغییری داده نشد. بهبود واقعی جای دیگری بود.
  - **تصویر محتوا: ETag به‌جای عمر Cache بلند.** `/media/[id]` طبق DEC-0160 باید هر بار تصمیم «آیا این محتوا هنوز دیدنی است» را بگیرد، پس `must-revalidate` سر جایش ماند. اما همان فایل از قبل `sha256` دارد؛ حالا به‌عنوان ETag برمی‌گردد و بازدید دوم با ۳۰۴ پاسخ می‌گیرد. یعنی بایت‌ها یک‌بار از شبکه می‌گذرند بدون اینکه تصمیم Moderation در حافظه پنهان زنده بماند.
  - **فضای تصویر رزرو شد، چون ابعاد ذخیره نمی‌شود.** نه `stored_file` و نه `content_item` عرض و ارتفاع ندارند، پس گذاشتن `width`/`height` واقعی نیازمند Migration و اندازه‌گیری هنگام آپلود بود — خارج از این پرامپت. به‌جایش تصویر بالای مقاله همان `aspect-[16/9]` کارت‌ها را گرفت تا متن را پایین نپراند (CLS) و چون بزرگ‌ترین عنصر بالای صفحه است با `fetchPriority="high"` و بدون lazy بارگذاری می‌شود؛ تصویر کارت‌ها lazy می‌ماند.
  - **تست تکراری نوشته نشد.** RTL بدون سرریز افقی، شناسه LTR داخل متن فارسی، اعلام خطای فرم، تله focus در Overlay، ترتیب RTL کنش‌ها و اندازه هدف لمسی از قبل در `tests/browser/rtl.test.ts` و گیت `visual.test.ts` اثبات شده‌اند. سوییت تازه فقط چیزی را می‌سنجد که آن دو از آن قدیمی‌ترند: صفحه‌های عمومی ساخته‌شده در ۰۱۲ تا ۰۱۶، مسیر صفحه‌کلید تا محتوا، و رفتار تصویر.
- **دلیل:** Requirements-Phase-2 §۱۹، §۲۴؛ DEC-0160 (تصویر فقط تا وقتی محتوا دیدنی است)، DEC-0150 (canonical از زمان اجرا)، قاعده کیفیت «پرهیز از تکرار تست».
- **اثر:** اگر روزی ابعاد تصویر هنگام آپلود ذخیره شود، `width`/`height` واقعی جای `aspect-ratio` را می‌گیرد. srcset پاسخگو ساخته نشد چون مسیر رسانه یک نسخه اصلی می‌دهد؛ نسخه‌های چندگانه تصمیم و خط لوله تصویر خودش را می‌خواهد.

## DEC-0179 — مهاجرت افزایشی به‌جای ادعای برگشت‌پذیری؛ Seed نسخه‌دار Taxonomy؛ دو تست شکننده که نقص محصول جا می‌زدند

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۱۹ · **نوع:** DATA/QUALITY
- **تصمیم:**
  - **«Migration برگشت‌پذیر» با فایل `down` پاسخ داده نشد.** drizzle-kit آن را نمی‌سازد و نوشتن دستی سی اسکریپت بازگشت یعنی سی اسکریپت آزمون‌نشده که فقط در بدترین روز اجرا می‌شوند. چیزی که واقعاً بازگشت نسخه را ممکن می‌کند این است که هیچ مهاجرتی چیزی را که نسخه قبلی کد می‌خواند حذف یا تغییرنام ندهد. همین ثابت در `tests/domain/migrations.test.ts` روی **متن خود SQL** سنجیده می‌شود: نبودِ `DROP TABLE`/`DROP COLUMN`/`RENAME`/`DROP CONSTRAINT`، و اینکه هر `SET NOT NULL` پیش از خودش همان ستون را پر کرده باشد. `DROP NOT NULL` عمداً مجاز است چون شل‌کننده است، نه شکننده.
  - **Taxonomyها Seed نسخه‌دار گرفتند، بدون جابه‌جاکردن داده موجود.** مهاجرت‌های ۰۰۱۷، ۰۰۲۰ و ۰۰۲۲ نسل اول را درج کرده بودند؛ مهاجرت اما یک رویداد یک‌بارمصرف است و نه ورودی تازه‌ای اضافه می‌کند، نه می‌گوید این پایگاه‌داده کدام نسل را دارد. حالا کاتالوگ در `src/db/seed/taxonomy.ts` است و `taxonomy_seed` (مهاجرت ۰۰۲۹) نسخه هر Taxonomy را جدا نگه می‌دارد تا افزودن یک گونه، ادعای بازبینی امکانات مرکز نباشد.
  - **قاعده اجرای دوباره: فقط ورودی غایب اضافه می‌شود.** ردیف موجود هرگز به‌روزرسانی، تغییرنام یا دوباره فعال نمی‌شود؛ اپراتوری که نامی را عوض کرده یا ورودی‌ای را خاموش کرده تصمیم گرفته است و redeploy نباید آن را بی‌صدا لغو کند.
  - **جای نسخه، `product_setting` نشد.** آن ردیف‌ها روبه‌روی اپراتورند و گروه مجوز، برچسب و نسخه ممیزی‌شده خود را دارند؛ نسل نصب‌شده دفترداری داخلی است و گذاشتنش آنجا هم صفحه تنظیمات را گمراه می‌کرد و هم سطح مجوز را بی‌دلیل باز می‌کرد.
  - **هیچ داده‌ای اختراع نشد.** کاتالوگ عیناً از همان مهاجرت‌ها رونویسی شد و تستی آن را با محتوای واقعی پایگاه‌داده مقایسه می‌کند؛ یک تخصص یا نوع مرکزِ «منطقی به‌نظر رسیده» همان واقعیت ساختگی است که قواعد منع کرده‌اند.
  - **دو تست شکننده اصلاح شدند، نه محصول.** ماتریس کامل — که از `7c74577` اجرا نشده بود — دو شکست داد: ناوبری ادمین که فهرستش منجمد بود و صفحه‌های تازه را «پیوند مرده» می‌خواند، و انتخاب دامپزشک با `index: 1` از یک فهرست **سراسری** که با کاشتن یک دامپزشک در سوییت دیگر، مرکز را به حسابی بدون عضویت فعال می‌چسباند و شش تست Finder را با timeout می‌انداخت. هر دو ادعای «نقص محصول» بودند و نبودند.
  - **`KEEP_BROWSER_DB=1` به harness اضافه شد.** عیب‌یابی یک شکست مرورگر یعنی پرسیدن از خود داده؛ پایگاه‌داده اجرا به‌صورت پیش‌فرض حذف می‌شود (وگرنه تله اجرای بعدی است) و این پرچم فقط وقتی خواسته شود نگهش می‌دارد.
- **دلیل:** Requirements-Phase-2 §۲۳ و §۲۴؛ P2-D15؛ قاعده کیفیت «تست اجرانشده را موفق ننویس» و «شواهد واقعی، نه ادعا».
- **اثر:** افزودن ورودی تازه به هر Taxonomy از این پس با بالابردن نسخه همان کاتالوگ انجام می‌شود و مهاجرت نمی‌خواهد. اگر روزی مهاجرتی واقعاً به حذف ستون نیاز داشته باشد، تست افزایشی‌بودن آن را رد می‌کند و آن تغییر باید تصمیم و مسیر مهاجرت دومرحله‌ای خودش را داشته باشد.

## DEC-0180 — لینک‌ها دنبال می‌شوند نه ادعا؛ سلامت نسل Taxonomy را می‌گوید؛ اسناد تحویل فاز دو کنار فاز یک

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** فاز ۲ / ۰۲۰ · **نوع:** RELEASE/QUALITY
- **تصمیم:**
  - **«لینک خراب نداریم» با دنبال‌کردن لینک‌ها اثبات شد، نه با فهرست.** تست‌های موجود فقط می‌گفتند sitemap چه بخش‌هایی دارد و هدر چه چیزی را لینک می‌کند. `tests/browser/release.test.ts` حالا نشانی‌های واقعی را از خودِ sitemap می‌خواند، هر کدام را درخواست می‌کند و ۲۰۰ می‌خواهد؛ ضمناً تأیید می‌کند هیچ نشانی محافظت‌شده‌ای منتشر نشده باشد — نشتِ یک نشانی کاربردی در sitemap، بدتر از یک لینک مرده است. لینک‌های داخل صفحه‌های عمومی هم دنبال می‌شوند؛ لینک محافظت‌شده باید ورود بخواهد، نه ۴۰۴.
  - **سلامت، نسل Taxonomy را گزارش می‌کند.** DEC-0179 نسل نصب‌شده را در `taxonomy_seed` ثبت کرد؛ اپراتور نباید برای خواندنش به ردیف‌های جدول نگاه کند. `/api/health` که از قبل دسترسی پایگاه‌داده، شمار مهاجرت‌ها، کلیدهای تعیین‌نشده و وضعیت Adapterها را می‌گفت، حالا نسل هر Taxonomy را هم می‌گوید.
  - **`degraded` همچنان پاسخ درست است.** تا واردنشدن تعرفه و اعتبارنامه واقعی، سلامت باید بگوید ناقص است؛ تست مرورگر همین را قفل می‌کند تا کسی روزی آن را به `ok` «اصلاح» نکند.
  - **پیام شکست تست، خودش شاهد است.** `waitForText` در سوییت محتوا با timeout خالی می‌افتاد و هیچ نمی‌گفت؛ حالا متن واقعی بنر را در خطا می‌آورد. یک شکست که علتش را نمی‌گوید، نیم‌شاهد است.
  - **ناپایداری پنهان نشد.** همان سوییت در دو اجرای کامل روی گام آپلود تصویر timeout داد و در دو اجرای دیگر سبز شد. علت اثبات نشده است، پس نه «نقص محصول» اعلام شد و نه نادیده گرفته شد: به‌عنوان Known Gap با شواهد هر چهار اجرا ثبت شد.
  - **اسناد فاز یک بازنویسی نشدند.** `release-readiness.md` تاریخ تحویل فاز یک است؛ فاز دو سند خودش را گرفت و Runbook و راهنمای اپراتور فقط بخش تازه گرفتند. بازنویسی سند قبلی یعنی پاک‌کردن ثبت تاریخی چیزی که واقعاً آن روز دیده شده بود.
- **دلیل:** Requirements-Phase-2 §۲۴ و §۲۶؛ قواعد کیفیت «screenshot شاهد نیست» و «تست اجرانشده را موفق ننویس».
- **اثر:** افزودن هر بخش تازه به sitemap خودبه‌خود زیر پوشش بررسی لینک می‌رود. اگر روزی کلیدی واقعاً پر شود، تست سلامت باید به‌روز شود — و همان به‌روزرسانی، نشانه‌ای عمدی است که محصول از «ناقص» به «آماده» رفته.

## DEC-0181 — انطباق با DS برند به‌جای بازطراحی؛ آیکون‌ها از خود سند، نه تغییرمقیاس

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** خارج از بسته (درخواست مستقیم کاربر) · **نوع:** BRAND/QUALITY
- **تصمیم:**
  - **درخواست «بازطراحی سایت با Design System» اجرا نشد و دلیلش ثبت شد.** فایل `2GMJPgnnenGnr1zBN2yH5h` یک Design System رابط کاربری نیست: محتوایش لوگو، قفل‌های فارسی و انگلیسی، رنگ‌بندی سطح، آیکون اپ و favicon، فضای امن و قواعد مونو است. هیچ کامپوننت UI، مقیاس تایپوگرافی، توکن فاصله‌گذاری یا صفحه‌ای ندارد. بازطراحی بر پایه آن یعنی اختراع همه‌چیز از سلیقه، که خلاف ترتیب منابع CLAUDE.md است (پروتوتایپ برای چیدمان، DS برای توکن). کاربر میان سه گزینه، «انطباق با DS برند» را انتخاب کرد.
  - **سه دارایی favicon جدا شدند، نه یک تصویر تغییرمقیاس‌یافته.** بخش ۰۸ سند صریح می‌گوید زیر ۲۴ پیکسل باید گلیف ساده‌شده تک‌مرکب استفاده شود، چون جزئیات داخلی در اندازه favicon درهم می‌رود. پس ۱۶، ۳۲ و ۴۸ سه اثر متفاوت‌اند و هر سه از همان نودهای رسمی (`24:353`، `24:359`، `24:365`) خروجی گرفته شدند. تستی تأیید می‌کند این سه فایل واقعاً متفاوت‌اند؛ اگر روزی کسی یکی را کپی کند، همان تست می‌افتد.
  - **تصویر اشتراک از لوگوی سازمان جدا شد.** پیش‌تر هر دو `SITE_IMAGE` (نماد ۱۴۴×۱۶۲) بودند و محدودیت ثبت‌شده PROMPT-002 می‌گفت تصویر اشتراک واقعی وجود ندارد. حالا `SHARE_IMAGE` همان دارایی رسمی Social (`24:321`، ۵۱۲×۵۱۲) است و `SITE_IMAGE` برای schema.org نماد می‌ماند، چون آنجا نشانِ خالص انتظار می‌رود. تصویر ۱۲۰۰×۶۳۰ **ساخته نشد**؛ سند چنین دارایی ندارد و اختراعش یعنی طراحی‌کردن به‌جای طراح.
  - **manifest اندروید ساخته نشد و دارایی ۴۳۲ حذف شد.** سند آیکون اندروید و لایه‌های adaptive دارد، ولی manifest یعنی اعلام نصب‌پذیری محصول — تصمیمی محصولی که کسی نگرفته. فایل بلااستفاده هم نگه داشته نشد.
  - **قواعد بخش ۰۸ به‌صورت داده نوشته شدند، نه نثر.** جفت‌های مجاز و ممنوع Tone×Surface در `src/brand/assets.ts` هستند و تست تأیید می‌کند جفتی که محصول واقعاً رندر می‌کند مجاز است. حداقل کنتراست نشان از `AA_NON_TEXT` همان ماژول کنتراست خوانده می‌شود تا عدد دومی که می‌تواند واگرا شود ساخته نشود.
- **دلیل:** CLAUDE.md (ترتیب منابع، Figma فقط‌خواندنی، ثبت Decision برای کار خارج از پرامپت)؛ بخش‌های ۰۴، ۰۵، ۰۶ و ۰۸ همان سند؛ قاعده کیفیت «واقعیت جعلی نساز».
- **اثر:** اگر روزی سطح تیره به محصول اضافه شود، `PRODUCT_SURFACE` با آن جابه‌جا می‌شود و تست جفت‌ها بلافاصله ترکیب تازه را می‌سنجد. آیکون adaptive اندروید و تصویر اشتراک عریض، هر دو به‌عنوان کمبود ثبت شدند تا با دارایی رسمی بسته شوند، نه با حدس.

## DEC-0182 — بازطراحی سلیقه‌ای سطح عمومی، با مرزهای صریح

- **تاریخ:** ۲۰۲۶-۰۹-۱۲ · **PROMPT:** خارج از بسته (اجازه صریح کاربر) · **نوع:** DESIGN
- **زمینه:** کاربر پس از توضیح پیامدها، صریح گفت «سلیقه‌ای بازطراحی کن». این یعنی ترتیب منابع در یک نقطه کنار گذاشته می‌شود: ترکیب‌بندی سطح عمومی دیگر از پروتوتایپ تأییدشده نمی‌آید. چون CLAUDE.md برای هر کار خارج از پرامپت ثبت Decision می‌خواهد، مرزها اینجا نوشته می‌شوند تا بعداً معلوم باشد چه چیزی سلیقه بود و چه چیزی منبع.
- **تصمیم:**
  - **فقط سطح عمومی بازطراحی می‌شود.** پنل‌های عملیاتی (`/admin`، `/assoc`، `/genetics`، `/review`، `/content`، `/author`) دست نمی‌خورند: آن‌ها UI محصول‌اند نه صفحه بازاریابی، و بازطراحی سلیقه‌ای‌شان یعنی شکستن ده‌ها تست مرورگر بدون سود.
  - **پالت، لوگو، مسیرها، شناسه‌های تست و متن حفظ می‌شوند.** رنگ‌ها همان توکن‌های پروتوتایپ‌اند؛ تست کنتراست WCAG روی همین مقدارها بسته شده و تغییرشان یعنی شکستن شاهدی که ساخته شده. آنچه عوض می‌شود ترکیب‌بندی، سلسله‌مراتب، ریتم فاصله و حرکت است.
  - **حالت تیره ساخته نمی‌شود.** سند برند فقط توکن سطح روشن دارد و `globals.css` عمداً `color-scheme: light` است. ساختن پالت تیره یعنی اختراع رنگی که طراح نداده.
  - **حرکت فقط CSS بومی است.** پروژه هیچ کتابخانه انیمیشنی ندارد و فایل‌های بسته باید دست‌نخورده بمانند، پس Motion و GSAP نصب نشد. سطح حرکت واقع‌بینانه ۴ است: ورود مرحله‌ای hero، آشکارسازی هنگام اسکرول با `animation-timeline` بومی، و بازخورد لمسی روی کنش‌ها. همه زیر `prefers-reduced-motion` خاموش می‌شوند. ادعای حرکتی که ساخته نشده، نوشته نشد.
  - **یک توکن تازه اضافه شد و CODE-ONLY علامت خورد.** مقیاس تایپی منتشرشده در ۳۲px تمام می‌شود و برای تیتر نمایشی کافی نیست. `--text-display` با همان قاعده‌ای اضافه شد که مخزن از قبل برای `--size-content-*` و `--hz-focus-ring` داشت: مقدار غیرمنتشرشده، صریح علامت‌خورده، قابل ردیابی.
  - **هیچ عکسی اختراع نشد.** منابع پروژه هیچ عکاسی ندارند و ابزار تولید تصویر در دسترس نیست. به‌جای پرکردن صفحه با عکس تصادفی بی‌ربط یا تصویر ساختگی محصول، ترکیب‌بندی روی نشان رسمی برند و تایپوگرافی بنا شد و جای عکس‌های لازم صریح به کاربر گزارش شد.
- **دلیل:** درخواست صریح کاربر؛ CLAUDE.md (ثبت Decision برای کار خارج از پرامپت)؛ قواعد کیفیت (ادعای اجرانشده ممنوع، واقعیت ساختگی ممنوع).
- **اثر:** وفاداری ترکیب‌بندی سطح عمومی به پروتوتایپ از این پس **ادعا نمی‌شود**؛ گیت بصری همچنان توکن‌ها و نبودِ سرریز افقی را می‌سنجد، ولی «مطابق پروتوتایپ» برای این صفحه‌ها دیگر درست نیست و در سند تحویل هم همین نوشته شد.

## DEC-0183 — تصویر عمومی برای رکوردهای دایرکتوری: گسترش قاعده موجود، نه خط لوله دوم

- **تاریخ:** ۲۰۲۶-۰۹-۱۳ · **PROMPT:** خارج از بسته (درخواست کاربر) · **نوع:** FEATURE/MEDIA
- **زمینه:** تا امروز فقط محتوا تصویر داشت (DEC-0160) و در گزارش‌ها هم ثبت شده بود که تصویر مرکز و انجمن ساخته نشده. کاربر خواست همه بخش‌های عمومی تصویر داشته باشند.
- **تصمیم:**
  - **همان قاعده، نه قاعده تازه.** تصویر رکورد در ذخیره‌سازی خصوصی می‌ماند و فقط از `/media/[id]` سرو می‌شود، آن‌هم تا وقتی رکوردِ حاملش عمومی است. پنهان‌کردن یا پیش‌نویس‌کردن رکورد، تصویرش را هم برمی‌دارد و هر شناسه دیگر همان ۴۰۴ را می‌گیرد. این دقیقاً قاعده DEC-0160 است که به چهار موجودیت دیگر تعمیم داده شد.
  - **یک ماژول با توصیف‌گر، نه چهار سرویس کپی‌شده.** `src/media/public-image.ts` فقط جدول، ستون فایل، وضعیت عمومی و کنش Audit هر نوع را می‌داند؛ بقیه کار یکی است و یک‌بار نوشته شد. **مجوزدهی عمداً آنجا نیست:** هر سرویس خودش می‌داند چه کسی رکوردش را ویرایش می‌کند و پس از تصمیم‌گیری این ماژول را صدا می‌زند.
  - **نوع فایل تازه یعنی قاعده آپلود تازه.** چهار مقدار enum اضافه شد و هر چهار در `PURPOSE_RULES` فقط `image/jpeg` و `image/png` با سقف ۵ مگابایت می‌پذیرند (تشخیص از امضای بایت، نه از پسوند)، و در `FILE_REVIEWERS` هم ثبت شدند تا مسیر خصوصی فایل به‌طور پیش‌فرض به هیچ پوسته‌ای باز نشود.
  - **متن جایگزین اجباری است** و فضای تصویر با ابعاد واقعی رزرو می‌شود؛ تصویری که کسی نمی‌بیند بدتر از نبودِ تصویر است و تصویری که چیدمان را می‌پراند همان CLS است که ۰۱۸ بست.
  - **تصویر واقعی در کارت اشتراک‌گذاری.** صفحه نژاد، مرکز، دامپزشک و انجمن حالا تصویر خودشان را به `buildMetadata` می‌دهند، پس OG هر صفحه تصویر همان صفحه است نه نشان عمومی برند.
  - **عکس‌های فعلی placeholder توسعه‌اند و خودشان می‌گویند.** متن جایگزین هر کدام صریح نوشته «جایگزین موقت محیط توسعه، عکس واقعی نیست». هیچ عکسی بدون مجوز روشن در مخزن commit نشد؛ جایگزینی‌شان یک آپلود از پنل است، نه تغییر کد.
- **دلیل:** درخواست کاربر؛ DEC-0160 (تصویر عمومی فقط تا وقتی رکورد دیدنی است)؛ §۲۰ کنترل آپلود؛ DEC-0178 (رزرو فضا).
- **اثر:** افزودن تصویر به هر موجودیت تازه یعنی یک توصیف‌گر، نه یک سرویس. اگر روزی عکس واقعی با عکاس و مجوز بیاید، فقط فایل عوض می‌شود و متن جایگزین باید همراهش به‌روز شود؛ «جایگزین موقت» در متن جایگزین، همان نشانه‌ای است که یادآوری می‌کند هنوز جایگزین نشده.

## DEC-0184 — متن غنی بدون HTML نامعتمد: زبان محدود، رندر به المان، نه پاک‌سازی

- **تاریخ:** ۲۰۲۶-۰۹-۱۳ · **PROMPT:** خارج از بسته (درخواست کاربر) · **نوع:** FEATURE/SECURITY
- **زمینه:** بدنه محتوا تا امروز فقط بند ساده بود و در گزارش PROMPT-004 هم ثبت شده بود که متن غنی «تصمیم خودش را می‌خواهد». کاربر ویرایشگر متن غنی خواست.
- **تصمیم:**
  - **HTML ذخیره و پاک‌سازی نمی‌شود.** رایج‌ترین راه (ویرایشگر `contenteditable` که HTML می‌دهد و سرور آن را sanitize می‌کند) یعنی امنیت محصول برای همیشه به درستی یک sanitizer گره می‌خورد. به‌جایش یک **زبان نشانه‌گذاری محدود** ذخیره می‌شود و رندر آن **المان React** می‌سازد، نه رشته markup. هیچ `dangerouslySetInnerHTML` در مسیر نیست، پس `<script>`ی که نویسنده بچسباند فقط متنی درباره یک اسکریپت است. این ضمانت ساختاری است، نه فهرست سیاه.
  - **کتابخانه‌ای نصب نشد.** فایل‌های بسته باید دست‌نخورده بمانند، و این زبان آن‌قدر کوچک است که پارسر و رندرش کمتر از وابستگی تازه هزینه دارد: عنوان، زیرعنوان، فهرست نشانه‌دار و شماره‌دار، نقل قول، پررنگ، مورب و پیوند.
  - **نشانی پیوند فهرست سفید دارد:** فقط `http`، `https` و نشانی داخلی همین سایت. `javascript:`، `data:` و نشانی پروتکل‌نسبی (`//…`) رد می‌شوند و در آن حالت **متن پیوند حفظ می‌شود**، چون جمله باید خوانا بماند. پیوند بیرونی `rel="nofollow noopener noreferrer"` می‌گیرد.
  - **نشانه ناتمام، نویسه است.** اگر نویسنده `*` تنها بنویسد همان ستاره را می‌بیند. ویرایشگری که ورودی کاربر را «تصحیح» کند، چیزی می‌نویسد که نویسنده ننوشته.
  - **ویرایشگر همان textarea است، با نوار ابزار و پیش‌نمایش.** مقدار ذخیره‌شده متن می‌ماند، پس نسخه‌ها، تفاوت‌ها و بازگردانی revision دست‌نخورده کار می‌کنند.
- **دلیل:** درخواست کاربر؛ §۲۰ (ورودی کاربر نامعتمد است)؛ قاعده «هیچ وابستگی تازه بدون ضرورت».
- **اثر:** افزودن یک نشانه تازه یعنی یک شاخه در پارسر و یک المان در رندر؛ هیچ نقطه‌ای در مسیر باز نمی‌شود که HTML بپذیرد. اگر روزی جدول یا تصویر درون‌متنی لازم شود، همان‌جا اضافه می‌شود و همچنان HTML وارد نمی‌شود.

## DEC-0185 — ستون «مرتبط» فقط از رکورد منتشرشده، و خالی که رندر نمی‌شود

- **تاریخ:** ۲۰۲۶-۰۹-۱۳ · **PROMPT:** خارج از بسته (درخواست کاربر) · **نوع:** FEATURE/UX
- **تصمیم:**
  - **ستون کنار صفحه فقط رکورد منتشرشده را نشان می‌دهد** و هیچ رکوردی کنار خودش نمی‌آید. گروهی که چیزی ندارد رندر نمی‌شود و ستونی که هیچ گروهی ندارد اصلاً ساخته نمی‌شود؛ صفحه باریک‌تر بهتر از ستونی پر از جعبه خالی است.
  - **«مراکز مشابه» وعده نزدیکی جغرافیایی نمی‌دهد.** مرکز شهرش را روی شعبه‌هایش نگه می‌دارد نه روی خودش، پس ترتیب بر پایه نوع مرکز است و کامنت کد هم همین را می‌گوید. ادعای «نزدیک شما» بدون داده محل، ادعای ساختگی بود.
  - **محتوای وارداتی با ساختار خودش ذخیره شد.** سندهای گوگل بار اول تخت ذخیره شده بودند و عنوان‌هایشان بند معمولی خوانده می‌شد. با همان زبان قالب‌بندی (DEC-0184) دوباره وارد شدند: عنوان عنوان ماند، فهرست فهرست، و جدول سند به فهرست تبدیل شد. **هیچ جمله‌ای بازنویسی، اضافه یا حذف نشد؛ فقط نشانه‌های اطرافش عوض شد.**
  - **بند تکراری حذف شد:** جمله آغازین سند همان خلاصه است و صفحه خلاصه را بالای متن چاپ می‌کند، پس در بدنه تکرار نمی‌شود.
- **دلیل:** درخواست کاربر؛ قاعده «خالی را خالی نشان بده، نه پر از جعبه»؛ منع ادعای بی‌پشتوانه.
- **اثر:** افزودن گروه تازه به ستون یعنی یک تابع پرس‌وجو و یک ردیف در فهرست گروه‌ها. اگر روزی شهر مرکز روی خود رکورد ذخیره شود، «مشابه» می‌تواند واقعاً به معنی نزدیک باشد.
