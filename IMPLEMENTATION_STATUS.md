# وضعیت واقعی پیاده‌سازی

Status: MEMBERSHIP_READY — ورود، حساب، KYC، پرداخت و عضویت مادام‌العمر کار می‌کنند؛ فلوهای حیوان هنوز ساخته نشده‌اند.

وضعیت‌ها جدا نگه داشته می‌شوند و به یک checkbox تقلیل پیدا نمی‌کنند:

| بُعد | وضعیت | توضیح |
|---|---|---|
| code implementation | MEMBERSHIP_READY | زیرساخت، پوسته‌ها، هویت/KYC، پرداخت و عضویت مادام‌العمر با Eligibility مشترک. ثبت حیوان از PROMPT-006 |
| automated checks | PASS | ۱۴۱ تست واحد/سرویس + ۲۹ تست مرورگر واقعی + ۲۳ تست بسته |
| visual fidelity | UNVERIFIED | پوسته‌ها با توکن‌های واقعی رندر و در Chromium بازبینی شدند، اما هیچ تطبیق پیکسلی با پروتوتایپ ادعا نمی‌شود |
| integration readiness | NOT_CONFIGURED | SMS، درگاه، نقشه و رندر سند هیچ‌کدام پیکربندی نشده‌اند |
| production readiness | NOT_READY | ۱۱ کلید تنظیمات هنوز داده واقعی ندارند؛ آداپتور واقعی وجود ندارد |

## PROMPT-001 — بررسی کد و رفرنس‌ها و انتخاب معماری

**وضعیت:** COMPLETE · work commit `f15cf47`.

بازرسی مخزن و محیط، خواندن واقعی Flow Map/پروتوتایپ/DS با ثبت node-id، تأیید هش منبع، انتخاب Stack و ثبت DEC-0001…DEC-0009، و نوشتن مدل دامنه، ماتریس مجوز، جدول Eligibility و چک‌لیست مسیر برای ۲۹ بخش، ۱۹ تصمیم و ۳۳ ردیف پذیرش.

## PROMPT-002 — زیرساخت، داده، تنظیمات و تاریخچه

**مرحله جاری:** تکمیل‌شده.

**رفتار تکمیل‌شده:**

- اپلیکیشن Next.js 15 + TypeScript strict با پوسته RTL فارسی، build موفق و صفحه ورودی که **وضعیت واقعی زیرساخت** را نشان می‌دهد (نه صفحه موفقیت ساختگی).
- `docker-compose.yml` با PostgreSQL 16؛ migration `0000_foundation` با ۹ جدول و constraintهای یکتایی لازم.
- قراردادهای Typed: شناسه‌های Branded (§۲۳.۲)، خطا با قفل سه‌جزئی و `VERSION_STALE`، صفحه‌بندی، تقویم، پول و Resume Context.
- تنظیمات نسخه‌دار در دیتابیس با ۲۲ کلید، مجوز به تفکیک هفت گروه، Audit با before/after در همان تراکنش و snapshot نسخه.
- اعلان با Entity/Step/Route و تحویل idempotent؛ فایل خصوصی با کنترل امضای بایت، سقف حجم، امنیت مسیر و دانلود مجوزدار.
- آداپتورهای بیرونی با وضعیت صادقانه چهارگانه و امتناع صریح از اجرا در production.

**فایل‌ها:** `package.json`، `tsconfig.json`، `next.config.ts`، `postcss.config.mjs`، `drizzle.config.ts`، `docker-compose.yml`، `.env.example`، `app/**` (۴ فایل)، `src/**` (۲۰ فایل)، `tests/**` (۸ فایل).

**migration:** `src/db/migrations/0000_foundation.sql` — جدول‌های `account`، `account_role`، `product_setting`، `audit_event`، `notification`، `notification_delivery`، `stored_file`، `reference_breed`، `pedigree_issuer`.

**بررسی‌های واقعاً اجراشده:**

| بررسی | دستور | نتیجه |
|---|---|---|
| Typecheck | `npx tsc --noEmit` | PASS (exit 0) |
| تست محصول | `node --test "tests/**/*.test.ts"` | PASS — ۶۲/۶۲ |
| تست بسته | `node --test tools/tests/runner.test.mjs .claude/tests/guardrails.test.mjs` | PASS — ۲۳/۲۳ |
| Build | `npm run build` | PASS — ۴ مسیر |
| migration و seed واقعی | `node src/db/migrate.ts` + `node src/db/seed/run.ts` (هر کدام دو بار) | PASS — بار دوم: ۰ درج، ۲۲ حفظ |
| اجرا و health | `next start -p 3111` + `curl /api/health` | PASS — HTTP 200، `status: degraded` |
| دسترسی فایل خصوصی | `curl /api/files/<uuid>` بدون Session | PASS — HTTP 401 `UNAUTHENTICATED` |

**تصمیم‌های جدید:** DEC-0010 تا DEC-0018.

**وابستگی بیرونی:** SMS/OTP، درگاه پرداخت، نقشه و رندر سند — همگی `NOT_CONFIGURED`.

**مانع واقعی:** هیچ مانعی مرحله را مسدود نکرد. اجرای تست‌های دیتابیس به بالا بودن سرویس docker وابسته است (DEC-0017).

**دستور ادامه:** `node tools/runner.mjs prepare` سپس اجرای PROMPT-003.

## داده واقعی که هنوز وارد نشده است

۱۱ کلید تنظیمات با مقدار `NOT_CONFIGURED` و بدون هیچ مقدار نمونه: تعرفه برگه ثبتی، شجره‌نامه، مجوز جفت‌گیری، ثبت کنل و Puppy Card؛ نام، تماس، نشانی، شماره حساب، شماره کارت و هزینه آزمایش مرکز ژنتیک. فهرست صادرکنندگان موردتأیید انجمن نیز عمداً خالی است. این‌ها با `/api/health` قابل پیگیری‌اند.

## PROMPT-003 — دیزاین سیستم، پوسته‌ها و تجربه فارسی

**مرحله جاری:** تکمیل‌شده.

**رفتار تکمیل‌شده:**

- توکن‌های واقعی محصول از فایل پروتوتایپ خوانده و در `app/globals.css` با همان نام‌ها تعریف شدند (DEC-0019؛ تصحیح بخشی از DEC-0008).
- لوگوی رسمی از فایل Design System صادر و بدون هیچ تغییری استفاده شد؛ هیچ برند تازه‌ای کشیده یا برش داده نشد.
- کامپوننت‌های مشترک: Button و ActionRow با ترتیب RTL مطابق ERRATA v2.0، Field/Text/Select/Location، StatusBadge، Alert، Timeline، کارت‌های حیوان/درخواست/سرویس، کارت سرویس قفل‌شده با سه جزء §۵، Modal/Drawer/BottomSheet با تله فوکوس، و حالت‌های Loading/Empty/NetworkError/NeedsCorrection/Waiting.
- پوسته عمومی RTL برای کاربر، پرورش‌دهنده و دامپزشک معتمد با تب‌بار موبایل و ریل دسکتاپ؛ سه محیط عملیاتی مستقل انجمن، مرکز ژنتیک و سوپرادمین.
- Role Switcher فقط از نقش‌های فعال ساخته می‌شود، با یک Context اصلاً رندر نمی‌شود، و تغییر آن روی سرور بررسی و Context عملیاتی رد می‌شود.
- مجوز مسیر در خود صفحه اجرا می‌شود و مسیر ثبت‌نشده پیش‌فرض بسته است (DEC-0023).
- نقاط ترکیب داشبورد و پرونده حیوان بدون هیچ رکورد نمونه‌ای؛ صفحه اعلان‌ها داده واقعی DB را می‌خواند.

**فایل‌ها:** ۱۶ فایل در `app/**`، ۱۱ کامپوننت در `src/ui/**`، ۵ فایل در `src/authz/**`، `src/domain/eligibility/locks.ts`، دو دارایی برند، سه فایل تست و `docs/architecture/design-mapping.md`.

**migration:** ندارد (این مرحله تغییر schema ندارد).

**بررسی‌های واقعاً اجراشده:**

| بررسی | دستور | نتیجه |
|---|---|---|
| Gate `shell-permissions` | `node --test "tests/ui/*.test.ts" "tests/db/dev-actor.test.ts"` + بخش مربوط در تست مرورگر | PASS |
| Gate `rtl-browser-review` | `npm run build` + `next start -p 3111` + `npm run test:browser` | PASS — ۱۱/۱۱، ۲۴ اسکرین‌شات |
| تست واحد/سرویس | `npm test` | PASS — ۷۹/۷۹ |
| Typecheck | `npx tsc --noEmit` | PASS |
| Build | `npm run build` | PASS — ۱۷ مسیر |
| تست بسته | `node --test tools/tests/...` | PASS — ۲۳/۲۳ |

**تصمیم‌های جدید:** DEC-0019 تا DEC-0024.

**مانع واقعی:** هیچ مانعی مرحله را مسدود نکرد. دو اشکال واقعی حین بررسی مرورگر پیدا و رفع شد: redirect تغییر نقش مبدأ را از 127.0.0.1 به localhost می‌برد و کوکی را می‌انداخت (DEC-0024)، و ادعای اندازه ذاتی لوگو با بهینه‌ساز تصویر ناسازگار بود که با بررسی نسبت ابعاد در دو مرحله جایگزین شد.

**دستور ادامه:** `node tools/runner.mjs prepare` سپس اجرای PROMPT-004.

## PROMPT-004 — ورود، OTP، احراز هویت و حساب

**مرحله جاری:** تکمیل‌شده.

**رفتار تکمیل‌شده:**

- ورود واقعی: نرمال‌سازی موبایل (ارقام فارسی/عربی، `+98`، `0098`، بدون صفر)، صدور کد، ارسال مجدد با شمارش معکوس، انقضا، سقف تلاش، قفل و سقف ارسال ساعتی — همه از تنظیمات دیتابیس.
- نشست سرور با توکن تصادفی که فقط hash آن ذخیره می‌شود؛ Context روی ردیف نشست است و تغییر نقش تصمیم سرور است.
- حساب تازه با `PROFILE_INCOMPLETE` و هدایت به تکمیل حساب؛ حساب بازگشتی به درخواست مبدأ (`next`) برمی‌گردد و در نبود مبدأ به داشبورد.
- اطلاعات هویتی با کد ملی ده‌رقمی یکتا و رقم کنترلی، تاریخ تولد، و نام نمایشی اختیاری با تنظیم نمایش؛ سکونت کاملاً اختیاری با اعتبارسنجی کدپستی واردشده.
- KYC: بارگذاری تصویر کارت ملی (JPG/PNG/PDF تا ۱۰ مگابایت، بررسی روی بایت واقعی)، ارسال، صف انجمن، تأیید/اصلاح/رد با دلیل، حفظ فایل و داده معتبر در اصلاح، و اعلان بازگشت به همان پرونده.
- پس از تأیید: نام، نام خانوادگی و تاریخ تولد مستقیم قابل ویرایش؛ کد ملی فقط‌خواندنی.
- تغییر موبایل با کد روی شماره جدید؛ تا تأیید موفق شماره فعلی معتبر می‌ماند و لغو یا خطا هیچ چیز را عوض نمی‌کند. پس از تغییر، نشست‌های دیگر بسته می‌شوند.
- Actor توسعه‌ای PROMPT-003 **حذف شد**؛ تنها راه Actor شدن، نشست واقعی است و هیچ کد دور زدن OTP در هیچ محیطی وجود ندارد.

**فایل‌ها:** `src/identity/*` (۴ سرویس)، `src/domain/identity.ts`، `src/db/schema/identity.ts`، ۱۰ صفحه و ۳ فایل action در `app/**`، و ۴ فایل تست.

**migration:** `src/db/migrations/0001_identity.sql` — جدول‌های `profile`، `residence`، `kyc_case`، `otp_challenge`، `session` و `dev_outbound_sms`.

**بررسی‌های واقعاً اجراشده:**

| بررسی | دستور | نتیجه |
|---|---|---|
| Gate `identity-integration` | `npm test` (بخش هویت) | PASS — ۲۲ تست دیتابیسی + ۹ دامنه |
| Gate `identity-browser` | `npm run build` + `next start` + `npm run test:browser` | PASS — ۲۲/۲۲، ۹ اسکرین‌شات |
| تست واحد/سرویس | `npm test` | PASS — ۱۰۵/۱۰۵ |
| Typecheck | `npx tsc --noEmit` | PASS |
| Build | `npm run build` | PASS |
| migration و seed (هر کدام دو بار) | `node src/db/migrate.ts` + `node src/db/seed/run.ts --dev` | PASS — بدون replay، تنظیمات قبلی حفظ شد |
| تست بسته | `node --test tools/tests/...` | PASS — ۲۳/۲۳ |

**تصمیم‌های جدید:** DEC-0025 تا DEC-0030.

**مانع واقعی:** هیچ مانعی مرحله را مسدود نکرد. یک نقص واقعی در جریان ورود حین بررسی مرورگر پیدا و رفع شد: پاسخ خطای کد نادرست شناسه Challenge را برنمی‌گرداند و فرم به مرحله شماره برمی‌گشت.

**دستور ادامه:** `node tools/runner.mjs prepare` سپس اجرای PROMPT-005.

## PROMPT-005 — پرداخت، عضویت مادام‌العمر و دسترسی خدمات

**مرحله جاری:** تکمیل‌شده.

**رفتار تکمیل‌شده:**

- پرداخت با `payment_batch` / `payment_item` / `payment_attempt` / `payment_callback`. مبلغ همیشه سمت سرور از تنظیمات خوانده و همراه **نسخه تنظیم** روی قلم منجمد می‌شود؛ تبدیل به ریال با ×۱۰ و مقایسه مبلغ درگاه با همان عدد.
- تأیید فقط سمت سرور. بازگشت مرورگر صرفاً تأیید را راه می‌اندازد؛ داور اعمال اثر، به‌روزرسانی شرطی وضعیت Attempt است و اثر محصولی داخل همان تراکنش اجرا می‌شود.
- Callback تکراری و هم‌زمان دقیقاً یک بار اثر می‌گذارد؛ مبلغ دستکاری‌شده `AMOUNT_MISMATCH` می‌شود؛ pending/failed/cancelled اقلام و مبلغ منجمد را برای تلاش دوباره نگه می‌دارند.
- عضویت F14: hero، پیش‌نیاز و مزایا، مرور هزینه از داده مدیریت‌شده، پرداخت، تأیید و فعال‌سازی **مادام‌العمر**. هیچ ستون انقضا، تمدید یا Review پس از پرداخت وجود ندارد. شماره PENDING هیچ خدمتی را متوقف نمی‌کند و عضویت در سطح حساب است.
- Eligibility §۵ به‌صورت تابع خالص مشترک؛ داشبورد، صفحه‌ها و رد کردن سمت سرور همگی از همان قاعده می‌خوانند. Cooldown حتی به‌عنوان یک fact وجود ندارد.
- قاعده کار دامپزشک: عضویت غیرفعال کار جدید و حضور در Finder را می‌بندد، کار فعال قبلی را باز می‌گذارد و نقش و Location را دست‌نخورده نگه می‌دارد.

**فایل‌ها:** `src/billing/*` (۳ سرویس)، `src/domain/eligibility/*` (۲ فایل)، `src/db/schema/billing.ts`، ۷ صفحه و action در `app/**`، و ۴ فایل تست.

**migration:** `src/db/migrations/0002_billing.sql` — جدول‌های `payment_batch`، `payment_item`، `payment_attempt`، `payment_callback`، `membership` و `dev_payment_outcome`.

**بررسی‌های واقعاً اجراشده:**

| بررسی | دستور | نتیجه |
|---|---|---|
| Gate `payment-idempotency` | `node --test tests/db/payments.test.ts` + بخش مرورگر | PASS — ۱۵ تست |
| Gate `membership-eligibility` | `node --test tests/domain/eligibility.test.ts tests/db/membership.test.ts` | PASS — ۲۱ تست |
| تست واحد/سرویس | `npm test` | PASS — ۱۴۱/۱۴۱ |
| تست مرورگر | `npm run test:browser` | PASS — ۲۹/۲۹، ۸ اسکرین‌شات تازه |
| Typecheck و Build | `npx tsc --noEmit` + `npm run build` | PASS |
| migration (دو بار) | `node src/db/migrate.ts` | PASS — بدون replay |
| تست بسته | `node --test tools/tests/...` | PASS — ۲۳/۲۳ |

**تصمیم‌های جدید:** DEC-0031 تا DEC-0036.

**مانع واقعی:** هیچ مانعی مرحله را مسدود نکرد. دو انتظار تست قدیمی که با داشبورد جدید بی‌اعتبار شده بودند اصلاح شدند (متن کارت ثبت حیوان و تعداد CTAهای احراز هویت).

**دستور ادامه:** `node tools/runner.mjs prepare` سپس اجرای PROMPT-006.


## PROMPT-006 — ثبت حیوان، نسب و بررسی شجره‌نامه خارجی

**مرحله جاری:** تکمیل‌شده.

**رفتار تکمیل‌شده:**

- فرم شش‌مرحله‌ای PET-004…PET-008 روی یک رکورد پیش‌نویس واقعی. هر گام فقط پس از ذخیره شدن در سرور جلو می‌رود (DEC-0038) و بازگشت، همه مقادیر واردشده را برمی‌گرداند.
- سه مسیر §۹.۲: G0 بدون اسناد هویتی، G1+ با Resolve کد شجره‌نامه پدر و مادر در هم‌زیست، و شجره‌نامه صادرشده خارج از هم‌زیست.
- نسل هرگز ورودی نیست: `1 + min(نسل پدر، نسل مادر)`؛ G0×G0→G1 و G2×G1→G2. `generation`، `origin`، `petId`، `pedigreeCode`، والدین، میکروچیپ اعلامی، وضعیت و مالک از فرم عمومی ویرایش قابل تغییر نیستند.
- نبود واقعی والد با خطای فنی جست‌وجو اشتباه نمی‌شود: اولی G0 موقت + CTA ثبت والد گمشده، دومی نگه‌داشتن نسل فعلی و پیام «دوباره تلاش کنید».
- رفت‌وبرگشت ثبت والد گمشده با Resume Context، بدون ساخت رکورد تکراری و با حفظ کدهای واردشده (DEC-0040). پیوند به خود و حلقه نسب رد می‌شود.
- شجره‌نامه خارجی: روی برگه و پشت برگه مستقل، صادرکننده فقط از فهرست موردتأیید انجمن، ارسال، بازگشت برای اصلاح با دلیل و حفظ فایل قبلی، تأیید فقط با نسل استخراج‌شده که روی حیوان به‌صورت فقط‌خواندنی می‌نشیند، همراه Audit و اعلان.
- پروفایل حیوان §۱۰ با همه بخش‌ها؛ بخش‌های مربوط به مرحله‌های بعد صریحاً «هنوز نیست» می‌گویند و محتوای ساختگی ندارند. Timeline از Audit واقعی می‌آید.
- پرونده متعلق به مالک دیگر همان پاسخ «پیدا نشد» می‌گیرد، نه ۴۰۳ و نه خطای ۵۰۰ (DEC-0039).

**فایل‌ها:** `src/animals/*` (۲ سرویس)، `src/domain/lineage.ts`، `src/db/schema/animals.ts`، ۱۲ صفحه و action در `app/**`، و ۳ فایل تست.

**migration:** `src/db/migrations/0003_animals.sql` — جدول‌های `animal` و `foreign_pedigree_case` با دو ارجاع به خود جدول برای پدر و مادر.

**بررسی‌های واقعاً اجراشده:**

| بررسی | دستور | نتیجه |
|---|---|---|
| Gate `animal-lineage-integration` | `node --test tests/domain/lineage.test.ts tests/db/animals.test.ts` | PASS — ۲۸ تست |
| Gate `animal-registration-browser` | `node --test --test-concurrency=1 tests/browser/animals.test.ts` | PASS — ۶ تست، ۱۳ اسکرین‌شات |
| تست واحد/سرویس | `npm test` | PASS — ۱۶۹/۱۶۹ |
| تست مرورگر | `npm run test:browser` | PASS — ۳۵/۳۵ |
| Typecheck و Build | `npx tsc --noEmit` + `npm run build` | PASS |
| migration (دو بار) | `node src/db/migrate.ts` | PASS — بدون replay |
| تست بسته | `node --test tools/tests/...` | PASS — ۲۳/۲۳ |

**تصمیم‌های جدید:** DEC-0037 تا DEC-0041.

**مانع واقعی:** هیچ مانعی مرحله را مسدود نکرد. سه نقص واقعی حین بررسی مرورگر پیدا و رفع شد: از دست رفتن داده گام فرم به دلیل جلو رفتن گام پیش از ذخیره (DEC-0038)، ناپدید شدن پنل نسب و CTA والد گمشده پس از reload، و خطای ۵۰۰ به‌جای پاسخ «پیدا نشد» برای پرونده مالک دیگر (DEC-0039).

**دستور ادامه:** `node tools/runner.mjs prepare` سپس اجرای PROMPT-007.

## PROMPT-007 — یافتن دامپزشک و کد مراجعه مستقل

**مرحله جاری:** تکمیل‌شده.

**رفتار تکمیل‌شده:**

- Finder سه Context (میکروچیپ، DNA، بارداری) با جست‌وجوی نام دامپزشک، نام مرکز و محله و فیلترهای شهر و فاصله. هیچ نوبت، ساعت، تقویم ظرفیت یا مرتب‌سازی «زودترین نوبت» وجود ندارد.
- شرط حضور در Finder داخل خود کوئری است: نقش `TRUSTED_VET` فعال، عضویت فعال، مرکز فعالِ دارای پروانه معتبر با نشانی و تماس کامل و **همه** امکانات اجباری همان Context. قابلیت جزئی مسیر کم‌شده نمی‌سازد (DEC-0044).
- کد نظام دامپزشکی و پروانه Location دو ستون روی دو جدول جدا هستند (DEC-0043). داده دامپزشکان از قبل تأییدشده از `/admin/vets` وارد می‌شود و این صفحه فرم درخواست معتمدشدن نیست (DEC-0042، D01).
- انتخاب چند حیوان با نوع خدمت مستقل هر حیوان → انتخاب دامپزشک و مرکز → مرور → ساخت درخواست و صدور کد هر حیوان در یک تراکنش. گروه فقط نمایش است و هیچ ستون وضعیتی ندارد (DEC-0046).
- مهلت مراجعه از تنظیم `referral.validity_days` (مقدار اولیه ۲۱ روز) خوانده و همراه نسخه تنظیم روی خود کد ثبت می‌شود؛ همان مهلتی که نمایش داده و در سرور اجرا می‌شود. تغییر تنظیم عطف به ماسبق نیست (DEC-0047).
- پذیرش در محل یک‌بارمصرف و اتمی: به‌روزرسانی شرطی `ACTIVE → CONSUMED` داور رقابت است. کد نامعتبر، منقضی، لغوشده، جایگزین‌شده، استفاده‌شده یا متعلق به دامپزشک/مرکز دیگر با پیام خنثی رد می‌شود و پرونده کس دیگری را لو نمی‌دهد (DEC-0049).
- تغییر نوع خدمت در محل: فقط همان حیوان SUPERSEDED می‌شود، Audit قبلی می‌ماند، کد قبلی از کار می‌افتد، درخواست و کد جدید ساخته می‌شود و مسیر دوباره به پذیرش برمی‌گردد. حیوان‌های دیگر گروه تغییر نمی‌کنند.
- دامپزشک با عضویت غیرفعال یا نقش تعلیق‌شده: نه در Finder، نه دریافت کار جدید؛ ولی کار فعال قبلی قابل پذیرش و تکمیل است و پنل علت را نشان می‌دهد (§۷.۱).

**فایل‌ها:** `src/vets/*` (۲ سرویس)، `src/domain/referral.ts`، `src/db/schema/vets.ts`، ۱۴ صفحه و action در `app/**`، و ۳ فایل تست.

**migration:** `src/db/migrations/0004_vets.sql` — `vet_profile`، `vet_location`، `vet_visit_batch`، `vet_visit_request` و `referral_code`.

**بررسی‌های واقعاً اجراشده:**

| بررسی | دستور | نتیجه |
|---|---|---|
| Gate `referral-concurrency` | `node --test tests/domain/referral.test.ts tests/db/referrals.test.ts` | PASS — ۲۵ تست |
| Gate `finder-browser` | `node --test --test-concurrency=1 tests/browser/finder.test.ts` | PASS — ۶ تست، ۱۵ اسکرین‌شات |
| تست واحد/سرویس | `npm test` | PASS — ۱۹۴/۱۹۴ |
| تست مرورگر | `npm run test:browser` | PASS — ۴۱/۴۱ |
| Typecheck و Build | `npx tsc --noEmit` + `npm run build` | PASS |
| migration (دو بار) | `node src/db/migrate.ts` | PASS — بدون replay |
| تست بسته | `node --test tools/tests/...` | PASS — ۲۳/۲۳ |

**تصمیم‌های جدید:** DEC-0042 تا DEC-0049.

**مانع واقعی:** هیچ مانعی مرحله را مسدود نکرد. دو نقص واقعی حین بررسی مرورگر پیدا و رفع شد: پس از اصلاح نوع خدمت، فرم و پیام آن با رفرش صفحه ناپدید می‌شد و راه رسیدن به درخواست جدید بسته بود؛ و پیام صدور کد جدید پس از تمدید نیز با همان الگو گم می‌شد. هر دو با نمایش وضعیت روی خود پرونده حل شدند.

**دستور ادامه:** `node tools/runner.mjs prepare` سپس اجرای PROMPT-008.

## PROMPT-008 — میکروچیپ، نمونه‌گیری و نگهداری نمونه

**مرحله جاری:** تکمیل‌شده.

**رفتار تکمیل‌شده:**

- پرونده مراجعه در پنل دامپزشک با تصویر حیوان، کاربر مرتبط، نوع خدمت، کد مراجعه و مرکز — همه فقط پس از پذیرش کد. تصویر حیوان یک فایل خصوصی است و مجوز دیدنش رکورد-محور است (DEC-0057).
- چهار روش خواندن شماره به یک فیلد Canonical می‌رسند و روش روی رکورد ثبت می‌شود. آداپتور `chip-reader` تا پیکربندی `CHIP_READER_PROVIDER` وضعیت NOT_CONFIGURED دارد و صفحه همین را می‌گوید؛ ورود دستی همیشه کار می‌کند (DEC-0053).
- کاشت: خواندن سریال پیش از کاشت → کنترل یکتایی سراسری و نبود چیپ در طول عمر → ثبت کاشت → خواندن مجدد → تطبیق → اتصال دائمی. ناهمخوانی سریال تعارض ثبت می‌کند و هیچ اتصالی نمی‌سازد (DEC-0052).
- تأیید: همان چیپ/همان حیوان تأیید می‌شود؛ چیپ حیوان دیگر، چیپ دوم در طول عمر و ناهمخوانی، تعارض ثبت‌شده و توقف عملیات‌اند. چیپ فیزیکی بدون رکورد فقط پس از همان دو کنترل متصل می‌شود.
- قاعده «یک میکروچیپ در طول عمر» با دو ایندکس یکتا (`animal_id` و `number`) در مرز تراکنش اجرا می‌شود؛ هیچ مسیر تعویض، انتقال، بازنویسی یا حذف وجود ندارد (DEC-0050).
- نمونه خون در هر دو خدمت اجباری است و کد رهگیری فقط در همان تراکنش ثبت نمونه‌گیری صادر می‌شود. کد مراجعه (`HZ-`) و کد نمونه (`SM-`) دو شناسه جدا هستند (DEC-0054).
- Custody نزد همان دامپزشک؛ نمونه انقضای خودکار ندارد؛ ارسال فقط پس از دستور مرکز ژنتیک و به‌صورت رویداد روی همان کد (DEC-0055).
- نمونه‌گیری مجدد در همان درخواست: نمونه و دلیل قبلی حفظ می‌شود، کد جدید فقط پس از انجام واقعی صادر می‌شود و هیچ کاشت دوباره‌ای رخ نمی‌دهد (DEC-0056).
- مرور نهایی §۱۲.۵ با همه فیلدهای موجود و اطلاع‌رسانی امضای فیزیکی؛ هیچ Gate امضای دیجیتال وجود ندارد (D13).

**فایل‌ها:** `src/clinical/*` (۲ سرویس)، `src/domain/microchip.ts`، `src/db/schema/clinical.ts`، ۹ صفحه و action در `app/**`، و ۳ فایل تست.

**migration:** `src/db/migrations/0005_clinical.sql` — `microchip`، `microchip_conflict`، `chip_procedure`، `sample` و `sample_event`.

**بررسی‌های واقعاً اجراشده:**

| بررسی | دستور | نتیجه |
|---|---|---|
| Gate `chip-sample-integrity` | `node --test tests/domain/microchip.test.ts tests/db/microchip.test.ts` | PASS — ۱۸ تست |
| Gate `vet-service-browser` | `node --test --test-concurrency=1 tests/browser/vet-service.test.ts` | PASS — ۵ تست، ۱۴ اسکرین‌شات |
| تست واحد/سرویس | `npm test` | PASS — ۲۱۲/۲۱۲ |
| تست مرورگر | `npm run test:browser` | PASS — ۴۶/۴۶ |
| Typecheck و Build | `npx tsc --noEmit` + `npm run build` | PASS |
| migration (دو بار) | `node src/db/migrate.ts` | PASS — بدون replay |
| تست بسته | `node --test tools/tests/...` | PASS — ۲۳/۲۳ |

**تصمیم‌های جدید:** DEC-0050 تا DEC-0058.

**مانع واقعی:** هیچ مانعی مرحله را مسدود نکرد. یک نقص واقعی حین تست پیدا و رفع شد: تشخیص خطای یکتایی دیتابیس، کد خطا را روی خود خطا می‌خواند در حالی که لایه کوئری آن را در `cause` می‌گذارد؛ بدون این اصلاح، رقابت دو مراجعه روی یک شماره به‌جای تعارض ثبت‌شده، خطای خام می‌داد.

**دستور ادامه:** `node tools/runner.mjs prepare` سپس اجرای PROMPT-009.

## PROMPT-009 — صدور مستقل برگه ثبتی و پرداخت گروهی

**مرحله جاری:** تکمیل‌شده.

**رفتار تکمیل‌شده:**

- ترتیب §۱۳ به‌صورت قاعده اجرا می‌شود: ثبت اولیه، نبود برگه قبلی، میکروچیپ متصل و نمونه قابل استفاده. پیش از این‌ها هیچ Batch پرداختی ساخته نمی‌شود و مبلغ خدمت دامپزشک در این پرداخت نیست (DEC-0059).
- مرور هزینه با مبلغ هر حیوان از تعرفه فعال دیتابیس و منجمدشده روی همان قلم؛ تعرفه ثبت‌نشده مسیر پرداخت را باز نمی‌کند.
- یک پرداخت گروهی، صدور مستقل هر حیوان: وضعیت پرداخت Batch و وضعیت صدور هر حیوان جدا نگهداری و نمایش داده می‌شوند و حیوان متوقف، بقیه را متوقف نمی‌کند (DEC-0061).
- صدور، شماره برگه `RS-` و Pet ID `PET-` یکتا می‌سازد، Pet ID را روی حیوان می‌نشاند و سند را فقط برای مالک قابل مشاهده می‌کند (DEC-0060).
- اثر صدور داخل تراکنش تأیید پرداخت اجرا می‌شود و ایندکس یکتای حیوان داور تکرار است: Callback تکراری و تلاش دوباره، یک سند می‌سازند (DEC-0063).
- تنها ویرایش مجاز پس از پرداخت، تلاش دوباره صدور است؛ بازپرداخت، حذف قلم و پرداخت اضافه اختراع نشد (DEC-0062).
- پرداخت لغوشده یا ناموفق دیگر Batch را نمی‌بندد؛ همان اقلام و مبالغ منجمد دوباره قابل پرداخت‌اند (DEC-0064).
- پروفایل حیوان بخش برگه ثبتی و متن «نمونه خون دریافت شده؛ آزمایش Parentage هنوز انجام نشده است.» را نشان می‌دهد و صدور برگه، شرط سرویس بعدی (شجره‌نامه) را در همان قاعده §۵ باز می‌کند.
- پنل تنظیمات از فقط‌خواندنی خارج شد و برای گروه‌های مجاز فرم ویرایش با اعتبارسنجی، نسخه و Audit دارد (DEC-0065).

**فایل‌ها:** `src/documents/registration-sheet.ts`، `src/db/schema/documents.ts`، ۹ صفحه و action در `app/**`، و ۲ فایل تست.

**migration:** `src/db/migrations/0006_documents.sql` — `registration_sheet_item` و `registration_sheet`.

**بررسی‌های واقعاً اجراشده:**

| بررسی | دستور | نتیجه |
|---|---|---|
| Gate `registration-batch-integration` | `node --test tests/db/registration-sheet.test.ts` | PASS — ۱۱ تست |
| Gate `registration-browser` | `node --test --test-concurrency=1 tests/browser/registration.test.ts` | PASS — ۴ تست، ۱۱ اسکرین‌شات |
| تست واحد/سرویس | `npm test` | PASS — ۲۲۳/۲۲۳ |
| تست مرورگر | `npm run test:browser` | PASS — ۵۰/۵۰ |
| Typecheck و Build | `npx tsc --noEmit` + `npm run build` | PASS |
| migration (دو بار) | `node src/db/migrate.ts` | PASS — بدون replay |
| تست بسته | `node --test tools/tests/...` | PASS — ۲۳/۲۳ |

**تصمیم‌های جدید:** DEC-0059 تا DEC-0065.

**مانع واقعی:** هیچ مانعی مرحله را مسدود نکرد. یک نقص واقعی از مرحله پرداخت پیدا و رفع شد: یک بار لغو پرداخت، Batch را برای همیشه می‌بست و امکان تلاش دوباره را از بین می‌برد — خلاف §۲۶. حالا فقط پرداخت تأییدشده پایانی است.

**دستور ادامه:** `node tools/runner.mjs prepare` سپس اجرای PROMPT-010.

## PROMPT-010 — فیش ژنتیک، ارسال نمونه و ثبت نتیجه

**مرحله جاری:** تکمیل‌شده.

**رفتار تکمیل‌شده:**

- ورود به مسیر معمول شجره‌نامه فقط با برگه ثبتی، نمونه قابل استفاده و Custodian مشخص؛ همه از رکورد موجود خوانده می‌شوند و هیچ انتخاب دامپزشک، نمونه‌گیری دوباره یا Selector مرکز وجود ندارد (DEC-0066).
- اطلاعات یک مرکز ثابت فقط نمایش داده می‌شود و تا ثبت داده واقعی نام و مقصد پرداخت، مسیر فیش بسته است (DEC-0067).
- فیش به کد رهگیری دقیق نمونه‌های انتخاب‌شده نگاشت می‌شود؛ بررسی فقط با مرکز است، اصلاح روی همان فیش با حفظ فایل و کدها انجام می‌شود و تأیید فیش پرداخت صدور سند نیست.
- تأیید فیش، نمونه‌ها را SEND_INSTRUCTED می‌کند و به همان دامپزشک نگهدارنده اعلان می‌دهد؛ ارسال فقط از او و روی همان کد رهگیری ثبت می‌شود (DEC-0068).
- مرکز دریافت، اعتبارسنجی، پردازش و ثبت نتیجه دارد و هرگز نمونه‌گیری نمی‌کند؛ نمونه نامعتبر به مسیر موجود نمونه‌گیری مجدد برمی‌گردد (DEC-0072).
- پردازش و نمایش نتیجه هیچ وابستگی به پرداخت صدور در هم‌زیست ندارند (DEC-0069).
- خروجی ژنتیکی فقط Parentage Result است، نسخه‌دار و بدون شناسه دوم (DEC-0070). G0 نتیجه مستقیم می‌گیرد و G1+ تا کامل‌شدن نتیجه هر دو والد در وضعیت «در انتظار تکمیل نتایج والدین» می‌ماند و بعداً همان رکورد نهایی می‌شود (DEC-0071).
- صف و جزئیات مرکز برای فیش‌ها، نمونه‌های در انتظار/دریافت‌شده/نامعتبر/در حال پردازش و نتایج در انتظار والدین ساخته شد؛ هیچ اختیار تغییر مالکیت، میکروچیپ یا مجوز به مرکز داده نشد (DEC-0073).

**فایل‌ها:** `src/genetics/service.ts`، `src/db/schema/genetics.ts`، ۱۱ صفحه و action در `app/**`، و ۲ فایل تست.

**migration:** `src/db/migrations/0007_genetics.sql` — `genetics_receipt`، `genetics_receipt_item`، `parentage_result` و وضعیت‌های RECEIVED/PROCESSING روی نمونه.

**بررسی‌های واقعاً اجراشده:**

| بررسی | دستور | نتیجه |
|---|---|---|
| Gate `genetics-workflow` | `node --test tests/db/genetics.test.ts` | PASS — ۱۱ تست |
| Gate `genetics-browser` | `node --test --test-concurrency=1 tests/browser/genetics.test.ts` | PASS — ۴ تست، ۱۲ اسکرین‌شات |
| تست واحد/سرویس | `npm test` | PASS — ۲۳۴/۲۳۴ |
| تست مرورگر | `npm run test:browser` | PASS — ۵۴/۵۴ |
| Typecheck و Build | `npx tsc --noEmit` + `npm run build` | PASS |
| migration (دو بار) | `node src/db/migrate.ts` | PASS — بدون replay |
| تست بسته | `node --test tools/tests/...` | PASS — ۲۳/۲۳ |

**تصمیم‌های جدید:** DEC-0066 تا DEC-0073.

**مانع واقعی:** هیچ مانعی مرحله را مسدود نکرد. تنش واقعی بین «نمایش مشخصات پرداخت مرکز» و ممنوعیت درج داده مالی ساختگی، با بستن مسیر تا ورود داده واقعی و استفاده از رشته آشکارا آزمایشی در تست حل شد (DEC-0067).

**دستور ادامه:** `node tools/runner.mjs prepare` سپس اجرای PROMPT-011.

## PROMPT-011 — شجره‌نامه، اعتراض ژنتیک و درخواست پستی

**مرحله جاری:** تکمیل‌شده.

**رفتار تکمیل‌شده:**

- صدور شجره‌نامه اتصال دو شرطی است: نتیجه Parentage نهایی **و** پرداخت صدور تأییدشده در هم‌زیست. تأیید فیش مرکز جای این پرداخت را نمی‌گیرد و هر کدام دیرتر برسد کار را تمام می‌کند (DEC-0074).
- نتیجه بدون پرداخت صدور همچنان در پرونده دیده می‌شود؛ فقط سند در انتظار می‌ماند.
- صدور هر حیوان مستقل است: قلم پرداخت‌شده‌ای که نتیجه‌اش نهایی نیست BLOCKED با دلیل می‌شود و بعداً بدون پول جدید صادر می‌شود.
- اثر صدور داخل تراکنش تأیید پرداخت اجرا می‌شود و ایندکس یکتای حیوان، Callback تکراری و تلاش دوباره را به یک سند می‌رساند.
- سند، کد `PD-`، نسخه نتیجه مبنای صدور، والدین و نسل همان لحظه را ثبت می‌کند و فقط برای مالک قابل مشاهده است (DEC-0075).
- اعتراض از همان نتیجه و حیوان ثبت و به همان مرکز ارجاع می‌شود؛ مرکز بررسی و پاسخ می‌دهد و نتیجه اصلاحی نسخه جدید است. نتیجه مورد اعتراض و پیوندش حفظ می‌شود و کاربر هیچ مسیری برای ویرایش نتیجه ندارد (DEC-0077).
- سند صادرشده پس از نتیجه اصلاحی دست‌نخورده می‌ماند و فقط یادداشت نسخه و منشأ کنارش ثبت و اعلان داده می‌شود؛ هیچ ابطال یا بازنویسی رخ نمی‌دهد (DEC-0076).
- ثبت درخواست ارسال پستی پس از صدور سند، گره‌خورده به همان سند، با پیش‌پرکردن اختیاری نشانی ذخیره‌شده و بدون هیچ اتصال پستی، تعرفه، برچسب، کد رهگیری یا وضعیت تحویل (DEC-0078).

**فایل‌ها:** `src/documents/pedigree.ts`، `src/documents/postal.ts`، `src/genetics/appeals.ts`، `src/db/schema/pedigree.ts`، ۱۵ صفحه و action در `app/**`، و ۲ فایل تست.

**migration:** `src/db/migrations/0008_pedigree.sql` — `pedigree_issuance_item`، `pedigree` و `postal_request`.

**بررسی‌های واقعاً اجراشده:**

| بررسی | دستور | نتیجه |
|---|---|---|
| Gate `pedigree-appeal-integration` | `node --test tests/db/pedigree.test.ts` | PASS — ۱۱ تست |
| Gate `pedigree-browser` | `node --test --test-concurrency=1 tests/browser/pedigree.test.ts` | PASS — ۴ تست، ۱۱ اسکرین‌شات |
| تست واحد/سرویس | `npm test` | PASS — ۲۴۵/۲۴۵ |
| تست مرورگر | `npm run test:browser` | PASS — ۵۸/۵۸ (دو اجرای پیاپی) |
| Typecheck و Build | `npx tsc --noEmit` + `npm run build` | PASS |
| migration (دو بار) | `node src/db/migrate.ts` | PASS — بدون replay |
| تست بسته | `node --test tools/tests/...` | PASS — ۲۳/۲۳ |

**تصمیم‌های جدید:** DEC-0074 تا DEC-0079.

**مانع واقعی:** هیچ مانعی مرحله را مسدود نکرد. ابهام واقعی «سند صادرشده پس از نتیجه اصلاحی» با محافظه‌کارانه‌ترین رفتار سازگار حل و مستند شد (DEC-0076). در یک اجرای کامل تست مرورگر، یکی از موارد این مرحله به مهلت پیش‌فرض ۳۰ ثانیه Playwright برخورد؛ دو اجرای کامل بعدی هر دو ۵۸/۵۸ سبز بودند.

**دستور ادامه:** `node tools/runner.mjs prepare` سپس اجرای PROMPT-012.

## PROMPT-012 — فعال‌سازی پرورش‌دهنده و مدیریت کنل

**مرحله جاری:** تکمیل‌شده.

**رفتار تکمیل‌شده:**

- مسیر F11 با همان سه حالت «در حال بررسی»، «تأییدشده» و «نیازمند اصلاح»؛ نقش پرورش‌دهنده با تأیید کنل فعال می‌شود و شرط رسیدن به فرم نیست (DEC-0080).
- ورود با عنوان «شروع ثبت کنل»؛ پیش‌نیاز فقط عضویت فعال و حداقل یک برگه ثبتی. هیچ آپلود مدرک پرورش‌دهنده و هیچ پرداخت «فعال‌سازی نقش» اضافه نشد.
- نشانی و موقعیت کنل برای ارسال اجباری است؛ نشانی سکونت مستقل و اختیاری می‌ماند و خالی‌بودنش مانع نیست.
- جست‌وجوی نژاد فارسی/انگلیسی از همان فهرست مرجع، نمایش انتخاب‌شده‌ها و تعدادشان، حداقل یک نژاد و ممنوعیت حذف آخرین نژاد.
- پرداخت از تعرفه دیتابیسی و همان زیرساخت موجود؛ پرداخت فقط مرحله ارسال را باز می‌کند و تأیید نهایی با انجمن است (DEC-0081). اصلاح، پرداخت دوباره نمی‌خواهد (DEC-0082).
- صف، جزئیات، اصلاح، رد با دلیل و تأیید انجمن با Audit و اعلان؛ تأیید، نقش `BREEDER` را ACTIVE می‌کند.
- پروفایل کنل نام، وضعیت، موقعیت و نژادها را نشان می‌دهد و ویرایش نژاد پس از تأیید نه پرداخت می‌سازد و نه بررسی دوباره، با ثبت Before/After و Actor و زمان (DEC-0084).

**فایل‌ها:** `src/kennels/service.ts`، `src/db/schema/kennels.ts`، ۱۰ صفحه و action در `app/**`، و ۲ فایل تست.

**migration:** `src/db/migrations/0009_kennels.sql` — `kennel` و `kennel_breed`.

**بررسی‌های واقعاً اجراشده:**

| بررسی | دستور | نتیجه |
|---|---|---|
| Gate `kennel-workflow` | `node --test tests/db/kennel.test.ts` | PASS — ۱۱ تست |
| Gate `kennel-browser` | `node --test --test-concurrency=1 tests/browser/kennel.test.ts` | PASS — ۴ تست، ۱۴ اسکرین‌شات |
| تست واحد/سرویس | `npm test` | PASS — ۲۵۶/۲۵۶ |
| تست مرورگر | `npm run test:browser` | PASS — ۶۲/۶۲ (دو اجرای پیاپی) |
| Typecheck و Build | `npx tsc --noEmit` + `npm run build` | PASS |
| migration (دو بار) | `node src/db/migrate.ts` | PASS — بدون replay |
| تست بسته | `node --test tools/tests/...` | PASS — ۲۳/۲۳ |

**تصمیم‌های جدید:** DEC-0080 تا DEC-0084.

**مانع واقعی:** هیچ مانعی مرحله را مسدود نکرد. یک نقص واقعی از PROMPT-003 پیدا و رفع شد: مسیر `/kennels` فقط برای Context پرورش‌دهنده باز بود، یعنی برای ثبت کنل باید نقشی را می‌داشتید که فقط با تأیید همان کنل به دست می‌آید (DEC-0080). همچنین با بزرگ‌تر شدن صف نمونه‌های مرکز، دو انتظار در تست‌های قبلی به ۴۵ ثانیه افزایش یافت.

**دستور ادامه:** `node tools/runner.mjs prepare` سپس اجرای PROMPT-013.

## PROMPT-013 — مجوز رسمی جفت‌گیری

**مرحله جاری:** تکمیل‌شده.

- مسیر رسمی §۱۶ با موجودیت، شناسه `MP-…`، وضعیت و Route جدا از «اعلام توافق شخصی» ساخته شد؛ هیچ مسیری از پرداخت یا Submit این پرونده به Declaration بدون پرداخت وجود ندارد (DEC-0085).
- پیش‌نیاز واقعی همان چیزی است که سند می‌گوید: عضویت فعال و حیوان شجره‌دار خودِ کاربر؛ طرف مقابل از کد شجره‌نامه Resolve می‌شود و حساب او از مالک واقعی همان حیوان خوانده می‌شود، نه از فرم (DEC-0086).
- دعوت با اعلان و Resume به همان پرونده می‌رود؛ تأیید یا رد فقط از حساب طرف مقابل پذیرفته می‌شود و آغازکننده نمی‌تواند به‌جای او تأیید کند.
- توافق تقسیم ثابت/درصدی/ترکیبی پیش از تولد ثبت می‌شود و فقط ساختارش اعتبارسنجی می‌شود؛ هیچ تعداد توله‌ای فرض نمی‌شود و همه صفحه‌ها می‌گویند این توافق مالکیت توله مشخصی را نهایی نمی‌کند (DEC-0088).
- مرور طرفین، حیوان‌ها و توافق در همان پرونده؛ سپس پرداخت هزینه مجوز از تعرفه دیتابیسی و پیش از Submit نهایی. پرداخت هیچ مجوزی صادر نمی‌کند و فقط مرحله ارسال را باز می‌کند (DEC-0087).
- بررسی عملیاتی انجمن با صدور یک‌باره شماره مجوز، بازگشت برای اصلاح یا رد با دلیل؛ صدور، پرونده رسمی را در سوابق هر دو حیوان ثبت می‌کند و شرط Puppy Card را برای هر دو طرف باز می‌کند.
- بارداری، زایمان، تأیید دامپزشک و امضای فیزیکی هیچ‌کدام شرط صدور نیستند و هیچ فیلد امضای دیجیتالی در مسیر وجود ندارد (D13).
- اتصال Cooldown به تنظیمات انجام شد اما بدون تاریخ تأییدشده دوطرفه هیچ هشداری ساخته نمی‌شود و هشدار هرگز مانع ادامه نیست (DEC-0089).

**فایل‌ها:** `src/mating/permits.ts`، `src/domain/allocation.ts`، `src/db/schema/mating.ts`، ۸ صفحه و action در `app/**`، و ۲ فایل تست.

**migration:** `src/db/migrations/0010_mating.sql` — `mating_permit` و `permit_allocation_share`.

**بررسی‌های واقعاً اجراشده:**

| بررسی | دستور | نتیجه |
|---|---|---|
| Gate `permit-workflow` | `node --test tests/db/permit.test.ts` | PASS — ۷ تست |
| Gate `permit-browser` | `node --test --test-concurrency=1 tests/browser/permit.test.ts` | PASS — ۲ تست، ۱۰ اسکرین‌شات |
| تست واحد/سرویس | `npm test` | PASS — ۲۶۳/۲۶۳ |
| تست مرورگر | `npm run test:browser` | PASS — ۶۴/۶۴ |
| Typecheck و Build | `npx tsc --noEmit` + `npm run build` | PASS |
| migration (دو بار) | `node src/db/migrate.ts` | PASS — بدون replay |
| بسته | `node tools/validate-core.mjs` + `node --test tools/tests/... .claude/tests/...` | PASS — ۲۳/۲۳ |

**تصمیم‌های جدید:** DEC-0085 تا DEC-0089.

**مانع واقعی:** هیچ مانعی مرحله را مسدود نکرد. در اولین اجرای کامل تست‌های مرورگر، یک مورد قدیمی `pedigree.test.ts` به‌دلیل صف باقی‌مانده از اجراهای قبلی روی همان دیتابیس توسعه شکست خورد؛ در تست جدید همین مسئله با آدرس‌دهی مستقیم به فیش و پرونده مجوز (به‌جای «اولین ردیف صف») حل شد و اجرای کامل بعدی ۶۴/۶۴ سبز بود.

**دستور ادامه:** `node tools/runner.mjs prepare` سپس اجرای PROMPT-014.

## PROMPT-014 — تاریخ‌های دوطرفه و هشدار فاصله جفت‌گیری

**مرحله جاری:** تکمیل‌شده.

- ورودی تاریخ رسمی فقط پرونده‌ای است که مجوزش صادر شده؛ پیش از آن، همان صفحه صریحاً می‌گوید چرا بسته است.
- هر دو طرف می‌توانند یک یا چند تاریخ اعلام کنند، حتی چند بار در یک هفته. هر اعلام نسخه، اعلام‌کننده و زمان ثبت خودش را دارد و چیزی overwrite نمی‌شود (DEC-0090).
- اصلاح، نسخه تازه می‌سازد و نسخه قبلی را `SUPERSEDED` می‌کند؛ تأییدی که برای نسخه قبلی باز مانده بود نسخه جدید را تأیید نمی‌کند و باید دوباره از طرف مقابل گرفته شود.
- پاسخ با تاریخ متفاوت `DATE_CONFLICT` می‌سازد و هر دو مقدار در همان فهرست و در Audit دیده می‌شوند (DEC-0091).
- همه تاریخ‌های تأییدشده در تاریخچه می‌مانند؛ مبنای رسمی، جدیدترین تاریخ تأییدشده دوطرفه در همه پرونده‌های همان حیوان است و اعلام تأییدنشده — حتی با تاریخ جدیدتر — مبنا نمی‌شود (DEC-0092).
- حساب بازه: نر ۱۴ روز، ماده ۶ ماه تقویمی با Clamp پایان ماه؛ تاریخ‌ها Civil Date بدون ساعت و منطقه‌اند و روز پایان بازه دیگر داخل بازه نیست (DEC-0093). شش ماه هیچ‌جا به عدد روز تبدیل نمی‌شود.
- داخل بازه: هشدار با جنسیت، تاریخ مبنا و پایان بازه؛ CTA ادامه حذف یا قفل نمی‌شود و «ادامه آگاهانه» در لحظه اقدام در Audit ثبت می‌شود (DEC-0094). بدون سابقه تأییدشده، هیچ تاریخ فرضی و هیچ هشدار محاسبه‌شده‌ای وجود ندارد.
- پروفایل حیوان دو بخش واقعی گرفت: «مجوزها» و «تاریخ‌های جفت‌گیری» (فقط تأییدشده‌های دوطرفه)، و رویدادهای مجوز/تاریخ در Timeline عنوان فارسی دارند.

**فایل‌ها:** `src/mating/dates.ts`، `src/mating/cooldown.ts`، `src/db/schema/mating.ts`، صفحه `app/mating/permits/[id]/dates` با action و فرم‌هایش، به‌روزرسانی صفحه پرونده مجوز و پروفایل حیوان، و ۳ فایل تست (به‌علاوه fixture مشترک `tests/helpers/mating.ts`).

**migration:** `src/db/migrations/0011_mating-dates.sql` — `mating_date_declaration` با ایندکس یکتای پرونده+نسخه.

**بررسی‌های واقعاً اجراشده:**

| بررسی | دستور | نتیجه |
|---|---|---|
| Gate `dates-version-concurrency` | `node --test tests/db/dates.test.ts` | PASS — ۶ تست |
| Gate `cooldown-boundaries` | `node --test tests/domain/cooldown.test.ts` | PASS — ۹ تست |
| تست واحد/سرویس | `npm test` | PASS — ۲۷۸/۲۷۸ |
| تست مرورگر | `npm run test:browser` | PASS — ۶۵/۶۵ |
| Typecheck و Build | `npx tsc --noEmit` + `npm run build` | PASS |
| migration (دو بار) | `node src/db/migrate.ts` | PASS — بدون replay |
| بسته | `node tools/validate-core.mjs` + تست‌های بسته | PASS — ۲۳/۲۳ |

**تصمیم‌های جدید:** DEC-0090 تا DEC-0094.

**مانع واقعی:** هیچ مانعی مرحله را مسدود نکرد. Fixture سنگین مسیر مجوز (دو مالک با شجره‌نامه واقعی) از `tests/db/permit.test.ts` به `tests/helpers/mating.ts` منتقل شد تا این مرحله آن را دوباره کپی نکند؛ Gate مرحله قبل پس از این تغییر دوباره اجرا و ۷/۷ سبز ماند.

**دستور ادامه:** `node tools/runner.mjs prepare` سپس اجرای PROMPT-015.

## PROMPT-015 — بارداری، زایمان، تأیید اختیاری و اصلاح تعداد

**مرحله جاری:** تکمیل‌شده.

- اعلام رسمی بارداری و نتیجه زایمان فقط از پرونده دارای مجوز صادرشده شروع می‌شود؛ اعلام کاربر UNVERIFIED است، بدون دامپزشک ادامه می‌یابد و مجوز را صادر، مسدود، معلق، باطل یا تغییر نمی‌دهد (DEC-0095).
- تأیید دامپزشک اختیاری است: Finder در Context Pregnancy، مرکز کامل با پروانه معتبر، درخواست تخصیص‌یافته و ثبت نتیجه فقط از پنل همان دامپزشک؛ صف عمومی وجود ندارد و انقضای کد مراجعه فقط همان کد را باطل می‌کند (DEC-0096).
- نتیجه دامپزشک رکورد مستقل با نام، کد نظام، زمان و Location معاینه است؛ فیلد «محل جفت‌گیری» اضافه نشد و هیچ رکوردی دیگری را بازنویسی نمی‌کند.
- مغایرت فقط وضعیت نمایشی و اعلان خنثی است: هر دو مقدار با برچسب «مغایرت با اعلام مالک»، اعلان با Resume به همان پرونده و رویداد Audit با ارجاع هر دو رکورد (DEC-0097). برابری مقادیر اعلان نمی‌سازد.
- تعداد زنده و مرده دو عدد صحیح مستقل و نامنفی‌اند؛ برای هر توله زنده دقیقاً یک پرونده موقت با کد `PUP-…` ساخته می‌شود و برای مرده‌ی زمان تولد هیچ پرونده، کد موقت یا کارتی ساخته نمی‌شود. هر دو صفر، Litter بدون پرونده می‌سازد. نام توله تا مرحله میکروچیپ اختیاری است.
- اصلاح تعداد با Actor، زمان، علت و نسخه ثبت می‌شود و وجود پرونده توله آن را قفل نمی‌کند؛ افزایش فقط کمبود را می‌سازد و کاهش بدون تعیین پرونده‌های مشمول رد می‌شود و آن پرونده‌ها با علت باقی می‌مانند (DEC-0098).
- مرگ پس از تولد از مرده‌بودن در زمان تولد جداست: پرونده و گزارش اولیه حفظ می‌شود و فقط «زنده در حال حاضر» تغییر می‌کند؛ صفحه هر چهار عدد را جدا نشان می‌دهد (DEC-0099).

**فایل‌ها:** `src/mating/pregnancy.ts`، `src/mating/birth.ts`، `src/db/schema/breeding.ts`، صفحه‌های `app/mating/permits/[id]/pregnancy` و `.../birth` با action و فرم‌هایشان، پنل بارداری در `app/vet/requests/[id]`، و ۲ فایل تست.

**migration:** `src/db/migrations/0012_breeding.sql` — `pregnancy_declaration`، `pregnancy_check`، `vet_pregnancy_result`، `birth_event`، `litter` و `puppy`.

**بررسی‌های واقعاً اجراشده:**

| بررسی | دستور | نتیجه |
|---|---|---|
| Gate `birth-history-integrity` | `node --test tests/db/birth.test.ts` | PASS — ۷ تست |
| Gate `pregnancy-browser` | `node --test --test-concurrency=1 tests/browser/pregnancy.test.ts` | PASS — ۳ تست، ۹ اسکرین‌شات |
| تست واحد/سرویس | `npm test` | PASS — ۲۸۵/۲۸۵ |
| تست مرورگر | `npm run test:browser` | PASS — ۶۸/۶۸ |
| Typecheck و Build | `npx tsc --noEmit` + `npm run build` | PASS |
| migration (دو بار) | `node src/db/migrate.ts` | PASS — بدون replay |
| بسته | `node tools/validate-core.mjs` + تست‌های بسته | PASS — ۲۳/۲۳ |

**تصمیم‌های جدید:** DEC-0095 تا DEC-0099.

**مانع واقعی:** هیچ مانعی مرحله را مسدود نکرد. یک نکته رفتاری در تست‌های مرورگر روشن شد: React پس از هر Form Action فیلدهای uncontrolled را پاک می‌کند، بنابراین هر تلاش دوباره در تست باید کل فرم را دوباره پر کند؛ همچنین برای وضعیتی که از قبل روی صفحه است باید تغییر متن انتظار کشیده شود نه صرفاً وجود عنصر.

**دستور ادامه:** `node tools/runner.mjs prepare` سپس اجرای PROMPT-016.

## PROMPT-016 — تخصیص دوطرفه و صدور کارت توله

**مرحله جاری:** تکمیل‌شده.

- برای توله‌های واقعی ثبت‌شده، مالک پیشنهادی مشخص می‌شود و توافق پیش از تولد فقط «زمینه پیشنهاد» است؛ هیچ‌جا خودش مالکیت تولهٔ مشخصی را تعیین نمی‌کند و همین جمله روی صفحه است.
- هر نسخه تخصیص تأییدهای خودش را دارد: FINAL فقط با تأیید هر دو طرفِ واقعی روی همان نسخه؛ تأیید یک طرف، تأیید دوباره همان شخص، پاسخ شخص ثالث و پرداخت هیچ‌کدام این Gate را دور نمی‌زنند (DEC-0100).
- تغییر یا رد، نسخه جدید با صفر تأیید می‌سازد و پاسخ با شماره نسخه بسته می‌شود؛ نسخه ردشده در تاریخچه `REJECTED` می‌ماند و نسخه در انتظار `SUPERSEDED` می‌شود (DEC-0101). تا نهایی‌نشدن، وضعیت «در انتظار تأیید هر دو طرف» است، کارت قفل است و هر دو مقدار و تاریخچه خواندنی‌اند. داوری یا برنده خودکار وجود ندارد.
- کارت توله فقط با مجوز صادرشده، زایمان ثبت‌شده، توله زندهٔ واجد شرایط، تخصیص نهایی و قلم پرداخت‌شده صادر می‌شود؛ برگه ثبتی همان توله پیش‌نیاز نیست و کارت در مدل داده و متن از برگه ثبتی، شجره‌نامه و نتیجه ژنتیک جداست (DEC-0102).
- انتخاب یک یا چند توله، تعرفه دیتابیسی به‌ازای هر توله، یک Batch، پرداخت پیش از صدور و صدور مستقل هر کارت؛ توله‌ای که در لحظه تأیید پرداخت دیگر واجد شرایط نیست با دلیل ثبت می‌شود و کل Batch شکست نمی‌خورد.
- برای توله‌ای که مرگش ثبت شده کارت تازه صادر نمی‌شود، اما کارت، مالکیت و سوابق صادرشده حذف یا باطل نمی‌شوند (DEC-0103).
- مسیر شخصی هرگز صلاحیت مجوز، lineage یا کارت نمی‌سازد: همه بررسی‌ها سمت سرور و روی «مجوز صادرشده» انجام می‌شود و شمارش §۵ فقط تخصیص‌های FINAL را می‌شمارد (DEC-0104).

**فایل‌ها:** `src/mating/allocation.ts`، بخش تخصیص/کارت در `src/db/schema/breeding.ts`، صفحه‌های `app/litters/[id]/allocation`، `app/puppy-cards/{checkout,return}` و `app/documents/puppy-card/[id]` با action و فرم‌هایشان، اثر پرداخت در `src/billing/effects.ts`، شمارش §۵ در `src/domain/eligibility/service.ts`، و ۲ فایل تست.

**migration:** `src/db/migrations/0013_allocation.sql` — `puppy_allocation`، `allocation_item`، `allocation_approval` و `puppy_card`.

**بررسی‌های واقعاً اجراشده:**

| بررسی | دستور | نتیجه |
|---|---|---|
| Gate `allocation-version-integrity` | `node --test tests/db/allocation.test.ts` | PASS — ۶ تست |
| Gate `puppy-card-browser` | `node --test --test-concurrency=1 tests/browser/puppy-card.test.ts` | PASS — ۳ تست، ۹ اسکرین‌شات |
| تست واحد/سرویس | `npm test` | PASS — ۲۹۱/۲۹۱ |
| تست مرورگر | `npm run test:browser` | PASS — ۷۱/۷۱ |
| Typecheck و Build | `npx tsc --noEmit` + `npm run build` | PASS |
| migration (دو بار) | `node src/db/migrate.ts` | PASS — بدون replay |
| بسته | `node tools/validate-core.mjs` + تست‌های بسته | PASS — ۲۳/۲۳ |

**تصمیم‌های جدید:** DEC-0100 تا DEC-0104.

**مانع واقعی:** هیچ مانعی مرحله را مسدود نکرد. سرویس Docker در میانه اجرا خاموش بود و پس از راه‌اندازی دوباره، همه تست‌های دیتابیسی اجرا شدند؛ هیچ تستی بدون اجرا PASS گزارش نشده است.

**دستور ادامه:** `node tools/runner.mjs prepare` سپس اجرای PROMPT-017.

## PROMPT-017 — اعلام توافق شخصی جدا از مسیر رسمی

**مرحله جاری:** تکمیل‌شده.

- سرویس فقط «وجود توافق خارج از هم‌زیست» را ثبت می‌کند؛ هیچ متن، فایل، تصویر، امضا، شرط حقوقی یا سهمی ذخیره نمی‌شود و جدول اصلاً ستونی برای آن‌ها ندارد (DEC-0107).
- پیش‌نیازها همان‌هایی هستند که سند می‌گوید: KYC تأییدشده، عضویت فعال و رکورد حیوان موجود؛ برگه ثبتی و شجره‌نامه شرط نیستند و این مسیر هیچ پرداختی ندارد (DEC-0109).
- انتخاب حیوان طرف مقابل با شناسه یک رکورد موجود انجام می‌شود — میکروچیپ، Pet ID یا کد شجره‌نامه — و حیوان دستی پذیرفته نمی‌شود (DEC-0105). شماره دعوت باید متعلق به مالک همان حیوان باشد؛ در غیر این صورت نه رکوردی ساخته می‌شود و نه پیامکی ارسال (DEC-0106).
- دعوت از آداپتور پیامک خود محصول عبور می‌کند؛ بدون Provider پیکربندی‌شده به Outbox توسعه می‌رود و در Production اصلاً وجود ندارد، پس در حین ساخت چیزی برای کسی ارسال نمی‌شود.
- تا پاسخ طرف مقابل، وضعیت `PENDING_COUNTERPARTY_CONFIRMATION` است؛ پاسخ فقط از حساب دعوت‌شده پذیرفته می‌شود، رد نیازمند دلیل است و آغازکننده می‌تواند دعوت بی‌پاسخ را لغو کند. برای امضا هیچ کد یک‌بارمصرف تازه‌ای ساخته نشد؛ تنها OTP این مسیر همان کد ورود است.
- یادداشت‌های شخصی (تاریخ، بارداری، زایمان) روی همان اعلام و همیشه UNVERIFIED ثبت می‌شوند و هیچ کوئری مبنای رسمی آن‌ها را نمی‌خواند (DEC-0108).
- بازدیدکننده بدون نشست به `/login?next=…` هدایت می‌شود تا پس از ورود دقیقاً به همان اعلام برگردد (DEC-0110).

**فایل‌ها:** `src/mating/declaration.ts`، `src/domain/declaration-copy.ts`، `src/db/schema/declarations.ts`، صفحه‌های `app/declaration/*` با action و فرم‌هایشان، شرط §۵ در `src/domain/eligibility/rules.ts`، هدایت ورود در `src/authz/guard.ts`، و ۲ فایل تست جدید.

**migration:** `src/db/migrations/0014_declarations.sql` — `personal_declaration` و `personal_note`.

**بررسی‌های واقعاً اجراشده:**

| بررسی | دستور | نتیجه |
|---|---|---|
| Gate `personal-declaration-isolation` | `node --test tests/db/declaration.test.ts` | PASS — ۶ تست |
| Gate `personal-browser` | `node --test --test-concurrency=1 tests/browser/personal.test.ts` | PASS — ۴ تست، ۸ اسکرین‌شات |
| تست واحد/سرویس | `npm test` | PASS — ۲۹۷/۲۹۷ |
| تست مرورگر | `npm run test:browser` | PASS — ۷۵/۷۵ |
| Typecheck و Build | `npx tsc --noEmit` + `npm run build` | PASS |
| migration (دو بار) | `node src/db/migrate.ts` | PASS — بدون replay |
| بسته | `node tools/validate-core.mjs` + تست‌های بسته | PASS — ۲۳/۲۳ |

**تصمیم‌های جدید:** DEC-0105 تا DEC-0110.

**مانع واقعی:** هیچ مانعی مرحله را مسدود نکرد. دو اثر جانبی واقعی پیدا و رفع شد: افزودن شرط رکورد حیوان به §۵ سه تست عضویت را که از این خدمت به‌عنوان نمونه استفاده می‌کردند تغییر داد (به‌روزرسانی شدند تا همان معنا را با پیش‌نیاز درست بررسی کنند)، و هدایت کاربر بدون نشست به ورود، دو تست قدیمی مسیر/خروج را تغییر داد که آن‌ها هم به رفتار جدید به‌روزرسانی و اجرا شدند.

**دستور ادامه:** `node tools/runner.mjs prepare` سپس اجرای PROMPT-018.

## PROMPT-018 — تکمیل پنل‌ها، تنظیمات و مسیرهای ادامه

**مرحله جاری:** تکمیل‌شده.

- صفحه‌های ورودی `/assoc` و `/genetics` که تا این مرحله فهرستی از عنوان‌های ثابت با EmptyState همیشگی بودند، با شمارش واقعی هر صف از همان جدولی که صفحه جزئیات می‌خواند بازنویسی شدند و هر ردیف به همان صف باز می‌شود (DEC-0111).
- دو ورودی مرده ناوبری رفع شد: «دفتر عضویت» انجمن (`/assoc/members`) با همان سرویس‌های موجود صدور شماره و فعال/غیرفعال‌سازی با دلیل، و «تاریخچه» سوپرادمین (`/admin/audit`) به‌صورت فقط‌خواندنی روی همان رویدادهای Audit با فیلتر نوع پرونده (DEC-0112). هیچ API موازی تازه‌ای برای تغییر رکورد ساخته نشد.
- فهرست مرجع نژادها به داده مدیریت‌شده منتقل شد (`/admin/breeds`): افزودن از پنل، «کنارگذاشتن» به‌جای حذف، حفظ کامل حیوان‌های ثبت‌شده و ثبت Actor/زمان/مقدار قبلی در Audit (DEC-0113).
- فهرست درخواست‌های پستی انجمن (`/assoc/postal`) فقط ثبت درخواست را نشان می‌دهد؛ هیچ وضعیت ارسال، کد رهگیری یا اقدام تحویلی ساخته نشد و صفحه همین محدوده را اعلام می‌کند (DEC-0114).
- پنل دامپزشک از قبل کامل بود و در این مرحله رفتار §۷.۱ روی صفحه بررسی شد: با غیرفعال‌شدن عضویت، پیام محدودیت نمایش داده می‌شود، صف و پرونده‌های قبلی باز می‌مانند و مرکز او از Finder حذف می‌شود.
- ماتریس رد دسترسی داده‌محور ساخته شد: هر خواننده عملیاتی × هر Actor، به‌علاوه دستکاری شناسه با پیام یکسان «پیدا نشد» (DEC-0115).

**فایل‌ها:** `src/operations/service.ts`، صفحه‌های `app/assoc/{members,postal}`، `app/admin/{audit,breeds}` با action و فرم‌هایشان، بازنویسی `app/assoc/page.tsx` و `app/genetics/page.tsx`، ناوبری در `src/ui/shell.tsx`، و ۲ فایل تست جدید. migration جدیدی لازم نبود.

**بررسی‌های واقعاً اجراشده:**

| بررسی | دستور | نتیجه |
|---|---|---|
| Gate `operations-rbac` | `node --test tests/db/operations.test.ts` | PASS — ۷ تست |
| Gate `operations-browser` | `node --test --test-concurrency=1 tests/browser/operations.test.ts` | PASS — ۶ تست، ۱۳ اسکرین‌شات |
| تست واحد/سرویس | `npm test` | PASS — ۳۰۴/۳۰۴ |
| تست مرورگر | `npm run test:browser` | PASS — ۸۱/۸۱ |
| Typecheck و Build | `npx tsc --noEmit` + `npm run build` | PASS |
| migration | `node src/db/migrate.ts` | PASS — بدون تغییر جدید |
| بسته | `node tools/validate-core.mjs` + تست‌های بسته | PASS — ۲۳/۲۳ |

**تصمیم‌های جدید:** DEC-0111 تا DEC-0115.

**مانع واقعی:** هیچ مانعی مرحله را مسدود نکرد. دو نقص واقعی از مراحل قبل پیدا و رفع شد: ورودی‌های مرده ناوبری به `/assoc/members` و `/admin/audit`، و صفحه‌های ورودی دو پنل که داده واقعی نداشتند.

**دستور ادامه:** `node tools/runner.mjs prepare` سپس اجرای PROMPT-019.
