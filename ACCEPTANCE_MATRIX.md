# معیارهای پذیرش عیناً از بخش ۲۸

هر ردیف باید در اجرا به کد/تست/شاهد واقعی متصل شود. همه در زمان تولید بسته NOT_STARTED هستند. این جدول پوشش برنامه است، نه نتیجه تست محصول.

| ID | معیار اصلی | مراحل اجرا | شاهد اجرا |
|---|---|---|---|
| A-001 | ثبت حیوان پس از KYC بدون عضویت ممکن است؛ نشانی سکونت اجباری نشده است. | PROMPT-004, PROMPT-006, PROMPT-019 | VERIFIED(PROMPT-006) — مالک با KYC تأییدشده، بدون هیچ عضویت و بدون نشانی سکونت، فرم شش‌مرحله‌ای را کامل کرد و پرونده ثبت شد؛ پس از ثبت هم عضویت غیرفعال ماند. حساب بدون KYC در سرور رد می‌شود. شواهد: `tests/browser/animals.test.ts` (۲ مورد) + `tests/db/animals.test.ts` + docs/reports/screenshots/prompt-006/animal-new-entry.png و animal-new-locked.png و animal-profile-g0.png |
| A-002 | عضویت F14 پس از پرداخت موفق مادام‌العمر فعال است و سالانه/Review اجباری به آن اضافه نشده است. | PROMPT-005, PROMPT-019 | VERIFIED(PROMPT-005) — فعال‌سازی فقط پس از تأیید سرور؛ هیچ ستون انقضا، تمدید یا Review پس از پرداخت وجود ندارد. شواهد: `payments.test.ts`، `membership.test.ts` و تست مرورگر + docs/reports/screenshots/prompt-005/membership-active.png |
| A-003 | Role Switcher فقط Contextهای فعال را نشان می‌دهد؛ پنل‌های عملیاتی مستقل‌اند. | PROMPT-003, PROMPT-018, PROMPT-019 | SHELL-VERIFIED(PROMPT-003) — سوییچر فقط نقش فعال؛ Shell عملیاتی جدا و از سوییچر عمومی غیرقابل‌دسترس. شواهد: تست مرورگر + docs/reports/screenshots/prompt-003/role-switcher.png و denied-wrong-shell.png | · VERIFIED(PROMPT-018) — از نشست عمومی، هر هفت مسیر عملیاتی FORBIDDEN می‌گیرد و Role Switcher هیچ‌کدام از سه Context عملیاتی را پیشنهاد نمی‌کند؛ ماتریس رد دسترسی داده‌محور هم روی سرویس‌ها اجرا می‌شود (DEC-0115). شواهد: `tests/db/operations.test.ts` + `tests/browser/operations.test.ts` + docs/reports/screenshots/prompt-018/public-denied-operations.png
| A-004 | Location ناقص در Finder نیست؛ رزرو نوبت و انتخاب ساعت وجود ندارد. | PROMPT-007, PROMPT-019 | VERIFIED(PROMPT-007) — شرط پروانه معتبر، مرکز فعال، نشانی و تماس کامل و همه امکانات اجباری داخل خود کوئری Finder است؛ مرکزی با قابلیت جزئی حذف می‌شود و مسیر جایگزین نمی‌گیرد (DEC-0044). هیچ نوبت، ساعت، تقویم یا مرتب‌سازی «زودترین نوبت» در کد و در DOM وجود ندارد. شواهد: `tests/domain/referral.test.ts`، `tests/db/referrals.test.ts`، `tests/browser/finder.test.ts` + docs/reports/screenshots/prompt-007/finder-partial-capability.png و finder-results.png |
| A-005 | Referral هر حیوان مستقل و متعلق به دامپزشک/Location همان درخواست است. | PROMPT-007, PROMPT-019 | VERIFIED(PROMPT-007) — هر حیوان درخواست، کد و وضعیت جدا دارد و گروه هیچ وضعیتی ندارد (DEC-0046)؛ کد در دامپزشک یا مرکز دیگر رد می‌شود و پذیرش یک‌بارمصرف و اتمی است. شواهد: `tests/db/referrals.test.ts` (رقابت سه پذیرش هم‌زمان) + `tests/browser/finder.test.ts` + docs/reports/screenshots/prompt-007/referral-code.png و check-in-consumed.png |
| A-006 | تغییر خدمت فقط درخواست همان حیوان را جایگزین می‌کند و سابقه حفظ می‌شود. | PROMPT-007, PROMPT-019 | VERIFIED(PROMPT-007) — درخواست قبلی SUPERSEDED می‌شود، دلیل و Audit قبلی می‌ماند، کد قبلی از کار می‌افتد و حیوان دیگر همان گروه دست‌نخورده است. شواهد: `tests/db/referrals.test.ts` + `tests/browser/finder.test.ts` + docs/reports/screenshots/prompt-007/service-corrected.png |
| A-007 | کاشت و تأیید هر دو نمونه خون دارند؛ Sample Code بعد از نمونه‌گیری ساخته می‌شود. | PROMPT-008, PROMPT-019 | VERIFIED(PROMPT-008) — نمونه در هر دو مسیر اجباری است و کد رهگیری فقط در همان تراکنش ثبت نمونه‌گیری صادر می‌شود؛ پیش از تعیین‌تکلیف میکروچیپ هیچ کدی وجود ندارد و کد نمونه با کد مراجعه یکی نیست (DEC-0054). شواهد: `tests/db/microchip.test.ts` + `tests/browser/vet-service.test.ts` + docs/reports/screenshots/prompt-008/sample-recorded.png و owner-sample-code.png |
| A-008 | چیپ فیزیکی بدون رکورد با کنترل یکتایی و سابقه عمر قابل ثبت است؛ چیپ دوم یا انتقال وجود ندارد. | PROMPT-008, PROMPT-019 | VERIFIED(PROMPT-008) — اتصال چیپ موجود فقط پس از کنترل یکتایی سراسری و نبود چیپ دیگر انجام می‌شود؛ چیپ دوم، تعویض، انتقال و بازنویسی هیچ مسیری ندارند و قاعده با دو ایندکس یکتا در مرز تراکنش اجرا می‌شود (DEC-0050). شواهد: `tests/db/microchip.test.ts` (شامل رقابت دو مراجعه روی یک شماره) + docs/reports/screenshots/prompt-008/chip-bound.png |
| A-009 | نمونه قبلی در مسیر شجره‌نامه استفاده می‌شود و مرکز ژنتیک نمونه‌گیری نمی‌کند. | PROMPT-010, PROMPT-019 | VERIFIED(PROMPT-010) — مسیر شجره‌نامه از همان نمونه و همان Custodian ثبت‌شده استفاده می‌کند و هیچ انتخاب دامپزشک یا نمونه‌گیری دوباره‌ای ندارد؛ مرکز فقط دریافت، اعتبارسنجی، پردازش و ثبت نتیجه دارد (DEC-0066، DEC-0072). شواهد: `tests/db/genetics.test.ts` + `tests/browser/genetics.test.ts` + docs/reports/screenshots/prompt-010/centre-received.png |
| A-010 | تأیید فیش دستور ارسال را به همان Custodian می‌دهد. | PROMPT-010, PROMPT-019 | VERIFIED(PROMPT-010) — تأیید فیش، نمونه‌های همان فیش را SEND_INSTRUCTED می‌کند و اعلان به همان دامپزشک نگهدارنده می‌رسد؛ ارسال فقط از او پذیرفته می‌شود و روی همان کد رهگیری ثبت می‌شود (DEC-0068). شواهد: `tests/db/genetics.test.ts` + docs/reports/screenshots/prompt-010/custodian-shipment.png |
| A-011 | پرداخت صدور مانع پردازش یا نمایش Result نیست؛ صدور سند هر دو شرط را لازم دارد. | PROMPT-010, PROMPT-011, PROMPT-019 | PARTIAL(PROMPT-010) — پردازش و نمایش نتیجه هیچ وابستگی به پرداخت صدور ندارند و نتیجه بدون آن ثبت و دیده می‌شود (DEC-0069). شرط دوم، یعنی صدور سند با هر دو شرط، در PROMPT-011 بررسی می‌شود. شواهد: `tests/db/genetics.test.ts` + docs/reports/screenshots/prompt-010/owner-result.png · VERIFIED(PROMPT-011) — صدور سند فقط با نتیجه نهایی و پرداخت صدور تأییدشده انجام می‌شود و هر دو ترتیب تست شده است؛ نتیجه بدون پرداخت همچنان دیده می‌شود (DEC-0074). شواهد: `tests/db/pedigree.test.ts` + `tests/browser/pedigree.test.ts` + docs/reports/screenshots/prompt-011/issuance-issued.png |
| A-012 | G1+ بدون Result لازم والدین نتیجه نهایی ناقص نمی‌گیرد؛ نسل دستی قابل انتخاب نیست. | PROMPT-006, PROMPT-010, PROMPT-019 | PARTIAL(PROMPT-006) — نسل هیچ ورودی‌ای در فرم ندارد (نبودش در DOM تست شد) و فقط از 1 + min(نسل والدین) محاسبه می‌شود؛ تلاش برای تغییر آن از ویرایش عمومی رد می‌شود (DEC-0037). وابستگی نتیجه Parentage والدین در PROMPT-010 اضافه می‌شود. شواهد: `tests/domain/lineage.test.ts`، `tests/db/animals.test.ts`، docs/reports/screenshots/prompt-006/lineage-computed.png |
| A-013 | وضعیت و صدور حیوان‌های Batch مستقل است. | PROMPT-009, PROMPT-011, PROMPT-016, PROMPT-019 | VERIFIED(PROMPT-009) — وضعیت پرداخت Batch و وضعیت صدور هر حیوان جدا نگهداری و جدا نمایش داده می‌شوند؛ حیوان واجد شرایطِ پرداخت‌شده صادر می‌شود در حالی که حیوان دیگر همان Batch با دلیل متوقف می‌ماند و پرداختش محفوظ است (DEC-0061). شواهد: `tests/db/registration-sheet.test.ts` + `tests/browser/registration.test.ts` + docs/reports/screenshots/prompt-009/sheet-items.png · VERIFIED(PROMPT-016) — همین قاعده در کارت توله هم اجرا می‌شود: یک پرداخت برای چند توله، ارزیابی دوباره هر توله در لحظه تأیید پرداخت، صدور مستقل هر کارت و ثبت دلیل برای توله‌ای که واجد شرایط نمانده، بدون شکست کل Batch (DEC-0102). شواهد: `tests/db/allocation.test.ts` + docs/reports/screenshots/prompt-016/card-paid.png |
| A-014 | کنل Location مستقل و حداقل یک نژاد دارد؛ تغییر نژاد Review/پرداخت اضافه نمی‌سازد. | PROMPT-012, PROMPT-019 | VERIFIED(PROMPT-012) — نشانی کنل مستقل از سکونت و اجباری برای ارسال است، حداقل یک نژاد لازم است و حذف آخرین نژاد رد می‌شود؛ تغییر نژاد پس از تأیید نه پرداخت می‌سازد و نه بررسی دوباره و Before/After و Actor و زمان ثبت می‌شود (DEC-0084). شواهد: `tests/db/kennel.test.ts` + `tests/browser/kennel.test.ts` + docs/reports/screenshots/prompt-012/kennel-breed-edit.png |
| A-015 | مجوز رسمی با توافق شخصی جداست و پرداخت یکی وارد مسیر دیگری نمی‌شود. | PROMPT-013, PROMPT-017, PROMPT-019 | PARTIAL(PROMPT-013) — مسیر رسمی موجودیت، شناسه (`MP-…`)، وضعیت و Route جدا دارد؛ پرداخت `MATING_PERMIT` فقط همین پرونده را به مرحله ارسال می‌برد و هیچ مسیری از آن به Declaration بدون پرداخت وجود ندارد. داشبورد هر دو خدمت را با دو مسیر جدا نشان می‌دهد (DEC-0085). سمت Declaration در PROMPT-017 ساخته و بررسی می‌شود. شواهد: `tests/db/permit.test.ts` + `tests/browser/permit.test.ts` + docs/reports/screenshots/prompt-013/permit-issued-owner.png · VERIFIED(PROMPT-017) — مسیر شخصی موجودیت، وضعیت و Route جدا دارد، هیچ پرداختی ندارد و تأیید آن هیچ مجوز، lineage، تاریخ رسمی، تخصیص یا کارت تولهای نمی‌سازد؛ داشبورد هر دو خدمت را جدا نشان می‌دهد (DEC-0107). شواهد: `tests/db/declaration.test.ts` + `tests/browser/personal.test.ts` + docs/reports/screenshots/prompt-017/declaration-vs-official.png |
| A-016 | تاریخ اصلاحی رسمی دوباره به تأیید طرف مقابل برمی‌گردد. | PROMPT-014, PROMPT-019 | VERIFIED(PROMPT-014) — اصلاح، نسخه تازه می‌سازد، نسخه قبلی را `SUPERSEDED` می‌کند بدون تغییر مقدارش، و تأیید فقط با شناسه و شماره همان نسخه و تنها از حساب طرف مقابل پذیرفته می‌شود؛ تأیید کهنه رد می‌شود (DEC-0090). شواهد: `tests/db/dates.test.ts` + `tests/browser/permit.test.ts` + docs/reports/screenshots/prompt-014/dates-correction.png |
| A-017 | Cooldown هشدار است؛ نبود سابقه، تاریخ ساختگی تولید نمی‌کند. | PROMPT-014, PROMPT-019 | VERIFIED(PROMPT-014) — بدون تاریخ تأییدشده دوطرفه هیچ بازه‌ای ساخته نمی‌شود و صفحه همین را می‌گوید؛ با مبنای واقعی، هشدار جنسیت، تاریخ مبنا و پایان بازه را نشان می‌دهد، CTA ادامه باقی می‌ماند و ادامه آگاهانه در Audit ثبت می‌شود (DEC-0093، DEC-0094). شواهد: `tests/domain/cooldown.test.ts` + `tests/db/dates.test.ts` + docs/reports/screenshots/prompt-014/dates-confirmed-warning.png |
| A-018 | اعلام بارداری/زایمان پس از مجوز است و نبود تأیید دامپزشک مجوز را تغییر نمی‌دهد. | PROMPT-013, PROMPT-015, PROMPT-019 | PARTIAL(PROMPT-013) — صدور مجوز پیش از بارداری و زایمان انجام می‌شود و هیچ فیلد یا شرط بارداری، زایمان یا تأیید دامپزشک در مسیر صدور وجود ندارد؛ تست مرورگر نبود این فیلدها را در صفحه تصمیم انجمن بررسی می‌کند. خود اعلام بارداری/زایمان در PROMPT-015 ساخته می‌شود. شواهد: `tests/browser/permit.test.ts` + docs/reports/screenshots/prompt-013/permit-assoc-review.png · VERIFIED(PROMPT-015) — اعلام بارداری و زایمان فقط از پرونده دارای مجوز صادرشده شروع می‌شود، بدون هیچ تأیید دامپزشکی ادامه می‌یابد و در طول کل مسیر وضعیت، نسخه و شماره مجوز تغییر نمی‌کند (DEC-0095). شواهد: `tests/db/birth.test.ts` + docs/reports/screenshots/prompt-015/pregnancy-empty.png |
| A-019 | درخواست تأیید بارداری صف دامپزشک مشخص دارد؛ کاربر قادر به ثبت نتیجه دامپزشکی نیست. | PROMPT-007, PROMPT-015, PROMPT-019 | VERIFIED(PROMPT-015) — درخواست از Finder در Context Pregnancy فقط به همان دامپزشک تخصیص می‌یابد و صف عمومی وجود ندارد؛ ثبت نتیجه از حساب مالک یا شخص ثالث رد می‌شود و فقط از پنل دامپزشک تخصیص‌یافته، پس از پذیرش کد و از مرکز فعال با پروانه معتبر ممکن است (DEC-0096). شواهد: `tests/db/birth.test.ts` + `tests/browser/pregnancy.test.ts` + docs/reports/screenshots/prompt-015/vet-pregnancy-result.png |
| A-020 | مغایرت، هر دو رکورد را حفظ و اعلان به همان پرونده ایجاد می‌کند. | PROMPT-015, PROMPT-019 | VERIFIED(PROMPT-015) — هر دو مقدار با برچسب «مغایرت با اعلام مالک» نمایش داده می‌شوند، اعلان خنثی با Resume به همان پرونده می‌رود و رویداد Audit شناسه و نسخه هر دو رکورد را دارد؛ اصلاح نتیجه دامپزشک وضعیت مغایرت را برمی‌دارد (DEC-0097). شواهد: `tests/db/birth.test.ts` + docs/reports/screenshots/prompt-015/pregnancy-mismatch.png |
| A-021 | فقط توله زنده پروفایل می‌گیرد و تعداد رکوردهای اولیه با Live Count برابر است. | PROMPT-015, PROMPT-019 | VERIFIED(PROMPT-015، بازبینی‌شده در PROMPT-019) — گزارش «۲ زنده و ۱ مرده» دقیقاً دو پرونده موقت می‌سازد و برای مرده‌ی زمان تولد هیچ پرونده، کد موقت یا کارتی ساخته نمی‌شود؛ هر دو صفر هم Litter بدون پرونده می‌سازد. شواهد: `tests/db/birth.test.ts` + `tests/browser/pregnancy.test.ts` + `tests/db/regression.test.ts` + docs/reports/screenshots/prompt-015/birth-recorded.png |
| A-022 | Puppy Card بدون Allocation دوطرفه FINAL صادر نمی‌شود؛ برگه ثبتی توله پیش‌نیاز آن نیست. | PROMPT-016, PROMPT-019 | VERIFIED(PROMPT-016) — تخصیص فقط با تأیید هر دو طرفِ واقعی روی همان نسخه FINAL می‌شود؛ تأیید یک طرف، تأیید دوباره همان شخص، پاسخ شخص ثالث و پرداخت هیچ‌کدام این Gate را دور نمی‌زنند و تا نهایی‌شدن، کارت با دلیل صریح قفل است. تغییر یا رد، نسخه جدید با صفر تأیید می‌سازد و تأیید نسخه قبلی روی داده جدید کار نمی‌کند (DEC-0100، DEC-0101). شواهد: `tests/db/allocation.test.ts` + `tests/browser/puppy-card.test.ts` + docs/reports/screenshots/prompt-016/allocation-pending.png و card-locked.png |
| A-023 | امضای فیزیکی Gate دیجیتال نساخته است. | PROMPT-008, PROMPT-013, PROMPT-019 | PARTIAL(PROMPT-008) — مرور نهایی خدمت فقط اطلاع‌رسانی می‌کند که امضای برگه در محل انجام می‌شود و هیچ امضای دیجیتال یا انسداد صدور وجود ندارد (D13). بخش مجوز جفت‌گیری در PROMPT-013 بررسی می‌شود. شواهد: `tests/browser/vet-service.test.ts` + docs/reports/screenshots/prompt-008/service-summary.png · VERIFIED(PROMPT-013) — صدور مجوز رسمی هیچ Gate امضای دیجیتالی ندارد و در کل مسیر (تأیید طرف مقابل، توافق تقسیم، پرداخت، ارسال و تصمیم انجمن) هیچ فیلد امضا وجود ندارد. شواهد: `tests/browser/permit.test.ts` + docs/reports/screenshots/prompt-013/permit-assoc-review.png |
| A-024 | درخواست فعال دامپزشک با محدودیت پذیرش کار جدید قابل ادامه است. | PROMPT-005, PROMPT-007, PROMPT-018, PROMPT-019 | VERIFIED(PROMPT-007) — روی صف واقعی: دامپزشک تعلیق‌شده در Finder نیست و درخواست جدید نمی‌گیرد ولی کد مراجعه قبلی را پذیرش می‌کند و صف کارهای فعالش باز می‌ماند. شواهد: `tests/db/referrals.test.ts` | · VERIFIED(PROMPT-018) — از دفتر عضویت انجمن، عضویت همان دامپزشک با ثبت دلیل غیرفعال شد: پنل او پیام «پذیرش کار جدید محدود است» را نشان داد، صف و پرونده مراجعه قبلی باز ماند و مرکز او از Finder حذف شد؛ سپس وضعیت بازگردانده شد. شواهد: `tests/browser/operations.test.ts` + docs/reports/screenshots/prompt-018/vet-inactive-membership.png و finder-without-inactive-vet.png
| A-025 | خطا، Retry و اعلان‌ها به همان Draft/Request برمی‌گردند. | PROMPT-004, PROMPT-005, PROMPT-007, PROMPT-018, PROMPT-019 | PARTIAL(PROMPT-003) — Resume Context روی اعلان و CTA بازگشت به همان پرونده؛ خطا/Retry هر فلو در همان مرحله بررسی می‌شود · PARTIAL(PROMPT-004) — ورود به درخواست مبدأ برمی‌گردد، کد نادرست/منقضی نشست نمی‌سازد و تغییر شماره ناموفق داده را دست‌نخورده می‌گذارد | · VERIFIED(PROMPT-019) — بازگشت به همان پرونده پس از خروج و ورود دوباره، Replay بازگشت درگاه بدون اثر دوم، و اعلان‌های هر فلو با Resume به همان مرحله، همگی روی داده واقعی اجرا شدند. شواهد: `tests/db/regression.test.ts` + `tests/browser/operations.test.ts` + `tests/browser/cross-flow.test.ts` + docs/reports/screenshots/prompt-018/resume-after-login.png
| A-026 | موارد باز به‌جای نتیجه ساختگی، با وضعیت مشخص باقی می‌مانند. | PROMPT-001, PROMPT-019, PROMPT-020, PROMPT-019 | DISCOVERY — [integration-readiness.md](docs/discovery/integration-readiness.md) §۳ (موارد نامعلوم «تعیین‌نشده» می‌مانند) |
| A-027 | بررسی شجره‌نامه خارجی با انجمن و فهرست صادرکنندگان موردتأیید آن انجام می‌شود؛ SLA ثابت وعده داده نمی‌شود. | PROMPT-006, PROMPT-018, PROMPT-019 | VERIFIED(PROMPT-006) — صادرکننده فقط از فهرست موردتأیید انجمن انتخاب می‌شود (متن آزاد رد می‌شود) و فهرست تا ورود داده واقعی خالی است؛ بررسی فقط با Context انجمن، تأیید بدون نسل استخراج‌شده و اصلاح/رد بدون دلیل ممکن نیست؛ هیچ مهلت یا SLA وعده داده نشده است. شواهد: `tests/db/animals.test.ts` + `tests/browser/animals.test.ts` + docs/reports/screenshots/prompt-006/assoc-foreign-review.png و foreign-no-issuers.png و foreign-needs-correction.png |
| A-028 | مهلت اولیه Referral برابر ۲۱ روز و مقدار آن از دیتابیس و قابل تنظیم از پنل مدیریت است. | PROMPT-002, PROMPT-007, PROMPT-018, PROMPT-019 | VERIFIED(PROMPT-007) — مقدار فعال هنگام صدور خوانده و همراه نسخه تنظیم روی خود کد ثبت می‌شود؛ همان مقدار نمایش داده و در سرور اجرا می‌شود و عدد ۲۱ هیچ‌جا هاردکد نیست (DEC-0047). تغییر تنظیم به ۷ روز کد صادرشده قبلی را جابه‌جا نکرد. شواهد: `tests/db/referrals.test.ts` + docs/reports/screenshots/prompt-007/referral-code.png |
| A-029 | کد منقضی امکان درخواست مجدد با ارزیابی صلاحیت و حفظ سابقه دارد. | PROMPT-007, PROMPT-019 | VERIFIED(PROMPT-007) — پس از انقضا کد جدید صادر می‌شود، صلاحیت دامپزشک و مرکز دوباره سنجیده می‌شود (مرکز با پروانه باطل تمدید نمی‌گیرد) و کد قبلی به‌صورت EXPIRED در سابقه می‌ماند. شواهد: `tests/db/referrals.test.ts` + `tests/browser/finder.test.ts` + docs/reports/screenshots/prompt-007/referral-expired.png و referral-renewed.png |
| A-030 | تعرفه‌ها و پارامترهای مدیریتی هاردکد نیستند و تغییر آن‌ها تاریخچه دارد. | PROMPT-002, PROMPT-005, PROMPT-018, PROMPT-019 | PARTIAL(PROMPT-002) — تعرفه‌ها در DB با نسخه و Audit؛ مقدار نامعلوم NOT_CONFIGURED می‌ماند · PARTIAL(PROMPT-005) — مبلغ در لحظه ایجاد Intent با نسخه تنظیم منجمد می‌شود؛ تغییر تعرفه پرداخت قبلی را بازنویسی نمی‌کند (تست‌شده) · PARTIAL(PROMPT-009) — تعرفه برگه ثبتی از همان داده مدیریت‌شده خوانده و روی قلم منجمد می‌شود؛ فرم ویرایش تنظیمات با اعتبارسنجی، نسخه و Audit ساخته شد و مقدار خالی به «تعیین‌نشده» برمی‌گردد (DEC-0065) | · VERIFIED(PROMPT-018، PROMPT-019) — هیچ تعرفه یا پارامتری در کد هاردکد نیست: همه از `product_setting` خوانده می‌شوند، تغییر هر مقدار Actor، زمان، مقدار قبلی و دلیل دارد و در `/admin/audit` دیده می‌شود، و افزایش تعرفه مبلغ قلم‌های پرداخت‌شده قبلی را تغییر نمی‌دهد. شواهد: `tests/db/operations.test.ts` + `tests/db/settings.test.ts` + `tests/db/startup.test.ts` + docs/reports/screenshots/prompt-018/admin-audit.png
| A-031 | ارسال پستی فقط درخواست را ثبت می‌کند و ثبت درخواست به معنی ارسال واقعی معرفی نمی‌شود. | PROMPT-011, PROMPT-019 | VERIFIED(PROMPT-011، بازبینی‌شده در PROMPT-019) — درخواست پستی فقط ثبت درخواست است: جدول هیچ ستون کد رهگیری، ارسال یا تحویل ندارد، صفحه مالک و صف انجمن هر دو صریحاً می‌گویند چرخه ارسال در این فاز وجود ندارد و تست مرورگر نبود «کد رهگیری پستی» را روی صفحه سند بررسی می‌کند (DEC-0114). شواهد: `tests/db/regression.test.ts` + `tests/browser/cross-flow.test.ts` + docs/reports/screenshots/prompt-011/postal-request.png |
| A-032 | اصلاح تعداد توله برای کاربر مجاز ممکن است؛ مرگ بعدی، نسخه تولد و پروفایل تاریخی را حذف نمی‌کند. | PROMPT-015, PROMPT-016, PROMPT-019 | VERIFIED(PROMPT-015) — وجود پرونده توله فرم اصلاح را قفل نمی‌کند و بررسی انجمن دوباره لازم نیست؛ هر اصلاح نسخه با Actor، زمان و علت است، افزایش فقط کمبود را می‌سازد و کاهش بدون تعیین پرونده مشمول رد می‌شود و پرونده با علت «حذف‌شده با اصلاح گزارش اولیه» باقی می‌ماند؛ مرگ پس از تولد پرونده و گزارش اولیه را حفظ می‌کند و فقط تعداد زنده فعلی را تغییر می‌دهد (DEC-0098، DEC-0099). شواهد: `tests/db/birth.test.ts` + `tests/browser/pregnancy.test.ts` + docs/reports/screenshots/prompt-015/puppy-death.png و birth-withdrawn.png |
| A-033 | اعتراض Parentage داخل همزیست به همان مرکز ارجاع و با حفظ نتیجه قبلی پیگیری می‌شود. | PROMPT-011, PROMPT-018, PROMPT-019 | VERIFIED(PROMPT-011) — اعتراض از همان نتیجه و حیوان در هم‌زیست ثبت و به همان مرکز ارجاع می‌شود؛ نتیجه اصلاحی نسخه جدید است، نتیجه مورد اعتراض و پیوند آن حفظ می‌شود و سند صادرشده بازنویسی یا باطل نمی‌شود (DEC-0076، DEC-0077). شواهد: `tests/db/pedigree.test.ts` + docs/reports/screenshots/prompt-011/pedigree-correction-notice.png |
