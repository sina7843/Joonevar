# Contract for future revisions of this package

This is already a Hamzist-specific package, not a blank generator. Preserve the exact source, current user authorization, all final D decisions, flow precedence, independent typed entities and execution evidence. Update documentation, prompts, manifest, traceability and acceptance mappings coherently only when the user requests a revision.

Do not copy the inventory sample's business rules, files, stack or manual-user completion gates. Prompt count follows actual scope, 1–100. Every prompt has one copyable text block and predecessor dependency. Runtime/config changes are permitted when needed to honor the explicitly requested Claude-owned local commits and autonomous documented decisions; they must be tested.

Unknown technical choices belong to Claude at implementation time. Real unknown facts remain external data requirements. Do not turn technical freedom into permission to alter approved product behavior. Run tools/validate-core.mjs and runtime tests before returning a revised archive.
